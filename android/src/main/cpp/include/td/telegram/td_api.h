#pragma once


#include "td/tl/TlObject.h"

#include <cstdint>
#include <memory>
#include <utility>
#include <vector>

#include <jni.h>

#include <string>

namespace td {
class TlStorerToString;

namespace td_api {

class Object;
/**
 * This class is a base class for all TDLib API classes and functions.
 */
using BaseObject = Object;

/**
 * A smart wrapper to store a pointer to a TDLib API object. Can be treated as an analogue of std::unique_ptr.
 */
template <class Type>
using object_ptr = ::td::tl_object_ptr<Type>;

/**
 * A function to create a dynamically allocated TDLib API object. Can be treated as an analogue of std::make_unique.
 * Usage example:
 * \code
 * auto get_authorization_state_request = td::td_api::make_object<td::td_api::getAuthorizationState>();
 * auto message_text = td::td_api::make_object<td::td_api::formattedText>("Hello, world!!!",
 *                     std::vector<td::td_api::object_ptr<td::td_api::textEntities>>());
 * auto send_message_request = td::td_api::make_object<td::td_api::sendMessage>(chat_id, 0, false, false, nullptr,
 *      td::td_api::make_object<td::td_api::inputMessageText>(std::move(message_text), false, true));
 * \endcode
 *
 * \tparam Type Type of an object to construct.
 * \param[in] args Arguments to pass to the object constructor.
 * \return Wrapped pointer to the created object.
 */
template <class Type, class... Args>
object_ptr<Type> make_object(Args &&... args) {
  return object_ptr<Type>(new Type(std::forward<Args>(args)...));
}

/**
 * A function to cast a wrapped in td::td_api::object_ptr TDLib API object to its subclass or superclass.
 * Casting an object to an incorrect type will lead to undefined bejaviour.
 * Usage example:
 * \code
 * td::td_api::object_ptr<td::td_api::callState> call_state = ...;
 * switch (call_state->get_id()) {
 *   case td::td_api::callStatePending::ID: {
 *     auto state = td::td_api::move_object_as<td::td_api::callStatePending>(call_state);
 *     // use state
 *     break;
 *   }
 *   case td::td_api::callStateExchangingKeys::ID: {
 *     // no additional fields, no casting is needed
 *     break;
 *   }
 *   case td::td_api::callStateReady::ID: {
 *     auto state = td::td_api::move_object_as<td::td_api::callStateReady>(call_state);
 *     // use state
 *     break;
 *   }
 *   case td::td_api::callStateHangingUp::ID: {
 *     // no additional fields, no casting is needed
 *     break;
 *   }
 *   case td::td_api::callStateDiscarded::ID: {
 *     auto state = td::td_api::move_object_as<td::td_api::callStateDiscarded>(call_state);
 *     // use state
 *     break;
 *   }
 *   case td::td_api::callStateError::ID: {
 *     auto state = td::td_api::move_object_as<td::td_api::callStateError>(call_state);
 *     // use state
 *     break;
 *   }
 *   default:
 *     assert(false);
 * }
 * \endcode
 *
 * \tparam ToType Type of a TDLib API object to move to.
 * \tparam FromType Type of a TDLib API object to move from, this is auto-deduced.
 * \param[in] from Wrapped in td::td_api::object_ptr pointer to a TDLib API object.
 */
template <class ToType, class FromType>
object_ptr<ToType> move_object_as(FromType &&from) {
  return object_ptr<ToType>(static_cast<ToType *>(from.release()));
}

/**
 * Returns a string representation of the TDLib API object.
 * \param[in] value The object.
 * \return Object string representation.
 */
std::string to_string(const BaseObject &value);

/**
 * Returns a string representation of the TDLib API object.
 * \tparam T Object type, auto-deduced.
 * \param[in] value The object.
 * \return Object string representation.
 */
template <class T>
std::string to_string(const object_ptr<T> &value) {
  if (value == nullptr) {
    return "null";
  }

  return to_string(*value);
}

class accountTtl;

class address;

class animation;

class animations;

class audio;

class authenticationCodeInfo;

class AuthenticationCodeType;

class AuthorizationState;

class basicGroup;

class basicGroupFullInfo;

class botCommand;

class botInfo;

class call;

class callConnection;

class CallDiscardReason;

class callId;

class callProtocol;

class CallState;

class callbackQueryAnswer;

class CallbackQueryPayload;

class chat;

class ChatAction;

class chatEvent;

class ChatEventAction;

class chatEventLogFilters;

class chatEvents;

class chatInviteLink;

class chatInviteLinkInfo;

class chatMember;

class ChatMemberStatus;

class chatMembers;

class ChatMembersFilter;

class chatNotificationSettings;

class chatPhoto;

class ChatReportReason;

class chatReportSpamState;

class ChatType;

class chats;

class CheckChatUsernameResult;

class connectedWebsite;

class connectedWebsites;

class ConnectionState;

class contact;

class count;

class customRequestResult;

class date;

class datedFile;

class deepLinkInfo;

class DeviceToken;

class document;

class draftMessage;

class emailAddressAuthenticationCodeInfo;

class encryptedCredentials;

class encryptedPassportElement;

class error;

class file;

class FileType;

class formattedText;

class foundMessages;

class game;

class gameHighScore;

class gameHighScores;

class hashtags;

class identityDocument;

class importedContacts;

class inlineKeyboardButton;

class InlineKeyboardButtonType;

class InlineQueryResult;

class inlineQueryResults;

class InputCredentials;

class InputFile;

class inputIdentityDocument;

class InputInlineQueryResult;

class InputMessageContent;

class InputPassportElement;

class inputPassportElementError;

class InputPassportElementErrorSource;

class inputPersonalDocument;

class inputSticker;

class inputThumbnail;

class invoice;

class keyboardButton;

class KeyboardButtonType;

class labeledPricePart;

class languagePackInfo;

class languagePackString;

class LanguagePackStringValue;

class languagePackStrings;

class LinkState;

class localFile;

class localizationTargetInfo;

class location;

class MaskPoint;

class maskPosition;

class message;

class MessageContent;

class MessageForwardInfo;

class MessageSendingState;

class messages;

class networkStatistics;

class NetworkStatisticsEntry;

class NetworkType;

class NotificationSettingsScope;

class ok;

class OptionValue;

class orderInfo;

class PageBlock;

class passportAuthorizationForm;

class PassportElement;

class passportElementError;

class PassportElementErrorSource;

class PassportElementType;

class passportElements;

class passportRequiredElement;

class passportSuitableElement;

class passwordState;

class paymentForm;

class paymentReceipt;

class paymentResult;

class paymentsProviderStripe;

class personalDetails;

class personalDocument;

class photo;

class photoSize;

class profilePhoto;

class proxies;

class proxy;

class ProxyType;

class publicMessageLink;

class recoveryEmailAddress;

class remoteFile;

class ReplyMarkup;

class RichText;

class savedCredentials;

class scopeNotificationSettings;

class SearchMessagesFilter;

class seconds;

class secretChat;

class SecretChatState;

class session;

class sessions;

class shippingOption;

class sticker;

class stickerEmojis;

class stickerSet;

class stickerSetInfo;

class stickerSets;

class stickers;

class storageStatistics;

class storageStatisticsByChat;

class storageStatisticsByFileType;

class storageStatisticsFast;

class supergroup;

class supergroupFullInfo;

class SupergroupMembersFilter;

class tMeUrl;

class TMeUrlType;

class tMeUrls;

class tdlibParameters;

class temporaryPasswordState;

class termsOfService;

class testBytes;

class testInt;

class testString;

class testVectorInt;

class testVectorIntObject;

class testVectorString;

class testVectorStringObject;

class text;

class textEntities;

class textEntity;

class TextEntityType;

class TextParseMode;

class TopChatCategory;

class Update;

class user;

class userFullInfo;

class UserPrivacySetting;

class UserPrivacySettingRule;

class userPrivacySettingRules;

class userProfilePhotos;

class UserStatus;

class UserType;

class users;

class validatedOrderInfo;

class venue;

class video;

class videoNote;

class voiceNote;

class wallpaper;

class wallpapers;

class webPage;

class webPageInstantView;

class Object;

/**
 * This class is a base class for all TDLib API classes.
 */
class Object {
 public:
  virtual ~Object() {
  }

  virtual void store(JNIEnv *env, jobject &s) const {
  }

  virtual void store(TlStorerToString &s, const char *field_name) const = 0;

  static jclass Class;

  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  virtual std::int32_t get_id() const = 0;

  static object_ptr<Object> fetch(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * This class is a base class for all TDLib API functions.
 */
class Function: public Object {
 public:
  static jclass Class;

  static object_ptr<Function> fetch(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Contains information about the period of inactivity after which the current user's account will automatically be deleted.
 */
class accountTtl final : public Object {
 public:
  static jclass Class;
  /// Number of days of inactivity before the account will be flagged for deletion; should range from 30-366 days.
  std::int32_t days_;
  static jfieldID days_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  accountTtl();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] days_ Number of days of inactivity before the account will be flagged for deletion; should range from 30-366 days.
   */
  explicit accountTtl(std::int32_t days_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1324495492;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<accountTtl> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Describes an address.
 */
class address final : public Object {
 public:
  static jclass Class;
  /// A two-letter ISO 3166-1 alpha-2 country code.
  std::string country_code_;
  static jfieldID country_code_fieldID;
  /// State, if applicable.
  std::string state_;
  static jfieldID state_fieldID;
  /// City.
  std::string city_;
  static jfieldID city_fieldID;
  /// First line of the address.
  std::string street_line1_;
  static jfieldID street_line1_fieldID;
  /// Second line of the address.
  std::string street_line2_;
  static jfieldID street_line2_fieldID;
  /// Address postal code.
  std::string postal_code_;
  static jfieldID postal_code_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  address();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] country_code_ A two-letter ISO 3166-1 alpha-2 country code.
   * \param[in] state_ State, if applicable.
   * \param[in] city_ City.
   * \param[in] street_line1_ First line of the address.
   * \param[in] street_line2_ Second line of the address.
   * \param[in] postal_code_ Address postal code.
   */
  address(std::string const &country_code_, std::string const &state_, std::string const &city_, std::string const &street_line1_, std::string const &street_line2_, std::string const &postal_code_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -2043654342;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<address> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Describes an animation file. The animation must be encoded in GIF or MPEG4 format.
 */
class animation final : public Object {
 public:
  static jclass Class;
  /// Duration of the animation, in seconds; as defined by the sender.
  std::int32_t duration_;
  static jfieldID duration_fieldID;
  /// Width of the animation.
  std::int32_t width_;
  static jfieldID width_fieldID;
  /// Height of the animation.
  std::int32_t height_;
  static jfieldID height_fieldID;
  /// Original name of the file; as defined by the sender.
  std::string file_name_;
  static jfieldID file_name_fieldID;
  /// MIME type of the file, usually &quot;image/gif&quot; or &quot;video/mp4&quot;.
  std::string mime_type_;
  static jfieldID mime_type_fieldID;
  /// Animation thumbnail; may be null.
  object_ptr<photoSize> thumbnail_;
  static jfieldID thumbnail_fieldID;
  /// File containing the animation.
  object_ptr<file> animation_;
  static jfieldID animation_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  animation();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] duration_ Duration of the animation, in seconds; as defined by the sender.
   * \param[in] width_ Width of the animation.
   * \param[in] height_ Height of the animation.
   * \param[in] file_name_ Original name of the file; as defined by the sender.
   * \param[in] mime_type_ MIME type of the file, usually &quot;image/gif&quot; or &quot;video/mp4&quot;.
   * \param[in] thumbnail_ Animation thumbnail; may be null.
   * \param[in] animation_ File containing the animation.
   */
  animation(std::int32_t duration_, std::int32_t width_, std::int32_t height_, std::string const &file_name_, std::string const &mime_type_, object_ptr<photoSize> &&thumbnail_, object_ptr<file> &&animation_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1723168340;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<animation> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Represents a list of animations.
 */
class animations final : public Object {
 public:
  static jclass Class;
  /// List of animations.
  std::vector<object_ptr<animation>> animations_;
  static jfieldID animations_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  animations();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] animations_ List of animations.
   */
  explicit animations(std::vector<object_ptr<animation>> &&animations_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 344216945;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<animations> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Describes an audio file. Audio is usually in MP3 format.
 */
class audio final : public Object {
 public:
  static jclass Class;
  /// Duration of the audio, in seconds; as defined by the sender.
  std::int32_t duration_;
  static jfieldID duration_fieldID;
  /// Title of the audio; as defined by the sender.
  std::string title_;
  static jfieldID title_fieldID;
  /// Performer of the audio; as defined by the sender.
  std::string performer_;
  static jfieldID performer_fieldID;
  /// Original name of the file; as defined by the sender.
  std::string file_name_;
  static jfieldID file_name_fieldID;
  /// The MIME type of the file; as defined by the sender.
  std::string mime_type_;
  static jfieldID mime_type_fieldID;
  /// The thumbnail of the album cover; as defined by the sender. The full size thumbnail should be extracted from the downloaded file; may be null.
  object_ptr<photoSize> album_cover_thumbnail_;
  static jfieldID album_cover_thumbnail_fieldID;
  /// File containing the audio.
  object_ptr<file> audio_;
  static jfieldID audio_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  audio();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] duration_ Duration of the audio, in seconds; as defined by the sender.
   * \param[in] title_ Title of the audio; as defined by the sender.
   * \param[in] performer_ Performer of the audio; as defined by the sender.
   * \param[in] file_name_ Original name of the file; as defined by the sender.
   * \param[in] mime_type_ The MIME type of the file; as defined by the sender.
   * \param[in] album_cover_thumbnail_ The thumbnail of the album cover; as defined by the sender. The full size thumbnail should be extracted from the downloaded file; may be null.
   * \param[in] audio_ File containing the audio.
   */
  audio(std::int32_t duration_, std::string const &title_, std::string const &performer_, std::string const &file_name_, std::string const &mime_type_, object_ptr<photoSize> &&album_cover_thumbnail_, object_ptr<file> &&audio_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 383148432;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<audio> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Information about the authentication code that was sent.
 */
class authenticationCodeInfo final : public Object {
 public:
  static jclass Class;
  /// A phone number that is being authenticated.
  std::string phone_number_;
  static jfieldID phone_number_fieldID;
  /// Describes the way the code was sent to the user.
  object_ptr<AuthenticationCodeType> type_;
  static jfieldID type_fieldID;
  /// Describes the way the next code will be sent to the user; may be null.
  object_ptr<AuthenticationCodeType> next_type_;
  static jfieldID next_type_fieldID;
  /// Timeout before the code should be re-sent, in seconds.
  std::int32_t timeout_;
  static jfieldID timeout_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  authenticationCodeInfo();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] phone_number_ A phone number that is being authenticated.
   * \param[in] type_ Describes the way the code was sent to the user.
   * \param[in] next_type_ Describes the way the next code will be sent to the user; may be null.
   * \param[in] timeout_ Timeout before the code should be re-sent, in seconds.
   */
  authenticationCodeInfo(std::string const &phone_number_, object_ptr<AuthenticationCodeType> &&type_, object_ptr<AuthenticationCodeType> &&next_type_, std::int32_t timeout_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -860345416;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<authenticationCodeInfo> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * This class is an abstract base class.
 * Provides information about the method by which an authentication code is delivered to the user.
 */
class AuthenticationCodeType: public Object {
 public:
  static jclass Class;

  static object_ptr<AuthenticationCodeType> fetch(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * An authentication code is delivered via a private Telegram message, which can be viewed in another client.
 */
class authenticationCodeTypeTelegramMessage final : public AuthenticationCodeType {
 public:
  static jclass Class;
  /// Length of the code.
  std::int32_t length_;
  static jfieldID length_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  authenticationCodeTypeTelegramMessage();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] length_ Length of the code.
   */
  explicit authenticationCodeTypeTelegramMessage(std::int32_t length_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 2079628074;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<authenticationCodeTypeTelegramMessage> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * An authentication code is delivered via an SMS message to the specified phone number.
 */
class authenticationCodeTypeSms final : public AuthenticationCodeType {
 public:
  static jclass Class;
  /// Length of the code.
  std::int32_t length_;
  static jfieldID length_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  authenticationCodeTypeSms();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] length_ Length of the code.
   */
  explicit authenticationCodeTypeSms(std::int32_t length_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 962650760;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<authenticationCodeTypeSms> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * An authentication code is delivered via a phone call to the specified phone number.
 */
class authenticationCodeTypeCall final : public AuthenticationCodeType {
 public:
  static jclass Class;
  /// Length of the code.
  std::int32_t length_;
  static jfieldID length_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  authenticationCodeTypeCall();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] length_ Length of the code.
   */
  explicit authenticationCodeTypeCall(std::int32_t length_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1636265063;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<authenticationCodeTypeCall> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * An authentication code is delivered by an immediately cancelled call to the specified phone number. The number from which the call was made is the code.
 */
class authenticationCodeTypeFlashCall final : public AuthenticationCodeType {
 public:
  static jclass Class;
  /// Pattern of the phone number from which the call will be made.
  std::string pattern_;
  static jfieldID pattern_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  authenticationCodeTypeFlashCall();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] pattern_ Pattern of the phone number from which the call will be made.
   */
  explicit authenticationCodeTypeFlashCall(std::string const &pattern_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1395882402;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<authenticationCodeTypeFlashCall> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * This class is an abstract base class.
 * Represents the current authorization state of the client.
 */
class AuthorizationState: public Object {
 public:
  static jclass Class;

  static object_ptr<AuthorizationState> fetch(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * TDLib needs TdlibParameters for initialization.
 */
class authorizationStateWaitTdlibParameters final : public AuthorizationState {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  authorizationStateWaitTdlibParameters();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 904720988;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<authorizationStateWaitTdlibParameters> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * TDLib needs an encryption key to decrypt the local database.
 */
class authorizationStateWaitEncryptionKey final : public AuthorizationState {
 public:
  static jclass Class;
  /// True, if the database is currently encrypted.
  bool is_encrypted_;
  static jfieldID is_encrypted_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  authorizationStateWaitEncryptionKey();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] is_encrypted_ True, if the database is currently encrypted.
   */
  explicit authorizationStateWaitEncryptionKey(bool is_encrypted_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 612103496;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<authorizationStateWaitEncryptionKey> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * TDLib needs the user's phone number to authorize.
 */
class authorizationStateWaitPhoneNumber final : public AuthorizationState {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  authorizationStateWaitPhoneNumber();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 306402531;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<authorizationStateWaitPhoneNumber> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * TDLib needs the user's authentication code to finalize authorization.
 */
class authorizationStateWaitCode final : public AuthorizationState {
 public:
  static jclass Class;
  /// True, if the user is already registered.
  bool is_registered_;
  static jfieldID is_registered_fieldID;
  /// Telegram terms of service, which should be accepted before user can continue registration; may be null.
  object_ptr<termsOfService> terms_of_service_;
  static jfieldID terms_of_service_fieldID;
  /// Information about the authorization code that was sent.
  object_ptr<authenticationCodeInfo> code_info_;
  static jfieldID code_info_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  authorizationStateWaitCode();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] is_registered_ True, if the user is already registered.
   * \param[in] terms_of_service_ Telegram terms of service, which should be accepted before user can continue registration; may be null.
   * \param[in] code_info_ Information about the authorization code that was sent.
   */
  authorizationStateWaitCode(bool is_registered_, object_ptr<termsOfService> &&terms_of_service_, object_ptr<authenticationCodeInfo> &&code_info_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -122899120;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<authorizationStateWaitCode> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * The user has been authorized, but needs to enter a password to start using the application.
 */
class authorizationStateWaitPassword final : public AuthorizationState {
 public:
  static jclass Class;
  /// Hint for the password; can be empty.
  std::string password_hint_;
  static jfieldID password_hint_fieldID;
  /// True if a recovery email address has been set up.
  bool has_recovery_email_address_;
  static jfieldID has_recovery_email_address_fieldID;
  /// Pattern of the email address to which the recovery email was sent; empty until a recovery email has been sent.
  std::string recovery_email_address_pattern_;
  static jfieldID recovery_email_address_pattern_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  authorizationStateWaitPassword();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] password_hint_ Hint for the password; can be empty.
   * \param[in] has_recovery_email_address_ True if a recovery email address has been set up.
   * \param[in] recovery_email_address_pattern_ Pattern of the email address to which the recovery email was sent; empty until a recovery email has been sent.
   */
  authorizationStateWaitPassword(std::string const &password_hint_, bool has_recovery_email_address_, std::string const &recovery_email_address_pattern_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 187548796;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<authorizationStateWaitPassword> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * The user has been successfully authorized. TDLib is now ready to answer queries.
 */
class authorizationStateReady final : public AuthorizationState {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  authorizationStateReady();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1834871737;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<authorizationStateReady> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * The user is currently logging out.
 */
class authorizationStateLoggingOut final : public AuthorizationState {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  authorizationStateLoggingOut();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 154449270;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<authorizationStateLoggingOut> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * TDLib is closing, all subsequent queries will be answered with the error 500. Note that closing TDLib can take a while. All resources will be freed only after authorizationStateClosed has been received.
 */
class authorizationStateClosing final : public AuthorizationState {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  authorizationStateClosing();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 445855311;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<authorizationStateClosing> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * TDLib client is in its final state. All databases are closed and all resources are released. No other updates will be received after this. All queries will be responded to with error code 500. To continue working, one should create a new instance of the TDLib client.
 */
class authorizationStateClosed final : public AuthorizationState {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  authorizationStateClosed();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1526047584;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<authorizationStateClosed> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Represents a basic group of 0-200 users (must be upgraded to a supergroup to accommodate more than 200 users).
 */
class basicGroup final : public Object {
 public:
  static jclass Class;
  /// Group identifier.
  std::int32_t id_;
  static jfieldID id_fieldID;
  /// Number of members in the group.
  std::int32_t member_count_;
  static jfieldID member_count_fieldID;
  /// Status of the current user in the group.
  object_ptr<ChatMemberStatus> status_;
  static jfieldID status_fieldID;
  /// True, if all members have been granted administrator rights in the group.
  bool everyone_is_administrator_;
  static jfieldID everyone_is_administrator_fieldID;
  /// True, if the group is active.
  bool is_active_;
  static jfieldID is_active_fieldID;
  /// Identifier of the supergroup to which this group was upgraded; 0 if none.
  std::int32_t upgraded_to_supergroup_id_;
  static jfieldID upgraded_to_supergroup_id_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  basicGroup();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] id_ Group identifier.
   * \param[in] member_count_ Number of members in the group.
   * \param[in] status_ Status of the current user in the group.
   * \param[in] everyone_is_administrator_ True, if all members have been granted administrator rights in the group.
   * \param[in] is_active_ True, if the group is active.
   * \param[in] upgraded_to_supergroup_id_ Identifier of the supergroup to which this group was upgraded; 0 if none.
   */
  basicGroup(std::int32_t id_, std::int32_t member_count_, object_ptr<ChatMemberStatus> &&status_, bool everyone_is_administrator_, bool is_active_, std::int32_t upgraded_to_supergroup_id_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1572712718;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<basicGroup> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Contains full information about a basic group.
 */
class basicGroupFullInfo final : public Object {
 public:
  static jclass Class;
  /// User identifier of the creator of the group; 0 if unknown.
  std::int32_t creator_user_id_;
  static jfieldID creator_user_id_fieldID;
  /// Group members.
  std::vector<object_ptr<chatMember>> members_;
  static jfieldID members_fieldID;
  /// Invite link for this group; available only for the group creator and only after it has been generated at least once.
  std::string invite_link_;
  static jfieldID invite_link_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  basicGroupFullInfo();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] creator_user_id_ User identifier of the creator of the group; 0 if unknown.
   * \param[in] members_ Group members.
   * \param[in] invite_link_ Invite link for this group; available only for the group creator and only after it has been generated at least once.
   */
  basicGroupFullInfo(std::int32_t creator_user_id_, std::vector<object_ptr<chatMember>> &&members_, std::string const &invite_link_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 952266076;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<basicGroupFullInfo> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Represents commands supported by a bot.
 */
class botCommand final : public Object {
 public:
  static jclass Class;
  /// Text of the bot command.
  std::string command_;
  static jfieldID command_fieldID;
  /// Description of the bot command.
  std::string description_;
  static jfieldID description_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  botCommand();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] command_ Text of the bot command.
   * \param[in] description_ Description of the bot command.
   */
  botCommand(std::string const &command_, std::string const &description_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1032140601;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<botCommand> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Provides information about a bot and its supported commands.
 */
class botInfo final : public Object {
 public:
  static jclass Class;
  /// Long description shown on the user info page.
  std::string description_;
  static jfieldID description_fieldID;
  /// A list of commands supported by the bot.
  std::vector<object_ptr<botCommand>> commands_;
  static jfieldID commands_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  botInfo();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] description_ Long description shown on the user info page.
   * \param[in] commands_ A list of commands supported by the bot.
   */
  botInfo(std::string const &description_, std::vector<object_ptr<botCommand>> &&commands_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1296528907;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<botInfo> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Describes a call.
 */
class call final : public Object {
 public:
  static jclass Class;
  /// Call identifier, not persistent.
  std::int32_t id_;
  static jfieldID id_fieldID;
  /// Peer user identifier.
  std::int32_t user_id_;
  static jfieldID user_id_fieldID;
  /// True, if the call is outgoing.
  bool is_outgoing_;
  static jfieldID is_outgoing_fieldID;
  /// Call state.
  object_ptr<CallState> state_;
  static jfieldID state_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  call();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] id_ Call identifier, not persistent.
   * \param[in] user_id_ Peer user identifier.
   * \param[in] is_outgoing_ True, if the call is outgoing.
   * \param[in] state_ Call state.
   */
  call(std::int32_t id_, std::int32_t user_id_, bool is_outgoing_, object_ptr<CallState> &&state_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1837599107;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<call> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Describes the address of UDP reflectors.
 */
class callConnection final : public Object {
 public:
  static jclass Class;
  /// Reflector identifier.
  std::int64_t id_;
  static jfieldID id_fieldID;
  /// IPv4 reflector address.
  std::string ip_;
  static jfieldID ip_fieldID;
  /// IPv6 reflector address.
  std::string ipv6_;
  static jfieldID ipv6_fieldID;
  /// Reflector port number.
  std::int32_t port_;
  static jfieldID port_fieldID;
  /// Connection peer tag.
  std::string peer_tag_;
  static jfieldID peer_tag_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  callConnection();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] id_ Reflector identifier.
   * \param[in] ip_ IPv4 reflector address.
   * \param[in] ipv6_ IPv6 reflector address.
   * \param[in] port_ Reflector port number.
   * \param[in] peer_tag_ Connection peer tag.
   */
  callConnection(std::int64_t id_, std::string const &ip_, std::string const &ipv6_, std::int32_t port_, std::string const &peer_tag_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1318542714;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<callConnection> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * This class is an abstract base class.
 * Describes the reason why a call was discarded.
 */
class CallDiscardReason: public Object {
 public:
  static jclass Class;

  static object_ptr<CallDiscardReason> fetch(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * The call wasn't discarded, or the reason is unknown.
 */
class callDiscardReasonEmpty final : public CallDiscardReason {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  callDiscardReasonEmpty();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1258917949;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<callDiscardReasonEmpty> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * The call was ended before the conversation started. It was cancelled by the caller or missed by the other party.
 */
class callDiscardReasonMissed final : public CallDiscardReason {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  callDiscardReasonMissed();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1680358012;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<callDiscardReasonMissed> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * The call was ended before the conversation started. It was declined by the other party.
 */
class callDiscardReasonDeclined final : public CallDiscardReason {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  callDiscardReasonDeclined();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1729926094;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<callDiscardReasonDeclined> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * The call was ended during the conversation because the users were disconnected.
 */
class callDiscardReasonDisconnected final : public CallDiscardReason {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  callDiscardReasonDisconnected();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1342872670;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<callDiscardReasonDisconnected> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * The call was ended because one of the parties hung up.
 */
class callDiscardReasonHungUp final : public CallDiscardReason {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  callDiscardReasonHungUp();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 438216166;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<callDiscardReasonHungUp> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Contains the call identifier.
 */
class callId final : public Object {
 public:
  static jclass Class;
  /// Call identifier.
  std::int32_t id_;
  static jfieldID id_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  callId();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] id_ Call identifier.
   */
  explicit callId(std::int32_t id_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 65717769;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<callId> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Specifies the supported call protocols.
 */
class callProtocol final : public Object {
 public:
  static jclass Class;
  /// True, if UDP peer-to-peer connections are supported.
  bool udp_p2p_;
  static jfieldID udp_p2p_fieldID;
  /// True, if connection through UDP reflectors is supported.
  bool udp_reflector_;
  static jfieldID udp_reflector_fieldID;
  /// Minimum supported API layer; use 65.
  std::int32_t min_layer_;
  static jfieldID min_layer_fieldID;
  /// Maximum supported API layer; use 65.
  std::int32_t max_layer_;
  static jfieldID max_layer_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  callProtocol();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] udp_p2p_ True, if UDP peer-to-peer connections are supported.
   * \param[in] udp_reflector_ True, if connection through UDP reflectors is supported.
   * \param[in] min_layer_ Minimum supported API layer; use 65.
   * \param[in] max_layer_ Maximum supported API layer; use 65.
   */
  callProtocol(bool udp_p2p_, bool udp_reflector_, std::int32_t min_layer_, std::int32_t max_layer_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1042830667;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<callProtocol> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * This class is an abstract base class.
 * Describes the current call state.
 */
class CallState: public Object {
 public:
  static jclass Class;

  static object_ptr<CallState> fetch(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * The call is pending, waiting to be accepted by a user.
 */
class callStatePending final : public CallState {
 public:
  static jclass Class;
  /// True, if the call has already been created by the server.
  bool is_created_;
  static jfieldID is_created_fieldID;
  /// True, if the call has already been received by the other party.
  bool is_received_;
  static jfieldID is_received_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  callStatePending();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] is_created_ True, if the call has already been created by the server.
   * \param[in] is_received_ True, if the call has already been received by the other party.
   */
  callStatePending(bool is_created_, bool is_received_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1073048620;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<callStatePending> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * The call has been answered and encryption keys are being exchanged.
 */
class callStateExchangingKeys final : public CallState {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  callStateExchangingKeys();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1848149403;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<callStateExchangingKeys> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * The call is ready to use.
 */
class callStateReady final : public CallState {
 public:
  static jclass Class;
  /// Call protocols supported by the peer.
  object_ptr<callProtocol> protocol_;
  static jfieldID protocol_fieldID;
  /// Available UDP reflectors.
  std::vector<object_ptr<callConnection>> connections_;
  static jfieldID connections_fieldID;
  /// A JSON-encoded call config.
  std::string config_;
  static jfieldID config_fieldID;
  /// Call encryption key.
  std::string encryption_key_;
  static jfieldID encryption_key_fieldID;
  /// Encryption key emojis fingerprint.
  std::vector<std::string> emojis_;
  static jfieldID emojis_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  callStateReady();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] protocol_ Call protocols supported by the peer.
   * \param[in] connections_ Available UDP reflectors.
   * \param[in] config_ A JSON-encoded call config.
   * \param[in] encryption_key_ Call encryption key.
   * \param[in] emojis_ Encryption key emojis fingerprint.
   */
  callStateReady(object_ptr<callProtocol> &&protocol_, std::vector<object_ptr<callConnection>> &&connections_, std::string const &config_, std::string const &encryption_key_, std::vector<std::string> &&emojis_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1518705438;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<callStateReady> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * The call is hanging up after discardCall has been called.
 */
class callStateHangingUp final : public CallState {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  callStateHangingUp();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -2133790038;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<callStateHangingUp> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * The call has ended successfully.
 */
class callStateDiscarded final : public CallState {
 public:
  static jclass Class;
  /// The reason, why the call has ended.
  object_ptr<CallDiscardReason> reason_;
  static jfieldID reason_fieldID;
  /// True, if the call rating should be sent to the server.
  bool need_rating_;
  static jfieldID need_rating_fieldID;
  /// True, if the call debug information should be sent to the server.
  bool need_debug_information_;
  static jfieldID need_debug_information_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  callStateDiscarded();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] reason_ The reason, why the call has ended.
   * \param[in] need_rating_ True, if the call rating should be sent to the server.
   * \param[in] need_debug_information_ True, if the call debug information should be sent to the server.
   */
  callStateDiscarded(object_ptr<CallDiscardReason> &&reason_, bool need_rating_, bool need_debug_information_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -190853167;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<callStateDiscarded> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * The call has ended with an error.
 */
class callStateError final : public CallState {
 public:
  static jclass Class;
  /// Error. An error with the code 4005000 will be returned if an outgoing call is missed because of an expired timeout.
  object_ptr<error> error_;
  static jfieldID error_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  callStateError();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] error_ Error. An error with the code 4005000 will be returned if an outgoing call is missed because of an expired timeout.
   */
  explicit callStateError(object_ptr<error> &&error_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -975215467;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<callStateError> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Contains a bot's answer to a callback query.
 */
class callbackQueryAnswer final : public Object {
 public:
  static jclass Class;
  /// Text of the answer.
  std::string text_;
  static jfieldID text_fieldID;
  /// True, if an alert should be shown to the user instead of a toast notification.
  bool show_alert_;
  static jfieldID show_alert_fieldID;
  /// URL to be opened.
  std::string url_;
  static jfieldID url_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  callbackQueryAnswer();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] text_ Text of the answer.
   * \param[in] show_alert_ True, if an alert should be shown to the user instead of a toast notification.
   * \param[in] url_ URL to be opened.
   */
  callbackQueryAnswer(std::string const &text_, bool show_alert_, std::string const &url_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 360867933;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<callbackQueryAnswer> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * This class is an abstract base class.
 * Represents a payload of a callback query.
 */
class CallbackQueryPayload: public Object {
 public:
  static jclass Class;

  static object_ptr<CallbackQueryPayload> fetch(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * The payload from a general callback button.
 */
class callbackQueryPayloadData final : public CallbackQueryPayload {
 public:
  static jclass Class;
  /// Data that was attached to the callback button.
  std::string data_;
  static jfieldID data_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  callbackQueryPayloadData();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] data_ Data that was attached to the callback button.
   */
  explicit callbackQueryPayloadData(std::string const &data_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1977729946;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<callbackQueryPayloadData> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * The payload from a game callback button.
 */
class callbackQueryPayloadGame final : public CallbackQueryPayload {
 public:
  static jclass Class;
  /// A short name of the game that was attached to the callback button.
  std::string game_short_name_;
  static jfieldID game_short_name_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  callbackQueryPayloadGame();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] game_short_name_ A short name of the game that was attached to the callback button.
   */
  explicit callbackQueryPayloadGame(std::string const &game_short_name_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1303571512;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<callbackQueryPayloadGame> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A chat. (Can be a private chat, basic group, supergroup, or secret chat.)
 */
class chat final : public Object {
 public:
  static jclass Class;
  /// Chat unique identifier.
  std::int64_t id_;
  static jfieldID id_fieldID;
  /// Type of the chat.
  object_ptr<ChatType> type_;
  static jfieldID type_fieldID;
  /// Chat title.
  std::string title_;
  static jfieldID title_fieldID;
  /// Chat photo; may be null.
  object_ptr<chatPhoto> photo_;
  static jfieldID photo_fieldID;
  /// Last message in the chat; may be null.
  object_ptr<message> last_message_;
  static jfieldID last_message_fieldID;
  /// Descending parameter by which chats are sorted in the main chat list. If the order number of two chats is the same, they must be sorted in descending order by ID. If 0, the position of the chat in the list is undetermined.
  std::int64_t order_;
  static jfieldID order_fieldID;
  /// True, if the chat is pinned.
  bool is_pinned_;
  static jfieldID is_pinned_fieldID;
  /// True, if the chat is marked as unread.
  bool is_marked_as_unread_;
  static jfieldID is_marked_as_unread_fieldID;
  /// True, if the chat is sponsored by the user's MTProxy server.
  bool is_sponsored_;
  static jfieldID is_sponsored_fieldID;
  /// True, if the chat can be reported to Telegram moderators through reportChat.
  bool can_be_reported_;
  static jfieldID can_be_reported_fieldID;
  /// Default value of the disable_notification parameter, used when a message is sent to the chat.
  bool default_disable_notification_;
  static jfieldID default_disable_notification_fieldID;
  /// Number of unread messages in the chat.
  std::int32_t unread_count_;
  static jfieldID unread_count_fieldID;
  /// Identifier of the last read incoming message.
  std::int64_t last_read_inbox_message_id_;
  static jfieldID last_read_inbox_message_id_fieldID;
  /// Identifier of the last read outgoing message.
  std::int64_t last_read_outbox_message_id_;
  static jfieldID last_read_outbox_message_id_fieldID;
  /// Number of unread messages with a mention/reply in the chat.
  std::int32_t unread_mention_count_;
  static jfieldID unread_mention_count_fieldID;
  /// Notification settings for this chat.
  object_ptr<chatNotificationSettings> notification_settings_;
  static jfieldID notification_settings_fieldID;
  /// Identifier of the message from which reply markup needs to be used; 0 if there is no default custom reply markup in the chat.
  std::int64_t reply_markup_message_id_;
  static jfieldID reply_markup_message_id_fieldID;
  /// A draft of a message in the chat; may be null.
  object_ptr<draftMessage> draft_message_;
  static jfieldID draft_message_fieldID;
  /// Contains client-specific data associated with the chat. (For example, the chat position or local chat notification settings can be stored here.) Persistent if a message database is used.
  std::string client_data_;
  static jfieldID client_data_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  chat();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] id_ Chat unique identifier.
   * \param[in] type_ Type of the chat.
   * \param[in] title_ Chat title.
   * \param[in] photo_ Chat photo; may be null.
   * \param[in] last_message_ Last message in the chat; may be null.
   * \param[in] order_ Descending parameter by which chats are sorted in the main chat list. If the order number of two chats is the same, they must be sorted in descending order by ID. If 0, the position of the chat in the list is undetermined.
   * \param[in] is_pinned_ True, if the chat is pinned.
   * \param[in] is_marked_as_unread_ True, if the chat is marked as unread.
   * \param[in] is_sponsored_ True, if the chat is sponsored by the user's MTProxy server.
   * \param[in] can_be_reported_ True, if the chat can be reported to Telegram moderators through reportChat.
   * \param[in] default_disable_notification_ Default value of the disable_notification parameter, used when a message is sent to the chat.
   * \param[in] unread_count_ Number of unread messages in the chat.
   * \param[in] last_read_inbox_message_id_ Identifier of the last read incoming message.
   * \param[in] last_read_outbox_message_id_ Identifier of the last read outgoing message.
   * \param[in] unread_mention_count_ Number of unread messages with a mention/reply in the chat.
   * \param[in] notification_settings_ Notification settings for this chat.
   * \param[in] reply_markup_message_id_ Identifier of the message from which reply markup needs to be used; 0 if there is no default custom reply markup in the chat.
   * \param[in] draft_message_ A draft of a message in the chat; may be null.
   * \param[in] client_data_ Contains client-specific data associated with the chat. (For example, the chat position or local chat notification settings can be stored here.) Persistent if a message database is used.
   */
  chat(std::int64_t id_, object_ptr<ChatType> &&type_, std::string const &title_, object_ptr<chatPhoto> &&photo_, object_ptr<message> &&last_message_, std::int64_t order_, bool is_pinned_, bool is_marked_as_unread_, bool is_sponsored_, bool can_be_reported_, bool default_disable_notification_, std::int32_t unread_count_, std::int64_t last_read_inbox_message_id_, std::int64_t last_read_outbox_message_id_, std::int32_t unread_mention_count_, object_ptr<chatNotificationSettings> &&notification_settings_, std::int64_t reply_markup_message_id_, object_ptr<draftMessage> &&draft_message_, std::string const &client_data_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -4728182;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<chat> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * This class is an abstract base class.
 * Describes the different types of activity in a chat.
 */
class ChatAction: public Object {
 public:
  static jclass Class;

  static object_ptr<ChatAction> fetch(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * The user is typing a message.
 */
class chatActionTyping final : public ChatAction {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  chatActionTyping();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 380122167;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<chatActionTyping> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * The user is recording a video.
 */
class chatActionRecordingVideo final : public ChatAction {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  chatActionRecordingVideo();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 216553362;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<chatActionRecordingVideo> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * The user is uploading a video.
 */
class chatActionUploadingVideo final : public ChatAction {
 public:
  static jclass Class;
  /// Upload progress, as a percentage.
  std::int32_t progress_;
  static jfieldID progress_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  chatActionUploadingVideo();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] progress_ Upload progress, as a percentage.
   */
  explicit chatActionUploadingVideo(std::int32_t progress_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1234185270;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<chatActionUploadingVideo> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * The user is recording a voice note.
 */
class chatActionRecordingVoiceNote final : public ChatAction {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  chatActionRecordingVoiceNote();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -808850058;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<chatActionRecordingVoiceNote> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * The user is uploading a voice note.
 */
class chatActionUploadingVoiceNote final : public ChatAction {
 public:
  static jclass Class;
  /// Upload progress, as a percentage.
  std::int32_t progress_;
  static jfieldID progress_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  chatActionUploadingVoiceNote();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] progress_ Upload progress, as a percentage.
   */
  explicit chatActionUploadingVoiceNote(std::int32_t progress_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -613643666;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<chatActionUploadingVoiceNote> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * The user is uploading a photo.
 */
class chatActionUploadingPhoto final : public ChatAction {
 public:
  static jclass Class;
  /// Upload progress, as a percentage.
  std::int32_t progress_;
  static jfieldID progress_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  chatActionUploadingPhoto();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] progress_ Upload progress, as a percentage.
   */
  explicit chatActionUploadingPhoto(std::int32_t progress_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 654240583;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<chatActionUploadingPhoto> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * The user is uploading a document.
 */
class chatActionUploadingDocument final : public ChatAction {
 public:
  static jclass Class;
  /// Upload progress, as a percentage.
  std::int32_t progress_;
  static jfieldID progress_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  chatActionUploadingDocument();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] progress_ Upload progress, as a percentage.
   */
  explicit chatActionUploadingDocument(std::int32_t progress_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 167884362;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<chatActionUploadingDocument> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * The user is picking a location or venue to send.
 */
class chatActionChoosingLocation final : public ChatAction {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  chatActionChoosingLocation();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -2017893596;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<chatActionChoosingLocation> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * The user is picking a contact to send.
 */
class chatActionChoosingContact final : public ChatAction {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  chatActionChoosingContact();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1222507496;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<chatActionChoosingContact> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * The user has started to play a game.
 */
class chatActionStartPlayingGame final : public ChatAction {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  chatActionStartPlayingGame();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -865884164;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<chatActionStartPlayingGame> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * The user is recording a video note.
 */
class chatActionRecordingVideoNote final : public ChatAction {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  chatActionRecordingVideoNote();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 16523393;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<chatActionRecordingVideoNote> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * The user is uploading a video note.
 */
class chatActionUploadingVideoNote final : public ChatAction {
 public:
  static jclass Class;
  /// Upload progress, as a percentage.
  std::int32_t progress_;
  static jfieldID progress_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  chatActionUploadingVideoNote();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] progress_ Upload progress, as a percentage.
   */
  explicit chatActionUploadingVideoNote(std::int32_t progress_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1172364918;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<chatActionUploadingVideoNote> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * The user has cancelled the previous action.
 */
class chatActionCancel final : public ChatAction {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  chatActionCancel();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1160523958;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<chatActionCancel> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Represents a chat event.
 */
class chatEvent final : public Object {
 public:
  static jclass Class;
  /// Chat event identifier.
  std::int64_t id_;
  static jfieldID id_fieldID;
  /// Point in time (Unix timestamp) when the event happened.
  std::int32_t date_;
  static jfieldID date_fieldID;
  /// Identifier of the user who performed the action that triggered the event.
  std::int32_t user_id_;
  static jfieldID user_id_fieldID;
  /// Action performed by the user.
  object_ptr<ChatEventAction> action_;
  static jfieldID action_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  chatEvent();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] id_ Chat event identifier.
   * \param[in] date_ Point in time (Unix timestamp) when the event happened.
   * \param[in] user_id_ Identifier of the user who performed the action that triggered the event.
   * \param[in] action_ Action performed by the user.
   */
  chatEvent(std::int64_t id_, std::int32_t date_, std::int32_t user_id_, object_ptr<ChatEventAction> &&action_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -609912404;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<chatEvent> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * This class is an abstract base class.
 * Represents a chat event.
 */
class ChatEventAction: public Object {
 public:
  static jclass Class;

  static object_ptr<ChatEventAction> fetch(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A message was edited.
 */
class chatEventMessageEdited final : public ChatEventAction {
 public:
  static jclass Class;
  /// The original message before the edit.
  object_ptr<message> old_message_;
  static jfieldID old_message_fieldID;
  /// The message after it was edited.
  object_ptr<message> new_message_;
  static jfieldID new_message_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  chatEventMessageEdited();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] old_message_ The original message before the edit.
   * \param[in] new_message_ The message after it was edited.
   */
  chatEventMessageEdited(object_ptr<message> &&old_message_, object_ptr<message> &&new_message_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -430967304;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<chatEventMessageEdited> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A message was deleted.
 */
class chatEventMessageDeleted final : public ChatEventAction {
 public:
  static jclass Class;
  /// Deleted message.
  object_ptr<message> message_;
  static jfieldID message_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  chatEventMessageDeleted();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] message_ Deleted message.
   */
  explicit chatEventMessageDeleted(object_ptr<message> &&message_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -892974601;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<chatEventMessageDeleted> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A message was pinned.
 */
class chatEventMessagePinned final : public ChatEventAction {
 public:
  static jclass Class;
  /// Pinned message.
  object_ptr<message> message_;
  static jfieldID message_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  chatEventMessagePinned();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] message_ Pinned message.
   */
  explicit chatEventMessagePinned(object_ptr<message> &&message_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 438742298;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<chatEventMessagePinned> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A message was unpinned.
 */
class chatEventMessageUnpinned final : public ChatEventAction {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  chatEventMessageUnpinned();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 2002594849;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<chatEventMessageUnpinned> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A new member joined the chat.
 */
class chatEventMemberJoined final : public ChatEventAction {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  chatEventMemberJoined();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -235468508;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<chatEventMemberJoined> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A member left the chat.
 */
class chatEventMemberLeft final : public ChatEventAction {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  chatEventMemberLeft();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -948420593;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<chatEventMemberLeft> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A new chat member was invited.
 */
class chatEventMemberInvited final : public ChatEventAction {
 public:
  static jclass Class;
  /// New member user identifier.
  std::int32_t user_id_;
  static jfieldID user_id_fieldID;
  /// New member status.
  object_ptr<ChatMemberStatus> status_;
  static jfieldID status_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  chatEventMemberInvited();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] user_id_ New member user identifier.
   * \param[in] status_ New member status.
   */
  chatEventMemberInvited(std::int32_t user_id_, object_ptr<ChatMemberStatus> &&status_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -2093688706;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<chatEventMemberInvited> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A chat member has gained/lost administrator status, or the list of their administrator privileges has changed.
 */
class chatEventMemberPromoted final : public ChatEventAction {
 public:
  static jclass Class;
  /// Chat member user identifier.
  std::int32_t user_id_;
  static jfieldID user_id_fieldID;
  /// Previous status of the chat member.
  object_ptr<ChatMemberStatus> old_status_;
  static jfieldID old_status_fieldID;
  /// New status of the chat member.
  object_ptr<ChatMemberStatus> new_status_;
  static jfieldID new_status_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  chatEventMemberPromoted();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] user_id_ Chat member user identifier.
   * \param[in] old_status_ Previous status of the chat member.
   * \param[in] new_status_ New status of the chat member.
   */
  chatEventMemberPromoted(std::int32_t user_id_, object_ptr<ChatMemberStatus> &&old_status_, object_ptr<ChatMemberStatus> &&new_status_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1887176186;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<chatEventMemberPromoted> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A chat member was restricted/unrestricted or banned/unbanned, or the list of their restrictions has changed.
 */
class chatEventMemberRestricted final : public ChatEventAction {
 public:
  static jclass Class;
  /// Chat member user identifier.
  std::int32_t user_id_;
  static jfieldID user_id_fieldID;
  /// Previous status of the chat member.
  object_ptr<ChatMemberStatus> old_status_;
  static jfieldID old_status_fieldID;
  /// New status of the chat member.
  object_ptr<ChatMemberStatus> new_status_;
  static jfieldID new_status_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  chatEventMemberRestricted();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] user_id_ Chat member user identifier.
   * \param[in] old_status_ Previous status of the chat member.
   * \param[in] new_status_ New status of the chat member.
   */
  chatEventMemberRestricted(std::int32_t user_id_, object_ptr<ChatMemberStatus> &&old_status_, object_ptr<ChatMemberStatus> &&new_status_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 584946294;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<chatEventMemberRestricted> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * The chat title was changed.
 */
class chatEventTitleChanged final : public ChatEventAction {
 public:
  static jclass Class;
  /// Previous chat title.
  std::string old_title_;
  static jfieldID old_title_fieldID;
  /// New chat title.
  std::string new_title_;
  static jfieldID new_title_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  chatEventTitleChanged();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] old_title_ Previous chat title.
   * \param[in] new_title_ New chat title.
   */
  chatEventTitleChanged(std::string const &old_title_, std::string const &new_title_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1134103250;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<chatEventTitleChanged> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * The chat description was changed.
 */
class chatEventDescriptionChanged final : public ChatEventAction {
 public:
  static jclass Class;
  /// Previous chat description.
  std::string old_description_;
  static jfieldID old_description_fieldID;
  /// New chat description.
  std::string new_description_;
  static jfieldID new_description_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  chatEventDescriptionChanged();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] old_description_ Previous chat description.
   * \param[in] new_description_ New chat description.
   */
  chatEventDescriptionChanged(std::string const &old_description_, std::string const &new_description_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 39112478;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<chatEventDescriptionChanged> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * The chat username was changed.
 */
class chatEventUsernameChanged final : public ChatEventAction {
 public:
  static jclass Class;
  /// Previous chat username.
  std::string old_username_;
  static jfieldID old_username_fieldID;
  /// New chat username.
  std::string new_username_;
  static jfieldID new_username_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  chatEventUsernameChanged();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] old_username_ Previous chat username.
   * \param[in] new_username_ New chat username.
   */
  chatEventUsernameChanged(std::string const &old_username_, std::string const &new_username_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1728558443;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<chatEventUsernameChanged> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * The chat photo was changed.
 */
class chatEventPhotoChanged final : public ChatEventAction {
 public:
  static jclass Class;
  /// Previous chat photo value; may be null.
  object_ptr<chatPhoto> old_photo_;
  static jfieldID old_photo_fieldID;
  /// New chat photo value; may be null.
  object_ptr<chatPhoto> new_photo_;
  static jfieldID new_photo_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  chatEventPhotoChanged();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] old_photo_ Previous chat photo value; may be null.
   * \param[in] new_photo_ New chat photo value; may be null.
   */
  chatEventPhotoChanged(object_ptr<chatPhoto> &&old_photo_, object_ptr<chatPhoto> &&new_photo_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -811572541;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<chatEventPhotoChanged> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * The anyone_can_invite setting of a supergroup chat was toggled.
 */
class chatEventInvitesToggled final : public ChatEventAction {
 public:
  static jclass Class;
  /// New value of anyone_can_invite.
  bool anyone_can_invite_;
  static jfieldID anyone_can_invite_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  chatEventInvitesToggled();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] anyone_can_invite_ New value of anyone_can_invite.
   */
  explicit chatEventInvitesToggled(bool anyone_can_invite_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 568706937;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<chatEventInvitesToggled> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * The sign_messages setting of a channel was toggled.
 */
class chatEventSignMessagesToggled final : public ChatEventAction {
 public:
  static jclass Class;
  /// New value of sign_messages.
  bool sign_messages_;
  static jfieldID sign_messages_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  chatEventSignMessagesToggled();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] sign_messages_ New value of sign_messages.
   */
  explicit chatEventSignMessagesToggled(bool sign_messages_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1313265634;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<chatEventSignMessagesToggled> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * The supergroup sticker set was changed.
 */
class chatEventStickerSetChanged final : public ChatEventAction {
 public:
  static jclass Class;
  /// Previous identifier of the chat sticker set; 0 if none.
  std::int64_t old_sticker_set_id_;
  static jfieldID old_sticker_set_id_fieldID;
  /// New identifier of the chat sticker set; 0 if none.
  std::int64_t new_sticker_set_id_;
  static jfieldID new_sticker_set_id_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  chatEventStickerSetChanged();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] old_sticker_set_id_ Previous identifier of the chat sticker set; 0 if none.
   * \param[in] new_sticker_set_id_ New identifier of the chat sticker set; 0 if none.
   */
  chatEventStickerSetChanged(std::int64_t old_sticker_set_id_, std::int64_t new_sticker_set_id_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1243130481;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<chatEventStickerSetChanged> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * The is_all_history_available setting of a supergroup was toggled.
 */
class chatEventIsAllHistoryAvailableToggled final : public ChatEventAction {
 public:
  static jclass Class;
  /// New value of is_all_history_available.
  bool is_all_history_available_;
  static jfieldID is_all_history_available_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  chatEventIsAllHistoryAvailableToggled();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] is_all_history_available_ New value of is_all_history_available.
   */
  explicit chatEventIsAllHistoryAvailableToggled(bool is_all_history_available_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1599063019;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<chatEventIsAllHistoryAvailableToggled> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Represents a set of filters used to obtain a chat event log.
 */
class chatEventLogFilters final : public Object {
 public:
  static jclass Class;
  /// True, if message edits should be returned.
  bool message_edits_;
  static jfieldID message_edits_fieldID;
  /// True, if message deletions should be returned.
  bool message_deletions_;
  static jfieldID message_deletions_fieldID;
  /// True, if pin/unpin events should be returned.
  bool message_pins_;
  static jfieldID message_pins_fieldID;
  /// True, if members joining events should be returned.
  bool member_joins_;
  static jfieldID member_joins_fieldID;
  /// True, if members leaving events should be returned.
  bool member_leaves_;
  static jfieldID member_leaves_fieldID;
  /// True, if invited member events should be returned.
  bool member_invites_;
  static jfieldID member_invites_fieldID;
  /// True, if member promotion/demotion events should be returned.
  bool member_promotions_;
  static jfieldID member_promotions_fieldID;
  /// True, if member restricted/unrestricted/banned/unbanned events should be returned.
  bool member_restrictions_;
  static jfieldID member_restrictions_fieldID;
  /// True, if changes in chat information should be returned.
  bool info_changes_;
  static jfieldID info_changes_fieldID;
  /// True, if changes in chat settings should be returned.
  bool setting_changes_;
  static jfieldID setting_changes_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  chatEventLogFilters();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] message_edits_ True, if message edits should be returned.
   * \param[in] message_deletions_ True, if message deletions should be returned.
   * \param[in] message_pins_ True, if pin/unpin events should be returned.
   * \param[in] member_joins_ True, if members joining events should be returned.
   * \param[in] member_leaves_ True, if members leaving events should be returned.
   * \param[in] member_invites_ True, if invited member events should be returned.
   * \param[in] member_promotions_ True, if member promotion/demotion events should be returned.
   * \param[in] member_restrictions_ True, if member restricted/unrestricted/banned/unbanned events should be returned.
   * \param[in] info_changes_ True, if changes in chat information should be returned.
   * \param[in] setting_changes_ True, if changes in chat settings should be returned.
   */
  chatEventLogFilters(bool message_edits_, bool message_deletions_, bool message_pins_, bool member_joins_, bool member_leaves_, bool member_invites_, bool member_promotions_, bool member_restrictions_, bool info_changes_, bool setting_changes_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 941939684;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<chatEventLogFilters> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Contains a list of chat events.
 */
class chatEvents final : public Object {
 public:
  static jclass Class;
  /// List of events.
  std::vector<object_ptr<chatEvent>> events_;
  static jfieldID events_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  chatEvents();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] events_ List of events.
   */
  explicit chatEvents(std::vector<object_ptr<chatEvent>> &&events_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -585329664;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<chatEvents> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Contains a chat invite link.
 */
class chatInviteLink final : public Object {
 public:
  static jclass Class;
  /// Chat invite link.
  std::string invite_link_;
  static jfieldID invite_link_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  chatInviteLink();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] invite_link_ Chat invite link.
   */
  explicit chatInviteLink(std::string const &invite_link_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -882072492;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<chatInviteLink> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Contains information about a chat invite link.
 */
class chatInviteLinkInfo final : public Object {
 public:
  static jclass Class;
  /// Chat identifier of the invite link; 0 if the user is not a member of this chat.
  std::int64_t chat_id_;
  static jfieldID chat_id_fieldID;
  /// Contains information about the type of the chat.
  object_ptr<ChatType> type_;
  static jfieldID type_fieldID;
  /// Title of the chat.
  std::string title_;
  static jfieldID title_fieldID;
  /// Chat photo; may be null.
  object_ptr<chatPhoto> photo_;
  static jfieldID photo_fieldID;
  /// Number of members.
  std::int32_t member_count_;
  static jfieldID member_count_fieldID;
  /// User identifiers of some chat members that may be known to the current user.
  std::vector<std::int32_t> member_user_ids_;
  static jfieldID member_user_ids_fieldID;
  /// True, if the chat is a public supergroup or channel with a username.
  bool is_public_;
  static jfieldID is_public_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  chatInviteLinkInfo();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] chat_id_ Chat identifier of the invite link; 0 if the user is not a member of this chat.
   * \param[in] type_ Contains information about the type of the chat.
   * \param[in] title_ Title of the chat.
   * \param[in] photo_ Chat photo; may be null.
   * \param[in] member_count_ Number of members.
   * \param[in] member_user_ids_ User identifiers of some chat members that may be known to the current user.
   * \param[in] is_public_ True, if the chat is a public supergroup or channel with a username.
   */
  chatInviteLinkInfo(std::int64_t chat_id_, object_ptr<ChatType> &&type_, std::string const &title_, object_ptr<chatPhoto> &&photo_, std::int32_t member_count_, std::vector<std::int32_t> &&member_user_ids_, bool is_public_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -323394424;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<chatInviteLinkInfo> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A user with information about joining/leaving a chat.
 */
class chatMember final : public Object {
 public:
  static jclass Class;
  /// User identifier of the chat member.
  std::int32_t user_id_;
  static jfieldID user_id_fieldID;
  /// Identifier of a user that invited/promoted/banned this member in the chat; 0 if unknown.
  std::int32_t inviter_user_id_;
  static jfieldID inviter_user_id_fieldID;
  /// Point in time (Unix timestamp) when the user joined a chat.
  std::int32_t joined_chat_date_;
  static jfieldID joined_chat_date_fieldID;
  /// Status of the member in the chat.
  object_ptr<ChatMemberStatus> status_;
  static jfieldID status_fieldID;
  /// If the user is a bot, information about the bot; may be null. Can be null even for a bot if the bot is not a chat member.
  object_ptr<botInfo> bot_info_;
  static jfieldID bot_info_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  chatMember();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] user_id_ User identifier of the chat member.
   * \param[in] inviter_user_id_ Identifier of a user that invited/promoted/banned this member in the chat; 0 if unknown.
   * \param[in] joined_chat_date_ Point in time (Unix timestamp) when the user joined a chat.
   * \param[in] status_ Status of the member in the chat.
   * \param[in] bot_info_ If the user is a bot, information about the bot; may be null. Can be null even for a bot if the bot is not a chat member.
   */
  chatMember(std::int32_t user_id_, std::int32_t inviter_user_id_, std::int32_t joined_chat_date_, object_ptr<ChatMemberStatus> &&status_, object_ptr<botInfo> &&bot_info_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -806137076;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<chatMember> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * This class is an abstract base class.
 * Provides information about the status of a member in a chat.
 */
class ChatMemberStatus: public Object {
 public:
  static jclass Class;

  static object_ptr<ChatMemberStatus> fetch(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * The user is the creator of a chat and has all the administrator privileges.
 */
class chatMemberStatusCreator final : public ChatMemberStatus {
 public:
  static jclass Class;
  /// True, if the user is a member of the chat.
  bool is_member_;
  static jfieldID is_member_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  chatMemberStatusCreator();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] is_member_ True, if the user is a member of the chat.
   */
  explicit chatMemberStatusCreator(bool is_member_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1756320508;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<chatMemberStatusCreator> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * The user is a member of a chat and has some additional privileges. In basic groups, administrators can edit and delete messages sent by others, add new members, and ban unprivileged members. In supergroups and channels, there are more detailed options for administrator privileges.
 */
class chatMemberStatusAdministrator final : public ChatMemberStatus {
 public:
  static jclass Class;
  /// True, if the current user can edit the administrator privileges for the called user.
  bool can_be_edited_;
  static jfieldID can_be_edited_fieldID;
  /// True, if the administrator can change the chat title, photo, and other settings.
  bool can_change_info_;
  static jfieldID can_change_info_fieldID;
  /// True, if the administrator can create channel posts; applicable to channels only.
  bool can_post_messages_;
  static jfieldID can_post_messages_fieldID;
  /// True, if the administrator can edit messages of other users and pin messages; applicable to channels only.
  bool can_edit_messages_;
  static jfieldID can_edit_messages_fieldID;
  /// True, if the administrator can delete messages of other users.
  bool can_delete_messages_;
  static jfieldID can_delete_messages_fieldID;
  /// True, if the administrator can invite new users to the chat.
  bool can_invite_users_;
  static jfieldID can_invite_users_fieldID;
  /// True, if the administrator can restrict, ban, or unban chat members.
  bool can_restrict_members_;
  static jfieldID can_restrict_members_fieldID;
  /// True, if the administrator can pin messages; applicable to supergroups only.
  bool can_pin_messages_;
  static jfieldID can_pin_messages_fieldID;
  /// True, if the administrator can add new administrators with a subset of his own privileges or demote administrators that were directly or indirectly promoted by him.
  bool can_promote_members_;
  static jfieldID can_promote_members_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  chatMemberStatusAdministrator();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] can_be_edited_ True, if the current user can edit the administrator privileges for the called user.
   * \param[in] can_change_info_ True, if the administrator can change the chat title, photo, and other settings.
   * \param[in] can_post_messages_ True, if the administrator can create channel posts; applicable to channels only.
   * \param[in] can_edit_messages_ True, if the administrator can edit messages of other users and pin messages; applicable to channels only.
   * \param[in] can_delete_messages_ True, if the administrator can delete messages of other users.
   * \param[in] can_invite_users_ True, if the administrator can invite new users to the chat.
   * \param[in] can_restrict_members_ True, if the administrator can restrict, ban, or unban chat members.
   * \param[in] can_pin_messages_ True, if the administrator can pin messages; applicable to supergroups only.
   * \param[in] can_promote_members_ True, if the administrator can add new administrators with a subset of his own privileges or demote administrators that were directly or indirectly promoted by him.
   */
  chatMemberStatusAdministrator(bool can_be_edited_, bool can_change_info_, bool can_post_messages_, bool can_edit_messages_, bool can_delete_messages_, bool can_invite_users_, bool can_restrict_members_, bool can_pin_messages_, bool can_promote_members_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 45106688;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<chatMemberStatusAdministrator> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * The user is a member of a chat, without any additional privileges or restrictions.
 */
class chatMemberStatusMember final : public ChatMemberStatus {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  chatMemberStatusMember();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 844723285;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<chatMemberStatusMember> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * The user is under certain restrictions in the chat. Not supported in basic groups and channels.
 */
class chatMemberStatusRestricted final : public ChatMemberStatus {
 public:
  static jclass Class;
  /// True, if the user is a member of the chat.
  bool is_member_;
  static jfieldID is_member_fieldID;
  /// Point in time (Unix timestamp) when restrictions will be lifted from the user; 0 if never. If the user is restricted for more than 366 days or for less than 30 seconds from the current time, the user is considered to be restricted forever.
  std::int32_t restricted_until_date_;
  static jfieldID restricted_until_date_fieldID;
  /// True, if the user can send text messages, contacts, locations, and venues.
  bool can_send_messages_;
  static jfieldID can_send_messages_fieldID;
  /// True, if the user can send audio files, documents, photos, videos, video notes, and voice notes. Implies can_send_messages permissions.
  bool can_send_media_messages_;
  static jfieldID can_send_media_messages_fieldID;
  /// True, if the user can send animations, games, and stickers and use inline bots. Implies can_send_media_messages permissions.
  bool can_send_other_messages_;
  static jfieldID can_send_other_messages_fieldID;
  /// True, if the user may add a web page preview to his messages. Implies can_send_messages permissions.
  bool can_add_web_page_previews_;
  static jfieldID can_add_web_page_previews_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  chatMemberStatusRestricted();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] is_member_ True, if the user is a member of the chat.
   * \param[in] restricted_until_date_ Point in time (Unix timestamp) when restrictions will be lifted from the user; 0 if never. If the user is restricted for more than 366 days or for less than 30 seconds from the current time, the user is considered to be restricted forever.
   * \param[in] can_send_messages_ True, if the user can send text messages, contacts, locations, and venues.
   * \param[in] can_send_media_messages_ True, if the user can send audio files, documents, photos, videos, video notes, and voice notes. Implies can_send_messages permissions.
   * \param[in] can_send_other_messages_ True, if the user can send animations, games, and stickers and use inline bots. Implies can_send_media_messages permissions.
   * \param[in] can_add_web_page_previews_ True, if the user may add a web page preview to his messages. Implies can_send_messages permissions.
   */
  chatMemberStatusRestricted(bool is_member_, std::int32_t restricted_until_date_, bool can_send_messages_, bool can_send_media_messages_, bool can_send_other_messages_, bool can_add_web_page_previews_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 2068116214;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<chatMemberStatusRestricted> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * The user is not a chat member.
 */
class chatMemberStatusLeft final : public ChatMemberStatus {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  chatMemberStatusLeft();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -5815259;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<chatMemberStatusLeft> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * The user was banned (and hence is not a member of the chat). Implies the user can't return to the chat or view messages.
 */
class chatMemberStatusBanned final : public ChatMemberStatus {
 public:
  static jclass Class;
  /// Point in time (Unix timestamp) when the user will be unbanned; 0 if never. If the user is banned for more than 366 days or for less than 30 seconds from the current time, the user is considered to be banned forever.
  std::int32_t banned_until_date_;
  static jfieldID banned_until_date_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  chatMemberStatusBanned();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] banned_until_date_ Point in time (Unix timestamp) when the user will be unbanned; 0 if never. If the user is banned for more than 366 days or for less than 30 seconds from the current time, the user is considered to be banned forever.
   */
  explicit chatMemberStatusBanned(std::int32_t banned_until_date_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1653518666;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<chatMemberStatusBanned> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Contains a list of chat members.
 */
class chatMembers final : public Object {
 public:
  static jclass Class;
  /// Approximate total count of chat members found.
  std::int32_t total_count_;
  static jfieldID total_count_fieldID;
  /// A list of chat members.
  std::vector<object_ptr<chatMember>> members_;
  static jfieldID members_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  chatMembers();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] total_count_ Approximate total count of chat members found.
   * \param[in] members_ A list of chat members.
   */
  chatMembers(std::int32_t total_count_, std::vector<object_ptr<chatMember>> &&members_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -497558622;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<chatMembers> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * This class is an abstract base class.
 * Specifies the kind of chat members to return in searchChatMembers.
 */
class ChatMembersFilter: public Object {
 public:
  static jclass Class;

  static object_ptr<ChatMembersFilter> fetch(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Returns the creator and administrators.
 */
class chatMembersFilterAdministrators final : public ChatMembersFilter {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  chatMembersFilterAdministrators();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1266893796;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<chatMembersFilterAdministrators> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Returns all chat members, including restricted chat members.
 */
class chatMembersFilterMembers final : public ChatMembersFilter {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  chatMembersFilterMembers();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 670504342;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<chatMembersFilterMembers> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Returns users under certain restrictions in the chat; can be used only by administrators in a supergroup.
 */
class chatMembersFilterRestricted final : public ChatMembersFilter {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  chatMembersFilterRestricted();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1256282813;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<chatMembersFilterRestricted> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Returns users banned from the chat; can be used only by administrators in a supergroup or in a channel.
 */
class chatMembersFilterBanned final : public ChatMembersFilter {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  chatMembersFilterBanned();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1863102648;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<chatMembersFilterBanned> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Returns bot members of the chat.
 */
class chatMembersFilterBots final : public ChatMembersFilter {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  chatMembersFilterBots();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1422567288;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<chatMembersFilterBots> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Contains information about notification settings for a chat.
 */
class chatNotificationSettings final : public Object {
 public:
  static jclass Class;
  /// If true, mute_for is ignored and the value for the relevant type of chat is used instead.
  bool use_default_mute_for_;
  static jfieldID use_default_mute_for_fieldID;
  /// Time left before notifications will be unmuted, in seconds.
  std::int32_t mute_for_;
  static jfieldID mute_for_fieldID;
  /// If true, sound is ignored and the value for the relevant type of chat is used instead.
  bool use_default_sound_;
  static jfieldID use_default_sound_fieldID;
  /// The name of an audio file to be used for notification sounds; only applies to iOS applications.
  std::string sound_;
  static jfieldID sound_fieldID;
  /// If true, show_preview is ignored and the value for the relevant type of chat is used instead.
  bool use_default_show_preview_;
  static jfieldID use_default_show_preview_fieldID;
  /// True, if message content should be displayed in notifications.
  bool show_preview_;
  static jfieldID show_preview_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  chatNotificationSettings();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] use_default_mute_for_ If true, mute_for is ignored and the value for the relevant type of chat is used instead.
   * \param[in] mute_for_ Time left before notifications will be unmuted, in seconds.
   * \param[in] use_default_sound_ If true, sound is ignored and the value for the relevant type of chat is used instead.
   * \param[in] sound_ The name of an audio file to be used for notification sounds; only applies to iOS applications.
   * \param[in] use_default_show_preview_ If true, show_preview is ignored and the value for the relevant type of chat is used instead.
   * \param[in] show_preview_ True, if message content should be displayed in notifications.
   */
  chatNotificationSettings(bool use_default_mute_for_, std::int32_t mute_for_, bool use_default_sound_, std::string const &sound_, bool use_default_show_preview_, bool show_preview_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1949924445;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<chatNotificationSettings> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Describes the photo of a chat.
 */
class chatPhoto final : public Object {
 public:
  static jclass Class;
  /// A small (160x160) chat photo.
  object_ptr<file> small_;
  static jfieldID small_fieldID;
  /// A big (640x640) chat photo.
  object_ptr<file> big_;
  static jfieldID big_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  chatPhoto();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] small_ A small (160x160) chat photo.
   * \param[in] big_ A big (640x640) chat photo.
   */
  chatPhoto(object_ptr<file> &&small_, object_ptr<file> &&big_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -217062456;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<chatPhoto> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * This class is an abstract base class.
 * Describes the reason why a chat is reported.
 */
class ChatReportReason: public Object {
 public:
  static jclass Class;

  static object_ptr<ChatReportReason> fetch(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * The chat contains spam messages.
 */
class chatReportReasonSpam final : public ChatReportReason {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  chatReportReasonSpam();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -510848863;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<chatReportReasonSpam> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * The chat promotes violence.
 */
class chatReportReasonViolence final : public ChatReportReason {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  chatReportReasonViolence();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1330235395;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<chatReportReasonViolence> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * The chat contains pornographic messages.
 */
class chatReportReasonPornography final : public ChatReportReason {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  chatReportReasonPornography();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 722614385;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<chatReportReasonPornography> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * The chat contains copyrighted content.
 */
class chatReportReasonCopyright final : public ChatReportReason {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  chatReportReasonCopyright();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 986898080;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<chatReportReasonCopyright> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A custom reason provided by the user.
 */
class chatReportReasonCustom final : public ChatReportReason {
 public:
  static jclass Class;
  /// Report text.
  std::string text_;
  static jfieldID text_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  chatReportReasonCustom();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] text_ Report text.
   */
  explicit chatReportReasonCustom(std::string const &text_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 544575454;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<chatReportReasonCustom> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Contains information about the availability of the &quot;Report spam&quot; action for a chat.
 */
class chatReportSpamState final : public Object {
 public:
  static jclass Class;
  /// True, if a prompt with the &quot;Report spam&quot; action should be shown to the user.
  bool can_report_spam_;
  static jfieldID can_report_spam_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  chatReportSpamState();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] can_report_spam_ True, if a prompt with the &quot;Report spam&quot; action should be shown to the user.
   */
  explicit chatReportSpamState(bool can_report_spam_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1919240972;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<chatReportSpamState> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * This class is an abstract base class.
 * Describes the type of a chat.
 */
class ChatType: public Object {
 public:
  static jclass Class;

  static object_ptr<ChatType> fetch(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * An ordinary chat with a user.
 */
class chatTypePrivate final : public ChatType {
 public:
  static jclass Class;
  /// User identifier.
  std::int32_t user_id_;
  static jfieldID user_id_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  chatTypePrivate();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] user_id_ User identifier.
   */
  explicit chatTypePrivate(std::int32_t user_id_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1700720838;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<chatTypePrivate> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A basic group (i.e., a chat with 0-200 other users).
 */
class chatTypeBasicGroup final : public ChatType {
 public:
  static jclass Class;
  /// Basic group identifier.
  std::int32_t basic_group_id_;
  static jfieldID basic_group_id_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  chatTypeBasicGroup();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] basic_group_id_ Basic group identifier.
   */
  explicit chatTypeBasicGroup(std::int32_t basic_group_id_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 21815278;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<chatTypeBasicGroup> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A supergroup (i.e. a chat with up to GetOption(&quot;supergroup_max_size&quot;) other users), or channel (with unlimited members).
 */
class chatTypeSupergroup final : public ChatType {
 public:
  static jclass Class;
  /// Supergroup or channel identifier.
  std::int32_t supergroup_id_;
  static jfieldID supergroup_id_fieldID;
  /// True, if the supergroup is a channel.
  bool is_channel_;
  static jfieldID is_channel_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  chatTypeSupergroup();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] supergroup_id_ Supergroup or channel identifier.
   * \param[in] is_channel_ True, if the supergroup is a channel.
   */
  chatTypeSupergroup(std::int32_t supergroup_id_, bool is_channel_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 955152366;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<chatTypeSupergroup> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A secret chat with a user.
 */
class chatTypeSecret final : public ChatType {
 public:
  static jclass Class;
  /// Secret chat identifier.
  std::int32_t secret_chat_id_;
  static jfieldID secret_chat_id_fieldID;
  /// User identifier of the secret chat peer.
  std::int32_t user_id_;
  static jfieldID user_id_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  chatTypeSecret();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] secret_chat_id_ Secret chat identifier.
   * \param[in] user_id_ User identifier of the secret chat peer.
   */
  chatTypeSecret(std::int32_t secret_chat_id_, std::int32_t user_id_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 136722563;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<chatTypeSecret> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Represents a list of chats.
 */
class chats final : public Object {
 public:
  static jclass Class;
  /// List of chat identifiers.
  std::vector<std::int64_t> chat_ids_;
  static jfieldID chat_ids_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  chats();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] chat_ids_ List of chat identifiers.
   */
  explicit chats(std::vector<std::int64_t> &&chat_ids_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1687756019;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<chats> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * This class is an abstract base class.
 * Represents result of checking whether a username can be set for a chat.
 */
class CheckChatUsernameResult: public Object {
 public:
  static jclass Class;

  static object_ptr<CheckChatUsernameResult> fetch(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * The username can be set.
 */
class checkChatUsernameResultOk final : public CheckChatUsernameResult {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  checkChatUsernameResultOk();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1498956964;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<checkChatUsernameResultOk> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * The username is invalid.
 */
class checkChatUsernameResultUsernameInvalid final : public CheckChatUsernameResult {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  checkChatUsernameResultUsernameInvalid();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -636979370;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<checkChatUsernameResultUsernameInvalid> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * The username is occupied.
 */
class checkChatUsernameResultUsernameOccupied final : public CheckChatUsernameResult {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  checkChatUsernameResultUsernameOccupied();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1320892201;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<checkChatUsernameResultUsernameOccupied> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * The user has too much public chats, one of them should be made private first.
 */
class checkChatUsernameResultPublicChatsTooMuch final : public CheckChatUsernameResult {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  checkChatUsernameResultPublicChatsTooMuch();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 858247741;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<checkChatUsernameResultPublicChatsTooMuch> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * The user can't be a member of a public supergroup.
 */
class checkChatUsernameResultPublicGroupsUnavailable final : public CheckChatUsernameResult {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  checkChatUsernameResultPublicGroupsUnavailable();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -51833641;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<checkChatUsernameResultPublicGroupsUnavailable> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Contains information about one website the current user is logged in with Telegram.
 */
class connectedWebsite final : public Object {
 public:
  static jclass Class;
  /// Website identifier.
  std::int64_t id_;
  static jfieldID id_fieldID;
  /// The domain name of the website.
  std::string domain_name_;
  static jfieldID domain_name_fieldID;
  /// User identifier of a bot linked with the website.
  std::int32_t bot_user_id_;
  static jfieldID bot_user_id_fieldID;
  /// The version of a browser used to log in.
  std::string browser_;
  static jfieldID browser_fieldID;
  /// Operating system the browser is running on.
  std::string platform_;
  static jfieldID platform_fieldID;
  /// Point in time (Unix timestamp) when the user was logged in.
  std::int32_t log_in_date_;
  static jfieldID log_in_date_fieldID;
  /// Point in time (Unix timestamp) when obtained authorization was last used.
  std::int32_t last_active_date_;
  static jfieldID last_active_date_fieldID;
  /// IP address from which the user was logged in, in human-readable format.
  std::string ip_;
  static jfieldID ip_fieldID;
  /// Human-readable description of a country and a region, from which the user was logged in, based on the IP address.
  std::string location_;
  static jfieldID location_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  connectedWebsite();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] id_ Website identifier.
   * \param[in] domain_name_ The domain name of the website.
   * \param[in] bot_user_id_ User identifier of a bot linked with the website.
   * \param[in] browser_ The version of a browser used to log in.
   * \param[in] platform_ Operating system the browser is running on.
   * \param[in] log_in_date_ Point in time (Unix timestamp) when the user was logged in.
   * \param[in] last_active_date_ Point in time (Unix timestamp) when obtained authorization was last used.
   * \param[in] ip_ IP address from which the user was logged in, in human-readable format.
   * \param[in] location_ Human-readable description of a country and a region, from which the user was logged in, based on the IP address.
   */
  connectedWebsite(std::int64_t id_, std::string const &domain_name_, std::int32_t bot_user_id_, std::string const &browser_, std::string const &platform_, std::int32_t log_in_date_, std::int32_t last_active_date_, std::string const &ip_, std::string const &location_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1538986855;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<connectedWebsite> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Contains a list of websites the current user is logged in with Telegram.
 */
class connectedWebsites final : public Object {
 public:
  static jclass Class;
  /// List of connected websites.
  std::vector<object_ptr<connectedWebsite>> websites_;
  static jfieldID websites_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  connectedWebsites();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] websites_ List of connected websites.
   */
  explicit connectedWebsites(std::vector<object_ptr<connectedWebsite>> &&websites_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1727949694;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<connectedWebsites> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * This class is an abstract base class.
 * Describes the current state of the connection to Telegram servers.
 */
class ConnectionState: public Object {
 public:
  static jclass Class;

  static object_ptr<ConnectionState> fetch(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Currently waiting for the network to become available. Use SetNetworkType to change the available network type.
 */
class connectionStateWaitingForNetwork final : public ConnectionState {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  connectionStateWaitingForNetwork();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1695405912;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<connectionStateWaitingForNetwork> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Currently establishing a connection with a proxy server.
 */
class connectionStateConnectingToProxy final : public ConnectionState {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  connectionStateConnectingToProxy();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -93187239;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<connectionStateConnectingToProxy> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Currently establishing a connection to the Telegram servers.
 */
class connectionStateConnecting final : public ConnectionState {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  connectionStateConnecting();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1298400670;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<connectionStateConnecting> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Downloading data received while the client was offline.
 */
class connectionStateUpdating final : public ConnectionState {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  connectionStateUpdating();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -188104009;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<connectionStateUpdating> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * There is a working connection to the Telegram servers.
 */
class connectionStateReady final : public ConnectionState {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  connectionStateReady();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 48608492;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<connectionStateReady> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Describes a user contact.
 */
class contact final : public Object {
 public:
  static jclass Class;
  /// Phone number of the user.
  std::string phone_number_;
  static jfieldID phone_number_fieldID;
  /// First name of the user; 1-255 characters in length.
  std::string first_name_;
  static jfieldID first_name_fieldID;
  /// Last name of the user.
  std::string last_name_;
  static jfieldID last_name_fieldID;
  /// Additional data about the user in a form of vCard; 0-2048 bytes in length.
  std::string vcard_;
  static jfieldID vcard_fieldID;
  /// Identifier of the user, if known; otherwise 0.
  std::int32_t user_id_;
  static jfieldID user_id_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  contact();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] phone_number_ Phone number of the user.
   * \param[in] first_name_ First name of the user; 1-255 characters in length.
   * \param[in] last_name_ Last name of the user.
   * \param[in] vcard_ Additional data about the user in a form of vCard; 0-2048 bytes in length.
   * \param[in] user_id_ Identifier of the user, if known; otherwise 0.
   */
  contact(std::string const &phone_number_, std::string const &first_name_, std::string const &last_name_, std::string const &vcard_, std::int32_t user_id_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1483002540;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<contact> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Contains a counter.
 */
class count final : public Object {
 public:
  static jclass Class;
  /// Count.
  std::int32_t count_;
  static jfieldID count_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  count();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] count_ Count.
   */
  explicit count(std::int32_t count_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1295577348;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<count> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Contains the result of a custom request.
 */
class customRequestResult final : public Object {
 public:
  static jclass Class;
  /// A JSON-serialized result.
  std::string result_;
  static jfieldID result_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  customRequestResult();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] result_ A JSON-serialized result.
   */
  explicit customRequestResult(std::string const &result_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -2009960452;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<customRequestResult> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Represents a date according to the Gregorian calendar.
 */
class date final : public Object {
 public:
  static jclass Class;
  /// Day of the month, 1-31.
  std::int32_t day_;
  static jfieldID day_fieldID;
  /// Month, 1-12.
  std::int32_t month_;
  static jfieldID month_fieldID;
  /// Year, 1-9999.
  std::int32_t year_;
  static jfieldID year_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  date();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] day_ Day of the month, 1-31.
   * \param[in] month_ Month, 1-12.
   * \param[in] year_ Year, 1-9999.
   */
  date(std::int32_t day_, std::int32_t month_, std::int32_t year_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -277956960;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<date> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * File with the date it was uploaded.
 */
class datedFile final : public Object {
 public:
  static jclass Class;
  /// The file.
  object_ptr<file> file_;
  static jfieldID file_fieldID;
  /// Point in time (Unix timestamp) when the file was uploaded.
  std::int32_t date_;
  static jfieldID date_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  datedFile();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] file_ The file.
   * \param[in] date_ Point in time (Unix timestamp) when the file was uploaded.
   */
  datedFile(object_ptr<file> &&file_, std::int32_t date_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1840795491;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<datedFile> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Contains information about a tg:// deep link.
 */
class deepLinkInfo final : public Object {
 public:
  static jclass Class;
  /// Text to be shown to the user.
  object_ptr<formattedText> text_;
  static jfieldID text_fieldID;
  /// True, if user should be asked to update the application.
  bool need_update_application_;
  static jfieldID need_update_application_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  deepLinkInfo();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] text_ Text to be shown to the user.
   * \param[in] need_update_application_ True, if user should be asked to update the application.
   */
  deepLinkInfo(object_ptr<formattedText> &&text_, bool need_update_application_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1864081662;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<deepLinkInfo> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * This class is an abstract base class.
 * Represents a data needed to subscribe for push notifications. To use specific push notification service, you must specify the correct application platform and upload valid server authentication data at https://my.telegram.org.
 */
class DeviceToken: public Object {
 public:
  static jclass Class;

  static object_ptr<DeviceToken> fetch(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A token for Google Cloud Messaging.
 */
class deviceTokenGoogleCloudMessaging final : public DeviceToken {
 public:
  static jclass Class;
  /// Device registration token; may be empty to de-register a device.
  std::string token_;
  static jfieldID token_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  deviceTokenGoogleCloudMessaging();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] token_ Device registration token; may be empty to de-register a device.
   */
  explicit deviceTokenGoogleCloudMessaging(std::string const &token_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1092220225;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<deviceTokenGoogleCloudMessaging> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A token for Apple Push Notification service.
 */
class deviceTokenApplePush final : public DeviceToken {
 public:
  static jclass Class;
  /// Device token; may be empty to de-register a device.
  std::string device_token_;
  static jfieldID device_token_fieldID;
  /// True, if App Sandbox is enabled.
  bool is_app_sandbox_;
  static jfieldID is_app_sandbox_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  deviceTokenApplePush();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] device_token_ Device token; may be empty to de-register a device.
   * \param[in] is_app_sandbox_ True, if App Sandbox is enabled.
   */
  deviceTokenApplePush(std::string const &device_token_, bool is_app_sandbox_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 387541955;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<deviceTokenApplePush> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A token for Apple Push Notification service VoIP notifications.
 */
class deviceTokenApplePushVoIP final : public DeviceToken {
 public:
  static jclass Class;
  /// Device token; may be empty to de-register a device.
  std::string device_token_;
  static jfieldID device_token_fieldID;
  /// True, if App Sandbox is enabled.
  bool is_app_sandbox_;
  static jfieldID is_app_sandbox_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  deviceTokenApplePushVoIP();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] device_token_ Device token; may be empty to de-register a device.
   * \param[in] is_app_sandbox_ True, if App Sandbox is enabled.
   */
  deviceTokenApplePushVoIP(std::string const &device_token_, bool is_app_sandbox_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -327676505;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<deviceTokenApplePushVoIP> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A token for Windows Push Notification Services.
 */
class deviceTokenWindowsPush final : public DeviceToken {
 public:
  static jclass Class;
  /// The access token that will be used to send notifications; may be empty to de-register a device.
  std::string access_token_;
  static jfieldID access_token_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  deviceTokenWindowsPush();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] access_token_ The access token that will be used to send notifications; may be empty to de-register a device.
   */
  explicit deviceTokenWindowsPush(std::string const &access_token_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1410514289;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<deviceTokenWindowsPush> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A token for Microsoft Push Notification Service.
 */
class deviceTokenMicrosoftPush final : public DeviceToken {
 public:
  static jclass Class;
  /// Push notification channel URI; may be empty to de-register a device.
  std::string channel_uri_;
  static jfieldID channel_uri_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  deviceTokenMicrosoftPush();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] channel_uri_ Push notification channel URI; may be empty to de-register a device.
   */
  explicit deviceTokenMicrosoftPush(std::string const &channel_uri_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1224269900;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<deviceTokenMicrosoftPush> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A token for Microsoft Push Notification Service VoIP channel.
 */
class deviceTokenMicrosoftPushVoIP final : public DeviceToken {
 public:
  static jclass Class;
  /// Push notification channel URI; may be empty to de-register a device.
  std::string channel_uri_;
  static jfieldID channel_uri_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  deviceTokenMicrosoftPushVoIP();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] channel_uri_ Push notification channel URI; may be empty to de-register a device.
   */
  explicit deviceTokenMicrosoftPushVoIP(std::string const &channel_uri_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -785603759;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<deviceTokenMicrosoftPushVoIP> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A token for web Push API.
 */
class deviceTokenWebPush final : public DeviceToken {
 public:
  static jclass Class;
  /// Absolute URL exposed by the push service where the application server can send push messages; may be empty to de-register a device.
  std::string endpoint_;
  static jfieldID endpoint_fieldID;
  /// Base64url-encoded P-256 elliptic curve Diffie-Hellman public key.
  std::string p256dh_base64url_;
  static jfieldID p256dh_base64url_fieldID;
  /// Base64url-encoded authentication secret.
  std::string auth_base64url_;
  static jfieldID auth_base64url_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  deviceTokenWebPush();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] endpoint_ Absolute URL exposed by the push service where the application server can send push messages; may be empty to de-register a device.
   * \param[in] p256dh_base64url_ Base64url-encoded P-256 elliptic curve Diffie-Hellman public key.
   * \param[in] auth_base64url_ Base64url-encoded authentication secret.
   */
  deviceTokenWebPush(std::string const &endpoint_, std::string const &p256dh_base64url_, std::string const &auth_base64url_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1694507273;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<deviceTokenWebPush> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A token for Simple Push API for Firefox OS.
 */
class deviceTokenSimplePush final : public DeviceToken {
 public:
  static jclass Class;
  /// Absolute URL exposed by the push service where the application server can send push messages; may be empty to de-register a device.
  std::string endpoint_;
  static jfieldID endpoint_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  deviceTokenSimplePush();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] endpoint_ Absolute URL exposed by the push service where the application server can send push messages; may be empty to de-register a device.
   */
  explicit deviceTokenSimplePush(std::string const &endpoint_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 49584736;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<deviceTokenSimplePush> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A token for Ubuntu Push Client service.
 */
class deviceTokenUbuntuPush final : public DeviceToken {
 public:
  static jclass Class;
  /// Token; may be empty to de-register a device.
  std::string token_;
  static jfieldID token_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  deviceTokenUbuntuPush();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] token_ Token; may be empty to de-register a device.
   */
  explicit deviceTokenUbuntuPush(std::string const &token_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1782320422;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<deviceTokenUbuntuPush> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A token for BlackBerry Push Service.
 */
class deviceTokenBlackBerryPush final : public DeviceToken {
 public:
  static jclass Class;
  /// Token; may be empty to de-register a device.
  std::string token_;
  static jfieldID token_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  deviceTokenBlackBerryPush();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] token_ Token; may be empty to de-register a device.
   */
  explicit deviceTokenBlackBerryPush(std::string const &token_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1559167234;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<deviceTokenBlackBerryPush> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A token for Tizen Push Service.
 */
class deviceTokenTizenPush final : public DeviceToken {
 public:
  static jclass Class;
  /// Push service registration identifier; may be empty to de-register a device.
  std::string reg_id_;
  static jfieldID reg_id_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  deviceTokenTizenPush();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] reg_id_ Push service registration identifier; may be empty to de-register a device.
   */
  explicit deviceTokenTizenPush(std::string const &reg_id_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1359947213;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<deviceTokenTizenPush> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Describes a document of any type.
 */
class document final : public Object {
 public:
  static jclass Class;
  /// Original name of the file; as defined by the sender.
  std::string file_name_;
  static jfieldID file_name_fieldID;
  /// MIME type of the file; as defined by the sender.
  std::string mime_type_;
  static jfieldID mime_type_fieldID;
  /// Document thumbnail; as defined by the sender; may be null.
  object_ptr<photoSize> thumbnail_;
  static jfieldID thumbnail_fieldID;
  /// File containing the document.
  object_ptr<file> document_;
  static jfieldID document_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  document();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] file_name_ Original name of the file; as defined by the sender.
   * \param[in] mime_type_ MIME type of the file; as defined by the sender.
   * \param[in] thumbnail_ Document thumbnail; as defined by the sender; may be null.
   * \param[in] document_ File containing the document.
   */
  document(std::string const &file_name_, std::string const &mime_type_, object_ptr<photoSize> &&thumbnail_, object_ptr<file> &&document_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -736037786;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<document> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Contains information about a message draft.
 */
class draftMessage final : public Object {
 public:
  static jclass Class;
  /// Identifier of the message to reply to; 0 if none.
  std::int64_t reply_to_message_id_;
  static jfieldID reply_to_message_id_fieldID;
  /// Content of the message draft; this should always be of type inputMessageText.
  object_ptr<InputMessageContent> input_message_text_;
  static jfieldID input_message_text_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  draftMessage();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] reply_to_message_id_ Identifier of the message to reply to; 0 if none.
   * \param[in] input_message_text_ Content of the message draft; this should always be of type inputMessageText.
   */
  draftMessage(std::int64_t reply_to_message_id_, object_ptr<InputMessageContent> &&input_message_text_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1902914742;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<draftMessage> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Information about the email address authentication code that was sent.
 */
class emailAddressAuthenticationCodeInfo final : public Object {
 public:
  static jclass Class;
  /// Pattern of the email address to which an authentication code was sent.
  std::string email_address_pattern_;
  static jfieldID email_address_pattern_fieldID;
  /// Length of the code; 0 if unknown.
  std::int32_t length_;
  static jfieldID length_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  emailAddressAuthenticationCodeInfo();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] email_address_pattern_ Pattern of the email address to which an authentication code was sent.
   * \param[in] length_ Length of the code; 0 if unknown.
   */
  emailAddressAuthenticationCodeInfo(std::string const &email_address_pattern_, std::int32_t length_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1151066659;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<emailAddressAuthenticationCodeInfo> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Contains encrypted Telegram Passport data credentials.
 */
class encryptedCredentials final : public Object {
 public:
  static jclass Class;
  /// The encrypted credentials.
  std::string data_;
  static jfieldID data_fieldID;
  /// The decrypted data hash.
  std::string hash_;
  static jfieldID hash_fieldID;
  /// Secret for data decryption, encrypted with the service's public key.
  std::string secret_;
  static jfieldID secret_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  encryptedCredentials();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] data_ The encrypted credentials.
   * \param[in] hash_ The decrypted data hash.
   * \param[in] secret_ Secret for data decryption, encrypted with the service's public key.
   */
  encryptedCredentials(std::string const &data_, std::string const &hash_, std::string const &secret_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1331106766;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<encryptedCredentials> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Contains information about an encrypted Telegram Passport element; for bots only.
 */
class encryptedPassportElement final : public Object {
 public:
  static jclass Class;
  /// Type of Telegram Passport element.
  object_ptr<PassportElementType> type_;
  static jfieldID type_fieldID;
  /// Encrypted JSON-encoded data about the user.
  std::string data_;
  static jfieldID data_fieldID;
  /// The front side of an identity document.
  object_ptr<datedFile> front_side_;
  static jfieldID front_side_fieldID;
  /// The reverse side of an identity document; may be null.
  object_ptr<datedFile> reverse_side_;
  static jfieldID reverse_side_fieldID;
  /// Selfie with the document; may be null.
  object_ptr<datedFile> selfie_;
  static jfieldID selfie_fieldID;
  /// List of files containing a certified English translation of the document.
  std::vector<object_ptr<datedFile>> translation_;
  static jfieldID translation_fieldID;
  /// List of attached files.
  std::vector<object_ptr<datedFile>> files_;
  static jfieldID files_fieldID;
  /// Unencrypted data, phone number or email address.
  std::string value_;
  static jfieldID value_fieldID;
  /// Hash of the entire element.
  std::string hash_;
  static jfieldID hash_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  encryptedPassportElement();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] type_ Type of Telegram Passport element.
   * \param[in] data_ Encrypted JSON-encoded data about the user.
   * \param[in] front_side_ The front side of an identity document.
   * \param[in] reverse_side_ The reverse side of an identity document; may be null.
   * \param[in] selfie_ Selfie with the document; may be null.
   * \param[in] translation_ List of files containing a certified English translation of the document.
   * \param[in] files_ List of attached files.
   * \param[in] value_ Unencrypted data, phone number or email address.
   * \param[in] hash_ Hash of the entire element.
   */
  encryptedPassportElement(object_ptr<PassportElementType> &&type_, std::string const &data_, object_ptr<datedFile> &&front_side_, object_ptr<datedFile> &&reverse_side_, object_ptr<datedFile> &&selfie_, std::vector<object_ptr<datedFile>> &&translation_, std::vector<object_ptr<datedFile>> &&files_, std::string const &value_, std::string const &hash_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 2002386193;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<encryptedPassportElement> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * An object of this type can be returned on every function call, in case of an error.
 */
class error final : public Object {
 public:
  static jclass Class;
  /// Error code; subject to future changes. If the error code is 406, the error message must not be processed in any way and must not be displayed to the user.
  std::int32_t code_;
  static jfieldID code_fieldID;
  /// Error message; subject to future changes.
  std::string message_;
  static jfieldID message_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  error();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] code_ Error code; subject to future changes. If the error code is 406, the error message must not be processed in any way and must not be displayed to the user.
   * \param[in] message_ Error message; subject to future changes.
   */
  error(std::int32_t code_, std::string const &message_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1679978726;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<error> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Represents a file.
 */
class file final : public Object {
 public:
  static jclass Class;
  /// Unique file identifier.
  std::int32_t id_;
  static jfieldID id_fieldID;
  /// File size; 0 if unknown.
  std::int32_t size_;
  static jfieldID size_fieldID;
  /// Expected file size in case the exact file size is unknown, but an approximate size is known. Can be used to show download/upload progress.
  std::int32_t expected_size_;
  static jfieldID expected_size_fieldID;
  /// Information about the local copy of the file.
  object_ptr<localFile> local_;
  static jfieldID local_fieldID;
  /// Information about the remote copy of the file.
  object_ptr<remoteFile> remote_;
  static jfieldID remote_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  file();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] id_ Unique file identifier.
   * \param[in] size_ File size; 0 if unknown.
   * \param[in] expected_size_ Expected file size in case the exact file size is unknown, but an approximate size is known. Can be used to show download/upload progress.
   * \param[in] local_ Information about the local copy of the file.
   * \param[in] remote_ Information about the remote copy of the file.
   */
  file(std::int32_t id_, std::int32_t size_, std::int32_t expected_size_, object_ptr<localFile> &&local_, object_ptr<remoteFile> &&remote_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 766337656;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<file> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * This class is an abstract base class.
 * Represents the type of a file.
 */
class FileType: public Object {
 public:
  static jclass Class;

  static object_ptr<FileType> fetch(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * The data is not a file.
 */
class fileTypeNone final : public FileType {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  fileTypeNone();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 2003009189;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<fileTypeNone> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * The file is an animation.
 */
class fileTypeAnimation final : public FileType {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  fileTypeAnimation();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -290816582;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<fileTypeAnimation> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * The file is an audio file.
 */
class fileTypeAudio final : public FileType {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  fileTypeAudio();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -709112160;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<fileTypeAudio> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * The file is a document.
 */
class fileTypeDocument final : public FileType {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  fileTypeDocument();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -564722929;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<fileTypeDocument> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * The file is a photo.
 */
class fileTypePhoto final : public FileType {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  fileTypePhoto();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1718914651;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<fileTypePhoto> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * The file is a profile photo.
 */
class fileTypeProfilePhoto final : public FileType {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  fileTypeProfilePhoto();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1795089315;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<fileTypeProfilePhoto> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * The file was sent to a secret chat (the file type is not known to the server).
 */
class fileTypeSecret final : public FileType {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  fileTypeSecret();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1871899401;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<fileTypeSecret> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * The file is a thumbnail of a file from a secret chat.
 */
class fileTypeSecretThumbnail final : public FileType {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  fileTypeSecretThumbnail();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1401326026;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<fileTypeSecretThumbnail> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * The file is a file from Secure storage used for storing Telegram Passport files.
 */
class fileTypeSecure final : public FileType {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  fileTypeSecure();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1419133146;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<fileTypeSecure> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * The file is a sticker.
 */
class fileTypeSticker final : public FileType {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  fileTypeSticker();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 475233385;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<fileTypeSticker> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * The file is a thumbnail of another file.
 */
class fileTypeThumbnail final : public FileType {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  fileTypeThumbnail();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -12443298;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<fileTypeThumbnail> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * The file type is not yet known.
 */
class fileTypeUnknown final : public FileType {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  fileTypeUnknown();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -2011566768;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<fileTypeUnknown> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * The file is a video.
 */
class fileTypeVideo final : public FileType {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  fileTypeVideo();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1430816539;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<fileTypeVideo> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * The file is a video note.
 */
class fileTypeVideoNote final : public FileType {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  fileTypeVideoNote();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -518412385;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<fileTypeVideoNote> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * The file is a voice note.
 */
class fileTypeVoiceNote final : public FileType {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  fileTypeVoiceNote();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -588681661;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<fileTypeVoiceNote> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * The file is a wallpaper.
 */
class fileTypeWallpaper final : public FileType {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  fileTypeWallpaper();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1854930076;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<fileTypeWallpaper> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A text with some entities.
 */
class formattedText final : public Object {
 public:
  static jclass Class;
  /// The text.
  std::string text_;
  static jfieldID text_fieldID;
  /// Entities contained in the text.
  std::vector<object_ptr<textEntity>> entities_;
  static jfieldID entities_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  formattedText();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] text_ The text.
   * \param[in] entities_ Entities contained in the text.
   */
  formattedText(std::string const &text_, std::vector<object_ptr<textEntity>> &&entities_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -252624564;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<formattedText> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Contains a list of messages found by a search.
 */
class foundMessages final : public Object {
 public:
  static jclass Class;
  /// List of messages.
  std::vector<object_ptr<message>> messages_;
  static jfieldID messages_fieldID;
  /// Value to pass as from_search_id to get more results.
  std::int64_t next_from_search_id_;
  static jfieldID next_from_search_id_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  foundMessages();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] messages_ List of messages.
   * \param[in] next_from_search_id_ Value to pass as from_search_id to get more results.
   */
  foundMessages(std::vector<object_ptr<message>> &&messages_, std::int64_t next_from_search_id_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 2135623881;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<foundMessages> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Describes a game.
 */
class game final : public Object {
 public:
  static jclass Class;
  /// Game ID.
  std::int64_t id_;
  static jfieldID id_fieldID;
  /// Game short name. To share a game use the URL https://t.me/{bot_username}?game={game_short_name}.
  std::string short_name_;
  static jfieldID short_name_fieldID;
  /// Game title.
  std::string title_;
  static jfieldID title_fieldID;
  /// Game text, usually containing scoreboards for a game.
  object_ptr<formattedText> text_;
  static jfieldID text_fieldID;
  /// Game description.
  std::string description_;
  static jfieldID description_fieldID;
  /// Game photo.
  object_ptr<photo> photo_;
  static jfieldID photo_fieldID;
  /// Game animation; may be null.
  object_ptr<animation> animation_;
  static jfieldID animation_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  game();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] id_ Game ID.
   * \param[in] short_name_ Game short name. To share a game use the URL https://t.me/{bot_username}?game={game_short_name}.
   * \param[in] title_ Game title.
   * \param[in] text_ Game text, usually containing scoreboards for a game.
   * \param[in] description_ Game description.
   * \param[in] photo_ Game photo.
   * \param[in] animation_ Game animation; may be null.
   */
  game(std::int64_t id_, std::string const &short_name_, std::string const &title_, object_ptr<formattedText> &&text_, std::string const &description_, object_ptr<photo> &&photo_, object_ptr<animation> &&animation_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1565597752;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<game> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Contains one row of the game high score table.
 */
class gameHighScore final : public Object {
 public:
  static jclass Class;
  /// Position in the high score table.
  std::int32_t position_;
  static jfieldID position_fieldID;
  /// User identifier.
  std::int32_t user_id_;
  static jfieldID user_id_fieldID;
  /// User score.
  std::int32_t score_;
  static jfieldID score_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  gameHighScore();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] position_ Position in the high score table.
   * \param[in] user_id_ User identifier.
   * \param[in] score_ User score.
   */
  gameHighScore(std::int32_t position_, std::int32_t user_id_, std::int32_t score_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -30778358;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<gameHighScore> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Contains a list of game high scores.
 */
class gameHighScores final : public Object {
 public:
  static jclass Class;
  /// A list of game high scores.
  std::vector<object_ptr<gameHighScore>> scores_;
  static jfieldID scores_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  gameHighScores();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] scores_ A list of game high scores.
   */
  explicit gameHighScores(std::vector<object_ptr<gameHighScore>> &&scores_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -725770727;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<gameHighScores> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Contains a list of hashtags.
 */
class hashtags final : public Object {
 public:
  static jclass Class;
  /// A list of hashtags.
  std::vector<std::string> hashtags_;
  static jfieldID hashtags_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  hashtags();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] hashtags_ A list of hashtags.
   */
  explicit hashtags(std::vector<std::string> &&hashtags_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 676798885;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<hashtags> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * An identity document.
 */
class identityDocument final : public Object {
 public:
  static jclass Class;
  /// Document number; 1-24 characters.
  std::string number_;
  static jfieldID number_fieldID;
  /// Document expiry date; may be null.
  object_ptr<date> expiry_date_;
  static jfieldID expiry_date_fieldID;
  /// Front side of the document.
  object_ptr<datedFile> front_side_;
  static jfieldID front_side_fieldID;
  /// Reverse side of the document; only for driver license and identity card.
  object_ptr<datedFile> reverse_side_;
  static jfieldID reverse_side_fieldID;
  /// Selfie with the document; may be null.
  object_ptr<datedFile> selfie_;
  static jfieldID selfie_fieldID;
  /// List of files containing a certified English translation of the document.
  std::vector<object_ptr<datedFile>> translation_;
  static jfieldID translation_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  identityDocument();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] number_ Document number; 1-24 characters.
   * \param[in] expiry_date_ Document expiry date; may be null.
   * \param[in] front_side_ Front side of the document.
   * \param[in] reverse_side_ Reverse side of the document; only for driver license and identity card.
   * \param[in] selfie_ Selfie with the document; may be null.
   * \param[in] translation_ List of files containing a certified English translation of the document.
   */
  identityDocument(std::string const &number_, object_ptr<date> &&expiry_date_, object_ptr<datedFile> &&front_side_, object_ptr<datedFile> &&reverse_side_, object_ptr<datedFile> &&selfie_, std::vector<object_ptr<datedFile>> &&translation_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 445952972;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<identityDocument> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Represents the result of an ImportContacts request.
 */
class importedContacts final : public Object {
 public:
  static jclass Class;
  /// User identifiers of the imported contacts in the same order as they were specified in the request; 0 if the contact is not yet a registered user.
  std::vector<std::int32_t> user_ids_;
  static jfieldID user_ids_fieldID;
  /// The number of users that imported the corresponding contact; 0 for already registered users or if unavailable.
  std::vector<std::int32_t> importer_count_;
  static jfieldID importer_count_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  importedContacts();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] user_ids_ User identifiers of the imported contacts in the same order as they were specified in the request; 0 if the contact is not yet a registered user.
   * \param[in] importer_count_ The number of users that imported the corresponding contact; 0 for already registered users or if unavailable.
   */
  importedContacts(std::vector<std::int32_t> &&user_ids_, std::vector<std::int32_t> &&importer_count_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -741685354;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<importedContacts> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Represents a single button in an inline keyboard.
 */
class inlineKeyboardButton final : public Object {
 public:
  static jclass Class;
  /// Text of the button.
  std::string text_;
  static jfieldID text_fieldID;
  /// Type of the button.
  object_ptr<InlineKeyboardButtonType> type_;
  static jfieldID type_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  inlineKeyboardButton();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] text_ Text of the button.
   * \param[in] type_ Type of the button.
   */
  inlineKeyboardButton(std::string const &text_, object_ptr<InlineKeyboardButtonType> &&type_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -372105704;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<inlineKeyboardButton> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * This class is an abstract base class.
 * Describes the type of an inline keyboard button.
 */
class InlineKeyboardButtonType: public Object {
 public:
  static jclass Class;

  static object_ptr<InlineKeyboardButtonType> fetch(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A button that opens a specified URL.
 */
class inlineKeyboardButtonTypeUrl final : public InlineKeyboardButtonType {
 public:
  static jclass Class;
  /// HTTP or tg:// URL to open.
  std::string url_;
  static jfieldID url_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  inlineKeyboardButtonTypeUrl();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] url_ HTTP or tg:// URL to open.
   */
  explicit inlineKeyboardButtonTypeUrl(std::string const &url_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1130741420;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<inlineKeyboardButtonTypeUrl> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A button that sends a special callback query to a bot.
 */
class inlineKeyboardButtonTypeCallback final : public InlineKeyboardButtonType {
 public:
  static jclass Class;
  /// Data to be sent to the bot via a callback query.
  std::string data_;
  static jfieldID data_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  inlineKeyboardButtonTypeCallback();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] data_ Data to be sent to the bot via a callback query.
   */
  explicit inlineKeyboardButtonTypeCallback(std::string const &data_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1127515139;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<inlineKeyboardButtonTypeCallback> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A button with a game that sends a special callback query to a bot. This button must be in the first column and row of the keyboard and can be attached only to a message with content of the type messageGame.
 */
class inlineKeyboardButtonTypeCallbackGame final : public InlineKeyboardButtonType {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  inlineKeyboardButtonTypeCallbackGame();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -383429528;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<inlineKeyboardButtonTypeCallbackGame> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A button that forces an inline query to the bot to be inserted in the input field.
 */
class inlineKeyboardButtonTypeSwitchInline final : public InlineKeyboardButtonType {
 public:
  static jclass Class;
  /// Inline query to be sent to the bot.
  std::string query_;
  static jfieldID query_fieldID;
  /// True, if the inline query should be sent from the current chat.
  bool in_current_chat_;
  static jfieldID in_current_chat_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  inlineKeyboardButtonTypeSwitchInline();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] query_ Inline query to be sent to the bot.
   * \param[in] in_current_chat_ True, if the inline query should be sent from the current chat.
   */
  inlineKeyboardButtonTypeSwitchInline(std::string const &query_, bool in_current_chat_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -2035563307;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<inlineKeyboardButtonTypeSwitchInline> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A button to buy something. This button must be in the first column and row of the keyboard and can be attached only to a message with content of the type messageInvoice.
 */
class inlineKeyboardButtonTypeBuy final : public InlineKeyboardButtonType {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  inlineKeyboardButtonTypeBuy();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1360739440;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<inlineKeyboardButtonTypeBuy> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * This class is an abstract base class.
 * Represents a single result of an inline query.
 */
class InlineQueryResult: public Object {
 public:
  static jclass Class;

  static object_ptr<InlineQueryResult> fetch(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Represents a link to an article or web page.
 */
class inlineQueryResultArticle final : public InlineQueryResult {
 public:
  static jclass Class;
  /// Unique identifier of the query result.
  std::string id_;
  static jfieldID id_fieldID;
  /// URL of the result, if it exists.
  std::string url_;
  static jfieldID url_fieldID;
  /// True, if the URL must be not shown.
  bool hide_url_;
  static jfieldID hide_url_fieldID;
  /// Title of the result.
  std::string title_;
  static jfieldID title_fieldID;
  /// A short description of the result.
  std::string description_;
  static jfieldID description_fieldID;
  /// Result thumbnail; may be null.
  object_ptr<photoSize> thumbnail_;
  static jfieldID thumbnail_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  inlineQueryResultArticle();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] id_ Unique identifier of the query result.
   * \param[in] url_ URL of the result, if it exists.
   * \param[in] hide_url_ True, if the URL must be not shown.
   * \param[in] title_ Title of the result.
   * \param[in] description_ A short description of the result.
   * \param[in] thumbnail_ Result thumbnail; may be null.
   */
  inlineQueryResultArticle(std::string const &id_, std::string const &url_, bool hide_url_, std::string const &title_, std::string const &description_, object_ptr<photoSize> &&thumbnail_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -518366710;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<inlineQueryResultArticle> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Represents a user contact.
 */
class inlineQueryResultContact final : public InlineQueryResult {
 public:
  static jclass Class;
  /// Unique identifier of the query result.
  std::string id_;
  static jfieldID id_fieldID;
  /// A user contact.
  object_ptr<contact> contact_;
  static jfieldID contact_fieldID;
  /// Result thumbnail; may be null.
  object_ptr<photoSize> thumbnail_;
  static jfieldID thumbnail_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  inlineQueryResultContact();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] id_ Unique identifier of the query result.
   * \param[in] contact_ A user contact.
   * \param[in] thumbnail_ Result thumbnail; may be null.
   */
  inlineQueryResultContact(std::string const &id_, object_ptr<contact> &&contact_, object_ptr<photoSize> &&thumbnail_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 410081985;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<inlineQueryResultContact> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Represents a point on the map.
 */
class inlineQueryResultLocation final : public InlineQueryResult {
 public:
  static jclass Class;
  /// Unique identifier of the query result.
  std::string id_;
  static jfieldID id_fieldID;
  /// Location result.
  object_ptr<location> location_;
  static jfieldID location_fieldID;
  /// Title of the result.
  std::string title_;
  static jfieldID title_fieldID;
  /// Result thumbnail; may be null.
  object_ptr<photoSize> thumbnail_;
  static jfieldID thumbnail_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  inlineQueryResultLocation();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] id_ Unique identifier of the query result.
   * \param[in] location_ Location result.
   * \param[in] title_ Title of the result.
   * \param[in] thumbnail_ Result thumbnail; may be null.
   */
  inlineQueryResultLocation(std::string const &id_, object_ptr<location> &&location_, std::string const &title_, object_ptr<photoSize> &&thumbnail_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -158305341;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<inlineQueryResultLocation> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Represents information about a venue.
 */
class inlineQueryResultVenue final : public InlineQueryResult {
 public:
  static jclass Class;
  /// Unique identifier of the query result.
  std::string id_;
  static jfieldID id_fieldID;
  /// Venue result.
  object_ptr<venue> venue_;
  static jfieldID venue_fieldID;
  /// Result thumbnail; may be null.
  object_ptr<photoSize> thumbnail_;
  static jfieldID thumbnail_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  inlineQueryResultVenue();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] id_ Unique identifier of the query result.
   * \param[in] venue_ Venue result.
   * \param[in] thumbnail_ Result thumbnail; may be null.
   */
  inlineQueryResultVenue(std::string const &id_, object_ptr<venue> &&venue_, object_ptr<photoSize> &&thumbnail_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1592932211;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<inlineQueryResultVenue> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Represents information about a game.
 */
class inlineQueryResultGame final : public InlineQueryResult {
 public:
  static jclass Class;
  /// Unique identifier of the query result.
  std::string id_;
  static jfieldID id_fieldID;
  /// Game result.
  object_ptr<game> game_;
  static jfieldID game_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  inlineQueryResultGame();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] id_ Unique identifier of the query result.
   * \param[in] game_ Game result.
   */
  inlineQueryResultGame(std::string const &id_, object_ptr<game> &&game_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1706916987;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<inlineQueryResultGame> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Represents an animation file.
 */
class inlineQueryResultAnimation final : public InlineQueryResult {
 public:
  static jclass Class;
  /// Unique identifier of the query result.
  std::string id_;
  static jfieldID id_fieldID;
  /// Animation file.
  object_ptr<animation> animation_;
  static jfieldID animation_fieldID;
  /// Animation title.
  std::string title_;
  static jfieldID title_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  inlineQueryResultAnimation();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] id_ Unique identifier of the query result.
   * \param[in] animation_ Animation file.
   * \param[in] title_ Animation title.
   */
  inlineQueryResultAnimation(std::string const &id_, object_ptr<animation> &&animation_, std::string const &title_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 2009984267;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<inlineQueryResultAnimation> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Represents an audio file.
 */
class inlineQueryResultAudio final : public InlineQueryResult {
 public:
  static jclass Class;
  /// Unique identifier of the query result.
  std::string id_;
  static jfieldID id_fieldID;
  /// Audio file.
  object_ptr<audio> audio_;
  static jfieldID audio_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  inlineQueryResultAudio();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] id_ Unique identifier of the query result.
   * \param[in] audio_ Audio file.
   */
  inlineQueryResultAudio(std::string const &id_, object_ptr<audio> &&audio_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 842650360;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<inlineQueryResultAudio> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Represents a document.
 */
class inlineQueryResultDocument final : public InlineQueryResult {
 public:
  static jclass Class;
  /// Unique identifier of the query result.
  std::string id_;
  static jfieldID id_fieldID;
  /// Document.
  object_ptr<document> document_;
  static jfieldID document_fieldID;
  /// Document title.
  std::string title_;
  static jfieldID title_fieldID;
  /// Document description.
  std::string description_;
  static jfieldID description_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  inlineQueryResultDocument();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] id_ Unique identifier of the query result.
   * \param[in] document_ Document.
   * \param[in] title_ Document title.
   * \param[in] description_ Document description.
   */
  inlineQueryResultDocument(std::string const &id_, object_ptr<document> &&document_, std::string const &title_, std::string const &description_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1491268539;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<inlineQueryResultDocument> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Represents a photo.
 */
class inlineQueryResultPhoto final : public InlineQueryResult {
 public:
  static jclass Class;
  /// Unique identifier of the query result.
  std::string id_;
  static jfieldID id_fieldID;
  /// Photo.
  object_ptr<photo> photo_;
  static jfieldID photo_fieldID;
  /// Title of the result, if known.
  std::string title_;
  static jfieldID title_fieldID;
  /// A short description of the result, if known.
  std::string description_;
  static jfieldID description_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  inlineQueryResultPhoto();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] id_ Unique identifier of the query result.
   * \param[in] photo_ Photo.
   * \param[in] title_ Title of the result, if known.
   * \param[in] description_ A short description of the result, if known.
   */
  inlineQueryResultPhoto(std::string const &id_, object_ptr<photo> &&photo_, std::string const &title_, std::string const &description_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1848319440;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<inlineQueryResultPhoto> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Represents a sticker.
 */
class inlineQueryResultSticker final : public InlineQueryResult {
 public:
  static jclass Class;
  /// Unique identifier of the query result.
  std::string id_;
  static jfieldID id_fieldID;
  /// Sticker.
  object_ptr<sticker> sticker_;
  static jfieldID sticker_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  inlineQueryResultSticker();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] id_ Unique identifier of the query result.
   * \param[in] sticker_ Sticker.
   */
  inlineQueryResultSticker(std::string const &id_, object_ptr<sticker> &&sticker_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1848224245;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<inlineQueryResultSticker> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Represents a video.
 */
class inlineQueryResultVideo final : public InlineQueryResult {
 public:
  static jclass Class;
  /// Unique identifier of the query result.
  std::string id_;
  static jfieldID id_fieldID;
  /// Video.
  object_ptr<video> video_;
  static jfieldID video_fieldID;
  /// Title of the video.
  std::string title_;
  static jfieldID title_fieldID;
  /// Description of the video.
  std::string description_;
  static jfieldID description_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  inlineQueryResultVideo();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] id_ Unique identifier of the query result.
   * \param[in] video_ Video.
   * \param[in] title_ Title of the video.
   * \param[in] description_ Description of the video.
   */
  inlineQueryResultVideo(std::string const &id_, object_ptr<video> &&video_, std::string const &title_, std::string const &description_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1373158683;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<inlineQueryResultVideo> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Represents a voice note.
 */
class inlineQueryResultVoiceNote final : public InlineQueryResult {
 public:
  static jclass Class;
  /// Unique identifier of the query result.
  std::string id_;
  static jfieldID id_fieldID;
  /// Voice note.
  object_ptr<voiceNote> voice_note_;
  static jfieldID voice_note_fieldID;
  /// Title of the voice note.
  std::string title_;
  static jfieldID title_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  inlineQueryResultVoiceNote();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] id_ Unique identifier of the query result.
   * \param[in] voice_note_ Voice note.
   * \param[in] title_ Title of the voice note.
   */
  inlineQueryResultVoiceNote(std::string const &id_, object_ptr<voiceNote> &&voice_note_, std::string const &title_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1897393105;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<inlineQueryResultVoiceNote> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Represents the results of the inline query. Use sendInlineQueryResultMessage to send the result of the query.
 */
class inlineQueryResults final : public Object {
 public:
  static jclass Class;
  /// Unique identifier of the inline query.
  std::int64_t inline_query_id_;
  static jfieldID inline_query_id_fieldID;
  /// The offset for the next request. If empty, there are no more results.
  std::string next_offset_;
  static jfieldID next_offset_fieldID;
  /// Results of the query.
  std::vector<object_ptr<InlineQueryResult>> results_;
  static jfieldID results_fieldID;
  /// If non-empty, this text should be shown on the button, which opens a private chat with the bot and sends the bot a start message with the switch_pm_parameter.
  std::string switch_pm_text_;
  static jfieldID switch_pm_text_fieldID;
  /// Parameter for the bot start message.
  std::string switch_pm_parameter_;
  static jfieldID switch_pm_parameter_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  inlineQueryResults();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] inline_query_id_ Unique identifier of the inline query.
   * \param[in] next_offset_ The offset for the next request. If empty, there are no more results.
   * \param[in] results_ Results of the query.
   * \param[in] switch_pm_text_ If non-empty, this text should be shown on the button, which opens a private chat with the bot and sends the bot a start message with the switch_pm_parameter.
   * \param[in] switch_pm_parameter_ Parameter for the bot start message.
   */
  inlineQueryResults(std::int64_t inline_query_id_, std::string const &next_offset_, std::vector<object_ptr<InlineQueryResult>> &&results_, std::string const &switch_pm_text_, std::string const &switch_pm_parameter_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1000709656;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<inlineQueryResults> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * This class is an abstract base class.
 * Contains information about the payment method chosen by the user.
 */
class InputCredentials: public Object {
 public:
  static jclass Class;

  static object_ptr<InputCredentials> fetch(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Applies if a user chooses some previously saved payment credentials. To use their previously saved credentials, the user must have a valid temporary password.
 */
class inputCredentialsSaved final : public InputCredentials {
 public:
  static jclass Class;
  /// Identifier of the saved credentials.
  std::string saved_credentials_id_;
  static jfieldID saved_credentials_id_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  inputCredentialsSaved();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] saved_credentials_id_ Identifier of the saved credentials.
   */
  explicit inputCredentialsSaved(std::string const &saved_credentials_id_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -2034385364;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<inputCredentialsSaved> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Applies if a user enters new credentials on a payment provider website.
 */
class inputCredentialsNew final : public InputCredentials {
 public:
  static jclass Class;
  /// Contains JSON-encoded data with a credential identifier from the payment provider.
  std::string data_;
  static jfieldID data_fieldID;
  /// True, if the credential identifier can be saved on the server side.
  bool allow_save_;
  static jfieldID allow_save_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  inputCredentialsNew();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] data_ Contains JSON-encoded data with a credential identifier from the payment provider.
   * \param[in] allow_save_ True, if the credential identifier can be saved on the server side.
   */
  inputCredentialsNew(std::string const &data_, bool allow_save_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -829689558;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<inputCredentialsNew> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Applies if a user enters new credentials using Android Pay.
 */
class inputCredentialsAndroidPay final : public InputCredentials {
 public:
  static jclass Class;
  /// JSON-encoded data with the credential identifier.
  std::string data_;
  static jfieldID data_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  inputCredentialsAndroidPay();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] data_ JSON-encoded data with the credential identifier.
   */
  explicit inputCredentialsAndroidPay(std::string const &data_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1979566832;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<inputCredentialsAndroidPay> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Applies if a user enters new credentials using Apple Pay.
 */
class inputCredentialsApplePay final : public InputCredentials {
 public:
  static jclass Class;
  /// JSON-encoded data with the credential identifier.
  std::string data_;
  static jfieldID data_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  inputCredentialsApplePay();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] data_ JSON-encoded data with the credential identifier.
   */
  explicit inputCredentialsApplePay(std::string const &data_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1246570799;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<inputCredentialsApplePay> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * This class is an abstract base class.
 * Points to a file.
 */
class InputFile: public Object {
 public:
  static jclass Class;

  static object_ptr<InputFile> fetch(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A file defined by its unique ID.
 */
class inputFileId final : public InputFile {
 public:
  static jclass Class;
  /// Unique file identifier.
  std::int32_t id_;
  static jfieldID id_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  inputFileId();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] id_ Unique file identifier.
   */
  explicit inputFileId(std::int32_t id_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1788906253;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<inputFileId> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A file defined by its remote ID.
 */
class inputFileRemote final : public InputFile {
 public:
  static jclass Class;
  /// Remote file identifier.
  std::string id_;
  static jfieldID id_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  inputFileRemote();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] id_ Remote file identifier.
   */
  explicit inputFileRemote(std::string const &id_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -107574466;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<inputFileRemote> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A file defined by a local path.
 */
class inputFileLocal final : public InputFile {
 public:
  static jclass Class;
  /// Local path to the file.
  std::string path_;
  static jfieldID path_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  inputFileLocal();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] path_ Local path to the file.
   */
  explicit inputFileLocal(std::string const &path_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 2056030919;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<inputFileLocal> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A file generated by the client.
 */
class inputFileGenerated final : public InputFile {
 public:
  static jclass Class;
  /// Local path to a file from which the file is generated; may be empty if there is no such file.
  std::string original_path_;
  static jfieldID original_path_fieldID;
  /// String specifying the conversion applied to the original file; should be persistent across application restarts.
  std::string conversion_;
  static jfieldID conversion_fieldID;
  /// Expected size of the generated file; 0 if unknown.
  std::int32_t expected_size_;
  static jfieldID expected_size_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  inputFileGenerated();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] original_path_ Local path to a file from which the file is generated; may be empty if there is no such file.
   * \param[in] conversion_ String specifying the conversion applied to the original file; should be persistent across application restarts.
   * \param[in] expected_size_ Expected size of the generated file; 0 if unknown.
   */
  inputFileGenerated(std::string const &original_path_, std::string const &conversion_, std::int32_t expected_size_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1781351885;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<inputFileGenerated> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * An identity document to be saved to Telegram Passport.
 */
class inputIdentityDocument final : public Object {
 public:
  static jclass Class;
  /// Document number; 1-24 characters.
  std::string number_;
  static jfieldID number_fieldID;
  /// Document expiry date, if available.
  object_ptr<date> expiry_date_;
  static jfieldID expiry_date_fieldID;
  /// Front side of the document.
  object_ptr<InputFile> front_side_;
  static jfieldID front_side_fieldID;
  /// Reverse side of the document; only for driver license and identity card.
  object_ptr<InputFile> reverse_side_;
  static jfieldID reverse_side_fieldID;
  /// Selfie with the document, if available.
  object_ptr<InputFile> selfie_;
  static jfieldID selfie_fieldID;
  /// List of files containing a certified English translation of the document.
  std::vector<object_ptr<InputFile>> translation_;
  static jfieldID translation_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  inputIdentityDocument();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] number_ Document number; 1-24 characters.
   * \param[in] expiry_date_ Document expiry date, if available.
   * \param[in] front_side_ Front side of the document.
   * \param[in] reverse_side_ Reverse side of the document; only for driver license and identity card.
   * \param[in] selfie_ Selfie with the document, if available.
   * \param[in] translation_ List of files containing a certified English translation of the document.
   */
  inputIdentityDocument(std::string const &number_, object_ptr<date> &&expiry_date_, object_ptr<InputFile> &&front_side_, object_ptr<InputFile> &&reverse_side_, object_ptr<InputFile> &&selfie_, std::vector<object_ptr<InputFile>> &&translation_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -381776063;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<inputIdentityDocument> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * This class is an abstract base class.
 * Represents a single result of an inline query; for bots only.
 */
class InputInlineQueryResult: public Object {
 public:
  static jclass Class;

  static object_ptr<InputInlineQueryResult> fetch(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Represents a link to an animated GIF.
 */
class inputInlineQueryResultAnimatedGif final : public InputInlineQueryResult {
 public:
  static jclass Class;
  /// Unique identifier of the query result.
  std::string id_;
  static jfieldID id_fieldID;
  /// Title of the query result.
  std::string title_;
  static jfieldID title_fieldID;
  /// URL of the static result thumbnail (JPEG or GIF), if it exists.
  std::string thumbnail_url_;
  static jfieldID thumbnail_url_fieldID;
  /// The URL of the GIF-file (file size must not exceed 1MB).
  std::string gif_url_;
  static jfieldID gif_url_fieldID;
  /// Duration of the GIF, in seconds.
  std::int32_t gif_duration_;
  static jfieldID gif_duration_fieldID;
  /// Width of the GIF.
  std::int32_t gif_width_;
  static jfieldID gif_width_fieldID;
  /// Height of the GIF.
  std::int32_t gif_height_;
  static jfieldID gif_height_fieldID;
  /// The message reply markup. Must be of type replyMarkupInlineKeyboard or null.
  object_ptr<ReplyMarkup> reply_markup_;
  static jfieldID reply_markup_fieldID;
  /// The content of the message to be sent. Must be one of the following types: InputMessageText, InputMessageAnimation, InputMessageLocation, InputMessageVenue or InputMessageContact.
  object_ptr<InputMessageContent> input_message_content_;
  static jfieldID input_message_content_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  inputInlineQueryResultAnimatedGif();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] id_ Unique identifier of the query result.
   * \param[in] title_ Title of the query result.
   * \param[in] thumbnail_url_ URL of the static result thumbnail (JPEG or GIF), if it exists.
   * \param[in] gif_url_ The URL of the GIF-file (file size must not exceed 1MB).
   * \param[in] gif_duration_ Duration of the GIF, in seconds.
   * \param[in] gif_width_ Width of the GIF.
   * \param[in] gif_height_ Height of the GIF.
   * \param[in] reply_markup_ The message reply markup. Must be of type replyMarkupInlineKeyboard or null.
   * \param[in] input_message_content_ The content of the message to be sent. Must be one of the following types: InputMessageText, InputMessageAnimation, InputMessageLocation, InputMessageVenue or InputMessageContact.
   */
  inputInlineQueryResultAnimatedGif(std::string const &id_, std::string const &title_, std::string const &thumbnail_url_, std::string const &gif_url_, std::int32_t gif_duration_, std::int32_t gif_width_, std::int32_t gif_height_, object_ptr<ReplyMarkup> &&reply_markup_, object_ptr<InputMessageContent> &&input_message_content_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -891474894;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<inputInlineQueryResultAnimatedGif> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Represents a link to an animated (i.e. without sound) H.264/MPEG-4 AVC video.
 */
class inputInlineQueryResultAnimatedMpeg4 final : public InputInlineQueryResult {
 public:
  static jclass Class;
  /// Unique identifier of the query result.
  std::string id_;
  static jfieldID id_fieldID;
  /// Title of the result.
  std::string title_;
  static jfieldID title_fieldID;
  /// URL of the static result thumbnail (JPEG or GIF), if it exists.
  std::string thumbnail_url_;
  static jfieldID thumbnail_url_fieldID;
  /// The URL of the MPEG4-file (file size must not exceed 1MB).
  std::string mpeg4_url_;
  static jfieldID mpeg4_url_fieldID;
  /// Duration of the video, in seconds.
  std::int32_t mpeg4_duration_;
  static jfieldID mpeg4_duration_fieldID;
  /// Width of the video.
  std::int32_t mpeg4_width_;
  static jfieldID mpeg4_width_fieldID;
  /// Height of the video.
  std::int32_t mpeg4_height_;
  static jfieldID mpeg4_height_fieldID;
  /// The message reply markup. Must be of type replyMarkupInlineKeyboard or null.
  object_ptr<ReplyMarkup> reply_markup_;
  static jfieldID reply_markup_fieldID;
  /// The content of the message to be sent. Must be one of the following types: InputMessageText, InputMessageAnimation, InputMessageLocation, InputMessageVenue or InputMessageContact.
  object_ptr<InputMessageContent> input_message_content_;
  static jfieldID input_message_content_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  inputInlineQueryResultAnimatedMpeg4();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] id_ Unique identifier of the query result.
   * \param[in] title_ Title of the result.
   * \param[in] thumbnail_url_ URL of the static result thumbnail (JPEG or GIF), if it exists.
   * \param[in] mpeg4_url_ The URL of the MPEG4-file (file size must not exceed 1MB).
   * \param[in] mpeg4_duration_ Duration of the video, in seconds.
   * \param[in] mpeg4_width_ Width of the video.
   * \param[in] mpeg4_height_ Height of the video.
   * \param[in] reply_markup_ The message reply markup. Must be of type replyMarkupInlineKeyboard or null.
   * \param[in] input_message_content_ The content of the message to be sent. Must be one of the following types: InputMessageText, InputMessageAnimation, InputMessageLocation, InputMessageVenue or InputMessageContact.
   */
  inputInlineQueryResultAnimatedMpeg4(std::string const &id_, std::string const &title_, std::string const &thumbnail_url_, std::string const &mpeg4_url_, std::int32_t mpeg4_duration_, std::int32_t mpeg4_width_, std::int32_t mpeg4_height_, object_ptr<ReplyMarkup> &&reply_markup_, object_ptr<InputMessageContent> &&input_message_content_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1629529888;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<inputInlineQueryResultAnimatedMpeg4> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Represents a link to an article or web page.
 */
class inputInlineQueryResultArticle final : public InputInlineQueryResult {
 public:
  static jclass Class;
  /// Unique identifier of the query result.
  std::string id_;
  static jfieldID id_fieldID;
  /// URL of the result, if it exists.
  std::string url_;
  static jfieldID url_fieldID;
  /// True, if the URL must be not shown.
  bool hide_url_;
  static jfieldID hide_url_fieldID;
  /// Title of the result.
  std::string title_;
  static jfieldID title_fieldID;
  /// A short description of the result.
  std::string description_;
  static jfieldID description_fieldID;
  /// URL of the result thumbnail, if it exists.
  std::string thumbnail_url_;
  static jfieldID thumbnail_url_fieldID;
  /// Thumbnail width, if known.
  std::int32_t thumbnail_width_;
  static jfieldID thumbnail_width_fieldID;
  /// Thumbnail height, if known.
  std::int32_t thumbnail_height_;
  static jfieldID thumbnail_height_fieldID;
  /// The message reply markup. Must be of type replyMarkupInlineKeyboard or null.
  object_ptr<ReplyMarkup> reply_markup_;
  static jfieldID reply_markup_fieldID;
  /// The content of the message to be sent. Must be one of the following types: InputMessageText, InputMessageLocation, InputMessageVenue or InputMessageContact.
  object_ptr<InputMessageContent> input_message_content_;
  static jfieldID input_message_content_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  inputInlineQueryResultArticle();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] id_ Unique identifier of the query result.
   * \param[in] url_ URL of the result, if it exists.
   * \param[in] hide_url_ True, if the URL must be not shown.
   * \param[in] title_ Title of the result.
   * \param[in] description_ A short description of the result.
   * \param[in] thumbnail_url_ URL of the result thumbnail, if it exists.
   * \param[in] thumbnail_width_ Thumbnail width, if known.
   * \param[in] thumbnail_height_ Thumbnail height, if known.
   * \param[in] reply_markup_ The message reply markup. Must be of type replyMarkupInlineKeyboard or null.
   * \param[in] input_message_content_ The content of the message to be sent. Must be one of the following types: InputMessageText, InputMessageLocation, InputMessageVenue or InputMessageContact.
   */
  inputInlineQueryResultArticle(std::string const &id_, std::string const &url_, bool hide_url_, std::string const &title_, std::string const &description_, std::string const &thumbnail_url_, std::int32_t thumbnail_width_, std::int32_t thumbnail_height_, object_ptr<ReplyMarkup> &&reply_markup_, object_ptr<InputMessageContent> &&input_message_content_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1973670156;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<inputInlineQueryResultArticle> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Represents a link to an MP3 audio file.
 */
class inputInlineQueryResultAudio final : public InputInlineQueryResult {
 public:
  static jclass Class;
  /// Unique identifier of the query result.
  std::string id_;
  static jfieldID id_fieldID;
  /// Title of the audio file.
  std::string title_;
  static jfieldID title_fieldID;
  /// Performer of the audio file.
  std::string performer_;
  static jfieldID performer_fieldID;
  /// The URL of the audio file.
  std::string audio_url_;
  static jfieldID audio_url_fieldID;
  /// Audio file duration, in seconds.
  std::int32_t audio_duration_;
  static jfieldID audio_duration_fieldID;
  /// The message reply markup. Must be of type replyMarkupInlineKeyboard or null.
  object_ptr<ReplyMarkup> reply_markup_;
  static jfieldID reply_markup_fieldID;
  /// The content of the message to be sent. Must be one of the following types: InputMessageText, InputMessageAudio, InputMessageLocation, InputMessageVenue or InputMessageContact.
  object_ptr<InputMessageContent> input_message_content_;
  static jfieldID input_message_content_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  inputInlineQueryResultAudio();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] id_ Unique identifier of the query result.
   * \param[in] title_ Title of the audio file.
   * \param[in] performer_ Performer of the audio file.
   * \param[in] audio_url_ The URL of the audio file.
   * \param[in] audio_duration_ Audio file duration, in seconds.
   * \param[in] reply_markup_ The message reply markup. Must be of type replyMarkupInlineKeyboard or null.
   * \param[in] input_message_content_ The content of the message to be sent. Must be one of the following types: InputMessageText, InputMessageAudio, InputMessageLocation, InputMessageVenue or InputMessageContact.
   */
  inputInlineQueryResultAudio(std::string const &id_, std::string const &title_, std::string const &performer_, std::string const &audio_url_, std::int32_t audio_duration_, object_ptr<ReplyMarkup> &&reply_markup_, object_ptr<InputMessageContent> &&input_message_content_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1260139988;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<inputInlineQueryResultAudio> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Represents a user contact.
 */
class inputInlineQueryResultContact final : public InputInlineQueryResult {
 public:
  static jclass Class;
  /// Unique identifier of the query result.
  std::string id_;
  static jfieldID id_fieldID;
  /// User contact.
  object_ptr<contact> contact_;
  static jfieldID contact_fieldID;
  /// URL of the result thumbnail, if it exists.
  std::string thumbnail_url_;
  static jfieldID thumbnail_url_fieldID;
  /// Thumbnail width, if known.
  std::int32_t thumbnail_width_;
  static jfieldID thumbnail_width_fieldID;
  /// Thumbnail height, if known.
  std::int32_t thumbnail_height_;
  static jfieldID thumbnail_height_fieldID;
  /// The message reply markup. Must be of type replyMarkupInlineKeyboard or null.
  object_ptr<ReplyMarkup> reply_markup_;
  static jfieldID reply_markup_fieldID;
  /// The content of the message to be sent. Must be one of the following types: InputMessageText, InputMessageLocation, InputMessageVenue or InputMessageContact.
  object_ptr<InputMessageContent> input_message_content_;
  static jfieldID input_message_content_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  inputInlineQueryResultContact();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] id_ Unique identifier of the query result.
   * \param[in] contact_ User contact.
   * \param[in] thumbnail_url_ URL of the result thumbnail, if it exists.
   * \param[in] thumbnail_width_ Thumbnail width, if known.
   * \param[in] thumbnail_height_ Thumbnail height, if known.
   * \param[in] reply_markup_ The message reply markup. Must be of type replyMarkupInlineKeyboard or null.
   * \param[in] input_message_content_ The content of the message to be sent. Must be one of the following types: InputMessageText, InputMessageLocation, InputMessageVenue or InputMessageContact.
   */
  inputInlineQueryResultContact(std::string const &id_, object_ptr<contact> &&contact_, std::string const &thumbnail_url_, std::int32_t thumbnail_width_, std::int32_t thumbnail_height_, object_ptr<ReplyMarkup> &&reply_markup_, object_ptr<InputMessageContent> &&input_message_content_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1846064594;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<inputInlineQueryResultContact> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Represents a link to a file.
 */
class inputInlineQueryResultDocument final : public InputInlineQueryResult {
 public:
  static jclass Class;
  /// Unique identifier of the query result.
  std::string id_;
  static jfieldID id_fieldID;
  /// Title of the resulting file.
  std::string title_;
  static jfieldID title_fieldID;
  /// Short description of the result, if known.
  std::string description_;
  static jfieldID description_fieldID;
  /// URL of the file.
  std::string document_url_;
  static jfieldID document_url_fieldID;
  /// MIME type of the file content; only &quot;application/pdf&quot; and &quot;application/zip&quot; are currently allowed.
  std::string mime_type_;
  static jfieldID mime_type_fieldID;
  /// The URL of the file thumbnail, if it exists.
  std::string thumbnail_url_;
  static jfieldID thumbnail_url_fieldID;
  /// Width of the thumbnail.
  std::int32_t thumbnail_width_;
  static jfieldID thumbnail_width_fieldID;
  /// Height of the thumbnail.
  std::int32_t thumbnail_height_;
  static jfieldID thumbnail_height_fieldID;
  /// The message reply markup. Must be of type replyMarkupInlineKeyboard or null.
  object_ptr<ReplyMarkup> reply_markup_;
  static jfieldID reply_markup_fieldID;
  /// The content of the message to be sent. Must be one of the following types: InputMessageText, InputMessageDocument, InputMessageLocation, InputMessageVenue or InputMessageContact.
  object_ptr<InputMessageContent> input_message_content_;
  static jfieldID input_message_content_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  inputInlineQueryResultDocument();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] id_ Unique identifier of the query result.
   * \param[in] title_ Title of the resulting file.
   * \param[in] description_ Short description of the result, if known.
   * \param[in] document_url_ URL of the file.
   * \param[in] mime_type_ MIME type of the file content; only &quot;application/pdf&quot; and &quot;application/zip&quot; are currently allowed.
   * \param[in] thumbnail_url_ The URL of the file thumbnail, if it exists.
   * \param[in] thumbnail_width_ Width of the thumbnail.
   * \param[in] thumbnail_height_ Height of the thumbnail.
   * \param[in] reply_markup_ The message reply markup. Must be of type replyMarkupInlineKeyboard or null.
   * \param[in] input_message_content_ The content of the message to be sent. Must be one of the following types: InputMessageText, InputMessageDocument, InputMessageLocation, InputMessageVenue or InputMessageContact.
   */
  inputInlineQueryResultDocument(std::string const &id_, std::string const &title_, std::string const &description_, std::string const &document_url_, std::string const &mime_type_, std::string const &thumbnail_url_, std::int32_t thumbnail_width_, std::int32_t thumbnail_height_, object_ptr<ReplyMarkup> &&reply_markup_, object_ptr<InputMessageContent> &&input_message_content_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 578801869;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<inputInlineQueryResultDocument> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Represents a game.
 */
class inputInlineQueryResultGame final : public InputInlineQueryResult {
 public:
  static jclass Class;
  /// Unique identifier of the query result.
  std::string id_;
  static jfieldID id_fieldID;
  /// Short name of the game.
  std::string game_short_name_;
  static jfieldID game_short_name_fieldID;
  /// Message reply markup. Must be of type replyMarkupInlineKeyboard or null.
  object_ptr<ReplyMarkup> reply_markup_;
  static jfieldID reply_markup_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  inputInlineQueryResultGame();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] id_ Unique identifier of the query result.
   * \param[in] game_short_name_ Short name of the game.
   * \param[in] reply_markup_ Message reply markup. Must be of type replyMarkupInlineKeyboard or null.
   */
  inputInlineQueryResultGame(std::string const &id_, std::string const &game_short_name_, object_ptr<ReplyMarkup> &&reply_markup_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 966074327;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<inputInlineQueryResultGame> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Represents a point on the map.
 */
class inputInlineQueryResultLocation final : public InputInlineQueryResult {
 public:
  static jclass Class;
  /// Unique identifier of the query result.
  std::string id_;
  static jfieldID id_fieldID;
  /// Location result.
  object_ptr<location> location_;
  static jfieldID location_fieldID;
  /// Amount of time relative to the message sent time until the location can be updated, in seconds.
  std::int32_t live_period_;
  static jfieldID live_period_fieldID;
  /// Title of the result.
  std::string title_;
  static jfieldID title_fieldID;
  /// URL of the result thumbnail, if it exists.
  std::string thumbnail_url_;
  static jfieldID thumbnail_url_fieldID;
  /// Thumbnail width, if known.
  std::int32_t thumbnail_width_;
  static jfieldID thumbnail_width_fieldID;
  /// Thumbnail height, if known.
  std::int32_t thumbnail_height_;
  static jfieldID thumbnail_height_fieldID;
  /// The message reply markup. Must be of type replyMarkupInlineKeyboard or null.
  object_ptr<ReplyMarkup> reply_markup_;
  static jfieldID reply_markup_fieldID;
  /// The content of the message to be sent. Must be one of the following types: InputMessageText, InputMessageLocation, InputMessageVenue or InputMessageContact.
  object_ptr<InputMessageContent> input_message_content_;
  static jfieldID input_message_content_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  inputInlineQueryResultLocation();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] id_ Unique identifier of the query result.
   * \param[in] location_ Location result.
   * \param[in] live_period_ Amount of time relative to the message sent time until the location can be updated, in seconds.
   * \param[in] title_ Title of the result.
   * \param[in] thumbnail_url_ URL of the result thumbnail, if it exists.
   * \param[in] thumbnail_width_ Thumbnail width, if known.
   * \param[in] thumbnail_height_ Thumbnail height, if known.
   * \param[in] reply_markup_ The message reply markup. Must be of type replyMarkupInlineKeyboard or null.
   * \param[in] input_message_content_ The content of the message to be sent. Must be one of the following types: InputMessageText, InputMessageLocation, InputMessageVenue or InputMessageContact.
   */
  inputInlineQueryResultLocation(std::string const &id_, object_ptr<location> &&location_, std::int32_t live_period_, std::string const &title_, std::string const &thumbnail_url_, std::int32_t thumbnail_width_, std::int32_t thumbnail_height_, object_ptr<ReplyMarkup> &&reply_markup_, object_ptr<InputMessageContent> &&input_message_content_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1887650218;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<inputInlineQueryResultLocation> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Represents link to a JPEG image.
 */
class inputInlineQueryResultPhoto final : public InputInlineQueryResult {
 public:
  static jclass Class;
  /// Unique identifier of the query result.
  std::string id_;
  static jfieldID id_fieldID;
  /// Title of the result, if known.
  std::string title_;
  static jfieldID title_fieldID;
  /// A short description of the result, if known.
  std::string description_;
  static jfieldID description_fieldID;
  /// URL of the photo thumbnail, if it exists.
  std::string thumbnail_url_;
  static jfieldID thumbnail_url_fieldID;
  /// The URL of the JPEG photo (photo size must not exceed 5MB).
  std::string photo_url_;
  static jfieldID photo_url_fieldID;
  /// Width of the photo.
  std::int32_t photo_width_;
  static jfieldID photo_width_fieldID;
  /// Height of the photo.
  std::int32_t photo_height_;
  static jfieldID photo_height_fieldID;
  /// The message reply markup. Must be of type replyMarkupInlineKeyboard or null.
  object_ptr<ReplyMarkup> reply_markup_;
  static jfieldID reply_markup_fieldID;
  /// The content of the message to be sent. Must be one of the following types: InputMessageText, InputMessagePhoto, InputMessageLocation, InputMessageVenue or InputMessageContact.
  object_ptr<InputMessageContent> input_message_content_;
  static jfieldID input_message_content_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  inputInlineQueryResultPhoto();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] id_ Unique identifier of the query result.
   * \param[in] title_ Title of the result, if known.
   * \param[in] description_ A short description of the result, if known.
   * \param[in] thumbnail_url_ URL of the photo thumbnail, if it exists.
   * \param[in] photo_url_ The URL of the JPEG photo (photo size must not exceed 5MB).
   * \param[in] photo_width_ Width of the photo.
   * \param[in] photo_height_ Height of the photo.
   * \param[in] reply_markup_ The message reply markup. Must be of type replyMarkupInlineKeyboard or null.
   * \param[in] input_message_content_ The content of the message to be sent. Must be one of the following types: InputMessageText, InputMessagePhoto, InputMessageLocation, InputMessageVenue or InputMessageContact.
   */
  inputInlineQueryResultPhoto(std::string const &id_, std::string const &title_, std::string const &description_, std::string const &thumbnail_url_, std::string const &photo_url_, std::int32_t photo_width_, std::int32_t photo_height_, object_ptr<ReplyMarkup> &&reply_markup_, object_ptr<InputMessageContent> &&input_message_content_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1123338721;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<inputInlineQueryResultPhoto> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Represents a link to a WEBP sticker.
 */
class inputInlineQueryResultSticker final : public InputInlineQueryResult {
 public:
  static jclass Class;
  /// Unique identifier of the query result.
  std::string id_;
  static jfieldID id_fieldID;
  /// URL of the sticker thumbnail, if it exists.
  std::string thumbnail_url_;
  static jfieldID thumbnail_url_fieldID;
  /// The URL of the WEBP sticker (sticker file size must not exceed 5MB).
  std::string sticker_url_;
  static jfieldID sticker_url_fieldID;
  /// Width of the sticker.
  std::int32_t sticker_width_;
  static jfieldID sticker_width_fieldID;
  /// Height of the sticker.
  std::int32_t sticker_height_;
  static jfieldID sticker_height_fieldID;
  /// The message reply markup. Must be of type replyMarkupInlineKeyboard or null.
  object_ptr<ReplyMarkup> reply_markup_;
  static jfieldID reply_markup_fieldID;
  /// The content of the message to be sent. Must be one of the following types: InputMessageText, inputMessageSticker, InputMessageLocation, InputMessageVenue or InputMessageContact.
  object_ptr<InputMessageContent> input_message_content_;
  static jfieldID input_message_content_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  inputInlineQueryResultSticker();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] id_ Unique identifier of the query result.
   * \param[in] thumbnail_url_ URL of the sticker thumbnail, if it exists.
   * \param[in] sticker_url_ The URL of the WEBP sticker (sticker file size must not exceed 5MB).
   * \param[in] sticker_width_ Width of the sticker.
   * \param[in] sticker_height_ Height of the sticker.
   * \param[in] reply_markup_ The message reply markup. Must be of type replyMarkupInlineKeyboard or null.
   * \param[in] input_message_content_ The content of the message to be sent. Must be one of the following types: InputMessageText, inputMessageSticker, InputMessageLocation, InputMessageVenue or InputMessageContact.
   */
  inputInlineQueryResultSticker(std::string const &id_, std::string const &thumbnail_url_, std::string const &sticker_url_, std::int32_t sticker_width_, std::int32_t sticker_height_, object_ptr<ReplyMarkup> &&reply_markup_, object_ptr<InputMessageContent> &&input_message_content_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 274007129;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<inputInlineQueryResultSticker> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Represents information about a venue.
 */
class inputInlineQueryResultVenue final : public InputInlineQueryResult {
 public:
  static jclass Class;
  /// Unique identifier of the query result.
  std::string id_;
  static jfieldID id_fieldID;
  /// Venue result.
  object_ptr<venue> venue_;
  static jfieldID venue_fieldID;
  /// URL of the result thumbnail, if it exists.
  std::string thumbnail_url_;
  static jfieldID thumbnail_url_fieldID;
  /// Thumbnail width, if known.
  std::int32_t thumbnail_width_;
  static jfieldID thumbnail_width_fieldID;
  /// Thumbnail height, if known.
  std::int32_t thumbnail_height_;
  static jfieldID thumbnail_height_fieldID;
  /// The message reply markup. Must be of type replyMarkupInlineKeyboard or null.
  object_ptr<ReplyMarkup> reply_markup_;
  static jfieldID reply_markup_fieldID;
  /// The content of the message to be sent. Must be one of the following types: InputMessageText, InputMessageLocation, InputMessageVenue or InputMessageContact.
  object_ptr<InputMessageContent> input_message_content_;
  static jfieldID input_message_content_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  inputInlineQueryResultVenue();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] id_ Unique identifier of the query result.
   * \param[in] venue_ Venue result.
   * \param[in] thumbnail_url_ URL of the result thumbnail, if it exists.
   * \param[in] thumbnail_width_ Thumbnail width, if known.
   * \param[in] thumbnail_height_ Thumbnail height, if known.
   * \param[in] reply_markup_ The message reply markup. Must be of type replyMarkupInlineKeyboard or null.
   * \param[in] input_message_content_ The content of the message to be sent. Must be one of the following types: InputMessageText, InputMessageLocation, InputMessageVenue or InputMessageContact.
   */
  inputInlineQueryResultVenue(std::string const &id_, object_ptr<venue> &&venue_, std::string const &thumbnail_url_, std::int32_t thumbnail_width_, std::int32_t thumbnail_height_, object_ptr<ReplyMarkup> &&reply_markup_, object_ptr<InputMessageContent> &&input_message_content_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 541704509;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<inputInlineQueryResultVenue> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Represents a link to a page containing an embedded video player or a video file.
 */
class inputInlineQueryResultVideo final : public InputInlineQueryResult {
 public:
  static jclass Class;
  /// Unique identifier of the query result.
  std::string id_;
  static jfieldID id_fieldID;
  /// Title of the result.
  std::string title_;
  static jfieldID title_fieldID;
  /// A short description of the result, if known.
  std::string description_;
  static jfieldID description_fieldID;
  /// The URL of the video thumbnail (JPEG), if it exists.
  std::string thumbnail_url_;
  static jfieldID thumbnail_url_fieldID;
  /// URL of the embedded video player or video file.
  std::string video_url_;
  static jfieldID video_url_fieldID;
  /// MIME type of the content of the video URL, only &quot;text/html&quot; or &quot;video/mp4&quot; are currently supported.
  std::string mime_type_;
  static jfieldID mime_type_fieldID;
  /// Width of the video.
  std::int32_t video_width_;
  static jfieldID video_width_fieldID;
  /// Height of the video.
  std::int32_t video_height_;
  static jfieldID video_height_fieldID;
  /// Video duration, in seconds.
  std::int32_t video_duration_;
  static jfieldID video_duration_fieldID;
  /// The message reply markup. Must be of type replyMarkupInlineKeyboard or null.
  object_ptr<ReplyMarkup> reply_markup_;
  static jfieldID reply_markup_fieldID;
  /// The content of the message to be sent. Must be one of the following types: InputMessageText, InputMessageVideo, InputMessageLocation, InputMessageVenue or InputMessageContact.
  object_ptr<InputMessageContent> input_message_content_;
  static jfieldID input_message_content_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  inputInlineQueryResultVideo();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] id_ Unique identifier of the query result.
   * \param[in] title_ Title of the result.
   * \param[in] description_ A short description of the result, if known.
   * \param[in] thumbnail_url_ The URL of the video thumbnail (JPEG), if it exists.
   * \param[in] video_url_ URL of the embedded video player or video file.
   * \param[in] mime_type_ MIME type of the content of the video URL, only &quot;text/html&quot; or &quot;video/mp4&quot; are currently supported.
   * \param[in] video_width_ Width of the video.
   * \param[in] video_height_ Height of the video.
   * \param[in] video_duration_ Video duration, in seconds.
   * \param[in] reply_markup_ The message reply markup. Must be of type replyMarkupInlineKeyboard or null.
   * \param[in] input_message_content_ The content of the message to be sent. Must be one of the following types: InputMessageText, InputMessageVideo, InputMessageLocation, InputMessageVenue or InputMessageContact.
   */
  inputInlineQueryResultVideo(std::string const &id_, std::string const &title_, std::string const &description_, std::string const &thumbnail_url_, std::string const &video_url_, std::string const &mime_type_, std::int32_t video_width_, std::int32_t video_height_, std::int32_t video_duration_, object_ptr<ReplyMarkup> &&reply_markup_, object_ptr<InputMessageContent> &&input_message_content_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1724073191;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<inputInlineQueryResultVideo> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Represents a link to an opus-encoded audio file within an OGG container, single channel audio.
 */
class inputInlineQueryResultVoiceNote final : public InputInlineQueryResult {
 public:
  static jclass Class;
  /// Unique identifier of the query result.
  std::string id_;
  static jfieldID id_fieldID;
  /// Title of the voice note.
  std::string title_;
  static jfieldID title_fieldID;
  /// The URL of the voice note file.
  std::string voice_note_url_;
  static jfieldID voice_note_url_fieldID;
  /// Duration of the voice note, in seconds.
  std::int32_t voice_note_duration_;
  static jfieldID voice_note_duration_fieldID;
  /// The message reply markup. Must be of type replyMarkupInlineKeyboard or null.
  object_ptr<ReplyMarkup> reply_markup_;
  static jfieldID reply_markup_fieldID;
  /// The content of the message to be sent. Must be one of the following types: InputMessageText, InputMessageVoiceNote, InputMessageLocation, InputMessageVenue or InputMessageContact.
  object_ptr<InputMessageContent> input_message_content_;
  static jfieldID input_message_content_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  inputInlineQueryResultVoiceNote();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] id_ Unique identifier of the query result.
   * \param[in] title_ Title of the voice note.
   * \param[in] voice_note_url_ The URL of the voice note file.
   * \param[in] voice_note_duration_ Duration of the voice note, in seconds.
   * \param[in] reply_markup_ The message reply markup. Must be of type replyMarkupInlineKeyboard or null.
   * \param[in] input_message_content_ The content of the message to be sent. Must be one of the following types: InputMessageText, InputMessageVoiceNote, InputMessageLocation, InputMessageVenue or InputMessageContact.
   */
  inputInlineQueryResultVoiceNote(std::string const &id_, std::string const &title_, std::string const &voice_note_url_, std::int32_t voice_note_duration_, object_ptr<ReplyMarkup> &&reply_markup_, object_ptr<InputMessageContent> &&input_message_content_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1790072503;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<inputInlineQueryResultVoiceNote> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * This class is an abstract base class.
 * The content of a message to send.
 */
class InputMessageContent: public Object {
 public:
  static jclass Class;

  static object_ptr<InputMessageContent> fetch(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A text message.
 */
class inputMessageText final : public InputMessageContent {
 public:
  static jclass Class;
  /// Formatted text to be sent; 1-GetOption(&quot;message_text_length_max&quot;) characters. Only Bold, Italic, Code, Pre, PreCode and TextUrl entities are allowed to be specified manually.
  object_ptr<formattedText> text_;
  static jfieldID text_fieldID;
  /// True, if rich web page previews for URLs in the message text should be disabled.
  bool disable_web_page_preview_;
  static jfieldID disable_web_page_preview_fieldID;
  /// True, if a chat message draft should be deleted.
  bool clear_draft_;
  static jfieldID clear_draft_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  inputMessageText();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] text_ Formatted text to be sent; 1-GetOption(&quot;message_text_length_max&quot;) characters. Only Bold, Italic, Code, Pre, PreCode and TextUrl entities are allowed to be specified manually.
   * \param[in] disable_web_page_preview_ True, if rich web page previews for URLs in the message text should be disabled.
   * \param[in] clear_draft_ True, if a chat message draft should be deleted.
   */
  inputMessageText(object_ptr<formattedText> &&text_, bool disable_web_page_preview_, bool clear_draft_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 247050392;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<inputMessageText> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * An animation message (GIF-style).
 */
class inputMessageAnimation final : public InputMessageContent {
 public:
  static jclass Class;
  /// Animation file to be sent.
  object_ptr<InputFile> animation_;
  static jfieldID animation_fieldID;
  /// Animation thumbnail, if available.
  object_ptr<inputThumbnail> thumbnail_;
  static jfieldID thumbnail_fieldID;
  /// Duration of the animation, in seconds.
  std::int32_t duration_;
  static jfieldID duration_fieldID;
  /// Width of the animation; may be replaced by the server.
  std::int32_t width_;
  static jfieldID width_fieldID;
  /// Height of the animation; may be replaced by the server.
  std::int32_t height_;
  static jfieldID height_fieldID;
  /// Animation caption; 0-GetOption(&quot;message_caption_length_max&quot;) characters.
  object_ptr<formattedText> caption_;
  static jfieldID caption_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  inputMessageAnimation();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] animation_ Animation file to be sent.
   * \param[in] thumbnail_ Animation thumbnail, if available.
   * \param[in] duration_ Duration of the animation, in seconds.
   * \param[in] width_ Width of the animation; may be replaced by the server.
   * \param[in] height_ Height of the animation; may be replaced by the server.
   * \param[in] caption_ Animation caption; 0-GetOption(&quot;message_caption_length_max&quot;) characters.
   */
  inputMessageAnimation(object_ptr<InputFile> &&animation_, object_ptr<inputThumbnail> &&thumbnail_, std::int32_t duration_, std::int32_t width_, std::int32_t height_, object_ptr<formattedText> &&caption_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 926542724;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<inputMessageAnimation> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * An audio message.
 */
class inputMessageAudio final : public InputMessageContent {
 public:
  static jclass Class;
  /// Audio file to be sent.
  object_ptr<InputFile> audio_;
  static jfieldID audio_fieldID;
  /// Thumbnail of the cover for the album, if available.
  object_ptr<inputThumbnail> album_cover_thumbnail_;
  static jfieldID album_cover_thumbnail_fieldID;
  /// Duration of the audio, in seconds; may be replaced by the server.
  std::int32_t duration_;
  static jfieldID duration_fieldID;
  /// Title of the audio; 0-64 characters; may be replaced by the server.
  std::string title_;
  static jfieldID title_fieldID;
  /// Performer of the audio; 0-64 characters, may be replaced by the server.
  std::string performer_;
  static jfieldID performer_fieldID;
  /// Audio caption; 0-GetOption(&quot;message_caption_length_max&quot;) characters.
  object_ptr<formattedText> caption_;
  static jfieldID caption_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  inputMessageAudio();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] audio_ Audio file to be sent.
   * \param[in] album_cover_thumbnail_ Thumbnail of the cover for the album, if available.
   * \param[in] duration_ Duration of the audio, in seconds; may be replaced by the server.
   * \param[in] title_ Title of the audio; 0-64 characters; may be replaced by the server.
   * \param[in] performer_ Performer of the audio; 0-64 characters, may be replaced by the server.
   * \param[in] caption_ Audio caption; 0-GetOption(&quot;message_caption_length_max&quot;) characters.
   */
  inputMessageAudio(object_ptr<InputFile> &&audio_, object_ptr<inputThumbnail> &&album_cover_thumbnail_, std::int32_t duration_, std::string const &title_, std::string const &performer_, object_ptr<formattedText> &&caption_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -626786126;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<inputMessageAudio> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A document message (general file).
 */
class inputMessageDocument final : public InputMessageContent {
 public:
  static jclass Class;
  /// Document to be sent.
  object_ptr<InputFile> document_;
  static jfieldID document_fieldID;
  /// Document thumbnail, if available.
  object_ptr<inputThumbnail> thumbnail_;
  static jfieldID thumbnail_fieldID;
  /// Document caption; 0-GetOption(&quot;message_caption_length_max&quot;) characters.
  object_ptr<formattedText> caption_;
  static jfieldID caption_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  inputMessageDocument();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] document_ Document to be sent.
   * \param[in] thumbnail_ Document thumbnail, if available.
   * \param[in] caption_ Document caption; 0-GetOption(&quot;message_caption_length_max&quot;) characters.
   */
  inputMessageDocument(object_ptr<InputFile> &&document_, object_ptr<inputThumbnail> &&thumbnail_, object_ptr<formattedText> &&caption_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 937970604;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<inputMessageDocument> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A photo message.
 */
class inputMessagePhoto final : public InputMessageContent {
 public:
  static jclass Class;
  /// Photo to send.
  object_ptr<InputFile> photo_;
  static jfieldID photo_fieldID;
  /// Photo thumbnail to be sent, this is sent to the other party in secret chats only.
  object_ptr<inputThumbnail> thumbnail_;
  static jfieldID thumbnail_fieldID;
  /// File identifiers of the stickers added to the photo, if applicable.
  std::vector<std::int32_t> added_sticker_file_ids_;
  static jfieldID added_sticker_file_ids_fieldID;
  /// Photo width.
  std::int32_t width_;
  static jfieldID width_fieldID;
  /// Photo height.
  std::int32_t height_;
  static jfieldID height_fieldID;
  /// Photo caption; 0-GetOption(&quot;message_caption_length_max&quot;) characters.
  object_ptr<formattedText> caption_;
  static jfieldID caption_fieldID;
  /// Photo TTL (Time To Live), in seconds (0-60). A non-zero TTL can be specified only in private chats.
  std::int32_t ttl_;
  static jfieldID ttl_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  inputMessagePhoto();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] photo_ Photo to send.
   * \param[in] thumbnail_ Photo thumbnail to be sent, this is sent to the other party in secret chats only.
   * \param[in] added_sticker_file_ids_ File identifiers of the stickers added to the photo, if applicable.
   * \param[in] width_ Photo width.
   * \param[in] height_ Photo height.
   * \param[in] caption_ Photo caption; 0-GetOption(&quot;message_caption_length_max&quot;) characters.
   * \param[in] ttl_ Photo TTL (Time To Live), in seconds (0-60). A non-zero TTL can be specified only in private chats.
   */
  inputMessagePhoto(object_ptr<InputFile> &&photo_, object_ptr<inputThumbnail> &&thumbnail_, std::vector<std::int32_t> &&added_sticker_file_ids_, std::int32_t width_, std::int32_t height_, object_ptr<formattedText> &&caption_, std::int32_t ttl_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1648801584;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<inputMessagePhoto> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A sticker message.
 */
class inputMessageSticker final : public InputMessageContent {
 public:
  static jclass Class;
  /// Sticker to be sent.
  object_ptr<InputFile> sticker_;
  static jfieldID sticker_fieldID;
  /// Sticker thumbnail, if available.
  object_ptr<inputThumbnail> thumbnail_;
  static jfieldID thumbnail_fieldID;
  /// Sticker width.
  std::int32_t width_;
  static jfieldID width_fieldID;
  /// Sticker height.
  std::int32_t height_;
  static jfieldID height_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  inputMessageSticker();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] sticker_ Sticker to be sent.
   * \param[in] thumbnail_ Sticker thumbnail, if available.
   * \param[in] width_ Sticker width.
   * \param[in] height_ Sticker height.
   */
  inputMessageSticker(object_ptr<InputFile> &&sticker_, object_ptr<inputThumbnail> &&thumbnail_, std::int32_t width_, std::int32_t height_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 740776325;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<inputMessageSticker> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A video message.
 */
class inputMessageVideo final : public InputMessageContent {
 public:
  static jclass Class;
  /// Video to be sent.
  object_ptr<InputFile> video_;
  static jfieldID video_fieldID;
  /// Video thumbnail, if available.
  object_ptr<inputThumbnail> thumbnail_;
  static jfieldID thumbnail_fieldID;
  /// File identifiers of the stickers added to the video, if applicable.
  std::vector<std::int32_t> added_sticker_file_ids_;
  static jfieldID added_sticker_file_ids_fieldID;
  /// Duration of the video, in seconds.
  std::int32_t duration_;
  static jfieldID duration_fieldID;
  /// Video width.
  std::int32_t width_;
  static jfieldID width_fieldID;
  /// Video height.
  std::int32_t height_;
  static jfieldID height_fieldID;
  /// True, if the video should be tried to be streamed.
  bool supports_streaming_;
  static jfieldID supports_streaming_fieldID;
  /// Video caption; 0-GetOption(&quot;message_caption_length_max&quot;) characters.
  object_ptr<formattedText> caption_;
  static jfieldID caption_fieldID;
  /// Video TTL (Time To Live), in seconds (0-60). A non-zero TTL can be specified only in private chats.
  std::int32_t ttl_;
  static jfieldID ttl_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  inputMessageVideo();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] video_ Video to be sent.
   * \param[in] thumbnail_ Video thumbnail, if available.
   * \param[in] added_sticker_file_ids_ File identifiers of the stickers added to the video, if applicable.
   * \param[in] duration_ Duration of the video, in seconds.
   * \param[in] width_ Video width.
   * \param[in] height_ Video height.
   * \param[in] supports_streaming_ True, if the video should be tried to be streamed.
   * \param[in] caption_ Video caption; 0-GetOption(&quot;message_caption_length_max&quot;) characters.
   * \param[in] ttl_ Video TTL (Time To Live), in seconds (0-60). A non-zero TTL can be specified only in private chats.
   */
  inputMessageVideo(object_ptr<InputFile> &&video_, object_ptr<inputThumbnail> &&thumbnail_, std::vector<std::int32_t> &&added_sticker_file_ids_, std::int32_t duration_, std::int32_t width_, std::int32_t height_, bool supports_streaming_, object_ptr<formattedText> &&caption_, std::int32_t ttl_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -2108486755;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<inputMessageVideo> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A video note message.
 */
class inputMessageVideoNote final : public InputMessageContent {
 public:
  static jclass Class;
  /// Video note to be sent.
  object_ptr<InputFile> video_note_;
  static jfieldID video_note_fieldID;
  /// Video thumbnail, if available.
  object_ptr<inputThumbnail> thumbnail_;
  static jfieldID thumbnail_fieldID;
  /// Duration of the video, in seconds.
  std::int32_t duration_;
  static jfieldID duration_fieldID;
  /// Video width and height; must be positive and not greater than 640.
  std::int32_t length_;
  static jfieldID length_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  inputMessageVideoNote();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] video_note_ Video note to be sent.
   * \param[in] thumbnail_ Video thumbnail, if available.
   * \param[in] duration_ Duration of the video, in seconds.
   * \param[in] length_ Video width and height; must be positive and not greater than 640.
   */
  inputMessageVideoNote(object_ptr<InputFile> &&video_note_, object_ptr<inputThumbnail> &&thumbnail_, std::int32_t duration_, std::int32_t length_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 279108859;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<inputMessageVideoNote> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A voice note message.
 */
class inputMessageVoiceNote final : public InputMessageContent {
 public:
  static jclass Class;
  /// Voice note to be sent.
  object_ptr<InputFile> voice_note_;
  static jfieldID voice_note_fieldID;
  /// Duration of the voice note, in seconds.
  std::int32_t duration_;
  static jfieldID duration_fieldID;
  /// Waveform representation of the voice note, in 5-bit format.
  std::string waveform_;
  static jfieldID waveform_fieldID;
  /// Voice note caption; 0-GetOption(&quot;message_caption_length_max&quot;) characters.
  object_ptr<formattedText> caption_;
  static jfieldID caption_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  inputMessageVoiceNote();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] voice_note_ Voice note to be sent.
   * \param[in] duration_ Duration of the voice note, in seconds.
   * \param[in] waveform_ Waveform representation of the voice note, in 5-bit format.
   * \param[in] caption_ Voice note caption; 0-GetOption(&quot;message_caption_length_max&quot;) characters.
   */
  inputMessageVoiceNote(object_ptr<InputFile> &&voice_note_, std::int32_t duration_, std::string const &waveform_, object_ptr<formattedText> &&caption_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 2136519657;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<inputMessageVoiceNote> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A message with a location.
 */
class inputMessageLocation final : public InputMessageContent {
 public:
  static jclass Class;
  /// Location to be sent.
  object_ptr<location> location_;
  static jfieldID location_fieldID;
  /// Period for which the location can be updated, in seconds; should bebetween 60 and 86400 for a live location and 0 otherwise.
  std::int32_t live_period_;
  static jfieldID live_period_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  inputMessageLocation();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] location_ Location to be sent.
   * \param[in] live_period_ Period for which the location can be updated, in seconds; should bebetween 60 and 86400 for a live location and 0 otherwise.
   */
  inputMessageLocation(object_ptr<location> &&location_, std::int32_t live_period_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1624179655;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<inputMessageLocation> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A message with information about a venue.
 */
class inputMessageVenue final : public InputMessageContent {
 public:
  static jclass Class;
  /// Venue to send.
  object_ptr<venue> venue_;
  static jfieldID venue_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  inputMessageVenue();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] venue_ Venue to send.
   */
  explicit inputMessageVenue(object_ptr<venue> &&venue_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1447926269;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<inputMessageVenue> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A message containing a user contact.
 */
class inputMessageContact final : public InputMessageContent {
 public:
  static jclass Class;
  /// Contact to send.
  object_ptr<contact> contact_;
  static jfieldID contact_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  inputMessageContact();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] contact_ Contact to send.
   */
  explicit inputMessageContact(object_ptr<contact> &&contact_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -982446849;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<inputMessageContact> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A message with a game; not supported for channels or secret chats.
 */
class inputMessageGame final : public InputMessageContent {
 public:
  static jclass Class;
  /// User identifier of the bot that owns the game.
  std::int32_t bot_user_id_;
  static jfieldID bot_user_id_fieldID;
  /// Short name of the game.
  std::string game_short_name_;
  static jfieldID game_short_name_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  inputMessageGame();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] bot_user_id_ User identifier of the bot that owns the game.
   * \param[in] game_short_name_ Short name of the game.
   */
  inputMessageGame(std::int32_t bot_user_id_, std::string const &game_short_name_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1728000914;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<inputMessageGame> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A message with an invoice; can be used only by bots and only in private chats.
 */
class inputMessageInvoice final : public InputMessageContent {
 public:
  static jclass Class;
  /// Invoice.
  object_ptr<invoice> invoice_;
  static jfieldID invoice_fieldID;
  /// Product title; 1-32 characters.
  std::string title_;
  static jfieldID title_fieldID;
  /// Product description; 0-255 characters.
  std::string description_;
  static jfieldID description_fieldID;
  /// Product photo URL; optional.
  std::string photo_url_;
  static jfieldID photo_url_fieldID;
  /// Product photo size.
  std::int32_t photo_size_;
  static jfieldID photo_size_fieldID;
  /// Product photo width.
  std::int32_t photo_width_;
  static jfieldID photo_width_fieldID;
  /// Product photo height.
  std::int32_t photo_height_;
  static jfieldID photo_height_fieldID;
  /// The invoice payload.
  std::string payload_;
  static jfieldID payload_fieldID;
  /// Payment provider token.
  std::string provider_token_;
  static jfieldID provider_token_fieldID;
  /// JSON-encoded data about the invoice, which will be shared with the payment provider.
  std::string provider_data_;
  static jfieldID provider_data_fieldID;
  /// Unique invoice bot start_parameter for the generation of this invoice.
  std::string start_parameter_;
  static jfieldID start_parameter_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  inputMessageInvoice();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] invoice_ Invoice.
   * \param[in] title_ Product title; 1-32 characters.
   * \param[in] description_ Product description; 0-255 characters.
   * \param[in] photo_url_ Product photo URL; optional.
   * \param[in] photo_size_ Product photo size.
   * \param[in] photo_width_ Product photo width.
   * \param[in] photo_height_ Product photo height.
   * \param[in] payload_ The invoice payload.
   * \param[in] provider_token_ Payment provider token.
   * \param[in] provider_data_ JSON-encoded data about the invoice, which will be shared with the payment provider.
   * \param[in] start_parameter_ Unique invoice bot start_parameter for the generation of this invoice.
   */
  inputMessageInvoice(object_ptr<invoice> &&invoice_, std::string const &title_, std::string const &description_, std::string const &photo_url_, std::int32_t photo_size_, std::int32_t photo_width_, std::int32_t photo_height_, std::string const &payload_, std::string const &provider_token_, std::string const &provider_data_, std::string const &start_parameter_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1038812175;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<inputMessageInvoice> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A forwarded message.
 */
class inputMessageForwarded final : public InputMessageContent {
 public:
  static jclass Class;
  /// Identifier for the chat this forwarded message came from.
  std::int64_t from_chat_id_;
  static jfieldID from_chat_id_fieldID;
  /// Identifier of the message to forward.
  std::int64_t message_id_;
  static jfieldID message_id_fieldID;
  /// True, if a game message should be shared within a launched game; applies only to game messages.
  bool in_game_share_;
  static jfieldID in_game_share_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  inputMessageForwarded();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] from_chat_id_ Identifier for the chat this forwarded message came from.
   * \param[in] message_id_ Identifier of the message to forward.
   * \param[in] in_game_share_ True, if a game message should be shared within a launched game; applies only to game messages.
   */
  inputMessageForwarded(std::int64_t from_chat_id_, std::int64_t message_id_, bool in_game_share_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1561363198;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<inputMessageForwarded> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * This class is an abstract base class.
 * Contains information about a Telegram Passport element to be saved.
 */
class InputPassportElement: public Object {
 public:
  static jclass Class;

  static object_ptr<InputPassportElement> fetch(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A Telegram Passport element to be saved containing the user's personal details.
 */
class inputPassportElementPersonalDetails final : public InputPassportElement {
 public:
  static jclass Class;
  /// Personal details of the user.
  object_ptr<personalDetails> personal_details_;
  static jfieldID personal_details_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  inputPassportElementPersonalDetails();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] personal_details_ Personal details of the user.
   */
  explicit inputPassportElementPersonalDetails(object_ptr<personalDetails> &&personal_details_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 164791359;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<inputPassportElementPersonalDetails> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A Telegram Passport element to be saved containing the user's passport.
 */
class inputPassportElementPassport final : public InputPassportElement {
 public:
  static jclass Class;
  /// The passport to be saved.
  object_ptr<inputIdentityDocument> passport_;
  static jfieldID passport_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  inputPassportElementPassport();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] passport_ The passport to be saved.
   */
  explicit inputPassportElementPassport(object_ptr<inputIdentityDocument> &&passport_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -497011356;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<inputPassportElementPassport> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A Telegram Passport element to be saved containing the user's driver license.
 */
class inputPassportElementDriverLicense final : public InputPassportElement {
 public:
  static jclass Class;
  /// The driver license to be saved.
  object_ptr<inputIdentityDocument> driver_license_;
  static jfieldID driver_license_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  inputPassportElementDriverLicense();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] driver_license_ The driver license to be saved.
   */
  explicit inputPassportElementDriverLicense(object_ptr<inputIdentityDocument> &&driver_license_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 304813264;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<inputPassportElementDriverLicense> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A Telegram Passport element to be saved containing the user's identity card.
 */
class inputPassportElementIdentityCard final : public InputPassportElement {
 public:
  static jclass Class;
  /// The identity card to be saved.
  object_ptr<inputIdentityDocument> identity_card_;
  static jfieldID identity_card_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  inputPassportElementIdentityCard();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] identity_card_ The identity card to be saved.
   */
  explicit inputPassportElementIdentityCard(object_ptr<inputIdentityDocument> &&identity_card_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -9963390;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<inputPassportElementIdentityCard> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A Telegram Passport element to be saved containing the user's internal passport.
 */
class inputPassportElementInternalPassport final : public InputPassportElement {
 public:
  static jclass Class;
  /// The internal passport to be saved.
  object_ptr<inputIdentityDocument> internal_passport_;
  static jfieldID internal_passport_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  inputPassportElementInternalPassport();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] internal_passport_ The internal passport to be saved.
   */
  explicit inputPassportElementInternalPassport(object_ptr<inputIdentityDocument> &&internal_passport_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 715360043;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<inputPassportElementInternalPassport> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A Telegram Passport element to be saved containing the user's address.
 */
class inputPassportElementAddress final : public InputPassportElement {
 public:
  static jclass Class;
  /// The address to be saved.
  object_ptr<address> address_;
  static jfieldID address_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  inputPassportElementAddress();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] address_ The address to be saved.
   */
  explicit inputPassportElementAddress(object_ptr<address> &&address_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 461630480;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<inputPassportElementAddress> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A Telegram Passport element to be saved containing the user's utility bill.
 */
class inputPassportElementUtilityBill final : public InputPassportElement {
 public:
  static jclass Class;
  /// The utility bill to be saved.
  object_ptr<inputPersonalDocument> utility_bill_;
  static jfieldID utility_bill_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  inputPassportElementUtilityBill();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] utility_bill_ The utility bill to be saved.
   */
  explicit inputPassportElementUtilityBill(object_ptr<inputPersonalDocument> &&utility_bill_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1389203841;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<inputPassportElementUtilityBill> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A Telegram Passport element to be saved containing the user's bank statement.
 */
class inputPassportElementBankStatement final : public InputPassportElement {
 public:
  static jclass Class;
  /// The bank statement to be saved.
  object_ptr<inputPersonalDocument> bank_statement_;
  static jfieldID bank_statement_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  inputPassportElementBankStatement();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] bank_statement_ The bank statement to be saved.
   */
  explicit inputPassportElementBankStatement(object_ptr<inputPersonalDocument> &&bank_statement_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -26585208;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<inputPassportElementBankStatement> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A Telegram Passport element to be saved containing the user's rental agreement.
 */
class inputPassportElementRentalAgreement final : public InputPassportElement {
 public:
  static jclass Class;
  /// The rental agreement to be saved.
  object_ptr<inputPersonalDocument> rental_agreement_;
  static jfieldID rental_agreement_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  inputPassportElementRentalAgreement();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] rental_agreement_ The rental agreement to be saved.
   */
  explicit inputPassportElementRentalAgreement(object_ptr<inputPersonalDocument> &&rental_agreement_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1736154155;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<inputPassportElementRentalAgreement> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A Telegram Passport element to be saved containing the user's passport registration.
 */
class inputPassportElementPassportRegistration final : public InputPassportElement {
 public:
  static jclass Class;
  /// The passport registration page to be saved.
  object_ptr<inputPersonalDocument> passport_registration_;
  static jfieldID passport_registration_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  inputPassportElementPassportRegistration();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] passport_registration_ The passport registration page to be saved.
   */
  explicit inputPassportElementPassportRegistration(object_ptr<inputPersonalDocument> &&passport_registration_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1314562128;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<inputPassportElementPassportRegistration> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A Telegram Passport element to be saved containing the user's temporary registration.
 */
class inputPassportElementTemporaryRegistration final : public InputPassportElement {
 public:
  static jclass Class;
  /// The temporary registration document to be saved.
  object_ptr<inputPersonalDocument> temporary_registration_;
  static jfieldID temporary_registration_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  inputPassportElementTemporaryRegistration();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] temporary_registration_ The temporary registration document to be saved.
   */
  explicit inputPassportElementTemporaryRegistration(object_ptr<inputPersonalDocument> &&temporary_registration_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1913238047;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<inputPassportElementTemporaryRegistration> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A Telegram Passport element to be saved containing the user's phone number.
 */
class inputPassportElementPhoneNumber final : public InputPassportElement {
 public:
  static jclass Class;
  /// The phone number to be saved.
  std::string phone_number_;
  static jfieldID phone_number_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  inputPassportElementPhoneNumber();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] phone_number_ The phone number to be saved.
   */
  explicit inputPassportElementPhoneNumber(std::string const &phone_number_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1319357497;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<inputPassportElementPhoneNumber> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A Telegram Passport element to be saved containing the user's email address.
 */
class inputPassportElementEmailAddress final : public InputPassportElement {
 public:
  static jclass Class;
  /// The email address to be saved.
  std::string email_address_;
  static jfieldID email_address_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  inputPassportElementEmailAddress();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] email_address_ The email address to be saved.
   */
  explicit inputPassportElementEmailAddress(std::string const &email_address_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -248605659;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<inputPassportElementEmailAddress> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Contains the description of an error in a Telegram Passport element; for bots only.
 */
class inputPassportElementError final : public Object {
 public:
  static jclass Class;
  /// Type of Telegram Passport element that has the error.
  object_ptr<PassportElementType> type_;
  static jfieldID type_fieldID;
  /// Error message.
  std::string message_;
  static jfieldID message_fieldID;
  /// Error source.
  object_ptr<InputPassportElementErrorSource> source_;
  static jfieldID source_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  inputPassportElementError();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] type_ Type of Telegram Passport element that has the error.
   * \param[in] message_ Error message.
   * \param[in] source_ Error source.
   */
  inputPassportElementError(object_ptr<PassportElementType> &&type_, std::string const &message_, object_ptr<InputPassportElementErrorSource> &&source_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 285756898;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<inputPassportElementError> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * This class is an abstract base class.
 * Contains the description of an error in a Telegram Passport element; for bots only.
 */
class InputPassportElementErrorSource: public Object {
 public:
  static jclass Class;

  static object_ptr<InputPassportElementErrorSource> fetch(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * The element contains an error in an unspecified place. The error will be considered resolved when new data is added.
 */
class inputPassportElementErrorSourceUnspecified final : public InputPassportElementErrorSource {
 public:
  static jclass Class;
  /// Current hash of the entire element.
  std::string element_hash_;
  static jfieldID element_hash_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  inputPassportElementErrorSourceUnspecified();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] element_hash_ Current hash of the entire element.
   */
  explicit inputPassportElementErrorSourceUnspecified(std::string const &element_hash_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 267230319;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<inputPassportElementErrorSourceUnspecified> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A data field contains an error. The error is considered resolved when the field's value changes.
 */
class inputPassportElementErrorSourceDataField final : public InputPassportElementErrorSource {
 public:
  static jclass Class;
  /// Field name.
  std::string field_name_;
  static jfieldID field_name_fieldID;
  /// Current data hash.
  std::string data_hash_;
  static jfieldID data_hash_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  inputPassportElementErrorSourceDataField();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] field_name_ Field name.
   * \param[in] data_hash_ Current data hash.
   */
  inputPassportElementErrorSourceDataField(std::string const &field_name_, std::string const &data_hash_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -426795002;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<inputPassportElementErrorSourceDataField> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * The front side of the document contains an error. The error is considered resolved when the file with the front side of the document changes.
 */
class inputPassportElementErrorSourceFrontSide final : public InputPassportElementErrorSource {
 public:
  static jclass Class;
  /// Current hash of the file containing the front side.
  std::string file_hash_;
  static jfieldID file_hash_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  inputPassportElementErrorSourceFrontSide();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] file_hash_ Current hash of the file containing the front side.
   */
  explicit inputPassportElementErrorSourceFrontSide(std::string const &file_hash_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 588023741;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<inputPassportElementErrorSourceFrontSide> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * The reverse side of the document contains an error. The error is considered resolved when the file with the reverse side of the document changes.
 */
class inputPassportElementErrorSourceReverseSide final : public InputPassportElementErrorSource {
 public:
  static jclass Class;
  /// Current hash of the file containing the reverse side.
  std::string file_hash_;
  static jfieldID file_hash_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  inputPassportElementErrorSourceReverseSide();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] file_hash_ Current hash of the file containing the reverse side.
   */
  explicit inputPassportElementErrorSourceReverseSide(std::string const &file_hash_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 413072891;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<inputPassportElementErrorSourceReverseSide> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * The selfie contains an error. The error is considered resolved when the file with the selfie changes.
 */
class inputPassportElementErrorSourceSelfie final : public InputPassportElementErrorSource {
 public:
  static jclass Class;
  /// Current hash of the file containing the selfie.
  std::string file_hash_;
  static jfieldID file_hash_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  inputPassportElementErrorSourceSelfie();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] file_hash_ Current hash of the file containing the selfie.
   */
  explicit inputPassportElementErrorSourceSelfie(std::string const &file_hash_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -773575528;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<inputPassportElementErrorSourceSelfie> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * One of the files containing the translation of the document contains an error. The error is considered resolved when the file with the translation changes.
 */
class inputPassportElementErrorSourceTranslationFile final : public InputPassportElementErrorSource {
 public:
  static jclass Class;
  /// Current hash of the file containing the translation.
  std::string file_hash_;
  static jfieldID file_hash_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  inputPassportElementErrorSourceTranslationFile();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] file_hash_ Current hash of the file containing the translation.
   */
  explicit inputPassportElementErrorSourceTranslationFile(std::string const &file_hash_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 505842299;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<inputPassportElementErrorSourceTranslationFile> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * The translation of the document contains an error. The error is considered resolved when the list of files changes.
 */
class inputPassportElementErrorSourceTranslationFiles final : public InputPassportElementErrorSource {
 public:
  static jclass Class;
  /// Current hashes of all files with the translation.
  std::vector<std::string> file_hashes_;
  static jfieldID file_hashes_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  inputPassportElementErrorSourceTranslationFiles();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] file_hashes_ Current hashes of all files with the translation.
   */
  explicit inputPassportElementErrorSourceTranslationFiles(std::vector<std::string> &&file_hashes_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -527254048;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<inputPassportElementErrorSourceTranslationFiles> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * The file contains an error. The error is considered resolved when the file changes.
 */
class inputPassportElementErrorSourceFile final : public InputPassportElementErrorSource {
 public:
  static jclass Class;
  /// Current hash of the file which has the error.
  std::string file_hash_;
  static jfieldID file_hash_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  inputPassportElementErrorSourceFile();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] file_hash_ Current hash of the file which has the error.
   */
  explicit inputPassportElementErrorSourceFile(std::string const &file_hash_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -298492469;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<inputPassportElementErrorSourceFile> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * The list of attached files contains an error. The error is considered resolved when the file list changes.
 */
class inputPassportElementErrorSourceFiles final : public InputPassportElementErrorSource {
 public:
  static jclass Class;
  /// Current hashes of all attached files.
  std::vector<std::string> file_hashes_;
  static jfieldID file_hashes_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  inputPassportElementErrorSourceFiles();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] file_hashes_ Current hashes of all attached files.
   */
  explicit inputPassportElementErrorSourceFiles(std::vector<std::string> &&file_hashes_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -2008541640;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<inputPassportElementErrorSourceFiles> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A personal document to be saved to Telegram Passport.
 */
class inputPersonalDocument final : public Object {
 public:
  static jclass Class;
  /// List of files containing the pages of the document.
  std::vector<object_ptr<InputFile>> files_;
  static jfieldID files_fieldID;
  /// List of files containing a certified English translation of the document.
  std::vector<object_ptr<InputFile>> translation_;
  static jfieldID translation_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  inputPersonalDocument();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] files_ List of files containing the pages of the document.
   * \param[in] translation_ List of files containing a certified English translation of the document.
   */
  inputPersonalDocument(std::vector<object_ptr<InputFile>> &&files_, std::vector<object_ptr<InputFile>> &&translation_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1676966826;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<inputPersonalDocument> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Describes a sticker that should be added to a sticker set.
 */
class inputSticker final : public Object {
 public:
  static jclass Class;
  /// PNG image with the sticker; must be up to 512 kB in size and fit in a 512x512 square.
  object_ptr<InputFile> png_sticker_;
  static jfieldID png_sticker_fieldID;
  /// Emoji corresponding to the sticker.
  std::string emojis_;
  static jfieldID emojis_fieldID;
  /// For masks, position where the mask should be placed; may be null.
  object_ptr<maskPosition> mask_position_;
  static jfieldID mask_position_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  inputSticker();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] png_sticker_ PNG image with the sticker; must be up to 512 kB in size and fit in a 512x512 square.
   * \param[in] emojis_ Emoji corresponding to the sticker.
   * \param[in] mask_position_ For masks, position where the mask should be placed; may be null.
   */
  inputSticker(object_ptr<InputFile> &&png_sticker_, std::string const &emojis_, object_ptr<maskPosition> &&mask_position_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1998602205;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<inputSticker> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A thumbnail to be sent along with a file; should be in JPEG or WEBP format for stickers, and less than 200 kB in size.
 */
class inputThumbnail final : public Object {
 public:
  static jclass Class;
  /// Thumbnail file to send. Sending thumbnails by file_id is currently not supported.
  object_ptr<InputFile> thumbnail_;
  static jfieldID thumbnail_fieldID;
  /// Thumbnail width, usually shouldn't exceed 90. Use 0 if unknown.
  std::int32_t width_;
  static jfieldID width_fieldID;
  /// Thumbnail height, usually shouldn't exceed 90. Use 0 if unknown.
  std::int32_t height_;
  static jfieldID height_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  inputThumbnail();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] thumbnail_ Thumbnail file to send. Sending thumbnails by file_id is currently not supported.
   * \param[in] width_ Thumbnail width, usually shouldn't exceed 90. Use 0 if unknown.
   * \param[in] height_ Thumbnail height, usually shouldn't exceed 90. Use 0 if unknown.
   */
  inputThumbnail(object_ptr<InputFile> &&thumbnail_, std::int32_t width_, std::int32_t height_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1582387236;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<inputThumbnail> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Product invoice.
 */
class invoice final : public Object {
 public:
  static jclass Class;
  /// ISO 4217 currency code.
  std::string currency_;
  static jfieldID currency_fieldID;
  /// A list of objects used to calculate the total price of the product.
  std::vector<object_ptr<labeledPricePart>> price_parts_;
  static jfieldID price_parts_fieldID;
  /// True, if the payment is a test payment.
  bool is_test_;
  static jfieldID is_test_fieldID;
  /// True, if the user's name is needed for payment.
  bool need_name_;
  static jfieldID need_name_fieldID;
  /// True, if the user's phone number is needed for payment.
  bool need_phone_number_;
  static jfieldID need_phone_number_fieldID;
  /// True, if the user's email address is needed for payment.
  bool need_email_address_;
  static jfieldID need_email_address_fieldID;
  /// True, if the user's shipping address is needed for payment.
  bool need_shipping_address_;
  static jfieldID need_shipping_address_fieldID;
  /// True, if the user's phone number will be sent to the provider.
  bool send_phone_number_to_provider_;
  static jfieldID send_phone_number_to_provider_fieldID;
  /// True, if the user's email address will be sent to the provider.
  bool send_email_address_to_provider_;
  static jfieldID send_email_address_to_provider_fieldID;
  /// True, if the total price depends on the shipping method.
  bool is_flexible_;
  static jfieldID is_flexible_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  invoice();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] currency_ ISO 4217 currency code.
   * \param[in] price_parts_ A list of objects used to calculate the total price of the product.
   * \param[in] is_test_ True, if the payment is a test payment.
   * \param[in] need_name_ True, if the user's name is needed for payment.
   * \param[in] need_phone_number_ True, if the user's phone number is needed for payment.
   * \param[in] need_email_address_ True, if the user's email address is needed for payment.
   * \param[in] need_shipping_address_ True, if the user's shipping address is needed for payment.
   * \param[in] send_phone_number_to_provider_ True, if the user's phone number will be sent to the provider.
   * \param[in] send_email_address_to_provider_ True, if the user's email address will be sent to the provider.
   * \param[in] is_flexible_ True, if the total price depends on the shipping method.
   */
  invoice(std::string const &currency_, std::vector<object_ptr<labeledPricePart>> &&price_parts_, bool is_test_, bool need_name_, bool need_phone_number_, bool need_email_address_, bool need_shipping_address_, bool send_phone_number_to_provider_, bool send_email_address_to_provider_, bool is_flexible_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -368451690;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<invoice> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Represents a single button in a bot keyboard.
 */
class keyboardButton final : public Object {
 public:
  static jclass Class;
  /// Text of the button.
  std::string text_;
  static jfieldID text_fieldID;
  /// Type of the button.
  object_ptr<KeyboardButtonType> type_;
  static jfieldID type_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  keyboardButton();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] text_ Text of the button.
   * \param[in] type_ Type of the button.
   */
  keyboardButton(std::string const &text_, object_ptr<KeyboardButtonType> &&type_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -2069836172;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<keyboardButton> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * This class is an abstract base class.
 * Describes a keyboard button type.
 */
class KeyboardButtonType: public Object {
 public:
  static jclass Class;

  static object_ptr<KeyboardButtonType> fetch(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A simple button, with text that should be sent when the button is pressed.
 */
class keyboardButtonTypeText final : public KeyboardButtonType {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  keyboardButtonTypeText();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1773037256;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<keyboardButtonTypeText> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A button that sends the user's phone number when pressed; available only in private chats.
 */
class keyboardButtonTypeRequestPhoneNumber final : public KeyboardButtonType {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  keyboardButtonTypeRequestPhoneNumber();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1529235527;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<keyboardButtonTypeRequestPhoneNumber> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A button that sends the user's location when pressed; available only in private chats.
 */
class keyboardButtonTypeRequestLocation final : public KeyboardButtonType {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  keyboardButtonTypeRequestLocation();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -125661955;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<keyboardButtonTypeRequestLocation> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Portion of the price of a product (e.g., &quot;delivery cost&quot;, &quot;tax amount&quot;).
 */
class labeledPricePart final : public Object {
 public:
  static jclass Class;
  /// Label for this portion of the product price.
  std::string label_;
  static jfieldID label_fieldID;
  /// Currency amount in minimal quantity of the currency.
  std::int64_t amount_;
  static jfieldID amount_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  labeledPricePart();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] label_ Label for this portion of the product price.
   * \param[in] amount_ Currency amount in minimal quantity of the currency.
   */
  labeledPricePart(std::string const &label_, std::int64_t amount_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 552789798;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<labeledPricePart> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Contains information about a language pack.
 */
class languagePackInfo final : public Object {
 public:
  static jclass Class;
  /// Unique language pack identifier.
  std::string id_;
  static jfieldID id_fieldID;
  /// Language name.
  std::string name_;
  static jfieldID name_fieldID;
  /// Name of the language in that language.
  std::string native_name_;
  static jfieldID native_name_fieldID;
  /// Total number of non-deleted strings from the language pack available locally.
  std::int32_t local_string_count_;
  static jfieldID local_string_count_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  languagePackInfo();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] id_ Unique language pack identifier.
   * \param[in] name_ Language name.
   * \param[in] native_name_ Name of the language in that language.
   * \param[in] local_string_count_ Total number of non-deleted strings from the language pack available locally.
   */
  languagePackInfo(std::string const &id_, std::string const &name_, std::string const &native_name_, std::int32_t local_string_count_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1716484242;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<languagePackInfo> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Represents one language pack string.
 */
class languagePackString final : public Object {
 public:
  static jclass Class;
  /// String key.
  std::string key_;
  static jfieldID key_fieldID;
  /// String value.
  object_ptr<LanguagePackStringValue> value_;
  static jfieldID value_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  languagePackString();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] key_ String key.
   * \param[in] value_ String value.
   */
  languagePackString(std::string const &key_, object_ptr<LanguagePackStringValue> &&value_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1307632736;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<languagePackString> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * This class is an abstract base class.
 * Represents the value of a string in a language pack.
 */
class LanguagePackStringValue: public Object {
 public:
  static jclass Class;

  static object_ptr<LanguagePackStringValue> fetch(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * An ordinary language pack string.
 */
class languagePackStringValueOrdinary final : public LanguagePackStringValue {
 public:
  static jclass Class;
  /// String value.
  std::string value_;
  static jfieldID value_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  languagePackStringValueOrdinary();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] value_ String value.
   */
  explicit languagePackStringValueOrdinary(std::string const &value_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -249256352;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<languagePackStringValueOrdinary> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A language pack string which has different forms based on the number of some object it mentions.
 */
class languagePackStringValuePluralized final : public LanguagePackStringValue {
 public:
  static jclass Class;
  /// Value for zero objects.
  std::string zero_value_;
  static jfieldID zero_value_fieldID;
  /// Value for one object.
  std::string one_value_;
  static jfieldID one_value_fieldID;
  /// Value for two objects.
  std::string two_value_;
  static jfieldID two_value_fieldID;
  /// Value for few objects.
  std::string few_value_;
  static jfieldID few_value_fieldID;
  /// Value for many objects.
  std::string many_value_;
  static jfieldID many_value_fieldID;
  /// Default value.
  std::string other_value_;
  static jfieldID other_value_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  languagePackStringValuePluralized();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] zero_value_ Value for zero objects.
   * \param[in] one_value_ Value for one object.
   * \param[in] two_value_ Value for two objects.
   * \param[in] few_value_ Value for few objects.
   * \param[in] many_value_ Value for many objects.
   * \param[in] other_value_ Default value.
   */
  languagePackStringValuePluralized(std::string const &zero_value_, std::string const &one_value_, std::string const &two_value_, std::string const &few_value_, std::string const &many_value_, std::string const &other_value_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1906840261;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<languagePackStringValuePluralized> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A deleted language pack string, the value should be taken from the built-in english language pack.
 */
class languagePackStringValueDeleted final : public LanguagePackStringValue {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  languagePackStringValueDeleted();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1834792698;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<languagePackStringValueDeleted> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Contains a list of language pack strings.
 */
class languagePackStrings final : public Object {
 public:
  static jclass Class;
  /// A list of language pack strings.
  std::vector<object_ptr<languagePackString>> strings_;
  static jfieldID strings_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  languagePackStrings();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] strings_ A list of language pack strings.
   */
  explicit languagePackStrings(std::vector<object_ptr<languagePackString>> &&strings_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1172082922;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<languagePackStrings> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * This class is an abstract base class.
 * Represents the relationship between user A and user B. For incoming_link, user A is the current user; for outgoing_link, user B is the current user.
 */
class LinkState: public Object {
 public:
  static jclass Class;

  static object_ptr<LinkState> fetch(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * The phone number of user A is not known to user B.
 */
class linkStateNone final : public LinkState {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  linkStateNone();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 951430287;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<linkStateNone> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * The phone number of user A is known but that number has not been saved to the contacts list of user B.
 */
class linkStateKnowsPhoneNumber final : public LinkState {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  linkStateKnowsPhoneNumber();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 380898199;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<linkStateKnowsPhoneNumber> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * The phone number of user A has been saved to the contacts list of user B.
 */
class linkStateIsContact final : public LinkState {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  linkStateIsContact();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1000499465;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<linkStateIsContact> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Represents a local file.
 */
class localFile final : public Object {
 public:
  static jclass Class;
  /// Local path to the locally available file part; may be empty.
  std::string path_;
  static jfieldID path_fieldID;
  /// True, if it is possible to try to download or generate the file.
  bool can_be_downloaded_;
  static jfieldID can_be_downloaded_fieldID;
  /// True, if the file can be deleted.
  bool can_be_deleted_;
  static jfieldID can_be_deleted_fieldID;
  /// True, if the file is currently being downloaded (or a local copy is being generated by some other means).
  bool is_downloading_active_;
  static jfieldID is_downloading_active_fieldID;
  /// True, if the local copy is fully available.
  bool is_downloading_completed_;
  static jfieldID is_downloading_completed_fieldID;
  /// If is_downloading_completed is false, then only some prefix of the file is ready to be read. downloaded_prefix_size is the size of that prefix.
  std::int32_t downloaded_prefix_size_;
  static jfieldID downloaded_prefix_size_fieldID;
  /// Total downloaded file bytes. Should be used only for calculating download progress. The actual file size may be bigger, and some parts of it may contain garbage.
  std::int32_t downloaded_size_;
  static jfieldID downloaded_size_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  localFile();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] path_ Local path to the locally available file part; may be empty.
   * \param[in] can_be_downloaded_ True, if it is possible to try to download or generate the file.
   * \param[in] can_be_deleted_ True, if the file can be deleted.
   * \param[in] is_downloading_active_ True, if the file is currently being downloaded (or a local copy is being generated by some other means).
   * \param[in] is_downloading_completed_ True, if the local copy is fully available.
   * \param[in] downloaded_prefix_size_ If is_downloading_completed is false, then only some prefix of the file is ready to be read. downloaded_prefix_size is the size of that prefix.
   * \param[in] downloaded_size_ Total downloaded file bytes. Should be used only for calculating download progress. The actual file size may be bigger, and some parts of it may contain garbage.
   */
  localFile(std::string const &path_, bool can_be_downloaded_, bool can_be_deleted_, bool is_downloading_active_, bool is_downloading_completed_, std::int32_t downloaded_prefix_size_, std::int32_t downloaded_size_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 847939462;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<localFile> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Contains information about the current localization target.
 */
class localizationTargetInfo final : public Object {
 public:
  static jclass Class;
  /// List of available language packs for this application.
  std::vector<object_ptr<languagePackInfo>> language_packs_;
  static jfieldID language_packs_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  localizationTargetInfo();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] language_packs_ List of available language packs for this application.
   */
  explicit localizationTargetInfo(std::vector<object_ptr<languagePackInfo>> &&language_packs_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -2048670809;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<localizationTargetInfo> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Describes a location on planet Earth.
 */
class location final : public Object {
 public:
  static jclass Class;
  /// Latitude of the location in degrees; as defined by the sender.
  double latitude_;
  static jfieldID latitude_fieldID;
  /// Longitude of the location, in degrees; as defined by the sender.
  double longitude_;
  static jfieldID longitude_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  location();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] latitude_ Latitude of the location in degrees; as defined by the sender.
   * \param[in] longitude_ Longitude of the location, in degrees; as defined by the sender.
   */
  location(double latitude_, double longitude_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 749028016;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<location> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * This class is an abstract base class.
 * Part of the face, relative to which a mask should be placed.
 */
class MaskPoint: public Object {
 public:
  static jclass Class;

  static object_ptr<MaskPoint> fetch(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A mask should be placed relatively to the forehead.
 */
class maskPointForehead final : public MaskPoint {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  maskPointForehead();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1027512005;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<maskPointForehead> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A mask should be placed relatively to the eyes.
 */
class maskPointEyes final : public MaskPoint {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  maskPointEyes();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1748310861;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<maskPointEyes> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A mask should be placed relatively to the mouth.
 */
class maskPointMouth final : public MaskPoint {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  maskPointMouth();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 411773406;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<maskPointMouth> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A mask should be placed relatively to the chin.
 */
class maskPointChin final : public MaskPoint {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  maskPointChin();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 534995335;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<maskPointChin> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Position on a photo where a mask should be placed.
 */
class maskPosition final : public Object {
 public:
  static jclass Class;
  /// Part of the face, relative to which the mask should be placed.
  object_ptr<MaskPoint> point_;
  static jfieldID point_fieldID;
  /// Shift by X-axis measured in widths of the mask scaled to the face size, from left to right. (For example, -1.0 will place the mask just to the left of the default mask position.)
  double x_shift_;
  static jfieldID x_shift_fieldID;
  /// Shift by Y-axis measured in heights of the mask scaled to the face size, from top to bottom. (For example, 1.0 will place the mask just below the default mask position.)
  double y_shift_;
  static jfieldID y_shift_fieldID;
  /// Mask scaling coefficient. (For example, 2.0 means a doubled size.)
  double scale_;
  static jfieldID scale_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  maskPosition();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] point_ Part of the face, relative to which the mask should be placed.
   * \param[in] x_shift_ Shift by X-axis measured in widths of the mask scaled to the face size, from left to right. (For example, -1.0 will place the mask just to the left of the default mask position.)
   * \param[in] y_shift_ Shift by Y-axis measured in heights of the mask scaled to the face size, from top to bottom. (For example, 1.0 will place the mask just below the default mask position.)
   * \param[in] scale_ Mask scaling coefficient. (For example, 2.0 means a doubled size.)
   */
  maskPosition(object_ptr<MaskPoint> &&point_, double x_shift_, double y_shift_, double scale_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -2097433026;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<maskPosition> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Describes a message.
 */
class message final : public Object {
 public:
  static jclass Class;
  /// Message identifier, unique for the chat to which the message belongs.
  std::int64_t id_;
  static jfieldID id_fieldID;
  /// Identifier of the user who sent the message; 0 if unknown. It is unknown for channel posts.
  std::int32_t sender_user_id_;
  static jfieldID sender_user_id_fieldID;
  /// Chat identifier.
  std::int64_t chat_id_;
  static jfieldID chat_id_fieldID;
  /// Information about the sending state of the message; may be null.
  object_ptr<MessageSendingState> sending_state_;
  static jfieldID sending_state_fieldID;
  /// True, if the message is outgoing.
  bool is_outgoing_;
  static jfieldID is_outgoing_fieldID;
  /// True, if the message can be edited.
  bool can_be_edited_;
  static jfieldID can_be_edited_fieldID;
  /// True, if the message can be forwarded.
  bool can_be_forwarded_;
  static jfieldID can_be_forwarded_fieldID;
  /// True, if the message can be deleted only for the current user while other users will continue to see it.
  bool can_be_deleted_only_for_self_;
  static jfieldID can_be_deleted_only_for_self_fieldID;
  /// True, if the message can be deleted for all users.
  bool can_be_deleted_for_all_users_;
  static jfieldID can_be_deleted_for_all_users_fieldID;
  /// True, if the message is a channel post. All messages to channels are channel posts, all other messages are not channel posts.
  bool is_channel_post_;
  static jfieldID is_channel_post_fieldID;
  /// True, if the message contains an unread mention for the current user.
  bool contains_unread_mention_;
  static jfieldID contains_unread_mention_fieldID;
  /// Point in time (Unix timestamp) when the message was sent.
  std::int32_t date_;
  static jfieldID date_fieldID;
  /// Point in time (Unix timestamp) when the message was last edited.
  std::int32_t edit_date_;
  static jfieldID edit_date_fieldID;
  /// Information about the initial message sender; may be null.
  object_ptr<MessageForwardInfo> forward_info_;
  static jfieldID forward_info_fieldID;
  /// If non-zero, the identifier of the message this message is replying to; can be the identifier of a deleted message.
  std::int64_t reply_to_message_id_;
  static jfieldID reply_to_message_id_fieldID;
  /// For self-destructing messages, the message's TTL (Time To Live), in seconds; 0 if none. TDLib will send updateDeleteMessages or updateMessageContent once the TTL expires.
  std::int32_t ttl_;
  static jfieldID ttl_fieldID;
  /// Time left before the message expires, in seconds.
  double ttl_expires_in_;
  static jfieldID ttl_expires_in_fieldID;
  /// If non-zero, the user identifier of the bot through which this message was sent.
  std::int32_t via_bot_user_id_;
  static jfieldID via_bot_user_id_fieldID;
  /// For channel posts, optional author signature.
  std::string author_signature_;
  static jfieldID author_signature_fieldID;
  /// Number of times this message was viewed.
  std::int32_t views_;
  static jfieldID views_fieldID;
  /// Unique identifier of an album this message belongs to. Only photos and videos can be grouped together in albums.
  std::int64_t media_album_id_;
  static jfieldID media_album_id_fieldID;
  /// Content of the message.
  object_ptr<MessageContent> content_;
  static jfieldID content_fieldID;
  /// Reply markup for the message; may be null.
  object_ptr<ReplyMarkup> reply_markup_;
  static jfieldID reply_markup_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  message();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] id_ Message identifier, unique for the chat to which the message belongs.
   * \param[in] sender_user_id_ Identifier of the user who sent the message; 0 if unknown. It is unknown for channel posts.
   * \param[in] chat_id_ Chat identifier.
   * \param[in] sending_state_ Information about the sending state of the message; may be null.
   * \param[in] is_outgoing_ True, if the message is outgoing.
   * \param[in] can_be_edited_ True, if the message can be edited.
   * \param[in] can_be_forwarded_ True, if the message can be forwarded.
   * \param[in] can_be_deleted_only_for_self_ True, if the message can be deleted only for the current user while other users will continue to see it.
   * \param[in] can_be_deleted_for_all_users_ True, if the message can be deleted for all users.
   * \param[in] is_channel_post_ True, if the message is a channel post. All messages to channels are channel posts, all other messages are not channel posts.
   * \param[in] contains_unread_mention_ True, if the message contains an unread mention for the current user.
   * \param[in] date_ Point in time (Unix timestamp) when the message was sent.
   * \param[in] edit_date_ Point in time (Unix timestamp) when the message was last edited.
   * \param[in] forward_info_ Information about the initial message sender; may be null.
   * \param[in] reply_to_message_id_ If non-zero, the identifier of the message this message is replying to; can be the identifier of a deleted message.
   * \param[in] ttl_ For self-destructing messages, the message's TTL (Time To Live), in seconds; 0 if none. TDLib will send updateDeleteMessages or updateMessageContent once the TTL expires.
   * \param[in] ttl_expires_in_ Time left before the message expires, in seconds.
   * \param[in] via_bot_user_id_ If non-zero, the user identifier of the bot through which this message was sent.
   * \param[in] author_signature_ For channel posts, optional author signature.
   * \param[in] views_ Number of times this message was viewed.
   * \param[in] media_album_id_ Unique identifier of an album this message belongs to. Only photos and videos can be grouped together in albums.
   * \param[in] content_ Content of the message.
   * \param[in] reply_markup_ Reply markup for the message; may be null.
   */
  message(std::int64_t id_, std::int32_t sender_user_id_, std::int64_t chat_id_, object_ptr<MessageSendingState> &&sending_state_, bool is_outgoing_, bool can_be_edited_, bool can_be_forwarded_, bool can_be_deleted_only_for_self_, bool can_be_deleted_for_all_users_, bool is_channel_post_, bool contains_unread_mention_, std::int32_t date_, std::int32_t edit_date_, object_ptr<MessageForwardInfo> &&forward_info_, std::int64_t reply_to_message_id_, std::int32_t ttl_, double ttl_expires_in_, std::int32_t via_bot_user_id_, std::string const &author_signature_, std::int32_t views_, std::int64_t media_album_id_, object_ptr<MessageContent> &&content_, object_ptr<ReplyMarkup> &&reply_markup_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -675737627;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<message> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * This class is an abstract base class.
 * Contains the content of a message.
 */
class MessageContent: public Object {
 public:
  static jclass Class;

  static object_ptr<MessageContent> fetch(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A text message.
 */
class messageText final : public MessageContent {
 public:
  static jclass Class;
  /// Text of the message.
  object_ptr<formattedText> text_;
  static jfieldID text_fieldID;
  /// A preview of the web page that's mentioned in the text; may be null.
  object_ptr<webPage> web_page_;
  static jfieldID web_page_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  messageText();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] text_ Text of the message.
   * \param[in] web_page_ A preview of the web page that's mentioned in the text; may be null.
   */
  messageText(object_ptr<formattedText> &&text_, object_ptr<webPage> &&web_page_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1989037971;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<messageText> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * An animation message (GIF-style).
 */
class messageAnimation final : public MessageContent {
 public:
  static jclass Class;
  /// Message content.
  object_ptr<animation> animation_;
  static jfieldID animation_fieldID;
  /// Animation caption.
  object_ptr<formattedText> caption_;
  static jfieldID caption_fieldID;
  /// True, if the animation thumbnail must be blurred and the animation must be shown only while tapped.
  bool is_secret_;
  static jfieldID is_secret_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  messageAnimation();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] animation_ Message content.
   * \param[in] caption_ Animation caption.
   * \param[in] is_secret_ True, if the animation thumbnail must be blurred and the animation must be shown only while tapped.
   */
  messageAnimation(object_ptr<animation> &&animation_, object_ptr<formattedText> &&caption_, bool is_secret_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1306939396;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<messageAnimation> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * An audio message.
 */
class messageAudio final : public MessageContent {
 public:
  static jclass Class;
  /// Message content.
  object_ptr<audio> audio_;
  static jfieldID audio_fieldID;
  /// Audio caption.
  object_ptr<formattedText> caption_;
  static jfieldID caption_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  messageAudio();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] audio_ Message content.
   * \param[in] caption_ Audio caption.
   */
  messageAudio(object_ptr<audio> &&audio_, object_ptr<formattedText> &&caption_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 276722716;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<messageAudio> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A document message (general file).
 */
class messageDocument final : public MessageContent {
 public:
  static jclass Class;
  /// Message content.
  object_ptr<document> document_;
  static jfieldID document_fieldID;
  /// Document caption.
  object_ptr<formattedText> caption_;
  static jfieldID caption_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  messageDocument();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] document_ Message content.
   * \param[in] caption_ Document caption.
   */
  messageDocument(object_ptr<document> &&document_, object_ptr<formattedText> &&caption_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 596945783;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<messageDocument> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A photo message.
 */
class messagePhoto final : public MessageContent {
 public:
  static jclass Class;
  /// Message content.
  object_ptr<photo> photo_;
  static jfieldID photo_fieldID;
  /// Photo caption.
  object_ptr<formattedText> caption_;
  static jfieldID caption_fieldID;
  /// True, if the photo must be blurred and must be shown only while tapped.
  bool is_secret_;
  static jfieldID is_secret_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  messagePhoto();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] photo_ Message content.
   * \param[in] caption_ Photo caption.
   * \param[in] is_secret_ True, if the photo must be blurred and must be shown only while tapped.
   */
  messagePhoto(object_ptr<photo> &&photo_, object_ptr<formattedText> &&caption_, bool is_secret_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1851395174;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<messagePhoto> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * An expired photo message (self-destructed after TTL has elapsed).
 */
class messageExpiredPhoto final : public MessageContent {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  messageExpiredPhoto();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1404641801;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<messageExpiredPhoto> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A sticker message.
 */
class messageSticker final : public MessageContent {
 public:
  static jclass Class;
  /// Message content.
  object_ptr<sticker> sticker_;
  static jfieldID sticker_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  messageSticker();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] sticker_ Message content.
   */
  explicit messageSticker(object_ptr<sticker> &&sticker_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1779022878;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<messageSticker> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A video message.
 */
class messageVideo final : public MessageContent {
 public:
  static jclass Class;
  /// Message content.
  object_ptr<video> video_;
  static jfieldID video_fieldID;
  /// Video caption.
  object_ptr<formattedText> caption_;
  static jfieldID caption_fieldID;
  /// True, if the video thumbnail must be blurred and the video must be shown only while tapped.
  bool is_secret_;
  static jfieldID is_secret_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  messageVideo();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] video_ Message content.
   * \param[in] caption_ Video caption.
   * \param[in] is_secret_ True, if the video thumbnail must be blurred and the video must be shown only while tapped.
   */
  messageVideo(object_ptr<video> &&video_, object_ptr<formattedText> &&caption_, bool is_secret_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 2021281344;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<messageVideo> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * An expired video message (self-destructed after TTL has elapsed).
 */
class messageExpiredVideo final : public MessageContent {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  messageExpiredVideo();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1212209981;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<messageExpiredVideo> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A video note message.
 */
class messageVideoNote final : public MessageContent {
 public:
  static jclass Class;
  /// Message content.
  object_ptr<videoNote> video_note_;
  static jfieldID video_note_fieldID;
  /// True, if at least one of the recipients has viewed the video note.
  bool is_viewed_;
  static jfieldID is_viewed_fieldID;
  /// True, if the video note thumbnail must be blurred and the video note must be shown only while tapped.
  bool is_secret_;
  static jfieldID is_secret_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  messageVideoNote();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] video_note_ Message content.
   * \param[in] is_viewed_ True, if at least one of the recipients has viewed the video note.
   * \param[in] is_secret_ True, if the video note thumbnail must be blurred and the video note must be shown only while tapped.
   */
  messageVideoNote(object_ptr<videoNote> &&video_note_, bool is_viewed_, bool is_secret_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 963323014;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<messageVideoNote> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A voice note message.
 */
class messageVoiceNote final : public MessageContent {
 public:
  static jclass Class;
  /// Message content.
  object_ptr<voiceNote> voice_note_;
  static jfieldID voice_note_fieldID;
  /// Voice note caption.
  object_ptr<formattedText> caption_;
  static jfieldID caption_fieldID;
  /// True, if at least one of the recipients has listened to the voice note.
  bool is_listened_;
  static jfieldID is_listened_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  messageVoiceNote();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] voice_note_ Message content.
   * \param[in] caption_ Voice note caption.
   * \param[in] is_listened_ True, if at least one of the recipients has listened to the voice note.
   */
  messageVoiceNote(object_ptr<voiceNote> &&voice_note_, object_ptr<formattedText> &&caption_, bool is_listened_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 527777781;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<messageVoiceNote> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A message with a location.
 */
class messageLocation final : public MessageContent {
 public:
  static jclass Class;
  /// Message content.
  object_ptr<location> location_;
  static jfieldID location_fieldID;
  /// Time relative to the message sent date until which the location can be updated, in seconds.
  std::int32_t live_period_;
  static jfieldID live_period_fieldID;
  /// Left time for which the location can be updated, in seconds. updateMessageContent is not sent when this field changes.
  std::int32_t expires_in_;
  static jfieldID expires_in_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  messageLocation();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] location_ Message content.
   * \param[in] live_period_ Time relative to the message sent date until which the location can be updated, in seconds.
   * \param[in] expires_in_ Left time for which the location can be updated, in seconds. updateMessageContent is not sent when this field changes.
   */
  messageLocation(object_ptr<location> &&location_, std::int32_t live_period_, std::int32_t expires_in_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1301887786;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<messageLocation> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A message with information about a venue.
 */
class messageVenue final : public MessageContent {
 public:
  static jclass Class;
  /// Message content.
  object_ptr<venue> venue_;
  static jfieldID venue_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  messageVenue();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] venue_ Message content.
   */
  explicit messageVenue(object_ptr<venue> &&venue_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -2146492043;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<messageVenue> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A message with a user contact.
 */
class messageContact final : public MessageContent {
 public:
  static jclass Class;
  /// Message content.
  object_ptr<contact> contact_;
  static jfieldID contact_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  messageContact();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] contact_ Message content.
   */
  explicit messageContact(object_ptr<contact> &&contact_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -512684966;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<messageContact> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A message with a game.
 */
class messageGame final : public MessageContent {
 public:
  static jclass Class;
  /// Game.
  object_ptr<game> game_;
  static jfieldID game_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  messageGame();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] game_ Game.
   */
  explicit messageGame(object_ptr<game> &&game_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -69441162;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<messageGame> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A message with an invoice from a bot.
 */
class messageInvoice final : public MessageContent {
 public:
  static jclass Class;
  /// Product title.
  std::string title_;
  static jfieldID title_fieldID;
  /// Product description.
  std::string description_;
  static jfieldID description_fieldID;
  /// Product photo; may be null.
  object_ptr<photo> photo_;
  static jfieldID photo_fieldID;
  /// Currency for the product price.
  std::string currency_;
  static jfieldID currency_fieldID;
  /// Product total price in the minimal quantity of the currency.
  std::int64_t total_amount_;
  static jfieldID total_amount_fieldID;
  /// Unique invoice bot start_parameter. To share an invoice use the URL https://t.me/{bot_username}?start={start_parameter}.
  std::string start_parameter_;
  static jfieldID start_parameter_fieldID;
  /// True, if the invoice is a test invoice.
  bool is_test_;
  static jfieldID is_test_fieldID;
  /// True, if the shipping address should be specified.
  bool need_shipping_address_;
  static jfieldID need_shipping_address_fieldID;
  /// The identifier of the message with the receipt, after the product has been purchased.
  std::int64_t receipt_message_id_;
  static jfieldID receipt_message_id_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  messageInvoice();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] title_ Product title.
   * \param[in] description_ Product description.
   * \param[in] photo_ Product photo; may be null.
   * \param[in] currency_ Currency for the product price.
   * \param[in] total_amount_ Product total price in the minimal quantity of the currency.
   * \param[in] start_parameter_ Unique invoice bot start_parameter. To share an invoice use the URL https://t.me/{bot_username}?start={start_parameter}.
   * \param[in] is_test_ True, if the invoice is a test invoice.
   * \param[in] need_shipping_address_ True, if the shipping address should be specified.
   * \param[in] receipt_message_id_ The identifier of the message with the receipt, after the product has been purchased.
   */
  messageInvoice(std::string const &title_, std::string const &description_, object_ptr<photo> &&photo_, std::string const &currency_, std::int64_t total_amount_, std::string const &start_parameter_, bool is_test_, bool need_shipping_address_, std::int64_t receipt_message_id_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1916671476;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<messageInvoice> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A message with information about an ended call.
 */
class messageCall final : public MessageContent {
 public:
  static jclass Class;
  /// Reason why the call was discarded.
  object_ptr<CallDiscardReason> discard_reason_;
  static jfieldID discard_reason_fieldID;
  /// Call duration, in seconds.
  std::int32_t duration_;
  static jfieldID duration_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  messageCall();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] discard_reason_ Reason why the call was discarded.
   * \param[in] duration_ Call duration, in seconds.
   */
  messageCall(object_ptr<CallDiscardReason> &&discard_reason_, std::int32_t duration_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 366512596;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<messageCall> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A newly created basic group.
 */
class messageBasicGroupChatCreate final : public MessageContent {
 public:
  static jclass Class;
  /// Title of the basic group.
  std::string title_;
  static jfieldID title_fieldID;
  /// User identifiers of members in the basic group.
  std::vector<std::int32_t> member_user_ids_;
  static jfieldID member_user_ids_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  messageBasicGroupChatCreate();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] title_ Title of the basic group.
   * \param[in] member_user_ids_ User identifiers of members in the basic group.
   */
  messageBasicGroupChatCreate(std::string const &title_, std::vector<std::int32_t> &&member_user_ids_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1575377646;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<messageBasicGroupChatCreate> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A newly created supergroup or channel.
 */
class messageSupergroupChatCreate final : public MessageContent {
 public:
  static jclass Class;
  /// Title of the supergroup or channel.
  std::string title_;
  static jfieldID title_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  messageSupergroupChatCreate();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] title_ Title of the supergroup or channel.
   */
  explicit messageSupergroupChatCreate(std::string const &title_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -434325733;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<messageSupergroupChatCreate> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * An updated chat title.
 */
class messageChatChangeTitle final : public MessageContent {
 public:
  static jclass Class;
  /// New chat title.
  std::string title_;
  static jfieldID title_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  messageChatChangeTitle();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] title_ New chat title.
   */
  explicit messageChatChangeTitle(std::string const &title_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 748272449;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<messageChatChangeTitle> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * An updated chat photo.
 */
class messageChatChangePhoto final : public MessageContent {
 public:
  static jclass Class;
  /// New chat photo.
  object_ptr<photo> photo_;
  static jfieldID photo_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  messageChatChangePhoto();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] photo_ New chat photo.
   */
  explicit messageChatChangePhoto(object_ptr<photo> &&photo_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 319630249;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<messageChatChangePhoto> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A deleted chat photo.
 */
class messageChatDeletePhoto final : public MessageContent {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  messageChatDeletePhoto();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -184374809;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<messageChatDeletePhoto> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * New chat members were added.
 */
class messageChatAddMembers final : public MessageContent {
 public:
  static jclass Class;
  /// User identifiers of the new members.
  std::vector<std::int32_t> member_user_ids_;
  static jfieldID member_user_ids_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  messageChatAddMembers();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] member_user_ids_ User identifiers of the new members.
   */
  explicit messageChatAddMembers(std::vector<std::int32_t> &&member_user_ids_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 401228326;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<messageChatAddMembers> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A new member joined the chat by invite link.
 */
class messageChatJoinByLink final : public MessageContent {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  messageChatJoinByLink();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1846493311;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<messageChatJoinByLink> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A chat member was deleted.
 */
class messageChatDeleteMember final : public MessageContent {
 public:
  static jclass Class;
  /// User identifier of the deleted chat member.
  std::int32_t user_id_;
  static jfieldID user_id_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  messageChatDeleteMember();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] user_id_ User identifier of the deleted chat member.
   */
  explicit messageChatDeleteMember(std::int32_t user_id_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1164414043;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<messageChatDeleteMember> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A basic group was upgraded to a supergroup and was deactivated as the result.
 */
class messageChatUpgradeTo final : public MessageContent {
 public:
  static jclass Class;
  /// Identifier of the supergroup to which the basic group was upgraded.
  std::int32_t supergroup_id_;
  static jfieldID supergroup_id_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  messageChatUpgradeTo();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] supergroup_id_ Identifier of the supergroup to which the basic group was upgraded.
   */
  explicit messageChatUpgradeTo(std::int32_t supergroup_id_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1957816681;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<messageChatUpgradeTo> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A supergroup has been created from a basic group.
 */
class messageChatUpgradeFrom final : public MessageContent {
 public:
  static jclass Class;
  /// Title of the newly created supergroup.
  std::string title_;
  static jfieldID title_fieldID;
  /// The identifier of the original basic group.
  std::int32_t basic_group_id_;
  static jfieldID basic_group_id_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  messageChatUpgradeFrom();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] title_ Title of the newly created supergroup.
   * \param[in] basic_group_id_ The identifier of the original basic group.
   */
  messageChatUpgradeFrom(std::string const &title_, std::int32_t basic_group_id_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1642272558;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<messageChatUpgradeFrom> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A message has been pinned.
 */
class messagePinMessage final : public MessageContent {
 public:
  static jclass Class;
  /// Identifier of the pinned message, can be an identifier of a deleted message.
  std::int64_t message_id_;
  static jfieldID message_id_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  messagePinMessage();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] message_id_ Identifier of the pinned message, can be an identifier of a deleted message.
   */
  explicit messagePinMessage(std::int64_t message_id_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 953503801;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<messagePinMessage> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A screenshot of a message in the chat has been taken.
 */
class messageScreenshotTaken final : public MessageContent {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  messageScreenshotTaken();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1564971605;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<messageScreenshotTaken> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * The TTL (Time To Live) setting messages in a secret chat has been changed.
 */
class messageChatSetTtl final : public MessageContent {
 public:
  static jclass Class;
  /// New TTL.
  std::int32_t ttl_;
  static jfieldID ttl_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  messageChatSetTtl();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] ttl_ New TTL.
   */
  explicit messageChatSetTtl(std::int32_t ttl_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1810060209;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<messageChatSetTtl> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A non-standard action has happened in the chat.
 */
class messageCustomServiceAction final : public MessageContent {
 public:
  static jclass Class;
  /// Message text to be shown in the chat.
  std::string text_;
  static jfieldID text_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  messageCustomServiceAction();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] text_ Message text to be shown in the chat.
   */
  explicit messageCustomServiceAction(std::string const &text_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1435879282;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<messageCustomServiceAction> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A new high score was achieved in a game.
 */
class messageGameScore final : public MessageContent {
 public:
  static jclass Class;
  /// Identifier of the message with the game, can be an identifier of a deleted message.
  std::int64_t game_message_id_;
  static jfieldID game_message_id_fieldID;
  /// Identifier of the game, may be different from the games presented in the message with the game.
  std::int64_t game_id_;
  static jfieldID game_id_fieldID;
  /// New score.
  std::int32_t score_;
  static jfieldID score_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  messageGameScore();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] game_message_id_ Identifier of the message with the game, can be an identifier of a deleted message.
   * \param[in] game_id_ Identifier of the game, may be different from the games presented in the message with the game.
   * \param[in] score_ New score.
   */
  messageGameScore(std::int64_t game_message_id_, std::int64_t game_id_, std::int32_t score_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1344904575;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<messageGameScore> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A payment has been completed.
 */
class messagePaymentSuccessful final : public MessageContent {
 public:
  static jclass Class;
  /// Identifier of the message with the corresponding invoice; can be an identifier of a deleted message.
  std::int64_t invoice_message_id_;
  static jfieldID invoice_message_id_fieldID;
  /// Currency for the price of the product.
  std::string currency_;
  static jfieldID currency_fieldID;
  /// Total price for the product, in the minimal quantity of the currency.
  std::int64_t total_amount_;
  static jfieldID total_amount_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  messagePaymentSuccessful();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] invoice_message_id_ Identifier of the message with the corresponding invoice; can be an identifier of a deleted message.
   * \param[in] currency_ Currency for the price of the product.
   * \param[in] total_amount_ Total price for the product, in the minimal quantity of the currency.
   */
  messagePaymentSuccessful(std::int64_t invoice_message_id_, std::string const &currency_, std::int64_t total_amount_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -595962993;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<messagePaymentSuccessful> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A payment has been completed; for bots only.
 */
class messagePaymentSuccessfulBot final : public MessageContent {
 public:
  static jclass Class;
  /// Identifier of the message with the corresponding invoice; can be an identifier of a deleted message.
  std::int64_t invoice_message_id_;
  static jfieldID invoice_message_id_fieldID;
  /// Currency for price of the product.
  std::string currency_;
  static jfieldID currency_fieldID;
  /// Total price for the product, in the minimal quantity of the currency.
  std::int64_t total_amount_;
  static jfieldID total_amount_fieldID;
  /// Invoice payload.
  std::string invoice_payload_;
  static jfieldID invoice_payload_fieldID;
  /// Identifier of the shipping option chosen by the user; may be empty if not applicable.
  std::string shipping_option_id_;
  static jfieldID shipping_option_id_fieldID;
  /// Information about the order; may be null.
  object_ptr<orderInfo> order_info_;
  static jfieldID order_info_fieldID;
  /// Telegram payment identifier.
  std::string telegram_payment_charge_id_;
  static jfieldID telegram_payment_charge_id_fieldID;
  /// Provider payment identifier.
  std::string provider_payment_charge_id_;
  static jfieldID provider_payment_charge_id_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  messagePaymentSuccessfulBot();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] invoice_message_id_ Identifier of the message with the corresponding invoice; can be an identifier of a deleted message.
   * \param[in] currency_ Currency for price of the product.
   * \param[in] total_amount_ Total price for the product, in the minimal quantity of the currency.
   * \param[in] invoice_payload_ Invoice payload.
   * \param[in] shipping_option_id_ Identifier of the shipping option chosen by the user; may be empty if not applicable.
   * \param[in] order_info_ Information about the order; may be null.
   * \param[in] telegram_payment_charge_id_ Telegram payment identifier.
   * \param[in] provider_payment_charge_id_ Provider payment identifier.
   */
  messagePaymentSuccessfulBot(std::int64_t invoice_message_id_, std::string const &currency_, std::int64_t total_amount_, std::string const &invoice_payload_, std::string const &shipping_option_id_, object_ptr<orderInfo> &&order_info_, std::string const &telegram_payment_charge_id_, std::string const &provider_payment_charge_id_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -412310696;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<messagePaymentSuccessfulBot> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A contact has registered with Telegram.
 */
class messageContactRegistered final : public MessageContent {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  messageContactRegistered();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1502020353;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<messageContactRegistered> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * The current user has connected a website by logging in using Telegram Login Widget on it.
 */
class messageWebsiteConnected final : public MessageContent {
 public:
  static jclass Class;
  /// Domain name of the connected website.
  std::string domain_name_;
  static jfieldID domain_name_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  messageWebsiteConnected();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] domain_name_ Domain name of the connected website.
   */
  explicit messageWebsiteConnected(std::string const &domain_name_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1074551800;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<messageWebsiteConnected> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Telegram Passport data has been sent.
 */
class messagePassportDataSent final : public MessageContent {
 public:
  static jclass Class;
  /// List of Telegram Passport element types sent.
  std::vector<object_ptr<PassportElementType>> types_;
  static jfieldID types_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  messagePassportDataSent();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] types_ List of Telegram Passport element types sent.
   */
  explicit messagePassportDataSent(std::vector<object_ptr<PassportElementType>> &&types_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1017405171;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<messagePassportDataSent> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Telegram Passport data has been received; for bots only.
 */
class messagePassportDataReceived final : public MessageContent {
 public:
  static jclass Class;
  /// List of received Telegram Passport elements.
  std::vector<object_ptr<encryptedPassportElement>> elements_;
  static jfieldID elements_fieldID;
  /// Encrypted data credentials.
  object_ptr<encryptedCredentials> credentials_;
  static jfieldID credentials_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  messagePassportDataReceived();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] elements_ List of received Telegram Passport elements.
   * \param[in] credentials_ Encrypted data credentials.
   */
  messagePassportDataReceived(std::vector<object_ptr<encryptedPassportElement>> &&elements_, object_ptr<encryptedCredentials> &&credentials_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1367863624;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<messagePassportDataReceived> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Message content that is not supported by the client.
 */
class messageUnsupported final : public MessageContent {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  messageUnsupported();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1816726139;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<messageUnsupported> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * This class is an abstract base class.
 * Contains information about the initial sender of a forwarded message.
 */
class MessageForwardInfo: public Object {
 public:
  static jclass Class;

  static object_ptr<MessageForwardInfo> fetch(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * The message was originally written by a known user.
 */
class messageForwardedFromUser final : public MessageForwardInfo {
 public:
  static jclass Class;
  /// Identifier of the user that originally sent this message.
  std::int32_t sender_user_id_;
  static jfieldID sender_user_id_fieldID;
  /// Point in time (Unix timestamp) when the message was originally sent.
  std::int32_t date_;
  static jfieldID date_fieldID;
  /// For messages forwarded to the chat with the current user (saved messages), the identifier of the chat from which the message was forwarded; 0 if unknown.
  std::int64_t forwarded_from_chat_id_;
  static jfieldID forwarded_from_chat_id_fieldID;
  /// For messages forwarded to the chat with the current user (saved messages) the identifier of the original message from which the new message was forwarded; 0 if unknown.
  std::int64_t forwarded_from_message_id_;
  static jfieldID forwarded_from_message_id_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  messageForwardedFromUser();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] sender_user_id_ Identifier of the user that originally sent this message.
   * \param[in] date_ Point in time (Unix timestamp) when the message was originally sent.
   * \param[in] forwarded_from_chat_id_ For messages forwarded to the chat with the current user (saved messages), the identifier of the chat from which the message was forwarded; 0 if unknown.
   * \param[in] forwarded_from_message_id_ For messages forwarded to the chat with the current user (saved messages) the identifier of the original message from which the new message was forwarded; 0 if unknown.
   */
  messageForwardedFromUser(std::int32_t sender_user_id_, std::int32_t date_, std::int64_t forwarded_from_chat_id_, std::int64_t forwarded_from_message_id_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1004332765;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<messageForwardedFromUser> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * The message was originally a post in a channel.
 */
class messageForwardedPost final : public MessageForwardInfo {
 public:
  static jclass Class;
  /// Identifier of the chat from which the message was forwarded.
  std::int64_t chat_id_;
  static jfieldID chat_id_fieldID;
  /// Post author signature.
  std::string author_signature_;
  static jfieldID author_signature_fieldID;
  /// Point in time (Unix timestamp) when the message was originally sent.
  std::int32_t date_;
  static jfieldID date_fieldID;
  /// Message identifier of the original message from which the new message was forwarded; 0 if unknown.
  std::int64_t message_id_;
  static jfieldID message_id_fieldID;
  /// For messages forwarded to the chat with the current user (saved messages), the identifier of the chat from which the message was forwarded; 0 if unknown.
  std::int64_t forwarded_from_chat_id_;
  static jfieldID forwarded_from_chat_id_fieldID;
  /// For messages forwarded to the chat with the current user (saved messages), the identifier of the original message from which the new message was forwarded; 0 if unknown.
  std::int64_t forwarded_from_message_id_;
  static jfieldID forwarded_from_message_id_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  messageForwardedPost();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] chat_id_ Identifier of the chat from which the message was forwarded.
   * \param[in] author_signature_ Post author signature.
   * \param[in] date_ Point in time (Unix timestamp) when the message was originally sent.
   * \param[in] message_id_ Message identifier of the original message from which the new message was forwarded; 0 if unknown.
   * \param[in] forwarded_from_chat_id_ For messages forwarded to the chat with the current user (saved messages), the identifier of the chat from which the message was forwarded; 0 if unknown.
   * \param[in] forwarded_from_message_id_ For messages forwarded to the chat with the current user (saved messages), the identifier of the original message from which the new message was forwarded; 0 if unknown.
   */
  messageForwardedPost(std::int64_t chat_id_, std::string const &author_signature_, std::int32_t date_, std::int64_t message_id_, std::int64_t forwarded_from_chat_id_, std::int64_t forwarded_from_message_id_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -244050875;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<messageForwardedPost> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * This class is an abstract base class.
 * Contains information about the sending state of the message.
 */
class MessageSendingState: public Object {
 public:
  static jclass Class;

  static object_ptr<MessageSendingState> fetch(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * The message is being sent now, but has not yet been delivered to the server.
 */
class messageSendingStatePending final : public MessageSendingState {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  messageSendingStatePending();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1381803582;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<messageSendingStatePending> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * The message failed to be sent.
 */
class messageSendingStateFailed final : public MessageSendingState {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  messageSendingStateFailed();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -546610323;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<messageSendingStateFailed> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Contains a list of messages.
 */
class messages final : public Object {
 public:
  static jclass Class;
  /// Approximate total count of messages found.
  std::int32_t total_count_;
  static jfieldID total_count_fieldID;
  /// List of messages; messages may be null.
  std::vector<object_ptr<message>> messages_;
  static jfieldID messages_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  messages();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] total_count_ Approximate total count of messages found.
   * \param[in] messages_ List of messages; messages may be null.
   */
  messages(std::int32_t total_count_, std::vector<object_ptr<message>> &&messages_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -16498159;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<messages> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A full list of available network statistic entries.
 */
class networkStatistics final : public Object {
 public:
  static jclass Class;
  /// Point in time (Unix timestamp) when the app began collecting statistics.
  std::int32_t since_date_;
  static jfieldID since_date_fieldID;
  /// Network statistics entries.
  std::vector<object_ptr<NetworkStatisticsEntry>> entries_;
  static jfieldID entries_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  networkStatistics();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] since_date_ Point in time (Unix timestamp) when the app began collecting statistics.
   * \param[in] entries_ Network statistics entries.
   */
  networkStatistics(std::int32_t since_date_, std::vector<object_ptr<NetworkStatisticsEntry>> &&entries_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1615554212;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<networkStatistics> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * This class is an abstract base class.
 * Contains statistics about network usage.
 */
class NetworkStatisticsEntry: public Object {
 public:
  static jclass Class;

  static object_ptr<NetworkStatisticsEntry> fetch(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Contains information about the total amount of data that was used to send and receive files.
 */
class networkStatisticsEntryFile final : public NetworkStatisticsEntry {
 public:
  static jclass Class;
  /// Type of the file the data is part of.
  object_ptr<FileType> file_type_;
  static jfieldID file_type_fieldID;
  /// Type of the network the data was sent through. Call setNetworkType to maintain the actual network type.
  object_ptr<NetworkType> network_type_;
  static jfieldID network_type_fieldID;
  /// Total number of bytes sent.
  std::int64_t sent_bytes_;
  static jfieldID sent_bytes_fieldID;
  /// Total number of bytes received.
  std::int64_t received_bytes_;
  static jfieldID received_bytes_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  networkStatisticsEntryFile();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] file_type_ Type of the file the data is part of.
   * \param[in] network_type_ Type of the network the data was sent through. Call setNetworkType to maintain the actual network type.
   * \param[in] sent_bytes_ Total number of bytes sent.
   * \param[in] received_bytes_ Total number of bytes received.
   */
  networkStatisticsEntryFile(object_ptr<FileType> &&file_type_, object_ptr<NetworkType> &&network_type_, std::int64_t sent_bytes_, std::int64_t received_bytes_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 188452706;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<networkStatisticsEntryFile> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Contains information about the total amount of data that was used for calls.
 */
class networkStatisticsEntryCall final : public NetworkStatisticsEntry {
 public:
  static jclass Class;
  /// Type of the network the data was sent through. Call setNetworkType to maintain the actual network type.
  object_ptr<NetworkType> network_type_;
  static jfieldID network_type_fieldID;
  /// Total number of bytes sent.
  std::int64_t sent_bytes_;
  static jfieldID sent_bytes_fieldID;
  /// Total number of bytes received.
  std::int64_t received_bytes_;
  static jfieldID received_bytes_fieldID;
  /// Total call duration, in seconds.
  double duration_;
  static jfieldID duration_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  networkStatisticsEntryCall();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] network_type_ Type of the network the data was sent through. Call setNetworkType to maintain the actual network type.
   * \param[in] sent_bytes_ Total number of bytes sent.
   * \param[in] received_bytes_ Total number of bytes received.
   * \param[in] duration_ Total call duration, in seconds.
   */
  networkStatisticsEntryCall(object_ptr<NetworkType> &&network_type_, std::int64_t sent_bytes_, std::int64_t received_bytes_, double duration_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 737000365;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<networkStatisticsEntryCall> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * This class is an abstract base class.
 * Represents the type of a network.
 */
class NetworkType: public Object {
 public:
  static jclass Class;

  static object_ptr<NetworkType> fetch(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * The network is not available.
 */
class networkTypeNone final : public NetworkType {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  networkTypeNone();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1971691759;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<networkTypeNone> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A mobile network.
 */
class networkTypeMobile final : public NetworkType {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  networkTypeMobile();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 819228239;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<networkTypeMobile> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A mobile roaming network.
 */
class networkTypeMobileRoaming final : public NetworkType {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  networkTypeMobileRoaming();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1435199760;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<networkTypeMobileRoaming> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A Wi-Fi network.
 */
class networkTypeWiFi final : public NetworkType {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  networkTypeWiFi();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -633872070;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<networkTypeWiFi> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A different network type (e.g., Ethernet network).
 */
class networkTypeOther final : public NetworkType {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  networkTypeOther();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1942128539;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<networkTypeOther> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * This class is an abstract base class.
 * Describes the types of chats to which notification settings are applied.
 */
class NotificationSettingsScope: public Object {
 public:
  static jclass Class;

  static object_ptr<NotificationSettingsScope> fetch(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Notification settings applied to all private and secret chats when the corresponding chat setting has a default value.
 */
class notificationSettingsScopePrivateChats final : public NotificationSettingsScope {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  notificationSettingsScopePrivateChats();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 937446759;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<notificationSettingsScopePrivateChats> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Notification settings applied to all basic groups, supergroups and channels when the corresponding chat setting has a default value.
 */
class notificationSettingsScopeGroupChats final : public NotificationSettingsScope {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  notificationSettingsScopeGroupChats();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1212142067;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<notificationSettingsScopeGroupChats> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * An object of this type is returned on a successful function call for certain functions.
 */
class ok final : public Object {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  ok();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -722616727;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<ok> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * This class is an abstract base class.
 * Represents the value of an option.
 */
class OptionValue: public Object {
 public:
  static jclass Class;

  static object_ptr<OptionValue> fetch(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Boolean option.
 */
class optionValueBoolean final : public OptionValue {
 public:
  static jclass Class;
  /// The value of the option.
  bool value_;
  static jfieldID value_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  optionValueBoolean();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] value_ The value of the option.
   */
  explicit optionValueBoolean(bool value_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 63135518;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<optionValueBoolean> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * An unknown option or an option which has a default value.
 */
class optionValueEmpty final : public OptionValue {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  optionValueEmpty();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 918955155;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<optionValueEmpty> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * An integer option.
 */
class optionValueInteger final : public OptionValue {
 public:
  static jclass Class;
  /// The value of the option.
  std::int32_t value_;
  static jfieldID value_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  optionValueInteger();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] value_ The value of the option.
   */
  explicit optionValueInteger(std::int32_t value_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1400911104;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<optionValueInteger> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A string option.
 */
class optionValueString final : public OptionValue {
 public:
  static jclass Class;
  /// The value of the option.
  std::string value_;
  static jfieldID value_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  optionValueString();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] value_ The value of the option.
   */
  explicit optionValueString(std::string const &value_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 756248212;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<optionValueString> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Order information.
 */
class orderInfo final : public Object {
 public:
  static jclass Class;
  /// Name of the user.
  std::string name_;
  static jfieldID name_fieldID;
  /// Phone number of the user.
  std::string phone_number_;
  static jfieldID phone_number_fieldID;
  /// Email address of the user.
  std::string email_address_;
  static jfieldID email_address_fieldID;
  /// Shipping address for this order; may be null.
  object_ptr<address> shipping_address_;
  static jfieldID shipping_address_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  orderInfo();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] name_ Name of the user.
   * \param[in] phone_number_ Phone number of the user.
   * \param[in] email_address_ Email address of the user.
   * \param[in] shipping_address_ Shipping address for this order; may be null.
   */
  orderInfo(std::string const &name_, std::string const &phone_number_, std::string const &email_address_, object_ptr<address> &&shipping_address_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 783997294;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<orderInfo> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * This class is an abstract base class.
 * Describes a block of an instant view web page.
 */
class PageBlock: public Object {
 public:
  static jclass Class;

  static object_ptr<PageBlock> fetch(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * The title of a page.
 */
class pageBlockTitle final : public PageBlock {
 public:
  static jclass Class;
  /// Title.
  object_ptr<RichText> title_;
  static jfieldID title_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  pageBlockTitle();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] title_ Title.
   */
  explicit pageBlockTitle(object_ptr<RichText> &&title_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1629664784;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<pageBlockTitle> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * The subtitle of a page.
 */
class pageBlockSubtitle final : public PageBlock {
 public:
  static jclass Class;
  /// Subtitle.
  object_ptr<RichText> subtitle_;
  static jfieldID subtitle_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  pageBlockSubtitle();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] subtitle_ Subtitle.
   */
  explicit pageBlockSubtitle(object_ptr<RichText> &&subtitle_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 264524263;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<pageBlockSubtitle> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * The author and publishing date of a page.
 */
class pageBlockAuthorDate final : public PageBlock {
 public:
  static jclass Class;
  /// Author.
  object_ptr<RichText> author_;
  static jfieldID author_fieldID;
  /// Point in time (Unix timestamp) when the article was published; 0 if unknown.
  std::int32_t publish_date_;
  static jfieldID publish_date_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  pageBlockAuthorDate();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] author_ Author.
   * \param[in] publish_date_ Point in time (Unix timestamp) when the article was published; 0 if unknown.
   */
  pageBlockAuthorDate(object_ptr<RichText> &&author_, std::int32_t publish_date_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1300231184;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<pageBlockAuthorDate> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A header.
 */
class pageBlockHeader final : public PageBlock {
 public:
  static jclass Class;
  /// Header.
  object_ptr<RichText> header_;
  static jfieldID header_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  pageBlockHeader();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] header_ Header.
   */
  explicit pageBlockHeader(object_ptr<RichText> &&header_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1402854811;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<pageBlockHeader> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A subheader.
 */
class pageBlockSubheader final : public PageBlock {
 public:
  static jclass Class;
  /// Subheader.
  object_ptr<RichText> subheader_;
  static jfieldID subheader_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  pageBlockSubheader();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] subheader_ Subheader.
   */
  explicit pageBlockSubheader(object_ptr<RichText> &&subheader_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1263956774;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<pageBlockSubheader> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A text paragraph.
 */
class pageBlockParagraph final : public PageBlock {
 public:
  static jclass Class;
  /// Paragraph text.
  object_ptr<RichText> text_;
  static jfieldID text_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  pageBlockParagraph();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] text_ Paragraph text.
   */
  explicit pageBlockParagraph(object_ptr<RichText> &&text_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1182402406;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<pageBlockParagraph> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A preformatted text paragraph.
 */
class pageBlockPreformatted final : public PageBlock {
 public:
  static jclass Class;
  /// Paragraph text.
  object_ptr<RichText> text_;
  static jfieldID text_fieldID;
  /// Programming language for which the text should be formatted.
  std::string language_;
  static jfieldID language_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  pageBlockPreformatted();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] text_ Paragraph text.
   * \param[in] language_ Programming language for which the text should be formatted.
   */
  pageBlockPreformatted(object_ptr<RichText> &&text_, std::string const &language_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1066346178;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<pageBlockPreformatted> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * The footer of a page.
 */
class pageBlockFooter final : public PageBlock {
 public:
  static jclass Class;
  /// Footer.
  object_ptr<RichText> footer_;
  static jfieldID footer_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  pageBlockFooter();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] footer_ Footer.
   */
  explicit pageBlockFooter(object_ptr<RichText> &&footer_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 886429480;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<pageBlockFooter> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * An empty block separating a page.
 */
class pageBlockDivider final : public PageBlock {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  pageBlockDivider();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -618614392;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<pageBlockDivider> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * An invisible anchor on a page, which can be used in a URL to open the page from the specified anchor.
 */
class pageBlockAnchor final : public PageBlock {
 public:
  static jclass Class;
  /// Name of the anchor.
  std::string name_;
  static jfieldID name_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  pageBlockAnchor();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] name_ Name of the anchor.
   */
  explicit pageBlockAnchor(std::string const &name_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -837994576;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<pageBlockAnchor> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A list of texts.
 */
class pageBlockList final : public PageBlock {
 public:
  static jclass Class;
  /// Texts.
  std::vector<object_ptr<RichText>> items_;
  static jfieldID items_fieldID;
  /// True, if the items should be marked with numbers.
  bool is_ordered_;
  static jfieldID is_ordered_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  pageBlockList();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] items_ Texts.
   * \param[in] is_ordered_ True, if the items should be marked with numbers.
   */
  pageBlockList(std::vector<object_ptr<RichText>> &&items_, bool is_ordered_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1752631674;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<pageBlockList> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A block quote.
 */
class pageBlockBlockQuote final : public PageBlock {
 public:
  static jclass Class;
  /// Quote text.
  object_ptr<RichText> text_;
  static jfieldID text_fieldID;
  /// Quote caption.
  object_ptr<RichText> caption_;
  static jfieldID caption_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  pageBlockBlockQuote();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] text_ Quote text.
   * \param[in] caption_ Quote caption.
   */
  pageBlockBlockQuote(object_ptr<RichText> &&text_, object_ptr<RichText> &&caption_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -37421406;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<pageBlockBlockQuote> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A pull quote.
 */
class pageBlockPullQuote final : public PageBlock {
 public:
  static jclass Class;
  /// Quote text.
  object_ptr<RichText> text_;
  static jfieldID text_fieldID;
  /// Quote caption.
  object_ptr<RichText> caption_;
  static jfieldID caption_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  pageBlockPullQuote();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] text_ Quote text.
   * \param[in] caption_ Quote caption.
   */
  pageBlockPullQuote(object_ptr<RichText> &&text_, object_ptr<RichText> &&caption_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1799498665;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<pageBlockPullQuote> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * An animation.
 */
class pageBlockAnimation final : public PageBlock {
 public:
  static jclass Class;
  /// Animation file; may be null.
  object_ptr<animation> animation_;
  static jfieldID animation_fieldID;
  /// Animation caption.
  object_ptr<RichText> caption_;
  static jfieldID caption_fieldID;
  /// True, if the animation should be played automatically.
  bool need_autoplay_;
  static jfieldID need_autoplay_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  pageBlockAnimation();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] animation_ Animation file; may be null.
   * \param[in] caption_ Animation caption.
   * \param[in] need_autoplay_ True, if the animation should be played automatically.
   */
  pageBlockAnimation(object_ptr<animation> &&animation_, object_ptr<RichText> &&caption_, bool need_autoplay_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1639478661;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<pageBlockAnimation> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * An audio file.
 */
class pageBlockAudio final : public PageBlock {
 public:
  static jclass Class;
  /// Audio file; may be null.
  object_ptr<audio> audio_;
  static jfieldID audio_fieldID;
  /// Audio file caption.
  object_ptr<RichText> caption_;
  static jfieldID caption_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  pageBlockAudio();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] audio_ Audio file; may be null.
   * \param[in] caption_ Audio file caption.
   */
  pageBlockAudio(object_ptr<audio> &&audio_, object_ptr<RichText> &&caption_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1898245855;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<pageBlockAudio> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A photo.
 */
class pageBlockPhoto final : public PageBlock {
 public:
  static jclass Class;
  /// Photo file; may be null.
  object_ptr<photo> photo_;
  static jfieldID photo_fieldID;
  /// Photo caption.
  object_ptr<RichText> caption_;
  static jfieldID caption_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  pageBlockPhoto();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] photo_ Photo file; may be null.
   * \param[in] caption_ Photo caption.
   */
  pageBlockPhoto(object_ptr<photo> &&photo_, object_ptr<RichText> &&caption_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -637181825;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<pageBlockPhoto> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A video.
 */
class pageBlockVideo final : public PageBlock {
 public:
  static jclass Class;
  /// Video file; may be null.
  object_ptr<video> video_;
  static jfieldID video_fieldID;
  /// Video caption.
  object_ptr<RichText> caption_;
  static jfieldID caption_fieldID;
  /// True, if the video should be played automatically.
  bool need_autoplay_;
  static jfieldID need_autoplay_fieldID;
  /// True, if the video should be looped.
  bool is_looped_;
  static jfieldID is_looped_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  pageBlockVideo();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] video_ Video file; may be null.
   * \param[in] caption_ Video caption.
   * \param[in] need_autoplay_ True, if the video should be played automatically.
   * \param[in] is_looped_ True, if the video should be looped.
   */
  pageBlockVideo(object_ptr<video> &&video_, object_ptr<RichText> &&caption_, bool need_autoplay_, bool is_looped_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1610373002;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<pageBlockVideo> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A page cover.
 */
class pageBlockCover final : public PageBlock {
 public:
  static jclass Class;
  /// Cover.
  object_ptr<PageBlock> cover_;
  static jfieldID cover_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  pageBlockCover();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] cover_ Cover.
   */
  explicit pageBlockCover(object_ptr<PageBlock> &&cover_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 972174080;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<pageBlockCover> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * An embedded web page.
 */
class pageBlockEmbedded final : public PageBlock {
 public:
  static jclass Class;
  /// Web page URL, if available.
  std::string url_;
  static jfieldID url_fieldID;
  /// HTML-markup of the embedded page.
  std::string html_;
  static jfieldID html_fieldID;
  /// Poster photo, if available; may be null.
  object_ptr<photo> poster_photo_;
  static jfieldID poster_photo_fieldID;
  /// Block width.
  std::int32_t width_;
  static jfieldID width_fieldID;
  /// Block height.
  std::int32_t height_;
  static jfieldID height_fieldID;
  /// Block caption.
  object_ptr<RichText> caption_;
  static jfieldID caption_fieldID;
  /// True, if the block should be full width.
  bool is_full_width_;
  static jfieldID is_full_width_fieldID;
  /// True, if scrolling should be allowed.
  bool allow_scrolling_;
  static jfieldID allow_scrolling_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  pageBlockEmbedded();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] url_ Web page URL, if available.
   * \param[in] html_ HTML-markup of the embedded page.
   * \param[in] poster_photo_ Poster photo, if available; may be null.
   * \param[in] width_ Block width.
   * \param[in] height_ Block height.
   * \param[in] caption_ Block caption.
   * \param[in] is_full_width_ True, if the block should be full width.
   * \param[in] allow_scrolling_ True, if scrolling should be allowed.
   */
  pageBlockEmbedded(std::string const &url_, std::string const &html_, object_ptr<photo> &&poster_photo_, std::int32_t width_, std::int32_t height_, object_ptr<RichText> &&caption_, bool is_full_width_, bool allow_scrolling_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -211257334;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<pageBlockEmbedded> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * An embedded post.
 */
class pageBlockEmbeddedPost final : public PageBlock {
 public:
  static jclass Class;
  /// Web page URL.
  std::string url_;
  static jfieldID url_fieldID;
  /// Post author.
  std::string author_;
  static jfieldID author_fieldID;
  /// Post author photo.
  object_ptr<photo> author_photo_;
  static jfieldID author_photo_fieldID;
  /// Point in time (Unix timestamp) when the post was created; 0 if unknown.
  std::int32_t date_;
  static jfieldID date_fieldID;
  /// Post content.
  std::vector<object_ptr<PageBlock>> page_blocks_;
  static jfieldID page_blocks_fieldID;
  /// Post caption.
  object_ptr<RichText> caption_;
  static jfieldID caption_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  pageBlockEmbeddedPost();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] url_ Web page URL.
   * \param[in] author_ Post author.
   * \param[in] author_photo_ Post author photo.
   * \param[in] date_ Point in time (Unix timestamp) when the post was created; 0 if unknown.
   * \param[in] page_blocks_ Post content.
   * \param[in] caption_ Post caption.
   */
  pageBlockEmbeddedPost(std::string const &url_, std::string const &author_, object_ptr<photo> &&author_photo_, std::int32_t date_, std::vector<object_ptr<PageBlock>> &&page_blocks_, object_ptr<RichText> &&caption_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1049948772;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<pageBlockEmbeddedPost> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A collage.
 */
class pageBlockCollage final : public PageBlock {
 public:
  static jclass Class;
  /// Collage item contents.
  std::vector<object_ptr<PageBlock>> page_blocks_;
  static jfieldID page_blocks_fieldID;
  /// Block caption.
  object_ptr<RichText> caption_;
  static jfieldID caption_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  pageBlockCollage();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] page_blocks_ Collage item contents.
   * \param[in] caption_ Block caption.
   */
  pageBlockCollage(std::vector<object_ptr<PageBlock>> &&page_blocks_, object_ptr<RichText> &&caption_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 911142202;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<pageBlockCollage> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A slideshow.
 */
class pageBlockSlideshow final : public PageBlock {
 public:
  static jclass Class;
  /// Slideshow item contents.
  std::vector<object_ptr<PageBlock>> page_blocks_;
  static jfieldID page_blocks_fieldID;
  /// Block caption.
  object_ptr<RichText> caption_;
  static jfieldID caption_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  pageBlockSlideshow();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] page_blocks_ Slideshow item contents.
   * \param[in] caption_ Block caption.
   */
  pageBlockSlideshow(std::vector<object_ptr<PageBlock>> &&page_blocks_, object_ptr<RichText> &&caption_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 178557514;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<pageBlockSlideshow> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A link to a chat.
 */
class pageBlockChatLink final : public PageBlock {
 public:
  static jclass Class;
  /// Chat title.
  std::string title_;
  static jfieldID title_fieldID;
  /// Chat photo; may be null.
  object_ptr<chatPhoto> photo_;
  static jfieldID photo_fieldID;
  /// Chat username, by which all other information about the chat should be resolved.
  std::string username_;
  static jfieldID username_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  pageBlockChatLink();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] title_ Chat title.
   * \param[in] photo_ Chat photo; may be null.
   * \param[in] username_ Chat username, by which all other information about the chat should be resolved.
   */
  pageBlockChatLink(std::string const &title_, object_ptr<chatPhoto> &&photo_, std::string const &username_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 214606645;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<pageBlockChatLink> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Contains information about a Telegram Passport authorization form that was requested.
 */
class passportAuthorizationForm final : public Object {
 public:
  static jclass Class;
  /// Unique identifier of the authorization form.
  std::int32_t id_;
  static jfieldID id_fieldID;
  /// Information about the Telegram Passport elements that need to be provided to complete the form.
  std::vector<object_ptr<passportRequiredElement>> required_elements_;
  static jfieldID required_elements_fieldID;
  /// Already available Telegram Passport elements.
  std::vector<object_ptr<PassportElement>> elements_;
  static jfieldID elements_fieldID;
  /// Errors in the elements that are already available.
  std::vector<object_ptr<passportElementError>> errors_;
  static jfieldID errors_fieldID;
  /// URL for the privacy policy of the service; can be empty.
  std::string privacy_policy_url_;
  static jfieldID privacy_policy_url_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  passportAuthorizationForm();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] id_ Unique identifier of the authorization form.
   * \param[in] required_elements_ Information about the Telegram Passport elements that need to be provided to complete the form.
   * \param[in] elements_ Already available Telegram Passport elements.
   * \param[in] errors_ Errors in the elements that are already available.
   * \param[in] privacy_policy_url_ URL for the privacy policy of the service; can be empty.
   */
  passportAuthorizationForm(std::int32_t id_, std::vector<object_ptr<passportRequiredElement>> &&required_elements_, std::vector<object_ptr<PassportElement>> &&elements_, std::vector<object_ptr<passportElementError>> &&errors_, std::string const &privacy_policy_url_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1780995944;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<passportAuthorizationForm> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * This class is an abstract base class.
 * Contains information about a Telegram Passport element.
 */
class PassportElement: public Object {
 public:
  static jclass Class;

  static object_ptr<PassportElement> fetch(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A Telegram Passport element containing the user's personal details.
 */
class passportElementPersonalDetails final : public PassportElement {
 public:
  static jclass Class;
  /// Personal details of the user.
  object_ptr<personalDetails> personal_details_;
  static jfieldID personal_details_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  passportElementPersonalDetails();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] personal_details_ Personal details of the user.
   */
  explicit passportElementPersonalDetails(object_ptr<personalDetails> &&personal_details_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1217724035;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<passportElementPersonalDetails> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A Telegram Passport element containing the user's passport.
 */
class passportElementPassport final : public PassportElement {
 public:
  static jclass Class;
  /// Passport.
  object_ptr<identityDocument> passport_;
  static jfieldID passport_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  passportElementPassport();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] passport_ Passport.
   */
  explicit passportElementPassport(object_ptr<identityDocument> &&passport_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -263985373;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<passportElementPassport> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A Telegram Passport element containing the user's driver license.
 */
class passportElementDriverLicense final : public PassportElement {
 public:
  static jclass Class;
  /// Driver license.
  object_ptr<identityDocument> driver_license_;
  static jfieldID driver_license_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  passportElementDriverLicense();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] driver_license_ Driver license.
   */
  explicit passportElementDriverLicense(object_ptr<identityDocument> &&driver_license_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1643580589;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<passportElementDriverLicense> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A Telegram Passport element containing the user's identity card.
 */
class passportElementIdentityCard final : public PassportElement {
 public:
  static jclass Class;
  /// Identity card.
  object_ptr<identityDocument> identity_card_;
  static jfieldID identity_card_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  passportElementIdentityCard();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] identity_card_ Identity card.
   */
  explicit passportElementIdentityCard(object_ptr<identityDocument> &&identity_card_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 2083775797;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<passportElementIdentityCard> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A Telegram Passport element containing the user's internal passport.
 */
class passportElementInternalPassport final : public PassportElement {
 public:
  static jclass Class;
  /// Internal passport.
  object_ptr<identityDocument> internal_passport_;
  static jfieldID internal_passport_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  passportElementInternalPassport();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] internal_passport_ Internal passport.
   */
  explicit passportElementInternalPassport(object_ptr<identityDocument> &&internal_passport_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 36220295;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<passportElementInternalPassport> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A Telegram Passport element containing the user's address.
 */
class passportElementAddress final : public PassportElement {
 public:
  static jclass Class;
  /// Address.
  object_ptr<address> address_;
  static jfieldID address_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  passportElementAddress();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] address_ Address.
   */
  explicit passportElementAddress(object_ptr<address> &&address_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -782625232;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<passportElementAddress> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A Telegram Passport element containing the user's utility bill.
 */
class passportElementUtilityBill final : public PassportElement {
 public:
  static jclass Class;
  /// Utility bill.
  object_ptr<personalDocument> utility_bill_;
  static jfieldID utility_bill_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  passportElementUtilityBill();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] utility_bill_ Utility bill.
   */
  explicit passportElementUtilityBill(object_ptr<personalDocument> &&utility_bill_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -234611246;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<passportElementUtilityBill> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A Telegram Passport element containing the user's bank statement.
 */
class passportElementBankStatement final : public PassportElement {
 public:
  static jclass Class;
  /// Bank statement.
  object_ptr<personalDocument> bank_statement_;
  static jfieldID bank_statement_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  passportElementBankStatement();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] bank_statement_ Bank statement.
   */
  explicit passportElementBankStatement(object_ptr<personalDocument> &&bank_statement_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -366464408;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<passportElementBankStatement> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A Telegram Passport element containing the user's rental agreement.
 */
class passportElementRentalAgreement final : public PassportElement {
 public:
  static jclass Class;
  /// Rental agreement.
  object_ptr<personalDocument> rental_agreement_;
  static jfieldID rental_agreement_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  passportElementRentalAgreement();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] rental_agreement_ Rental agreement.
   */
  explicit passportElementRentalAgreement(object_ptr<personalDocument> &&rental_agreement_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -290141400;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<passportElementRentalAgreement> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A Telegram Passport element containing the user's passport registration pages.
 */
class passportElementPassportRegistration final : public PassportElement {
 public:
  static jclass Class;
  /// Passport registration pages.
  object_ptr<personalDocument> passport_registration_;
  static jfieldID passport_registration_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  passportElementPassportRegistration();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] passport_registration_ Passport registration pages.
   */
  explicit passportElementPassportRegistration(object_ptr<personalDocument> &&passport_registration_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 618323071;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<passportElementPassportRegistration> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A Telegram Passport element containing the user's temporary registration.
 */
class passportElementTemporaryRegistration final : public PassportElement {
 public:
  static jclass Class;
  /// Temporary registration.
  object_ptr<personalDocument> temporary_registration_;
  static jfieldID temporary_registration_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  passportElementTemporaryRegistration();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] temporary_registration_ Temporary registration.
   */
  explicit passportElementTemporaryRegistration(object_ptr<personalDocument> &&temporary_registration_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1237626864;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<passportElementTemporaryRegistration> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A Telegram Passport element containing the user's phone number.
 */
class passportElementPhoneNumber final : public PassportElement {
 public:
  static jclass Class;
  /// Phone number.
  std::string phone_number_;
  static jfieldID phone_number_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  passportElementPhoneNumber();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] phone_number_ Phone number.
   */
  explicit passportElementPhoneNumber(std::string const &phone_number_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1320118375;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<passportElementPhoneNumber> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A Telegram Passport element containing the user's email address.
 */
class passportElementEmailAddress final : public PassportElement {
 public:
  static jclass Class;
  /// Email address.
  std::string email_address_;
  static jfieldID email_address_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  passportElementEmailAddress();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] email_address_ Email address.
   */
  explicit passportElementEmailAddress(std::string const &email_address_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1528129531;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<passportElementEmailAddress> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Contains the description of an error in a Telegram Passport element.
 */
class passportElementError final : public Object {
 public:
  static jclass Class;
  /// Type of the Telegram Passport element which has the error.
  object_ptr<PassportElementType> type_;
  static jfieldID type_fieldID;
  /// Error message.
  std::string message_;
  static jfieldID message_fieldID;
  /// Error source.
  object_ptr<PassportElementErrorSource> source_;
  static jfieldID source_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  passportElementError();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] type_ Type of the Telegram Passport element which has the error.
   * \param[in] message_ Error message.
   * \param[in] source_ Error source.
   */
  passportElementError(object_ptr<PassportElementType> &&type_, std::string const &message_, object_ptr<PassportElementErrorSource> &&source_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1861902395;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<passportElementError> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * This class is an abstract base class.
 * Contains the description of an error in a Telegram Passport element.
 */
class PassportElementErrorSource: public Object {
 public:
  static jclass Class;

  static object_ptr<PassportElementErrorSource> fetch(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * The element contains an error in an unspecified place. The error will be considered resolved when new data is added.
 */
class passportElementErrorSourceUnspecified final : public PassportElementErrorSource {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  passportElementErrorSourceUnspecified();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -378320830;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<passportElementErrorSourceUnspecified> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * One of the data fields contains an error. The error will be considered resolved when the value of the field changes.
 */
class passportElementErrorSourceDataField final : public PassportElementErrorSource {
 public:
  static jclass Class;
  /// Field name.
  std::string field_name_;
  static jfieldID field_name_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  passportElementErrorSourceDataField();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] field_name_ Field name.
   */
  explicit passportElementErrorSourceDataField(std::string const &field_name_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -308650776;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<passportElementErrorSourceDataField> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * The front side of the document contains an error. The error will be considered resolved when the file with the front side changes.
 */
class passportElementErrorSourceFrontSide final : public PassportElementErrorSource {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  passportElementErrorSourceFrontSide();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1895658292;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<passportElementErrorSourceFrontSide> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * The reverse side of the document contains an error. The error will be considered resolved when the file with the reverse side changes.
 */
class passportElementErrorSourceReverseSide final : public PassportElementErrorSource {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  passportElementErrorSourceReverseSide();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1918630391;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<passportElementErrorSourceReverseSide> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * The selfie with the document contains an error. The error will be considered resolved when the file with the selfie changes.
 */
class passportElementErrorSourceSelfie final : public PassportElementErrorSource {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  passportElementErrorSourceSelfie();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -797043672;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<passportElementErrorSourceSelfie> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * One of files with the translation of the document contains an error. The error will be considered resolved when the file changes.
 */
class passportElementErrorSourceTranslationFile final : public PassportElementErrorSource {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  passportElementErrorSourceTranslationFile();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -643919323;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<passportElementErrorSourceTranslationFile> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * The translation of the document contains an error. The error will be considered resolved when the list of translation files changes.
 */
class passportElementErrorSourceTranslationFiles final : public PassportElementErrorSource {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  passportElementErrorSourceTranslationFiles();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 581280796;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<passportElementErrorSourceTranslationFiles> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * The file contains an error. The error will be considered resolved when the file changes.
 */
class passportElementErrorSourceFile final : public PassportElementErrorSource {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  passportElementErrorSourceFile();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -226596202;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<passportElementErrorSourceFile> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * The list of attached files contains an error. The error will be considered resolved when the list of files changes.
 */
class passportElementErrorSourceFiles final : public PassportElementErrorSource {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  passportElementErrorSourceFiles();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1894164178;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<passportElementErrorSourceFiles> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * This class is an abstract base class.
 * Contains the type of a Telegram Passport element.
 */
class PassportElementType: public Object {
 public:
  static jclass Class;

  static object_ptr<PassportElementType> fetch(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A Telegram Passport element containing the user's personal details.
 */
class passportElementTypePersonalDetails final : public PassportElementType {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  passportElementTypePersonalDetails();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1032136365;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<passportElementTypePersonalDetails> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A Telegram Passport element containing the user's passport.
 */
class passportElementTypePassport final : public PassportElementType {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  passportElementTypePassport();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -436360376;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<passportElementTypePassport> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A Telegram Passport element containing the user's driver license.
 */
class passportElementTypeDriverLicense final : public PassportElementType {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  passportElementTypeDriverLicense();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1827298379;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<passportElementTypeDriverLicense> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A Telegram Passport element containing the user's identity card.
 */
class passportElementTypeIdentityCard final : public PassportElementType {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  passportElementTypeIdentityCard();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -502356132;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<passportElementTypeIdentityCard> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A Telegram Passport element containing the user's internal passport.
 */
class passportElementTypeInternalPassport final : public PassportElementType {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  passportElementTypeInternalPassport();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -793781959;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<passportElementTypeInternalPassport> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A Telegram Passport element containing the user's address.
 */
class passportElementTypeAddress final : public PassportElementType {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  passportElementTypeAddress();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 496327874;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<passportElementTypeAddress> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A Telegram Passport element containing the user's utility bill.
 */
class passportElementTypeUtilityBill final : public PassportElementType {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  passportElementTypeUtilityBill();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 627084906;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<passportElementTypeUtilityBill> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A Telegram Passport element containing the user's bank statement.
 */
class passportElementTypeBankStatement final : public PassportElementType {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  passportElementTypeBankStatement();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 574095667;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<passportElementTypeBankStatement> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A Telegram Passport element containing the user's rental agreement.
 */
class passportElementTypeRentalAgreement final : public PassportElementType {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  passportElementTypeRentalAgreement();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -2060583280;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<passportElementTypeRentalAgreement> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A Telegram Passport element containing the registration page of the user's passport.
 */
class passportElementTypePassportRegistration final : public PassportElementType {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  passportElementTypePassportRegistration();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -159478209;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<passportElementTypePassportRegistration> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A Telegram Passport element containing the user's temporary registration.
 */
class passportElementTypeTemporaryRegistration final : public PassportElementType {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  passportElementTypeTemporaryRegistration();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1092498527;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<passportElementTypeTemporaryRegistration> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A Telegram Passport element containing the user's phone number.
 */
class passportElementTypePhoneNumber final : public PassportElementType {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  passportElementTypePhoneNumber();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -995361172;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<passportElementTypePhoneNumber> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A Telegram Passport element containing the user's email address.
 */
class passportElementTypeEmailAddress final : public PassportElementType {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  passportElementTypeEmailAddress();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -79321405;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<passportElementTypeEmailAddress> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Contains information about saved Telegram Passport elements.
 */
class passportElements final : public Object {
 public:
  static jclass Class;
  /// Telegram Passport elements.
  std::vector<object_ptr<PassportElement>> elements_;
  static jfieldID elements_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  passportElements();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] elements_ Telegram Passport elements.
   */
  explicit passportElements(std::vector<object_ptr<PassportElement>> &&elements_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1264617556;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<passportElements> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Contains a description of the required Telegram Passport element that was requested by a service.
 */
class passportRequiredElement final : public Object {
 public:
  static jclass Class;
  /// List of Telegram Passport elements any of which is enough to provide.
  std::vector<object_ptr<passportSuitableElement>> suitable_elements_;
  static jfieldID suitable_elements_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  passportRequiredElement();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] suitable_elements_ List of Telegram Passport elements any of which is enough to provide.
   */
  explicit passportRequiredElement(std::vector<object_ptr<passportSuitableElement>> &&suitable_elements_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1983641651;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<passportRequiredElement> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Contains information about a Telegram Passport element that was requested by a service.
 */
class passportSuitableElement final : public Object {
 public:
  static jclass Class;
  /// Type of the element.
  object_ptr<PassportElementType> type_;
  static jfieldID type_fieldID;
  /// True, if a selfie is required with the identity document.
  bool is_selfie_required_;
  static jfieldID is_selfie_required_fieldID;
  /// True, if a certified English translation is required with the document.
  bool is_translation_required_;
  static jfieldID is_translation_required_fieldID;
  /// True, if personal details must include the user's name in the language of their country of residence.
  bool is_native_name_required_;
  static jfieldID is_native_name_required_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  passportSuitableElement();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] type_ Type of the element.
   * \param[in] is_selfie_required_ True, if a selfie is required with the identity document.
   * \param[in] is_translation_required_ True, if a certified English translation is required with the document.
   * \param[in] is_native_name_required_ True, if personal details must include the user's name in the language of their country of residence.
   */
  passportSuitableElement(object_ptr<PassportElementType> &&type_, bool is_selfie_required_, bool is_translation_required_, bool is_native_name_required_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -789019876;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<passportSuitableElement> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Represents the current state of 2-step verification.
 */
class passwordState final : public Object {
 public:
  static jclass Class;
  /// True if a 2-step verification password is set.
  bool has_password_;
  static jfieldID has_password_fieldID;
  /// Hint for the password; can be empty.
  std::string password_hint_;
  static jfieldID password_hint_fieldID;
  /// True if a recovery email is set.
  bool has_recovery_email_address_;
  static jfieldID has_recovery_email_address_fieldID;
  /// True if some Telegram Passport elements were saved.
  bool has_passport_data_;
  static jfieldID has_passport_data_fieldID;
  /// Pattern of the email address to which the confirmation email was sent.
  std::string unconfirmed_recovery_email_address_pattern_;
  static jfieldID unconfirmed_recovery_email_address_pattern_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  passwordState();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] has_password_ True if a 2-step verification password is set.
   * \param[in] password_hint_ Hint for the password; can be empty.
   * \param[in] has_recovery_email_address_ True if a recovery email is set.
   * \param[in] has_passport_data_ True if some Telegram Passport elements were saved.
   * \param[in] unconfirmed_recovery_email_address_pattern_ Pattern of the email address to which the confirmation email was sent.
   */
  passwordState(bool has_password_, std::string const &password_hint_, bool has_recovery_email_address_, bool has_passport_data_, std::string const &unconfirmed_recovery_email_address_pattern_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1209872059;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<passwordState> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Contains information about an invoice payment form.
 */
class paymentForm final : public Object {
 public:
  static jclass Class;
  /// Full information of the invoice.
  object_ptr<invoice> invoice_;
  static jfieldID invoice_fieldID;
  /// Payment form URL.
  std::string url_;
  static jfieldID url_fieldID;
  /// Contains information about the payment provider, if available, to support it natively without the need for opening the URL; may be null.
  object_ptr<paymentsProviderStripe> payments_provider_;
  static jfieldID payments_provider_fieldID;
  /// Saved server-side order information; may be null.
  object_ptr<orderInfo> saved_order_info_;
  static jfieldID saved_order_info_fieldID;
  /// Contains information about saved card credentials; may be null.
  object_ptr<savedCredentials> saved_credentials_;
  static jfieldID saved_credentials_fieldID;
  /// True, if the user can choose to save credentials.
  bool can_save_credentials_;
  static jfieldID can_save_credentials_fieldID;
  /// True, if the user will be able to save credentials protected by a password they set up.
  bool need_password_;
  static jfieldID need_password_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  paymentForm();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] invoice_ Full information of the invoice.
   * \param[in] url_ Payment form URL.
   * \param[in] payments_provider_ Contains information about the payment provider, if available, to support it natively without the need for opening the URL; may be null.
   * \param[in] saved_order_info_ Saved server-side order information; may be null.
   * \param[in] saved_credentials_ Contains information about saved card credentials; may be null.
   * \param[in] can_save_credentials_ True, if the user can choose to save credentials.
   * \param[in] need_password_ True, if the user will be able to save credentials protected by a password they set up.
   */
  paymentForm(object_ptr<invoice> &&invoice_, std::string const &url_, object_ptr<paymentsProviderStripe> &&payments_provider_, object_ptr<orderInfo> &&saved_order_info_, object_ptr<savedCredentials> &&saved_credentials_, bool can_save_credentials_, bool need_password_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -200418230;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<paymentForm> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Contains information about a successful payment.
 */
class paymentReceipt final : public Object {
 public:
  static jclass Class;
  /// Point in time (Unix timestamp) when the payment was made.
  std::int32_t date_;
  static jfieldID date_fieldID;
  /// User identifier of the payment provider bot.
  std::int32_t payments_provider_user_id_;
  static jfieldID payments_provider_user_id_fieldID;
  /// Contains information about the invoice.
  object_ptr<invoice> invoice_;
  static jfieldID invoice_fieldID;
  /// Contains order information; may be null.
  object_ptr<orderInfo> order_info_;
  static jfieldID order_info_fieldID;
  /// Chosen shipping option; may be null.
  object_ptr<shippingOption> shipping_option_;
  static jfieldID shipping_option_fieldID;
  /// Title of the saved credentials.
  std::string credentials_title_;
  static jfieldID credentials_title_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  paymentReceipt();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] date_ Point in time (Unix timestamp) when the payment was made.
   * \param[in] payments_provider_user_id_ User identifier of the payment provider bot.
   * \param[in] invoice_ Contains information about the invoice.
   * \param[in] order_info_ Contains order information; may be null.
   * \param[in] shipping_option_ Chosen shipping option; may be null.
   * \param[in] credentials_title_ Title of the saved credentials.
   */
  paymentReceipt(std::int32_t date_, std::int32_t payments_provider_user_id_, object_ptr<invoice> &&invoice_, object_ptr<orderInfo> &&order_info_, object_ptr<shippingOption> &&shipping_option_, std::string const &credentials_title_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1171223545;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<paymentReceipt> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Contains the result of a payment request.
 */
class paymentResult final : public Object {
 public:
  static jclass Class;
  /// True, if the payment request was successful; otherwise the verification_url will be not empty.
  bool success_;
  static jfieldID success_fieldID;
  /// URL for additional payment credentials verification.
  std::string verification_url_;
  static jfieldID verification_url_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  paymentResult();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] success_ True, if the payment request was successful; otherwise the verification_url will be not empty.
   * \param[in] verification_url_ URL for additional payment credentials verification.
   */
  paymentResult(bool success_, std::string const &verification_url_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -804263843;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<paymentResult> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Stripe payment provider.
 */
class paymentsProviderStripe final : public Object {
 public:
  static jclass Class;
  /// Stripe API publishable key.
  std::string publishable_key_;
  static jfieldID publishable_key_fieldID;
  /// True, if the user country must be provided.
  bool need_country_;
  static jfieldID need_country_fieldID;
  /// True, if the user ZIP/postal code must be provided.
  bool need_postal_code_;
  static jfieldID need_postal_code_fieldID;
  /// True, if the cardholder name must be provided.
  bool need_cardholder_name_;
  static jfieldID need_cardholder_name_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  paymentsProviderStripe();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] publishable_key_ Stripe API publishable key.
   * \param[in] need_country_ True, if the user country must be provided.
   * \param[in] need_postal_code_ True, if the user ZIP/postal code must be provided.
   * \param[in] need_cardholder_name_ True, if the cardholder name must be provided.
   */
  paymentsProviderStripe(std::string const &publishable_key_, bool need_country_, bool need_postal_code_, bool need_cardholder_name_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1090791032;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<paymentsProviderStripe> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Contains the user's personal details.
 */
class personalDetails final : public Object {
 public:
  static jclass Class;
  /// First name of the user written in English; 1-255 characters.
  std::string first_name_;
  static jfieldID first_name_fieldID;
  /// Middle name of the user written in English; 0-255 characters.
  std::string middle_name_;
  static jfieldID middle_name_fieldID;
  /// Last name of the user written in English; 1-255 characters.
  std::string last_name_;
  static jfieldID last_name_fieldID;
  /// Native first name of the user; 1-255 characters.
  std::string native_first_name_;
  static jfieldID native_first_name_fieldID;
  /// Native middle name of the user; 0-255 characters.
  std::string native_middle_name_;
  static jfieldID native_middle_name_fieldID;
  /// Native last name of the user; 1-255 characters.
  std::string native_last_name_;
  static jfieldID native_last_name_fieldID;
  /// Birthdate of the user.
  object_ptr<date> birthdate_;
  static jfieldID birthdate_fieldID;
  /// Gender of the user, &quot;male&quot; or &quot;female&quot;.
  std::string gender_;
  static jfieldID gender_fieldID;
  /// A two-letter ISO 3166-1 alpha-2 country code of the user's country.
  std::string country_code_;
  static jfieldID country_code_fieldID;
  /// A two-letter ISO 3166-1 alpha-2 country code of the user's residence country.
  std::string residence_country_code_;
  static jfieldID residence_country_code_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  personalDetails();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] first_name_ First name of the user written in English; 1-255 characters.
   * \param[in] middle_name_ Middle name of the user written in English; 0-255 characters.
   * \param[in] last_name_ Last name of the user written in English; 1-255 characters.
   * \param[in] native_first_name_ Native first name of the user; 1-255 characters.
   * \param[in] native_middle_name_ Native middle name of the user; 0-255 characters.
   * \param[in] native_last_name_ Native last name of the user; 1-255 characters.
   * \param[in] birthdate_ Birthdate of the user.
   * \param[in] gender_ Gender of the user, &quot;male&quot; or &quot;female&quot;.
   * \param[in] country_code_ A two-letter ISO 3166-1 alpha-2 country code of the user's country.
   * \param[in] residence_country_code_ A two-letter ISO 3166-1 alpha-2 country code of the user's residence country.
   */
  personalDetails(std::string const &first_name_, std::string const &middle_name_, std::string const &last_name_, std::string const &native_first_name_, std::string const &native_middle_name_, std::string const &native_last_name_, object_ptr<date> &&birthdate_, std::string const &gender_, std::string const &country_code_, std::string const &residence_country_code_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1061656137;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<personalDetails> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A personal document, containing some information about a user.
 */
class personalDocument final : public Object {
 public:
  static jclass Class;
  /// List of files containing the pages of the document.
  std::vector<object_ptr<datedFile>> files_;
  static jfieldID files_fieldID;
  /// List of files containing a certified English translation of the document.
  std::vector<object_ptr<datedFile>> translation_;
  static jfieldID translation_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  personalDocument();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] files_ List of files containing the pages of the document.
   * \param[in] translation_ List of files containing a certified English translation of the document.
   */
  personalDocument(std::vector<object_ptr<datedFile>> &&files_, std::vector<object_ptr<datedFile>> &&translation_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1011634661;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<personalDocument> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Describes a photo.
 */
class photo final : public Object {
 public:
  static jclass Class;
  /// Photo identifier; 0 for deleted photos.
  std::int64_t id_;
  static jfieldID id_fieldID;
  /// True, if stickers were added to the photo.
  bool has_stickers_;
  static jfieldID has_stickers_fieldID;
  /// Available variants of the photo, in different sizes.
  std::vector<object_ptr<photoSize>> sizes_;
  static jfieldID sizes_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  photo();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] id_ Photo identifier; 0 for deleted photos.
   * \param[in] has_stickers_ True, if stickers were added to the photo.
   * \param[in] sizes_ Available variants of the photo, in different sizes.
   */
  photo(std::int64_t id_, bool has_stickers_, std::vector<object_ptr<photoSize>> &&sizes_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1949521787;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<photo> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Photo description.
 */
class photoSize final : public Object {
 public:
  static jclass Class;
  /// Thumbnail type (see https://core.telegram.org/constructor/photoSize).
  std::string type_;
  static jfieldID type_fieldID;
  /// Information about the photo file.
  object_ptr<file> photo_;
  static jfieldID photo_fieldID;
  /// Photo width.
  std::int32_t width_;
  static jfieldID width_fieldID;
  /// Photo height.
  std::int32_t height_;
  static jfieldID height_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  photoSize();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] type_ Thumbnail type (see https://core.telegram.org/constructor/photoSize).
   * \param[in] photo_ Information about the photo file.
   * \param[in] width_ Photo width.
   * \param[in] height_ Photo height.
   */
  photoSize(std::string const &type_, object_ptr<file> &&photo_, std::int32_t width_, std::int32_t height_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 421980227;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<photoSize> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Describes a user profile photo.
 */
class profilePhoto final : public Object {
 public:
  static jclass Class;
  /// Photo identifier; 0 for an empty photo. Can be used to find a photo in a list of userProfilePhotos.
  std::int64_t id_;
  static jfieldID id_fieldID;
  /// A small (160x160) user profile photo.
  object_ptr<file> small_;
  static jfieldID small_fieldID;
  /// A big (640x640) user profile photo.
  object_ptr<file> big_;
  static jfieldID big_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  profilePhoto();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] id_ Photo identifier; 0 for an empty photo. Can be used to find a photo in a list of userProfilePhotos.
   * \param[in] small_ A small (160x160) user profile photo.
   * \param[in] big_ A big (640x640) user profile photo.
   */
  profilePhoto(std::int64_t id_, object_ptr<file> &&small_, object_ptr<file> &&big_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 978085937;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<profilePhoto> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Represents a list of proxy servers.
 */
class proxies final : public Object {
 public:
  static jclass Class;
  /// List of proxy servers.
  std::vector<object_ptr<proxy>> proxies_;
  static jfieldID proxies_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  proxies();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] proxies_ List of proxy servers.
   */
  explicit proxies(std::vector<object_ptr<proxy>> &&proxies_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1200447205;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<proxies> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Contains information about a proxy server.
 */
class proxy final : public Object {
 public:
  static jclass Class;
  /// Unique identifier of the proxy.
  std::int32_t id_;
  static jfieldID id_fieldID;
  /// Proxy server IP address.
  std::string server_;
  static jfieldID server_fieldID;
  /// Proxy server port.
  std::int32_t port_;
  static jfieldID port_fieldID;
  /// Point in time (Unix timestamp) when the proxy was last used; 0 if never.
  std::int32_t last_used_date_;
  static jfieldID last_used_date_fieldID;
  /// True, if the proxy is enabled now.
  bool is_enabled_;
  static jfieldID is_enabled_fieldID;
  /// Type of the proxy.
  object_ptr<ProxyType> type_;
  static jfieldID type_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  proxy();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] id_ Unique identifier of the proxy.
   * \param[in] server_ Proxy server IP address.
   * \param[in] port_ Proxy server port.
   * \param[in] last_used_date_ Point in time (Unix timestamp) when the proxy was last used; 0 if never.
   * \param[in] is_enabled_ True, if the proxy is enabled now.
   * \param[in] type_ Type of the proxy.
   */
  proxy(std::int32_t id_, std::string const &server_, std::int32_t port_, std::int32_t last_used_date_, bool is_enabled_, object_ptr<ProxyType> &&type_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 196049779;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<proxy> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * This class is an abstract base class.
 * Describes the type of the proxy server.
 */
class ProxyType: public Object {
 public:
  static jclass Class;

  static object_ptr<ProxyType> fetch(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A SOCKS5 proxy server.
 */
class proxyTypeSocks5 final : public ProxyType {
 public:
  static jclass Class;
  /// Username for logging in; may be empty.
  std::string username_;
  static jfieldID username_fieldID;
  /// Password for logging in; may be empty.
  std::string password_;
  static jfieldID password_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  proxyTypeSocks5();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] username_ Username for logging in; may be empty.
   * \param[in] password_ Password for logging in; may be empty.
   */
  proxyTypeSocks5(std::string const &username_, std::string const &password_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -890027341;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<proxyTypeSocks5> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A HTTP transparent proxy server.
 */
class proxyTypeHttp final : public ProxyType {
 public:
  static jclass Class;
  /// Username for logging in; may be empty.
  std::string username_;
  static jfieldID username_fieldID;
  /// Password for logging in; may be empty.
  std::string password_;
  static jfieldID password_fieldID;
  /// Pass true, if the proxy supports only HTTP requests and doesn't support transparent TCP connections via HTTP CONNECT method.
  bool http_only_;
  static jfieldID http_only_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  proxyTypeHttp();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] username_ Username for logging in; may be empty.
   * \param[in] password_ Password for logging in; may be empty.
   * \param[in] http_only_ Pass true, if the proxy supports only HTTP requests and doesn't support transparent TCP connections via HTTP CONNECT method.
   */
  proxyTypeHttp(std::string const &username_, std::string const &password_, bool http_only_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1547188361;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<proxyTypeHttp> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * An MTProto proxy server.
 */
class proxyTypeMtproto final : public ProxyType {
 public:
  static jclass Class;
  /// The proxy's secret in hexadecimal encoding.
  std::string secret_;
  static jfieldID secret_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  proxyTypeMtproto();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] secret_ The proxy's secret in hexadecimal encoding.
   */
  explicit proxyTypeMtproto(std::string const &secret_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1964826627;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<proxyTypeMtproto> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Contains a public HTTPS link to a message in a public supergroup or channel.
 */
class publicMessageLink final : public Object {
 public:
  static jclass Class;
  /// Message link.
  std::string link_;
  static jfieldID link_fieldID;
  /// HTML-code for embedding the message.
  std::string html_;
  static jfieldID html_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  publicMessageLink();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] link_ Message link.
   * \param[in] html_ HTML-code for embedding the message.
   */
  publicMessageLink(std::string const &link_, std::string const &html_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -679603433;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<publicMessageLink> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Contains information about the current recovery email address.
 */
class recoveryEmailAddress final : public Object {
 public:
  static jclass Class;
  /// Recovery email address.
  std::string recovery_email_address_;
  static jfieldID recovery_email_address_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  recoveryEmailAddress();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] recovery_email_address_ Recovery email address.
   */
  explicit recoveryEmailAddress(std::string const &recovery_email_address_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1290526187;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<recoveryEmailAddress> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Represents a remote file.
 */
class remoteFile final : public Object {
 public:
  static jclass Class;
  /// Remote file identifier; may be empty. Can be used across application restarts or even from other devices for the current user. If the ID starts with &quot;http://&quot; or &quot;https://&quot;, it represents the HTTP URL of the file. TDLib is currently unable to download files if only their URL is known. If downloadFile is called on such a file or if it is sent to a secret chat, TDLib starts a file generation process by sending updateFileGenerationStart to the client with the HTTP URL in the original_path and &quot;\#url\#&quot; as the conversion string. Clients should generate the file by downloading it to the specified location.
  std::string id_;
  static jfieldID id_fieldID;
  /// True, if the file is currently being uploaded (or a remote copy is being generated by some other means).
  bool is_uploading_active_;
  static jfieldID is_uploading_active_fieldID;
  /// True, if a remote copy is fully available.
  bool is_uploading_completed_;
  static jfieldID is_uploading_completed_fieldID;
  /// Size of the remote available part of the file; 0 if unknown.
  std::int32_t uploaded_size_;
  static jfieldID uploaded_size_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  remoteFile();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] id_ Remote file identifier; may be empty. Can be used across application restarts or even from other devices for the current user. If the ID starts with &quot;http://&quot; or &quot;https://&quot;, it represents the HTTP URL of the file. TDLib is currently unable to download files if only their URL is known. If downloadFile is called on such a file or if it is sent to a secret chat, TDLib starts a file generation process by sending updateFileGenerationStart to the client with the HTTP URL in the original_path and &quot;\#url\#&quot; as the conversion string. Clients should generate the file by downloading it to the specified location.
   * \param[in] is_uploading_active_ True, if the file is currently being uploaded (or a remote copy is being generated by some other means).
   * \param[in] is_uploading_completed_ True, if a remote copy is fully available.
   * \param[in] uploaded_size_ Size of the remote available part of the file; 0 if unknown.
   */
  remoteFile(std::string const &id_, bool is_uploading_active_, bool is_uploading_completed_, std::int32_t uploaded_size_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1761289748;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<remoteFile> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * This class is an abstract base class.
 * Contains a description of a custom keyboard and actions that can be done with it to quickly reply to bots.
 */
class ReplyMarkup: public Object {
 public:
  static jclass Class;

  static object_ptr<ReplyMarkup> fetch(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Instructs clients to remove the keyboard once this message has been received. This kind of keyboard can't be received in an incoming message; instead, UpdateChatReplyMarkup with message_id == 0 will be sent.
 */
class replyMarkupRemoveKeyboard final : public ReplyMarkup {
 public:
  static jclass Class;
  /// True, if the keyboard is removed only for the mentioned users or the target user of a reply.
  bool is_personal_;
  static jfieldID is_personal_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  replyMarkupRemoveKeyboard();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] is_personal_ True, if the keyboard is removed only for the mentioned users or the target user of a reply.
   */
  explicit replyMarkupRemoveKeyboard(bool is_personal_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -691252879;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<replyMarkupRemoveKeyboard> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Instructs clients to force a reply to this message.
 */
class replyMarkupForceReply final : public ReplyMarkup {
 public:
  static jclass Class;
  /// True, if a forced reply must automatically be shown to the current user. For outgoing messages, specify true to show the forced reply only for the mentioned users and for the target user of a reply.
  bool is_personal_;
  static jfieldID is_personal_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  replyMarkupForceReply();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] is_personal_ True, if a forced reply must automatically be shown to the current user. For outgoing messages, specify true to show the forced reply only for the mentioned users and for the target user of a reply.
   */
  explicit replyMarkupForceReply(bool is_personal_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1039104593;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<replyMarkupForceReply> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Contains a custom keyboard layout to quickly reply to bots.
 */
class replyMarkupShowKeyboard final : public ReplyMarkup {
 public:
  static jclass Class;
  /// A list of rows of bot keyboard buttons.
  std::vector<std::vector<object_ptr<keyboardButton>>> rows_;
  static jfieldID rows_fieldID;
  /// True, if the client needs to resize the keyboard vertically.
  bool resize_keyboard_;
  static jfieldID resize_keyboard_fieldID;
  /// True, if the client needs to hide the keyboard after use.
  bool one_time_;
  static jfieldID one_time_fieldID;
  /// True, if the keyboard must automatically be shown to the current user. For outgoing messages, specify true to show the keyboard only for the mentioned users and for the target user of a reply.
  bool is_personal_;
  static jfieldID is_personal_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  replyMarkupShowKeyboard();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] rows_ A list of rows of bot keyboard buttons.
   * \param[in] resize_keyboard_ True, if the client needs to resize the keyboard vertically.
   * \param[in] one_time_ True, if the client needs to hide the keyboard after use.
   * \param[in] is_personal_ True, if the keyboard must automatically be shown to the current user. For outgoing messages, specify true to show the keyboard only for the mentioned users and for the target user of a reply.
   */
  replyMarkupShowKeyboard(std::vector<std::vector<object_ptr<keyboardButton>>> &&rows_, bool resize_keyboard_, bool one_time_, bool is_personal_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -992627133;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<replyMarkupShowKeyboard> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Contains an inline keyboard layout.
 */
class replyMarkupInlineKeyboard final : public ReplyMarkup {
 public:
  static jclass Class;
  /// A list of rows of inline keyboard buttons.
  std::vector<std::vector<object_ptr<inlineKeyboardButton>>> rows_;
  static jfieldID rows_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  replyMarkupInlineKeyboard();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] rows_ A list of rows of inline keyboard buttons.
   */
  explicit replyMarkupInlineKeyboard(std::vector<std::vector<object_ptr<inlineKeyboardButton>>> &&rows_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -619317658;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<replyMarkupInlineKeyboard> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * This class is an abstract base class.
 * Describes a text object inside an instant-view web page.
 */
class RichText: public Object {
 public:
  static jclass Class;

  static object_ptr<RichText> fetch(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A plain text.
 */
class richTextPlain final : public RichText {
 public:
  static jclass Class;
  /// Text.
  std::string text_;
  static jfieldID text_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  richTextPlain();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] text_ Text.
   */
  explicit richTextPlain(std::string const &text_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 482617702;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<richTextPlain> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A bold rich text.
 */
class richTextBold final : public RichText {
 public:
  static jclass Class;
  /// Text.
  object_ptr<RichText> text_;
  static jfieldID text_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  richTextBold();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] text_ Text.
   */
  explicit richTextBold(object_ptr<RichText> &&text_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1670844268;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<richTextBold> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * An italicized rich text.
 */
class richTextItalic final : public RichText {
 public:
  static jclass Class;
  /// Text.
  object_ptr<RichText> text_;
  static jfieldID text_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  richTextItalic();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] text_ Text.
   */
  explicit richTextItalic(object_ptr<RichText> &&text_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1853354047;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<richTextItalic> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * An underlined rich text.
 */
class richTextUnderline final : public RichText {
 public:
  static jclass Class;
  /// Text.
  object_ptr<RichText> text_;
  static jfieldID text_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  richTextUnderline();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] text_ Text.
   */
  explicit richTextUnderline(object_ptr<RichText> &&text_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -536019572;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<richTextUnderline> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A strike-through rich text.
 */
class richTextStrikethrough final : public RichText {
 public:
  static jclass Class;
  /// Text.
  object_ptr<RichText> text_;
  static jfieldID text_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  richTextStrikethrough();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] text_ Text.
   */
  explicit richTextStrikethrough(object_ptr<RichText> &&text_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 723413585;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<richTextStrikethrough> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A fixed-width rich text.
 */
class richTextFixed final : public RichText {
 public:
  static jclass Class;
  /// Text.
  object_ptr<RichText> text_;
  static jfieldID text_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  richTextFixed();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] text_ Text.
   */
  explicit richTextFixed(object_ptr<RichText> &&text_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1271496249;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<richTextFixed> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A rich text URL link.
 */
class richTextUrl final : public RichText {
 public:
  static jclass Class;
  /// Text.
  object_ptr<RichText> text_;
  static jfieldID text_fieldID;
  /// URL.
  std::string url_;
  static jfieldID url_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  richTextUrl();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] text_ Text.
   * \param[in] url_ URL.
   */
  richTextUrl(object_ptr<RichText> &&text_, std::string const &url_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1967248447;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<richTextUrl> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A rich text email link.
 */
class richTextEmailAddress final : public RichText {
 public:
  static jclass Class;
  /// Text.
  object_ptr<RichText> text_;
  static jfieldID text_fieldID;
  /// Email address.
  std::string email_address_;
  static jfieldID email_address_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  richTextEmailAddress();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] text_ Text.
   * \param[in] email_address_ Email address.
   */
  richTextEmailAddress(object_ptr<RichText> &&text_, std::string const &email_address_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 40018679;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<richTextEmailAddress> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A concatenation of rich texts.
 */
class richTexts final : public RichText {
 public:
  static jclass Class;
  /// Texts.
  std::vector<object_ptr<RichText>> texts_;
  static jfieldID texts_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  richTexts();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] texts_ Texts.
   */
  explicit richTexts(std::vector<object_ptr<RichText>> &&texts_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1647457821;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<richTexts> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Contains information about saved card credentials.
 */
class savedCredentials final : public Object {
 public:
  static jclass Class;
  /// Unique identifier of the saved credentials.
  std::string id_;
  static jfieldID id_fieldID;
  /// Title of the saved credentials.
  std::string title_;
  static jfieldID title_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  savedCredentials();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] id_ Unique identifier of the saved credentials.
   * \param[in] title_ Title of the saved credentials.
   */
  savedCredentials(std::string const &id_, std::string const &title_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -370273060;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<savedCredentials> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Contains information about notification settings for several chats.
 */
class scopeNotificationSettings final : public Object {
 public:
  static jclass Class;
  /// Time left before notifications will be unmuted, in seconds.
  std::int32_t mute_for_;
  static jfieldID mute_for_fieldID;
  /// The name of an audio file to be used for notification sounds; only applies to iOS applications.
  std::string sound_;
  static jfieldID sound_fieldID;
  /// True, if message content should be displayed in notifications.
  bool show_preview_;
  static jfieldID show_preview_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  scopeNotificationSettings();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] mute_for_ Time left before notifications will be unmuted, in seconds.
   * \param[in] sound_ The name of an audio file to be used for notification sounds; only applies to iOS applications.
   * \param[in] show_preview_ True, if message content should be displayed in notifications.
   */
  scopeNotificationSettings(std::int32_t mute_for_, std::string const &sound_, bool show_preview_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 607439283;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<scopeNotificationSettings> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * This class is an abstract base class.
 * Represents a filter for message search results.
 */
class SearchMessagesFilter: public Object {
 public:
  static jclass Class;

  static object_ptr<SearchMessagesFilter> fetch(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Returns all found messages, no filter is applied.
 */
class searchMessagesFilterEmpty final : public SearchMessagesFilter {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  searchMessagesFilterEmpty();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -869395657;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<searchMessagesFilterEmpty> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Returns only animation messages.
 */
class searchMessagesFilterAnimation final : public SearchMessagesFilter {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  searchMessagesFilterAnimation();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -155713339;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<searchMessagesFilterAnimation> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Returns only audio messages.
 */
class searchMessagesFilterAudio final : public SearchMessagesFilter {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  searchMessagesFilterAudio();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 867505275;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<searchMessagesFilterAudio> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Returns only document messages.
 */
class searchMessagesFilterDocument final : public SearchMessagesFilter {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  searchMessagesFilterDocument();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1526331215;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<searchMessagesFilterDocument> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Returns only photo messages.
 */
class searchMessagesFilterPhoto final : public SearchMessagesFilter {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  searchMessagesFilterPhoto();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 925932293;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<searchMessagesFilterPhoto> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Returns only video messages.
 */
class searchMessagesFilterVideo final : public SearchMessagesFilter {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  searchMessagesFilterVideo();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 115538222;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<searchMessagesFilterVideo> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Returns only voice note messages.
 */
class searchMessagesFilterVoiceNote final : public SearchMessagesFilter {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  searchMessagesFilterVoiceNote();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1841439357;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<searchMessagesFilterVoiceNote> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Returns only photo and video messages.
 */
class searchMessagesFilterPhotoAndVideo final : public SearchMessagesFilter {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  searchMessagesFilterPhotoAndVideo();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1352130963;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<searchMessagesFilterPhotoAndVideo> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Returns only messages containing URLs.
 */
class searchMessagesFilterUrl final : public SearchMessagesFilter {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  searchMessagesFilterUrl();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1828724341;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<searchMessagesFilterUrl> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Returns only messages containing chat photos.
 */
class searchMessagesFilterChatPhoto final : public SearchMessagesFilter {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  searchMessagesFilterChatPhoto();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1247751329;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<searchMessagesFilterChatPhoto> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Returns only call messages.
 */
class searchMessagesFilterCall final : public SearchMessagesFilter {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  searchMessagesFilterCall();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1305231012;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<searchMessagesFilterCall> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Returns only incoming call messages with missed/declined discard reasons.
 */
class searchMessagesFilterMissedCall final : public SearchMessagesFilter {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  searchMessagesFilterMissedCall();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 970663098;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<searchMessagesFilterMissedCall> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Returns only video note messages.
 */
class searchMessagesFilterVideoNote final : public SearchMessagesFilter {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  searchMessagesFilterVideoNote();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 564323321;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<searchMessagesFilterVideoNote> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Returns only voice and video note messages.
 */
class searchMessagesFilterVoiceAndVideoNote final : public SearchMessagesFilter {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  searchMessagesFilterVoiceAndVideoNote();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 664174819;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<searchMessagesFilterVoiceAndVideoNote> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Returns only messages with mentions of the current user, or messages that are replies to their messages.
 */
class searchMessagesFilterMention final : public SearchMessagesFilter {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  searchMessagesFilterMention();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 2001258652;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<searchMessagesFilterMention> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Returns only messages with unread mentions of the current user or messages that are replies to their messages. When using this filter the results can't be additionally filtered by a query or by the sending user.
 */
class searchMessagesFilterUnreadMention final : public SearchMessagesFilter {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  searchMessagesFilterUnreadMention();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -95769149;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<searchMessagesFilterUnreadMention> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Contains a value representing a number of seconds.
 */
class seconds final : public Object {
 public:
  static jclass Class;
  /// Number of seconds.
  double seconds_;
  static jfieldID seconds_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  seconds();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] seconds_ Number of seconds.
   */
  explicit seconds(double seconds_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 959899022;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<seconds> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Represents a secret chat.
 */
class secretChat final : public Object {
 public:
  static jclass Class;
  /// Secret chat identifier.
  std::int32_t id_;
  static jfieldID id_fieldID;
  /// Identifier of the chat partner.
  std::int32_t user_id_;
  static jfieldID user_id_fieldID;
  /// State of the secret chat.
  object_ptr<SecretChatState> state_;
  static jfieldID state_fieldID;
  /// True, if the chat was created by the current user; otherwise false.
  bool is_outbound_;
  static jfieldID is_outbound_fieldID;
  /// Current message Time To Live setting (self-destruct timer) for the chat, in seconds.
  std::int32_t ttl_;
  static jfieldID ttl_fieldID;
  /// Hash of the currently used key for comparison with the hash of the chat partner's key. This is a string of 36 bytes, which must be used to make a 12x12 square image with a color depth of 4. The first 16 bytes should be used to make a central 8x8 square, while the remaining 20 bytes should be used to construct a 2-pixel-wide border around that square. Alternatively, the first 32 bytes of the hash can be converted to the hexadecimal format and printed as 32 2-digit hex numbers.
  std::string key_hash_;
  static jfieldID key_hash_fieldID;
  /// Secret chat layer; determines features supported by the other client. Video notes are supported if the layer &gt;= 66.
  std::int32_t layer_;
  static jfieldID layer_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  secretChat();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] id_ Secret chat identifier.
   * \param[in] user_id_ Identifier of the chat partner.
   * \param[in] state_ State of the secret chat.
   * \param[in] is_outbound_ True, if the chat was created by the current user; otherwise false.
   * \param[in] ttl_ Current message Time To Live setting (self-destruct timer) for the chat, in seconds.
   * \param[in] key_hash_ Hash of the currently used key for comparison with the hash of the chat partner's key. This is a string of 36 bytes, which must be used to make a 12x12 square image with a color depth of 4. The first 16 bytes should be used to make a central 8x8 square, while the remaining 20 bytes should be used to construct a 2-pixel-wide border around that square. Alternatively, the first 32 bytes of the hash can be converted to the hexadecimal format and printed as 32 2-digit hex numbers.
   * \param[in] layer_ Secret chat layer; determines features supported by the other client. Video notes are supported if the layer &gt;= 66.
   */
  secretChat(std::int32_t id_, std::int32_t user_id_, object_ptr<SecretChatState> &&state_, bool is_outbound_, std::int32_t ttl_, std::string const &key_hash_, std::int32_t layer_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1279231629;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<secretChat> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * This class is an abstract base class.
 * Describes the current secret chat state.
 */
class SecretChatState: public Object {
 public:
  static jclass Class;

  static object_ptr<SecretChatState> fetch(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * The secret chat is not yet created; waiting for the other user to get online.
 */
class secretChatStatePending final : public SecretChatState {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  secretChatStatePending();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1637050756;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<secretChatStatePending> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * The secret chat is ready to use.
 */
class secretChatStateReady final : public SecretChatState {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  secretChatStateReady();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1611352087;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<secretChatStateReady> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * The secret chat is closed.
 */
class secretChatStateClosed final : public SecretChatState {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  secretChatStateClosed();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1945106707;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<secretChatStateClosed> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Contains information about one session in a Telegram application used by the current user.
 */
class session final : public Object {
 public:
  static jclass Class;
  /// Session identifier.
  std::int64_t id_;
  static jfieldID id_fieldID;
  /// True, if this session is the current session.
  bool is_current_;
  static jfieldID is_current_fieldID;
  /// Telegram API identifier, as provided by the application.
  std::int32_t api_id_;
  static jfieldID api_id_fieldID;
  /// Name of the application, as provided by the application.
  std::string application_name_;
  static jfieldID application_name_fieldID;
  /// The version of the application, as provided by the application.
  std::string application_version_;
  static jfieldID application_version_fieldID;
  /// True, if the application is an official application or uses the api_id of an official application.
  bool is_official_application_;
  static jfieldID is_official_application_fieldID;
  /// Model of the device the application has been run or is running on, as provided by the application.
  std::string device_model_;
  static jfieldID device_model_fieldID;
  /// Operating system the application has been run or is running on, as provided by the application.
  std::string platform_;
  static jfieldID platform_fieldID;
  /// Version of the operating system the application has been run or is running on, as provided by the application.
  std::string system_version_;
  static jfieldID system_version_fieldID;
  /// Point in time (Unix timestamp) when the user has logged in.
  std::int32_t log_in_date_;
  static jfieldID log_in_date_fieldID;
  /// Point in time (Unix timestamp) when the session was last used.
  std::int32_t last_active_date_;
  static jfieldID last_active_date_fieldID;
  /// IP address from which the session was created, in human-readable format.
  std::string ip_;
  static jfieldID ip_fieldID;
  /// A two-letter country code for the country from which the session was created, based on the IP address.
  std::string country_;
  static jfieldID country_fieldID;
  /// Region code from which the session was created, based on the IP address.
  std::string region_;
  static jfieldID region_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  session();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] id_ Session identifier.
   * \param[in] is_current_ True, if this session is the current session.
   * \param[in] api_id_ Telegram API identifier, as provided by the application.
   * \param[in] application_name_ Name of the application, as provided by the application.
   * \param[in] application_version_ The version of the application, as provided by the application.
   * \param[in] is_official_application_ True, if the application is an official application or uses the api_id of an official application.
   * \param[in] device_model_ Model of the device the application has been run or is running on, as provided by the application.
   * \param[in] platform_ Operating system the application has been run or is running on, as provided by the application.
   * \param[in] system_version_ Version of the operating system the application has been run or is running on, as provided by the application.
   * \param[in] log_in_date_ Point in time (Unix timestamp) when the user has logged in.
   * \param[in] last_active_date_ Point in time (Unix timestamp) when the session was last used.
   * \param[in] ip_ IP address from which the session was created, in human-readable format.
   * \param[in] country_ A two-letter country code for the country from which the session was created, based on the IP address.
   * \param[in] region_ Region code from which the session was created, based on the IP address.
   */
  session(std::int64_t id_, bool is_current_, std::int32_t api_id_, std::string const &application_name_, std::string const &application_version_, bool is_official_application_, std::string const &device_model_, std::string const &platform_, std::string const &system_version_, std::int32_t log_in_date_, std::int32_t last_active_date_, std::string const &ip_, std::string const &country_, std::string const &region_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1715359000;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<session> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Contains a list of sessions.
 */
class sessions final : public Object {
 public:
  static jclass Class;
  /// List of sessions.
  std::vector<object_ptr<session>> sessions_;
  static jfieldID sessions_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  sessions();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] sessions_ List of sessions.
   */
  explicit sessions(std::vector<object_ptr<session>> &&sessions_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -463118121;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<sessions> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * One shipping option.
 */
class shippingOption final : public Object {
 public:
  static jclass Class;
  /// Shipping option identifier.
  std::string id_;
  static jfieldID id_fieldID;
  /// Option title.
  std::string title_;
  static jfieldID title_fieldID;
  /// A list of objects used to calculate the total shipping costs.
  std::vector<object_ptr<labeledPricePart>> price_parts_;
  static jfieldID price_parts_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  shippingOption();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] id_ Shipping option identifier.
   * \param[in] title_ Option title.
   * \param[in] price_parts_ A list of objects used to calculate the total shipping costs.
   */
  shippingOption(std::string const &id_, std::string const &title_, std::vector<object_ptr<labeledPricePart>> &&price_parts_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1425690001;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<shippingOption> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Describes a sticker.
 */
class sticker final : public Object {
 public:
  static jclass Class;
  /// The identifier of the sticker set to which the sticker belongs; 0 if none.
  std::int64_t set_id_;
  static jfieldID set_id_fieldID;
  /// Sticker width; as defined by the sender.
  std::int32_t width_;
  static jfieldID width_fieldID;
  /// Sticker height; as defined by the sender.
  std::int32_t height_;
  static jfieldID height_fieldID;
  /// Emoji corresponding to the sticker.
  std::string emoji_;
  static jfieldID emoji_fieldID;
  /// True, if the sticker is a mask.
  bool is_mask_;
  static jfieldID is_mask_fieldID;
  /// Position where the mask should be placed; may be null.
  object_ptr<maskPosition> mask_position_;
  static jfieldID mask_position_fieldID;
  /// Sticker thumbnail in WEBP or JPEG format; may be null.
  object_ptr<photoSize> thumbnail_;
  static jfieldID thumbnail_fieldID;
  /// File containing the sticker.
  object_ptr<file> sticker_;
  static jfieldID sticker_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  sticker();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] set_id_ The identifier of the sticker set to which the sticker belongs; 0 if none.
   * \param[in] width_ Sticker width; as defined by the sender.
   * \param[in] height_ Sticker height; as defined by the sender.
   * \param[in] emoji_ Emoji corresponding to the sticker.
   * \param[in] is_mask_ True, if the sticker is a mask.
   * \param[in] mask_position_ Position where the mask should be placed; may be null.
   * \param[in] thumbnail_ Sticker thumbnail in WEBP or JPEG format; may be null.
   * \param[in] sticker_ File containing the sticker.
   */
  sticker(std::int64_t set_id_, std::int32_t width_, std::int32_t height_, std::string const &emoji_, bool is_mask_, object_ptr<maskPosition> &&mask_position_, object_ptr<photoSize> &&thumbnail_, object_ptr<file> &&sticker_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -876442962;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<sticker> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Represents a list of all emoji corresponding to a sticker in a sticker set. The list is only for informational purposes, because a sticker is always sent with a fixed emoji from the corresponding Sticker object.
 */
class stickerEmojis final : public Object {
 public:
  static jclass Class;
  /// List of emojis.
  std::vector<std::string> emojis_;
  static jfieldID emojis_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  stickerEmojis();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] emojis_ List of emojis.
   */
  explicit stickerEmojis(std::vector<std::string> &&emojis_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1781588570;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<stickerEmojis> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Represents a sticker set.
 */
class stickerSet final : public Object {
 public:
  static jclass Class;
  /// Identifier of the sticker set.
  std::int64_t id_;
  static jfieldID id_fieldID;
  /// Title of the sticker set.
  std::string title_;
  static jfieldID title_fieldID;
  /// Name of the sticker set.
  std::string name_;
  static jfieldID name_fieldID;
  /// True, if the sticker set has been installed by the current user.
  bool is_installed_;
  static jfieldID is_installed_fieldID;
  /// True, if the sticker set has been archived. A sticker set can't be installed and archived simultaneously.
  bool is_archived_;
  static jfieldID is_archived_fieldID;
  /// True, if the sticker set is official.
  bool is_official_;
  static jfieldID is_official_fieldID;
  /// True, if the stickers in the set are masks.
  bool is_masks_;
  static jfieldID is_masks_fieldID;
  /// True for already viewed trending sticker sets.
  bool is_viewed_;
  static jfieldID is_viewed_fieldID;
  /// List of stickers in this set.
  std::vector<object_ptr<sticker>> stickers_;
  static jfieldID stickers_fieldID;
  /// A list of emoji corresponding to the stickers in the same order.
  std::vector<object_ptr<stickerEmojis>> emojis_;
  static jfieldID emojis_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  stickerSet();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] id_ Identifier of the sticker set.
   * \param[in] title_ Title of the sticker set.
   * \param[in] name_ Name of the sticker set.
   * \param[in] is_installed_ True, if the sticker set has been installed by the current user.
   * \param[in] is_archived_ True, if the sticker set has been archived. A sticker set can't be installed and archived simultaneously.
   * \param[in] is_official_ True, if the sticker set is official.
   * \param[in] is_masks_ True, if the stickers in the set are masks.
   * \param[in] is_viewed_ True for already viewed trending sticker sets.
   * \param[in] stickers_ List of stickers in this set.
   * \param[in] emojis_ A list of emoji corresponding to the stickers in the same order.
   */
  stickerSet(std::int64_t id_, std::string const &title_, std::string const &name_, bool is_installed_, bool is_archived_, bool is_official_, bool is_masks_, bool is_viewed_, std::vector<object_ptr<sticker>> &&stickers_, std::vector<object_ptr<stickerEmojis>> &&emojis_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 72047469;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<stickerSet> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Represents short information about a sticker set.
 */
class stickerSetInfo final : public Object {
 public:
  static jclass Class;
  /// Identifier of the sticker set.
  std::int64_t id_;
  static jfieldID id_fieldID;
  /// Title of the sticker set.
  std::string title_;
  static jfieldID title_fieldID;
  /// Name of the sticker set.
  std::string name_;
  static jfieldID name_fieldID;
  /// True, if the sticker set has been installed by current user.
  bool is_installed_;
  static jfieldID is_installed_fieldID;
  /// True, if the sticker set has been archived. A sticker set can't be installed and archived simultaneously.
  bool is_archived_;
  static jfieldID is_archived_fieldID;
  /// True, if the sticker set is official.
  bool is_official_;
  static jfieldID is_official_fieldID;
  /// True, if the stickers in the set are masks.
  bool is_masks_;
  static jfieldID is_masks_fieldID;
  /// True for already viewed trending sticker sets.
  bool is_viewed_;
  static jfieldID is_viewed_fieldID;
  /// Total number of stickers in the set.
  std::int32_t size_;
  static jfieldID size_fieldID;
  /// Contains up to the first 5 stickers from the set, depending on the context. If the client needs more stickers the full set should be requested.
  std::vector<object_ptr<sticker>> covers_;
  static jfieldID covers_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  stickerSetInfo();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] id_ Identifier of the sticker set.
   * \param[in] title_ Title of the sticker set.
   * \param[in] name_ Name of the sticker set.
   * \param[in] is_installed_ True, if the sticker set has been installed by current user.
   * \param[in] is_archived_ True, if the sticker set has been archived. A sticker set can't be installed and archived simultaneously.
   * \param[in] is_official_ True, if the sticker set is official.
   * \param[in] is_masks_ True, if the stickers in the set are masks.
   * \param[in] is_viewed_ True for already viewed trending sticker sets.
   * \param[in] size_ Total number of stickers in the set.
   * \param[in] covers_ Contains up to the first 5 stickers from the set, depending on the context. If the client needs more stickers the full set should be requested.
   */
  stickerSetInfo(std::int64_t id_, std::string const &title_, std::string const &name_, bool is_installed_, bool is_archived_, bool is_official_, bool is_masks_, bool is_viewed_, std::int32_t size_, std::vector<object_ptr<sticker>> &&covers_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1469837113;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<stickerSetInfo> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Represents a list of sticker sets.
 */
class stickerSets final : public Object {
 public:
  static jclass Class;
  /// Approximate total number of sticker sets found.
  std::int32_t total_count_;
  static jfieldID total_count_fieldID;
  /// List of sticker sets.
  std::vector<object_ptr<stickerSetInfo>> sets_;
  static jfieldID sets_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  stickerSets();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] total_count_ Approximate total number of sticker sets found.
   * \param[in] sets_ List of sticker sets.
   */
  stickerSets(std::int32_t total_count_, std::vector<object_ptr<stickerSetInfo>> &&sets_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1883828812;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<stickerSets> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Represents a list of stickers.
 */
class stickers final : public Object {
 public:
  static jclass Class;
  /// List of stickers.
  std::vector<object_ptr<sticker>> stickers_;
  static jfieldID stickers_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  stickers();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] stickers_ List of stickers.
   */
  explicit stickers(std::vector<object_ptr<sticker>> &&stickers_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1974859260;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<stickers> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Contains the exact storage usage statistics split by chats and file type.
 */
class storageStatistics final : public Object {
 public:
  static jclass Class;
  /// Total size of files.
  std::int64_t size_;
  static jfieldID size_fieldID;
  /// Total number of files.
  std::int32_t count_;
  static jfieldID count_fieldID;
  /// Statistics split by chats.
  std::vector<object_ptr<storageStatisticsByChat>> by_chat_;
  static jfieldID by_chat_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  storageStatistics();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] size_ Total size of files.
   * \param[in] count_ Total number of files.
   * \param[in] by_chat_ Statistics split by chats.
   */
  storageStatistics(std::int64_t size_, std::int32_t count_, std::vector<object_ptr<storageStatisticsByChat>> &&by_chat_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 217237013;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<storageStatistics> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Contains the storage usage statistics for a specific chat.
 */
class storageStatisticsByChat final : public Object {
 public:
  static jclass Class;
  /// Chat identifier; 0 if none.
  std::int64_t chat_id_;
  static jfieldID chat_id_fieldID;
  /// Total size of the files in the chat.
  std::int64_t size_;
  static jfieldID size_fieldID;
  /// Total number of files in the chat.
  std::int32_t count_;
  static jfieldID count_fieldID;
  /// Statistics split by file types.
  std::vector<object_ptr<storageStatisticsByFileType>> by_file_type_;
  static jfieldID by_file_type_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  storageStatisticsByChat();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] chat_id_ Chat identifier; 0 if none.
   * \param[in] size_ Total size of the files in the chat.
   * \param[in] count_ Total number of files in the chat.
   * \param[in] by_file_type_ Statistics split by file types.
   */
  storageStatisticsByChat(std::int64_t chat_id_, std::int64_t size_, std::int32_t count_, std::vector<object_ptr<storageStatisticsByFileType>> &&by_file_type_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 635434531;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<storageStatisticsByChat> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Contains the storage usage statistics for a specific file type.
 */
class storageStatisticsByFileType final : public Object {
 public:
  static jclass Class;
  /// File type.
  object_ptr<FileType> file_type_;
  static jfieldID file_type_fieldID;
  /// Total size of the files.
  std::int64_t size_;
  static jfieldID size_fieldID;
  /// Total number of files.
  std::int32_t count_;
  static jfieldID count_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  storageStatisticsByFileType();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] file_type_ File type.
   * \param[in] size_ Total size of the files.
   * \param[in] count_ Total number of files.
   */
  storageStatisticsByFileType(object_ptr<FileType> &&file_type_, std::int64_t size_, std::int32_t count_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 714012840;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<storageStatisticsByFileType> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Contains approximate storage usage statistics, excluding files of unknown file type.
 */
class storageStatisticsFast final : public Object {
 public:
  static jclass Class;
  /// Approximate total size of files.
  std::int64_t files_size_;
  static jfieldID files_size_fieldID;
  /// Approximate number of files.
  std::int32_t file_count_;
  static jfieldID file_count_fieldID;
  /// Size of the database.
  std::int64_t database_size_;
  static jfieldID database_size_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  storageStatisticsFast();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] files_size_ Approximate total size of files.
   * \param[in] file_count_ Approximate number of files.
   * \param[in] database_size_ Size of the database.
   */
  storageStatisticsFast(std::int64_t files_size_, std::int32_t file_count_, std::int64_t database_size_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -2005401007;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<storageStatisticsFast> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Represents a supergroup or channel with zero or more members (subscribers in the case of channels). From the point of view of the system, a channel is a special kind of a supergroup: only administrators can post and see the list of members, and posts from all administrators use the name and photo of the channel instead of individual names and profile photos. Unlike supergroups, channels can have an unlimited number of subscribers.
 */
class supergroup final : public Object {
 public:
  static jclass Class;
  /// Supergroup or channel identifier.
  std::int32_t id_;
  static jfieldID id_fieldID;
  /// Username of the supergroup or channel; empty for private supergroups or channels.
  std::string username_;
  static jfieldID username_fieldID;
  /// Point in time (Unix timestamp) when the current user joined, or the point in time when the supergroup or channel was created, in case the user is not a member.
  std::int32_t date_;
  static jfieldID date_fieldID;
  /// Status of the current user in the supergroup or channel.
  object_ptr<ChatMemberStatus> status_;
  static jfieldID status_fieldID;
  /// Member count; 0 if unknown. Currently it is guaranteed to be known only if the supergroup or channel was found through SearchPublicChats.
  std::int32_t member_count_;
  static jfieldID member_count_fieldID;
  /// True, if any member of the supergroup can invite other members. This field has no meaning for channels.
  bool anyone_can_invite_;
  static jfieldID anyone_can_invite_fieldID;
  /// True, if messages sent to the channel should contain information about the sender. This field is only applicable to channels.
  bool sign_messages_;
  static jfieldID sign_messages_fieldID;
  /// True, if the supergroup is a channel.
  bool is_channel_;
  static jfieldID is_channel_fieldID;
  /// True, if the supergroup or channel is verified.
  bool is_verified_;
  static jfieldID is_verified_fieldID;
  /// If non-empty, contains the reason why access to this supergroup or channel must be restricted. Format of the string is &quot;{type}: {description}&quot;. {type} Contains the type of the restriction and at least one of the suffixes &quot;-all&quot;, &quot;-ios&quot;, &quot;-android&quot;, or &quot;-wp&quot;, which describe the platforms on which access should be restricted. (For example, &quot;terms-ios-android&quot;. {description} contains a human-readable description of the restriction, which can be shown to the user.)
  std::string restriction_reason_;
  static jfieldID restriction_reason_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  supergroup();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] id_ Supergroup or channel identifier.
   * \param[in] username_ Username of the supergroup or channel; empty for private supergroups or channels.
   * \param[in] date_ Point in time (Unix timestamp) when the current user joined, or the point in time when the supergroup or channel was created, in case the user is not a member.
   * \param[in] status_ Status of the current user in the supergroup or channel.
   * \param[in] member_count_ Member count; 0 if unknown. Currently it is guaranteed to be known only if the supergroup or channel was found through SearchPublicChats.
   * \param[in] anyone_can_invite_ True, if any member of the supergroup can invite other members. This field has no meaning for channels.
   * \param[in] sign_messages_ True, if messages sent to the channel should contain information about the sender. This field is only applicable to channels.
   * \param[in] is_channel_ True, if the supergroup is a channel.
   * \param[in] is_verified_ True, if the supergroup or channel is verified.
   * \param[in] restriction_reason_ If non-empty, contains the reason why access to this supergroup or channel must be restricted. Format of the string is &quot;{type}: {description}&quot;. {type} Contains the type of the restriction and at least one of the suffixes &quot;-all&quot;, &quot;-ios&quot;, &quot;-android&quot;, or &quot;-wp&quot;, which describe the platforms on which access should be restricted. (For example, &quot;terms-ios-android&quot;. {description} contains a human-readable description of the restriction, which can be shown to the user.)
   */
  supergroup(std::int32_t id_, std::string const &username_, std::int32_t date_, object_ptr<ChatMemberStatus> &&status_, std::int32_t member_count_, bool anyone_can_invite_, bool sign_messages_, bool is_channel_, bool is_verified_, std::string const &restriction_reason_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1737513476;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<supergroup> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Contains full information about a supergroup or channel.
 */
class supergroupFullInfo final : public Object {
 public:
  static jclass Class;
  /// Supergroup or channel description.
  std::string description_;
  static jfieldID description_fieldID;
  /// Number of members in the supergroup or channel; 0 if unknown.
  std::int32_t member_count_;
  static jfieldID member_count_fieldID;
  /// Number of privileged users in the supergroup or channel; 0 if unknown.
  std::int32_t administrator_count_;
  static jfieldID administrator_count_fieldID;
  /// Number of restricted users in the supergroup; 0 if unknown.
  std::int32_t restricted_count_;
  static jfieldID restricted_count_fieldID;
  /// Number of users banned from chat; 0 if unknown.
  std::int32_t banned_count_;
  static jfieldID banned_count_fieldID;
  /// True, if members of the chat can be retrieved.
  bool can_get_members_;
  static jfieldID can_get_members_fieldID;
  /// True, if the chat can be made public.
  bool can_set_username_;
  static jfieldID can_set_username_fieldID;
  /// True, if the supergroup sticker set can be changed.
  bool can_set_sticker_set_;
  static jfieldID can_set_sticker_set_fieldID;
  /// True, if new chat members will have access to old messages. In public supergroups and both public and private channels, old messages are always available, so this option affects only private supergroups. The value of this field is only available for chat administrators.
  bool is_all_history_available_;
  static jfieldID is_all_history_available_fieldID;
  /// Identifier of the supergroup sticker set; 0 if none.
  std::int64_t sticker_set_id_;
  static jfieldID sticker_set_id_fieldID;
  /// Invite link for this chat.
  std::string invite_link_;
  static jfieldID invite_link_fieldID;
  /// Identifier of the pinned message in the chat; 0 if none.
  std::int64_t pinned_message_id_;
  static jfieldID pinned_message_id_fieldID;
  /// Identifier of the basic group from which supergroup was upgraded; 0 if none.
  std::int32_t upgraded_from_basic_group_id_;
  static jfieldID upgraded_from_basic_group_id_fieldID;
  /// Identifier of the last message in the basic group from which supergroup was upgraded; 0 if none.
  std::int64_t upgraded_from_max_message_id_;
  static jfieldID upgraded_from_max_message_id_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  supergroupFullInfo();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] description_ Supergroup or channel description.
   * \param[in] member_count_ Number of members in the supergroup or channel; 0 if unknown.
   * \param[in] administrator_count_ Number of privileged users in the supergroup or channel; 0 if unknown.
   * \param[in] restricted_count_ Number of restricted users in the supergroup; 0 if unknown.
   * \param[in] banned_count_ Number of users banned from chat; 0 if unknown.
   * \param[in] can_get_members_ True, if members of the chat can be retrieved.
   * \param[in] can_set_username_ True, if the chat can be made public.
   * \param[in] can_set_sticker_set_ True, if the supergroup sticker set can be changed.
   * \param[in] is_all_history_available_ True, if new chat members will have access to old messages. In public supergroups and both public and private channels, old messages are always available, so this option affects only private supergroups. The value of this field is only available for chat administrators.
   * \param[in] sticker_set_id_ Identifier of the supergroup sticker set; 0 if none.
   * \param[in] invite_link_ Invite link for this chat.
   * \param[in] pinned_message_id_ Identifier of the pinned message in the chat; 0 if none.
   * \param[in] upgraded_from_basic_group_id_ Identifier of the basic group from which supergroup was upgraded; 0 if none.
   * \param[in] upgraded_from_max_message_id_ Identifier of the last message in the basic group from which supergroup was upgraded; 0 if none.
   */
  supergroupFullInfo(std::string const &description_, std::int32_t member_count_, std::int32_t administrator_count_, std::int32_t restricted_count_, std::int32_t banned_count_, bool can_get_members_, bool can_set_username_, bool can_set_sticker_set_, bool is_all_history_available_, std::int64_t sticker_set_id_, std::string const &invite_link_, std::int64_t pinned_message_id_, std::int32_t upgraded_from_basic_group_id_, std::int64_t upgraded_from_max_message_id_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1482349223;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<supergroupFullInfo> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * This class is an abstract base class.
 * Specifies the kind of chat members to return in getSupergroupMembers.
 */
class SupergroupMembersFilter: public Object {
 public:
  static jclass Class;

  static object_ptr<SupergroupMembersFilter> fetch(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Returns recently active users in reverse chronological order.
 */
class supergroupMembersFilterRecent final : public SupergroupMembersFilter {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  supergroupMembersFilterRecent();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1178199509;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<supergroupMembersFilterRecent> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Returns the creator and administrators.
 */
class supergroupMembersFilterAdministrators final : public SupergroupMembersFilter {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  supergroupMembersFilterAdministrators();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -2097380265;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<supergroupMembersFilterAdministrators> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Used to search for supergroup or channel members via a (string) query.
 */
class supergroupMembersFilterSearch final : public SupergroupMembersFilter {
 public:
  static jclass Class;
  /// Query to search for.
  std::string query_;
  static jfieldID query_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  supergroupMembersFilterSearch();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] query_ Query to search for.
   */
  explicit supergroupMembersFilterSearch(std::string const &query_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1696358469;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<supergroupMembersFilterSearch> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Returns restricted supergroup members; can be used only by administrators.
 */
class supergroupMembersFilterRestricted final : public SupergroupMembersFilter {
 public:
  static jclass Class;
  /// Query to search for.
  std::string query_;
  static jfieldID query_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  supergroupMembersFilterRestricted();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] query_ Query to search for.
   */
  explicit supergroupMembersFilterRestricted(std::string const &query_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1107800034;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<supergroupMembersFilterRestricted> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Returns users banned from the supergroup or channel; can be used only by administrators.
 */
class supergroupMembersFilterBanned final : public SupergroupMembersFilter {
 public:
  static jclass Class;
  /// Query to search for.
  std::string query_;
  static jfieldID query_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  supergroupMembersFilterBanned();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] query_ Query to search for.
   */
  explicit supergroupMembersFilterBanned(std::string const &query_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1210621683;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<supergroupMembersFilterBanned> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Returns bot members of the supergroup or channel.
 */
class supergroupMembersFilterBots final : public SupergroupMembersFilter {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  supergroupMembersFilterBots();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 492138918;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<supergroupMembersFilterBots> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Represents a URL linking to an internal Telegram entity.
 */
class tMeUrl final : public Object {
 public:
  static jclass Class;
  /// URL.
  std::string url_;
  static jfieldID url_fieldID;
  /// Type of the URL.
  object_ptr<TMeUrlType> type_;
  static jfieldID type_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  tMeUrl();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] url_ URL.
   * \param[in] type_ Type of the URL.
   */
  tMeUrl(std::string const &url_, object_ptr<TMeUrlType> &&type_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1140786622;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<tMeUrl> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * This class is an abstract base class.
 * Describes the type of a URL linking to an internal Telegram entity.
 */
class TMeUrlType: public Object {
 public:
  static jclass Class;

  static object_ptr<TMeUrlType> fetch(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A URL linking to a user.
 */
class tMeUrlTypeUser final : public TMeUrlType {
 public:
  static jclass Class;
  /// Identifier of the user.
  std::int32_t user_id_;
  static jfieldID user_id_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  tMeUrlTypeUser();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] user_id_ Identifier of the user.
   */
  explicit tMeUrlTypeUser(std::int32_t user_id_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1198700130;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<tMeUrlTypeUser> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A URL linking to a public supergroup or channel.
 */
class tMeUrlTypeSupergroup final : public TMeUrlType {
 public:
  static jclass Class;
  /// Identifier of the supergroup or channel.
  std::int64_t supergroup_id_;
  static jfieldID supergroup_id_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  tMeUrlTypeSupergroup();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] supergroup_id_ Identifier of the supergroup or channel.
   */
  explicit tMeUrlTypeSupergroup(std::int64_t supergroup_id_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1353369944;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<tMeUrlTypeSupergroup> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A chat invite link.
 */
class tMeUrlTypeChatInvite final : public TMeUrlType {
 public:
  static jclass Class;
  /// Chat invite link info.
  object_ptr<chatInviteLinkInfo> info_;
  static jfieldID info_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  tMeUrlTypeChatInvite();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] info_ Chat invite link info.
   */
  explicit tMeUrlTypeChatInvite(object_ptr<chatInviteLinkInfo> &&info_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 313907785;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<tMeUrlTypeChatInvite> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A URL linking to a sticker set.
 */
class tMeUrlTypeStickerSet final : public TMeUrlType {
 public:
  static jclass Class;
  /// Identifier of the sticker set.
  std::int64_t sticker_set_id_;
  static jfieldID sticker_set_id_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  tMeUrlTypeStickerSet();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] sticker_set_id_ Identifier of the sticker set.
   */
  explicit tMeUrlTypeStickerSet(std::int64_t sticker_set_id_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1602473196;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<tMeUrlTypeStickerSet> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Contains a list of t.me URLs.
 */
class tMeUrls final : public Object {
 public:
  static jclass Class;
  /// List of URLs.
  std::vector<object_ptr<tMeUrl>> urls_;
  static jfieldID urls_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  tMeUrls();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] urls_ List of URLs.
   */
  explicit tMeUrls(std::vector<object_ptr<tMeUrl>> &&urls_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1130595098;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<tMeUrls> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Contains parameters for TDLib initialization.
 */
class tdlibParameters final : public Object {
 public:
  static jclass Class;
  /// If set to true, the Telegram test environment will be used instead of the production environment.
  bool use_test_dc_;
  static jfieldID use_test_dc_fieldID;
  /// The path to the directory for the persistent database; if empty, the current working directory will be used.
  std::string database_directory_;
  static jfieldID database_directory_fieldID;
  /// The path to the directory for storing files; if empty, database_directory will be used.
  std::string files_directory_;
  static jfieldID files_directory_fieldID;
  /// If set to true, information about downloaded and uploaded files will be saved between application restarts.
  bool use_file_database_;
  static jfieldID use_file_database_fieldID;
  /// If set to true, the library will maintain a cache of users, basic groups, supergroups, channels and secret chats. Implies use_file_database.
  bool use_chat_info_database_;
  static jfieldID use_chat_info_database_fieldID;
  /// If set to true, the library will maintain a cache of chats and messages. Implies use_chat_info_database.
  bool use_message_database_;
  static jfieldID use_message_database_fieldID;
  /// If set to true, support for secret chats will be enabled.
  bool use_secret_chats_;
  static jfieldID use_secret_chats_fieldID;
  /// Application identifier for Telegram API access, which can be obtained at https://my.telegram.org.
  std::int32_t api_id_;
  static jfieldID api_id_fieldID;
  /// Application identifier hash for Telegram API access, which can be obtained at https://my.telegram.org.
  std::string api_hash_;
  static jfieldID api_hash_fieldID;
  /// IETF language tag of the user's operating system language; must be non-empty.
  std::string system_language_code_;
  static jfieldID system_language_code_fieldID;
  /// Model of the device the application is being run on; must be non-empty.
  std::string device_model_;
  static jfieldID device_model_fieldID;
  /// Version of the operating system the application is being run on; must be non-empty.
  std::string system_version_;
  static jfieldID system_version_fieldID;
  /// Application version; must be non-empty.
  std::string application_version_;
  static jfieldID application_version_fieldID;
  /// If set to true, old files will automatically be deleted.
  bool enable_storage_optimizer_;
  static jfieldID enable_storage_optimizer_fieldID;
  /// If set to true, original file names will be ignored. Otherwise, downloaded files will be saved under names as close as possible to the original name.
  bool ignore_file_names_;
  static jfieldID ignore_file_names_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  tdlibParameters();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] use_test_dc_ If set to true, the Telegram test environment will be used instead of the production environment.
   * \param[in] database_directory_ The path to the directory for the persistent database; if empty, the current working directory will be used.
   * \param[in] files_directory_ The path to the directory for storing files; if empty, database_directory will be used.
   * \param[in] use_file_database_ If set to true, information about downloaded and uploaded files will be saved between application restarts.
   * \param[in] use_chat_info_database_ If set to true, the library will maintain a cache of users, basic groups, supergroups, channels and secret chats. Implies use_file_database.
   * \param[in] use_message_database_ If set to true, the library will maintain a cache of chats and messages. Implies use_chat_info_database.
   * \param[in] use_secret_chats_ If set to true, support for secret chats will be enabled.
   * \param[in] api_id_ Application identifier for Telegram API access, which can be obtained at https://my.telegram.org.
   * \param[in] api_hash_ Application identifier hash for Telegram API access, which can be obtained at https://my.telegram.org.
   * \param[in] system_language_code_ IETF language tag of the user's operating system language; must be non-empty.
   * \param[in] device_model_ Model of the device the application is being run on; must be non-empty.
   * \param[in] system_version_ Version of the operating system the application is being run on; must be non-empty.
   * \param[in] application_version_ Application version; must be non-empty.
   * \param[in] enable_storage_optimizer_ If set to true, old files will automatically be deleted.
   * \param[in] ignore_file_names_ If set to true, original file names will be ignored. Otherwise, downloaded files will be saved under names as close as possible to the original name.
   */
  tdlibParameters(bool use_test_dc_, std::string const &database_directory_, std::string const &files_directory_, bool use_file_database_, bool use_chat_info_database_, bool use_message_database_, bool use_secret_chats_, std::int32_t api_id_, std::string const &api_hash_, std::string const &system_language_code_, std::string const &device_model_, std::string const &system_version_, std::string const &application_version_, bool enable_storage_optimizer_, bool ignore_file_names_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -761520773;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<tdlibParameters> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Returns information about the availability of a temporary password, which can be used for payments.
 */
class temporaryPasswordState final : public Object {
 public:
  static jclass Class;
  /// True, if a temporary password is available.
  bool has_password_;
  static jfieldID has_password_fieldID;
  /// Time left before the temporary password expires, in seconds.
  std::int32_t valid_for_;
  static jfieldID valid_for_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  temporaryPasswordState();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] has_password_ True, if a temporary password is available.
   * \param[in] valid_for_ Time left before the temporary password expires, in seconds.
   */
  temporaryPasswordState(bool has_password_, std::int32_t valid_for_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 939837410;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<temporaryPasswordState> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Contains Telegram terms of service.
 */
class termsOfService final : public Object {
 public:
  static jclass Class;
  /// Text of the terms of service.
  object_ptr<formattedText> text_;
  static jfieldID text_fieldID;
  /// Mininum age of a user to be able to accept the terms; 0 if any.
  std::int32_t min_user_age_;
  static jfieldID min_user_age_fieldID;
  /// True, if a blocking popup with terms of service must be shown to the user.
  bool show_popup_;
  static jfieldID show_popup_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  termsOfService();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] text_ Text of the terms of service.
   * \param[in] min_user_age_ Mininum age of a user to be able to accept the terms; 0 if any.
   * \param[in] show_popup_ True, if a blocking popup with terms of service must be shown to the user.
   */
  termsOfService(object_ptr<formattedText> &&text_, std::int32_t min_user_age_, bool show_popup_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 739422597;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<termsOfService> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A simple object containing a sequence of bytes; for testing only.
 */
class testBytes final : public Object {
 public:
  static jclass Class;
  /// Bytes.
  std::string value_;
  static jfieldID value_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  testBytes();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] value_ Bytes.
   */
  explicit testBytes(std::string const &value_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1541225250;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<testBytes> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A simple object containing a number; for testing only.
 */
class testInt final : public Object {
 public:
  static jclass Class;
  /// Number.
  std::int32_t value_;
  static jfieldID value_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  testInt();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] value_ Number.
   */
  explicit testInt(std::int32_t value_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -574804983;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<testInt> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A simple object containing a string; for testing only.
 */
class testString final : public Object {
 public:
  static jclass Class;
  /// String.
  std::string value_;
  static jfieldID value_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  testString();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] value_ String.
   */
  explicit testString(std::string const &value_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -27891572;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<testString> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A simple object containing a vector of numbers; for testing only.
 */
class testVectorInt final : public Object {
 public:
  static jclass Class;
  /// Vector of numbers.
  std::vector<std::int32_t> value_;
  static jfieldID value_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  testVectorInt();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] value_ Vector of numbers.
   */
  explicit testVectorInt(std::vector<std::int32_t> &&value_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 593682027;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<testVectorInt> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A simple object containing a vector of objects that hold a number; for testing only.
 */
class testVectorIntObject final : public Object {
 public:
  static jclass Class;
  /// Vector of objects.
  std::vector<object_ptr<testInt>> value_;
  static jfieldID value_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  testVectorIntObject();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] value_ Vector of objects.
   */
  explicit testVectorIntObject(std::vector<object_ptr<testInt>> &&value_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 125891546;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<testVectorIntObject> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A simple object containing a vector of strings; for testing only.
 */
class testVectorString final : public Object {
 public:
  static jclass Class;
  /// Vector of strings.
  std::vector<std::string> value_;
  static jfieldID value_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  testVectorString();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] value_ Vector of strings.
   */
  explicit testVectorString(std::vector<std::string> &&value_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 79339995;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<testVectorString> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A simple object containing a vector of objects that hold a string; for testing only.
 */
class testVectorStringObject final : public Object {
 public:
  static jclass Class;
  /// Vector of objects.
  std::vector<object_ptr<testString>> value_;
  static jfieldID value_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  testVectorStringObject();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] value_ Vector of objects.
   */
  explicit testVectorStringObject(std::vector<object_ptr<testString>> &&value_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 80780537;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<testVectorStringObject> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Contains some text.
 */
class text final : public Object {
 public:
  static jclass Class;
  /// Text.
  std::string text_;
  static jfieldID text_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  text();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] text_ Text.
   */
  explicit text(std::string const &text_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 578181272;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<text> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Contains a list of text entities.
 */
class textEntities final : public Object {
 public:
  static jclass Class;
  /// List of text entities.
  std::vector<object_ptr<textEntity>> entities_;
  static jfieldID entities_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  textEntities();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] entities_ List of text entities.
   */
  explicit textEntities(std::vector<object_ptr<textEntity>> &&entities_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -933199172;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<textEntities> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Represents a part of the text that needs to be formatted in some unusual way.
 */
class textEntity final : public Object {
 public:
  static jclass Class;
  /// Offset of the entity in UTF-16 code points.
  std::int32_t offset_;
  static jfieldID offset_fieldID;
  /// Length of the entity, in UTF-16 code points.
  std::int32_t length_;
  static jfieldID length_fieldID;
  /// Type of the entity.
  object_ptr<TextEntityType> type_;
  static jfieldID type_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  textEntity();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] offset_ Offset of the entity in UTF-16 code points.
   * \param[in] length_ Length of the entity, in UTF-16 code points.
   * \param[in] type_ Type of the entity.
   */
  textEntity(std::int32_t offset_, std::int32_t length_, object_ptr<TextEntityType> &&type_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1951688280;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<textEntity> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * This class is an abstract base class.
 * Represents a part of the text which must be formatted differently.
 */
class TextEntityType: public Object {
 public:
  static jclass Class;

  static object_ptr<TextEntityType> fetch(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A mention of a user by their username.
 */
class textEntityTypeMention final : public TextEntityType {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  textEntityTypeMention();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 934535013;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<textEntityTypeMention> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A hashtag text, beginning with &quot;\#&quot;.
 */
class textEntityTypeHashtag final : public TextEntityType {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  textEntityTypeHashtag();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1023958307;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<textEntityTypeHashtag> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A cashtag text, beginning with &quot;$&quot; and consisting of capital english letters (i.e. &quot;$USD&quot;).
 */
class textEntityTypeCashtag final : public TextEntityType {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  textEntityTypeCashtag();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1222915915;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<textEntityTypeCashtag> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A bot command, beginning with &quot;/&quot;. This shouldn't be highlighted if there are no bots in the chat.
 */
class textEntityTypeBotCommand final : public TextEntityType {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  textEntityTypeBotCommand();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1150997581;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<textEntityTypeBotCommand> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * An HTTP URL.
 */
class textEntityTypeUrl final : public TextEntityType {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  textEntityTypeUrl();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1312762756;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<textEntityTypeUrl> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * An email address.
 */
class textEntityTypeEmailAddress final : public TextEntityType {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  textEntityTypeEmailAddress();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1425545249;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<textEntityTypeEmailAddress> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A bold text.
 */
class textEntityTypeBold final : public TextEntityType {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  textEntityTypeBold();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1128210000;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<textEntityTypeBold> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * An italic text.
 */
class textEntityTypeItalic final : public TextEntityType {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  textEntityTypeItalic();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -118253987;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<textEntityTypeItalic> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Text that must be formatted as if inside a code HTML tag.
 */
class textEntityTypeCode final : public TextEntityType {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  textEntityTypeCode();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -974534326;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<textEntityTypeCode> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Text that must be formatted as if inside a pre HTML tag.
 */
class textEntityTypePre final : public TextEntityType {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  textEntityTypePre();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1648958606;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<textEntityTypePre> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Text that must be formatted as if inside pre, and code HTML tags.
 */
class textEntityTypePreCode final : public TextEntityType {
 public:
  static jclass Class;
  /// Programming language of the code; as defined by the sender.
  std::string language_;
  static jfieldID language_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  textEntityTypePreCode();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] language_ Programming language of the code; as defined by the sender.
   */
  explicit textEntityTypePreCode(std::string const &language_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -945325397;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<textEntityTypePreCode> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A text description shown instead of a raw URL.
 */
class textEntityTypeTextUrl final : public TextEntityType {
 public:
  static jclass Class;
  /// HTTP or tg:// URL to be opened when the link is clicked.
  std::string url_;
  static jfieldID url_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  textEntityTypeTextUrl();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] url_ HTTP or tg:// URL to be opened when the link is clicked.
   */
  explicit textEntityTypeTextUrl(std::string const &url_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 445719651;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<textEntityTypeTextUrl> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A text shows instead of a raw mention of the user (e.g., when the user has no username).
 */
class textEntityTypeMentionName final : public TextEntityType {
 public:
  static jclass Class;
  /// Identifier of the mentioned user.
  std::int32_t user_id_;
  static jfieldID user_id_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  textEntityTypeMentionName();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] user_id_ Identifier of the mentioned user.
   */
  explicit textEntityTypeMentionName(std::int32_t user_id_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -791517091;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<textEntityTypeMentionName> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A phone number.
 */
class textEntityTypePhoneNumber final : public TextEntityType {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  textEntityTypePhoneNumber();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1160140246;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<textEntityTypePhoneNumber> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * This class is an abstract base class.
 * Describes the way the text should be parsed for TextEntities.
 */
class TextParseMode: public Object {
 public:
  static jclass Class;

  static object_ptr<TextParseMode> fetch(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * The text should be parsed in markdown-style.
 */
class textParseModeMarkdown final : public TextParseMode {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  textParseModeMarkdown();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 969225580;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<textParseModeMarkdown> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * The text should be parsed in HTML-style.
 */
class textParseModeHTML final : public TextParseMode {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  textParseModeHTML();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1660208627;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<textParseModeHTML> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * This class is an abstract base class.
 * Represents the categories of chats for which a list of frequently used chats can be retrieved.
 */
class TopChatCategory: public Object {
 public:
  static jclass Class;

  static object_ptr<TopChatCategory> fetch(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A category containing frequently used private chats with non-bot users.
 */
class topChatCategoryUsers final : public TopChatCategory {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  topChatCategoryUsers();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1026706816;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<topChatCategoryUsers> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A category containing frequently used private chats with bot users.
 */
class topChatCategoryBots final : public TopChatCategory {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  topChatCategoryBots();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1577129195;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<topChatCategoryBots> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A category containing frequently used basic groups and supergroups.
 */
class topChatCategoryGroups final : public TopChatCategory {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  topChatCategoryGroups();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1530056846;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<topChatCategoryGroups> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A category containing frequently used channels.
 */
class topChatCategoryChannels final : public TopChatCategory {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  topChatCategoryChannels();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -500825885;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<topChatCategoryChannels> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A category containing frequently used chats with inline bots sorted by their usage in inline mode.
 */
class topChatCategoryInlineBots final : public TopChatCategory {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  topChatCategoryInlineBots();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 377023356;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<topChatCategoryInlineBots> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A category containing frequently used chats used for calls.
 */
class topChatCategoryCalls final : public TopChatCategory {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  topChatCategoryCalls();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 356208861;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<topChatCategoryCalls> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * This class is an abstract base class.
 * Contains notifications about data changes.
 */
class Update: public Object {
 public:
  static jclass Class;

  static object_ptr<Update> fetch(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * The user authorization state has changed.
 */
class updateAuthorizationState final : public Update {
 public:
  static jclass Class;
  /// New authorization state.
  object_ptr<AuthorizationState> authorization_state_;
  static jfieldID authorization_state_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  updateAuthorizationState();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] authorization_state_ New authorization state.
   */
  explicit updateAuthorizationState(object_ptr<AuthorizationState> &&authorization_state_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1622347490;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<updateAuthorizationState> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A new message was received; can also be an outgoing message.
 */
class updateNewMessage final : public Update {
 public:
  static jclass Class;
  /// The new message.
  object_ptr<message> message_;
  static jfieldID message_fieldID;
  /// True, if this message must not generate a notification.
  bool disable_notification_;
  static jfieldID disable_notification_fieldID;
  /// True, if the message contains a mention of the current user.
  bool contains_mention_;
  static jfieldID contains_mention_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  updateNewMessage();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] message_ The new message.
   * \param[in] disable_notification_ True, if this message must not generate a notification.
   * \param[in] contains_mention_ True, if the message contains a mention of the current user.
   */
  updateNewMessage(object_ptr<message> &&message_, bool disable_notification_, bool contains_mention_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 238944219;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<updateNewMessage> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A request to send a message has reached the Telegram server. This doesn't mean that the message will be sent successfully or even that the send message request will be processed. This update will be sent only if the option &quot;use_quick_ack&quot; is set to true. This update may be sent multiple times for the same message.
 */
class updateMessageSendAcknowledged final : public Update {
 public:
  static jclass Class;
  /// The chat identifier of the sent message.
  std::int64_t chat_id_;
  static jfieldID chat_id_fieldID;
  /// A temporary message identifier.
  std::int64_t message_id_;
  static jfieldID message_id_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  updateMessageSendAcknowledged();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] chat_id_ The chat identifier of the sent message.
   * \param[in] message_id_ A temporary message identifier.
   */
  updateMessageSendAcknowledged(std::int64_t chat_id_, std::int64_t message_id_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1302843961;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<updateMessageSendAcknowledged> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A message has been successfully sent.
 */
class updateMessageSendSucceeded final : public Update {
 public:
  static jclass Class;
  /// Information about the sent message. Usually only the message identifier, date, and content are changed, but almost all other fields can also change.
  object_ptr<message> message_;
  static jfieldID message_fieldID;
  /// The previous temporary message identifier.
  std::int64_t old_message_id_;
  static jfieldID old_message_id_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  updateMessageSendSucceeded();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] message_ Information about the sent message. Usually only the message identifier, date, and content are changed, but almost all other fields can also change.
   * \param[in] old_message_id_ The previous temporary message identifier.
   */
  updateMessageSendSucceeded(object_ptr<message> &&message_, std::int64_t old_message_id_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1815715197;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<updateMessageSendSucceeded> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A message failed to send. Be aware that some messages being sent can be irrecoverably deleted, in which case updateDeleteMessages will be received instead of this update.
 */
class updateMessageSendFailed final : public Update {
 public:
  static jclass Class;
  /// Contains information about the message that failed to send.
  object_ptr<message> message_;
  static jfieldID message_fieldID;
  /// The previous temporary message identifier.
  std::int64_t old_message_id_;
  static jfieldID old_message_id_fieldID;
  /// An error code.
  std::int32_t error_code_;
  static jfieldID error_code_fieldID;
  /// Error message.
  std::string error_message_;
  static jfieldID error_message_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  updateMessageSendFailed();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] message_ Contains information about the message that failed to send.
   * \param[in] old_message_id_ The previous temporary message identifier.
   * \param[in] error_code_ An error code.
   * \param[in] error_message_ Error message.
   */
  updateMessageSendFailed(object_ptr<message> &&message_, std::int64_t old_message_id_, std::int32_t error_code_, std::string const &error_message_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1032335779;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<updateMessageSendFailed> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * The message content has changed.
 */
class updateMessageContent final : public Update {
 public:
  static jclass Class;
  /// Chat identifier.
  std::int64_t chat_id_;
  static jfieldID chat_id_fieldID;
  /// Message identifier.
  std::int64_t message_id_;
  static jfieldID message_id_fieldID;
  /// New message content.
  object_ptr<MessageContent> new_content_;
  static jfieldID new_content_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  updateMessageContent();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] chat_id_ Chat identifier.
   * \param[in] message_id_ Message identifier.
   * \param[in] new_content_ New message content.
   */
  updateMessageContent(std::int64_t chat_id_, std::int64_t message_id_, object_ptr<MessageContent> &&new_content_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 506903332;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<updateMessageContent> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A message was edited. Changes in the message content will come in a separate updateMessageContent.
 */
class updateMessageEdited final : public Update {
 public:
  static jclass Class;
  /// Chat identifier.
  std::int64_t chat_id_;
  static jfieldID chat_id_fieldID;
  /// Message identifier.
  std::int64_t message_id_;
  static jfieldID message_id_fieldID;
  /// Point in time (Unix timestamp) when the message was edited.
  std::int32_t edit_date_;
  static jfieldID edit_date_fieldID;
  /// New message reply markup; may be null.
  object_ptr<ReplyMarkup> reply_markup_;
  static jfieldID reply_markup_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  updateMessageEdited();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] chat_id_ Chat identifier.
   * \param[in] message_id_ Message identifier.
   * \param[in] edit_date_ Point in time (Unix timestamp) when the message was edited.
   * \param[in] reply_markup_ New message reply markup; may be null.
   */
  updateMessageEdited(std::int64_t chat_id_, std::int64_t message_id_, std::int32_t edit_date_, object_ptr<ReplyMarkup> &&reply_markup_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -559545626;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<updateMessageEdited> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * The view count of the message has changed.
 */
class updateMessageViews final : public Update {
 public:
  static jclass Class;
  /// Chat identifier.
  std::int64_t chat_id_;
  static jfieldID chat_id_fieldID;
  /// Message identifier.
  std::int64_t message_id_;
  static jfieldID message_id_fieldID;
  /// New value of the view count.
  std::int32_t views_;
  static jfieldID views_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  updateMessageViews();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] chat_id_ Chat identifier.
   * \param[in] message_id_ Message identifier.
   * \param[in] views_ New value of the view count.
   */
  updateMessageViews(std::int64_t chat_id_, std::int64_t message_id_, std::int32_t views_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1854131125;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<updateMessageViews> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * The message content was opened. Updates voice note messages to &quot;listened&quot;, video note messages to &quot;viewed&quot; and starts the TTL timer for self-destructing messages.
 */
class updateMessageContentOpened final : public Update {
 public:
  static jclass Class;
  /// Chat identifier.
  std::int64_t chat_id_;
  static jfieldID chat_id_fieldID;
  /// Message identifier.
  std::int64_t message_id_;
  static jfieldID message_id_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  updateMessageContentOpened();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] chat_id_ Chat identifier.
   * \param[in] message_id_ Message identifier.
   */
  updateMessageContentOpened(std::int64_t chat_id_, std::int64_t message_id_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1520523131;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<updateMessageContentOpened> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A message with an unread mention was read.
 */
class updateMessageMentionRead final : public Update {
 public:
  static jclass Class;
  /// Chat identifier.
  std::int64_t chat_id_;
  static jfieldID chat_id_fieldID;
  /// Message identifier.
  std::int64_t message_id_;
  static jfieldID message_id_fieldID;
  /// The new number of unread mention messages left in the chat.
  std::int32_t unread_mention_count_;
  static jfieldID unread_mention_count_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  updateMessageMentionRead();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] chat_id_ Chat identifier.
   * \param[in] message_id_ Message identifier.
   * \param[in] unread_mention_count_ The new number of unread mention messages left in the chat.
   */
  updateMessageMentionRead(std::int64_t chat_id_, std::int64_t message_id_, std::int32_t unread_mention_count_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -252228282;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<updateMessageMentionRead> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A new chat has been loaded/created. This update is guaranteed to come before the chat identifier is returned to the client. The chat field changes will be reported through separate updates.
 */
class updateNewChat final : public Update {
 public:
  static jclass Class;
  /// The chat.
  object_ptr<chat> chat_;
  static jfieldID chat_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  updateNewChat();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] chat_ The chat.
   */
  explicit updateNewChat(object_ptr<chat> &&chat_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 2075757773;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<updateNewChat> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * The title of a chat was changed.
 */
class updateChatTitle final : public Update {
 public:
  static jclass Class;
  /// Chat identifier.
  std::int64_t chat_id_;
  static jfieldID chat_id_fieldID;
  /// The new chat title.
  std::string title_;
  static jfieldID title_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  updateChatTitle();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] chat_id_ Chat identifier.
   * \param[in] title_ The new chat title.
   */
  updateChatTitle(std::int64_t chat_id_, std::string const &title_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -175405660;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<updateChatTitle> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A chat photo was changed.
 */
class updateChatPhoto final : public Update {
 public:
  static jclass Class;
  /// Chat identifier.
  std::int64_t chat_id_;
  static jfieldID chat_id_fieldID;
  /// The new chat photo; may be null.
  object_ptr<chatPhoto> photo_;
  static jfieldID photo_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  updateChatPhoto();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] chat_id_ Chat identifier.
   * \param[in] photo_ The new chat photo; may be null.
   */
  updateChatPhoto(std::int64_t chat_id_, object_ptr<chatPhoto> &&photo_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -209353966;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<updateChatPhoto> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * The last message of a chat was changed. If last_message is null then the last message in the chat became unknown. Some new unknown messages might be added to the chat in this case.
 */
class updateChatLastMessage final : public Update {
 public:
  static jclass Class;
  /// Chat identifier.
  std::int64_t chat_id_;
  static jfieldID chat_id_fieldID;
  /// The new last message in the chat; may be null.
  object_ptr<message> last_message_;
  static jfieldID last_message_fieldID;
  /// New value of the chat order.
  std::int64_t order_;
  static jfieldID order_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  updateChatLastMessage();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] chat_id_ Chat identifier.
   * \param[in] last_message_ The new last message in the chat; may be null.
   * \param[in] order_ New value of the chat order.
   */
  updateChatLastMessage(std::int64_t chat_id_, object_ptr<message> &&last_message_, std::int64_t order_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 580348828;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<updateChatLastMessage> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * The order of the chat in the chats list has changed. Instead of this update updateChatLastMessage, updateChatIsPinned or updateChatDraftMessage might be sent.
 */
class updateChatOrder final : public Update {
 public:
  static jclass Class;
  /// Chat identifier.
  std::int64_t chat_id_;
  static jfieldID chat_id_fieldID;
  /// New value of the order.
  std::int64_t order_;
  static jfieldID order_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  updateChatOrder();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] chat_id_ Chat identifier.
   * \param[in] order_ New value of the order.
   */
  updateChatOrder(std::int64_t chat_id_, std::int64_t order_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1601888026;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<updateChatOrder> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A chat was pinned or unpinned.
 */
class updateChatIsPinned final : public Update {
 public:
  static jclass Class;
  /// Chat identifier.
  std::int64_t chat_id_;
  static jfieldID chat_id_fieldID;
  /// New value of is_pinned.
  bool is_pinned_;
  static jfieldID is_pinned_fieldID;
  /// New value of the chat order.
  std::int64_t order_;
  static jfieldID order_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  updateChatIsPinned();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] chat_id_ Chat identifier.
   * \param[in] is_pinned_ New value of is_pinned.
   * \param[in] order_ New value of the chat order.
   */
  updateChatIsPinned(std::int64_t chat_id_, bool is_pinned_, std::int64_t order_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 488876260;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<updateChatIsPinned> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A chat was marked as unread or was read.
 */
class updateChatIsMarkedAsUnread final : public Update {
 public:
  static jclass Class;
  /// Chat identifier.
  std::int64_t chat_id_;
  static jfieldID chat_id_fieldID;
  /// New value of is_marked_as_unread.
  bool is_marked_as_unread_;
  static jfieldID is_marked_as_unread_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  updateChatIsMarkedAsUnread();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] chat_id_ Chat identifier.
   * \param[in] is_marked_as_unread_ New value of is_marked_as_unread.
   */
  updateChatIsMarkedAsUnread(std::int64_t chat_id_, bool is_marked_as_unread_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1468347188;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<updateChatIsMarkedAsUnread> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A chat's is_sponsored field has changed.
 */
class updateChatIsSponsored final : public Update {
 public:
  static jclass Class;
  /// Chat identifier.
  std::int64_t chat_id_;
  static jfieldID chat_id_fieldID;
  /// New value of is_sponsored.
  bool is_sponsored_;
  static jfieldID is_sponsored_fieldID;
  /// New value of chat order.
  std::int64_t order_;
  static jfieldID order_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  updateChatIsSponsored();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] chat_id_ Chat identifier.
   * \param[in] is_sponsored_ New value of is_sponsored.
   * \param[in] order_ New value of chat order.
   */
  updateChatIsSponsored(std::int64_t chat_id_, bool is_sponsored_, std::int64_t order_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1196180070;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<updateChatIsSponsored> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * The value of the default disable_notification parameter, used when a message is sent to the chat, was changed.
 */
class updateChatDefaultDisableNotification final : public Update {
 public:
  static jclass Class;
  /// Chat identifier.
  std::int64_t chat_id_;
  static jfieldID chat_id_fieldID;
  /// The new default_disable_notification value.
  bool default_disable_notification_;
  static jfieldID default_disable_notification_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  updateChatDefaultDisableNotification();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] chat_id_ Chat identifier.
   * \param[in] default_disable_notification_ The new default_disable_notification value.
   */
  updateChatDefaultDisableNotification(std::int64_t chat_id_, bool default_disable_notification_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 464087707;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<updateChatDefaultDisableNotification> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Incoming messages were read or number of unread messages has been changed.
 */
class updateChatReadInbox final : public Update {
 public:
  static jclass Class;
  /// Chat identifier.
  std::int64_t chat_id_;
  static jfieldID chat_id_fieldID;
  /// Identifier of the last read incoming message.
  std::int64_t last_read_inbox_message_id_;
  static jfieldID last_read_inbox_message_id_fieldID;
  /// The number of unread messages left in the chat.
  std::int32_t unread_count_;
  static jfieldID unread_count_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  updateChatReadInbox();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] chat_id_ Chat identifier.
   * \param[in] last_read_inbox_message_id_ Identifier of the last read incoming message.
   * \param[in] unread_count_ The number of unread messages left in the chat.
   */
  updateChatReadInbox(std::int64_t chat_id_, std::int64_t last_read_inbox_message_id_, std::int32_t unread_count_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -797952281;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<updateChatReadInbox> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Outgoing messages were read.
 */
class updateChatReadOutbox final : public Update {
 public:
  static jclass Class;
  /// Chat identifier.
  std::int64_t chat_id_;
  static jfieldID chat_id_fieldID;
  /// Identifier of last read outgoing message.
  std::int64_t last_read_outbox_message_id_;
  static jfieldID last_read_outbox_message_id_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  updateChatReadOutbox();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] chat_id_ Chat identifier.
   * \param[in] last_read_outbox_message_id_ Identifier of last read outgoing message.
   */
  updateChatReadOutbox(std::int64_t chat_id_, std::int64_t last_read_outbox_message_id_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 708334213;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<updateChatReadOutbox> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * The chat unread_mention_count has changed.
 */
class updateChatUnreadMentionCount final : public Update {
 public:
  static jclass Class;
  /// Chat identifier.
  std::int64_t chat_id_;
  static jfieldID chat_id_fieldID;
  /// The number of unread mention messages left in the chat.
  std::int32_t unread_mention_count_;
  static jfieldID unread_mention_count_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  updateChatUnreadMentionCount();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] chat_id_ Chat identifier.
   * \param[in] unread_mention_count_ The number of unread mention messages left in the chat.
   */
  updateChatUnreadMentionCount(std::int64_t chat_id_, std::int32_t unread_mention_count_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -2131461348;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<updateChatUnreadMentionCount> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Notification settings for a chat were changed.
 */
class updateChatNotificationSettings final : public Update {
 public:
  static jclass Class;
  /// Chat identifier.
  std::int64_t chat_id_;
  static jfieldID chat_id_fieldID;
  /// The new notification settings.
  object_ptr<chatNotificationSettings> notification_settings_;
  static jfieldID notification_settings_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  updateChatNotificationSettings();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] chat_id_ Chat identifier.
   * \param[in] notification_settings_ The new notification settings.
   */
  updateChatNotificationSettings(std::int64_t chat_id_, object_ptr<chatNotificationSettings> &&notification_settings_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -803163050;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<updateChatNotificationSettings> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Notification settings for some type of chats were updated.
 */
class updateScopeNotificationSettings final : public Update {
 public:
  static jclass Class;
  /// Types of chats for which notification settings were updated.
  object_ptr<NotificationSettingsScope> scope_;
  static jfieldID scope_fieldID;
  /// The new notification settings.
  object_ptr<scopeNotificationSettings> notification_settings_;
  static jfieldID notification_settings_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  updateScopeNotificationSettings();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] scope_ Types of chats for which notification settings were updated.
   * \param[in] notification_settings_ The new notification settings.
   */
  updateScopeNotificationSettings(object_ptr<NotificationSettingsScope> &&scope_, object_ptr<scopeNotificationSettings> &&notification_settings_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1203975309;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<updateScopeNotificationSettings> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * The default chat reply markup was changed. Can occur because new messages with reply markup were received or because an old reply markup was hidden by the user.
 */
class updateChatReplyMarkup final : public Update {
 public:
  static jclass Class;
  /// Chat identifier.
  std::int64_t chat_id_;
  static jfieldID chat_id_fieldID;
  /// Identifier of the message from which reply markup needs to be used; 0 if there is no default custom reply markup in the chat.
  std::int64_t reply_markup_message_id_;
  static jfieldID reply_markup_message_id_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  updateChatReplyMarkup();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] chat_id_ Chat identifier.
   * \param[in] reply_markup_message_id_ Identifier of the message from which reply markup needs to be used; 0 if there is no default custom reply markup in the chat.
   */
  updateChatReplyMarkup(std::int64_t chat_id_, std::int64_t reply_markup_message_id_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1309386144;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<updateChatReplyMarkup> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A chat draft has changed. Be aware that the update may come in the currently opened chat but with old content of the draft. If the user has changed the content of the draft, this update shouldn't be applied.
 */
class updateChatDraftMessage final : public Update {
 public:
  static jclass Class;
  /// Chat identifier.
  std::int64_t chat_id_;
  static jfieldID chat_id_fieldID;
  /// The new draft message; may be null.
  object_ptr<draftMessage> draft_message_;
  static jfieldID draft_message_fieldID;
  /// New value of the chat order.
  std::int64_t order_;
  static jfieldID order_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  updateChatDraftMessage();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] chat_id_ Chat identifier.
   * \param[in] draft_message_ The new draft message; may be null.
   * \param[in] order_ New value of the chat order.
   */
  updateChatDraftMessage(std::int64_t chat_id_, object_ptr<draftMessage> &&draft_message_, std::int64_t order_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1436617498;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<updateChatDraftMessage> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Some messages were deleted.
 */
class updateDeleteMessages final : public Update {
 public:
  static jclass Class;
  /// Chat identifier.
  std::int64_t chat_id_;
  static jfieldID chat_id_fieldID;
  /// Identifiers of the deleted messages.
  std::vector<std::int64_t> message_ids_;
  static jfieldID message_ids_fieldID;
  /// True, if the messages are permanently deleted by a user (as opposed to just becoming unaccessible).
  bool is_permanent_;
  static jfieldID is_permanent_fieldID;
  /// True, if the messages are deleted only from the cache and can possibly be retrieved again in the future.
  bool from_cache_;
  static jfieldID from_cache_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  updateDeleteMessages();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] chat_id_ Chat identifier.
   * \param[in] message_ids_ Identifiers of the deleted messages.
   * \param[in] is_permanent_ True, if the messages are permanently deleted by a user (as opposed to just becoming unaccessible).
   * \param[in] from_cache_ True, if the messages are deleted only from the cache and can possibly be retrieved again in the future.
   */
  updateDeleteMessages(std::int64_t chat_id_, std::vector<std::int64_t> &&message_ids_, bool is_permanent_, bool from_cache_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1669252686;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<updateDeleteMessages> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * User activity in the chat has changed.
 */
class updateUserChatAction final : public Update {
 public:
  static jclass Class;
  /// Chat identifier.
  std::int64_t chat_id_;
  static jfieldID chat_id_fieldID;
  /// Identifier of a user performing an action.
  std::int32_t user_id_;
  static jfieldID user_id_fieldID;
  /// The action description.
  object_ptr<ChatAction> action_;
  static jfieldID action_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  updateUserChatAction();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] chat_id_ Chat identifier.
   * \param[in] user_id_ Identifier of a user performing an action.
   * \param[in] action_ The action description.
   */
  updateUserChatAction(std::int64_t chat_id_, std::int32_t user_id_, object_ptr<ChatAction> &&action_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1444133514;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<updateUserChatAction> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * The user went online or offline.
 */
class updateUserStatus final : public Update {
 public:
  static jclass Class;
  /// User identifier.
  std::int32_t user_id_;
  static jfieldID user_id_fieldID;
  /// New status of the user.
  object_ptr<UserStatus> status_;
  static jfieldID status_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  updateUserStatus();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] user_id_ User identifier.
   * \param[in] status_ New status of the user.
   */
  updateUserStatus(std::int32_t user_id_, object_ptr<UserStatus> &&status_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1443545195;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<updateUserStatus> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Some data of a user has changed. This update is guaranteed to come before the user identifier is returned to the client.
 */
class updateUser final : public Update {
 public:
  static jclass Class;
  /// New data about the user.
  object_ptr<user> user_;
  static jfieldID user_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  updateUser();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] user_ New data about the user.
   */
  explicit updateUser(object_ptr<user> &&user_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1183394041;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<updateUser> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Some data of a basic group has changed. This update is guaranteed to come before the basic group identifier is returned to the client.
 */
class updateBasicGroup final : public Update {
 public:
  static jclass Class;
  /// New data about the group.
  object_ptr<basicGroup> basic_group_;
  static jfieldID basic_group_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  updateBasicGroup();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] basic_group_ New data about the group.
   */
  explicit updateBasicGroup(object_ptr<basicGroup> &&basic_group_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1003239581;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<updateBasicGroup> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Some data of a supergroup or a channel has changed. This update is guaranteed to come before the supergroup identifier is returned to the client.
 */
class updateSupergroup final : public Update {
 public:
  static jclass Class;
  /// New data about the supergroup.
  object_ptr<supergroup> supergroup_;
  static jfieldID supergroup_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  updateSupergroup();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] supergroup_ New data about the supergroup.
   */
  explicit updateSupergroup(object_ptr<supergroup> &&supergroup_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -76782300;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<updateSupergroup> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Some data of a secret chat has changed. This update is guaranteed to come before the secret chat identifier is returned to the client.
 */
class updateSecretChat final : public Update {
 public:
  static jclass Class;
  /// New data about the secret chat.
  object_ptr<secretChat> secret_chat_;
  static jfieldID secret_chat_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  updateSecretChat();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] secret_chat_ New data about the secret chat.
   */
  explicit updateSecretChat(object_ptr<secretChat> &&secret_chat_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1666903253;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<updateSecretChat> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Some data from userFullInfo has been changed.
 */
class updateUserFullInfo final : public Update {
 public:
  static jclass Class;
  /// User identifier.
  std::int32_t user_id_;
  static jfieldID user_id_fieldID;
  /// New full information about the user.
  object_ptr<userFullInfo> user_full_info_;
  static jfieldID user_full_info_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  updateUserFullInfo();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] user_id_ User identifier.
   * \param[in] user_full_info_ New full information about the user.
   */
  updateUserFullInfo(std::int32_t user_id_, object_ptr<userFullInfo> &&user_full_info_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 222103874;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<updateUserFullInfo> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Some data from basicGroupFullInfo has been changed.
 */
class updateBasicGroupFullInfo final : public Update {
 public:
  static jclass Class;
  /// Identifier of a basic group.
  std::int32_t basic_group_id_;
  static jfieldID basic_group_id_fieldID;
  /// New full information about the group.
  object_ptr<basicGroupFullInfo> basic_group_full_info_;
  static jfieldID basic_group_full_info_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  updateBasicGroupFullInfo();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] basic_group_id_ Identifier of a basic group.
   * \param[in] basic_group_full_info_ New full information about the group.
   */
  updateBasicGroupFullInfo(std::int32_t basic_group_id_, object_ptr<basicGroupFullInfo> &&basic_group_full_info_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 924030531;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<updateBasicGroupFullInfo> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Some data from supergroupFullInfo has been changed.
 */
class updateSupergroupFullInfo final : public Update {
 public:
  static jclass Class;
  /// Identifier of the supergroup or channel.
  std::int32_t supergroup_id_;
  static jfieldID supergroup_id_fieldID;
  /// New full information about the supergroup.
  object_ptr<supergroupFullInfo> supergroup_full_info_;
  static jfieldID supergroup_full_info_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  updateSupergroupFullInfo();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] supergroup_id_ Identifier of the supergroup or channel.
   * \param[in] supergroup_full_info_ New full information about the supergroup.
   */
  updateSupergroupFullInfo(std::int32_t supergroup_id_, object_ptr<supergroupFullInfo> &&supergroup_full_info_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1288828758;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<updateSupergroupFullInfo> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Service notification from the server. Upon receiving this the client must show a popup with the content of the notification.
 */
class updateServiceNotification final : public Update {
 public:
  static jclass Class;
  /// Notification type. If type begins with &quot;AUTH_KEY_DROP_&quot;, then two buttons &quot;Cancel&quot; and &quot;Log out&quot; should be shown under notification; if user presses the second, all local data should be destroyed using Destroy method.
  std::string type_;
  static jfieldID type_fieldID;
  /// Notification content.
  object_ptr<MessageContent> content_;
  static jfieldID content_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  updateServiceNotification();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] type_ Notification type. If type begins with &quot;AUTH_KEY_DROP_&quot;, then two buttons &quot;Cancel&quot; and &quot;Log out&quot; should be shown under notification; if user presses the second, all local data should be destroyed using Destroy method.
   * \param[in] content_ Notification content.
   */
  updateServiceNotification(std::string const &type_, object_ptr<MessageContent> &&content_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1318622637;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<updateServiceNotification> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Information about a file was updated.
 */
class updateFile final : public Update {
 public:
  static jclass Class;
  /// New data about the file.
  object_ptr<file> file_;
  static jfieldID file_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  updateFile();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] file_ New data about the file.
   */
  explicit updateFile(object_ptr<file> &&file_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 114132831;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<updateFile> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * The file generation process needs to be started by the client.
 */
class updateFileGenerationStart final : public Update {
 public:
  static jclass Class;
  /// Unique identifier for the generation process.
  std::int64_t generation_id_;
  static jfieldID generation_id_fieldID;
  /// The path to a file from which a new file is generated; may be empty.
  std::string original_path_;
  static jfieldID original_path_fieldID;
  /// The path to a file that should be created and where the new file should be generated.
  std::string destination_path_;
  static jfieldID destination_path_fieldID;
  /// String specifying the conversion applied to the original file. If conversion is &quot;\#url\#&quot; than original_path contains an HTTP/HTTPS URL of a file, which should be downloaded by the client.
  std::string conversion_;
  static jfieldID conversion_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  updateFileGenerationStart();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] generation_id_ Unique identifier for the generation process.
   * \param[in] original_path_ The path to a file from which a new file is generated; may be empty.
   * \param[in] destination_path_ The path to a file that should be created and where the new file should be generated.
   * \param[in] conversion_ String specifying the conversion applied to the original file. If conversion is &quot;\#url\#&quot; than original_path contains an HTTP/HTTPS URL of a file, which should be downloaded by the client.
   */
  updateFileGenerationStart(std::int64_t generation_id_, std::string const &original_path_, std::string const &destination_path_, std::string const &conversion_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 216817388;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<updateFileGenerationStart> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * File generation is no longer needed.
 */
class updateFileGenerationStop final : public Update {
 public:
  static jclass Class;
  /// Unique identifier for the generation process.
  std::int64_t generation_id_;
  static jfieldID generation_id_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  updateFileGenerationStop();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] generation_id_ Unique identifier for the generation process.
   */
  explicit updateFileGenerationStop(std::int64_t generation_id_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1894449685;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<updateFileGenerationStop> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * New call was created or information about a call was updated.
 */
class updateCall final : public Update {
 public:
  static jclass Class;
  /// New data about a call.
  object_ptr<call> call_;
  static jfieldID call_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  updateCall();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] call_ New data about a call.
   */
  explicit updateCall(object_ptr<call> &&call_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1337184477;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<updateCall> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Some privacy setting rules have been changed.
 */
class updateUserPrivacySettingRules final : public Update {
 public:
  static jclass Class;
  /// The privacy setting.
  object_ptr<UserPrivacySetting> setting_;
  static jfieldID setting_fieldID;
  /// New privacy rules.
  object_ptr<userPrivacySettingRules> rules_;
  static jfieldID rules_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  updateUserPrivacySettingRules();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] setting_ The privacy setting.
   * \param[in] rules_ New privacy rules.
   */
  updateUserPrivacySettingRules(object_ptr<UserPrivacySetting> &&setting_, object_ptr<userPrivacySettingRules> &&rules_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -912960778;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<updateUserPrivacySettingRules> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Number of unread messages has changed. This update is sent only if a message database is used.
 */
class updateUnreadMessageCount final : public Update {
 public:
  static jclass Class;
  /// Total number of unread messages.
  std::int32_t unread_count_;
  static jfieldID unread_count_fieldID;
  /// Total number of unread messages in unmuted chats.
  std::int32_t unread_unmuted_count_;
  static jfieldID unread_unmuted_count_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  updateUnreadMessageCount();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] unread_count_ Total number of unread messages.
   * \param[in] unread_unmuted_count_ Total number of unread messages in unmuted chats.
   */
  updateUnreadMessageCount(std::int32_t unread_count_, std::int32_t unread_unmuted_count_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -824420376;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<updateUnreadMessageCount> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Number of unread chats, i.e. with unread messages or marked as unread, has changed. This update is sent only if a message database is used.
 */
class updateUnreadChatCount final : public Update {
 public:
  static jclass Class;
  /// Total number of unread chats.
  std::int32_t unread_count_;
  static jfieldID unread_count_fieldID;
  /// Total number of unread unmuted chats.
  std::int32_t unread_unmuted_count_;
  static jfieldID unread_unmuted_count_fieldID;
  /// Total number of chats marked as unread.
  std::int32_t marked_as_unread_count_;
  static jfieldID marked_as_unread_count_fieldID;
  /// Total number of unmuted chats marked as unread.
  std::int32_t marked_as_unread_unmuted_count_;
  static jfieldID marked_as_unread_unmuted_count_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  updateUnreadChatCount();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] unread_count_ Total number of unread chats.
   * \param[in] unread_unmuted_count_ Total number of unread unmuted chats.
   * \param[in] marked_as_unread_count_ Total number of chats marked as unread.
   * \param[in] marked_as_unread_unmuted_count_ Total number of unmuted chats marked as unread.
   */
  updateUnreadChatCount(std::int32_t unread_count_, std::int32_t unread_unmuted_count_, std::int32_t marked_as_unread_count_, std::int32_t marked_as_unread_unmuted_count_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 891150304;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<updateUnreadChatCount> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * An option changed its value.
 */
class updateOption final : public Update {
 public:
  static jclass Class;
  /// The option name.
  std::string name_;
  static jfieldID name_fieldID;
  /// The new option value.
  object_ptr<OptionValue> value_;
  static jfieldID value_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  updateOption();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] name_ The option name.
   * \param[in] value_ The new option value.
   */
  updateOption(std::string const &name_, object_ptr<OptionValue> &&value_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 900822020;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<updateOption> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * The list of installed sticker sets was updated.
 */
class updateInstalledStickerSets final : public Update {
 public:
  static jclass Class;
  /// True, if the list of installed mask sticker sets was updated.
  bool is_masks_;
  static jfieldID is_masks_fieldID;
  /// The new list of installed ordinary sticker sets.
  std::vector<std::int64_t> sticker_set_ids_;
  static jfieldID sticker_set_ids_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  updateInstalledStickerSets();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] is_masks_ True, if the list of installed mask sticker sets was updated.
   * \param[in] sticker_set_ids_ The new list of installed ordinary sticker sets.
   */
  updateInstalledStickerSets(bool is_masks_, std::vector<std::int64_t> &&sticker_set_ids_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1125575977;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<updateInstalledStickerSets> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * The list of trending sticker sets was updated or some of them were viewed.
 */
class updateTrendingStickerSets final : public Update {
 public:
  static jclass Class;
  /// The new list of trending sticker sets.
  object_ptr<stickerSets> sticker_sets_;
  static jfieldID sticker_sets_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  updateTrendingStickerSets();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] sticker_sets_ The new list of trending sticker sets.
   */
  explicit updateTrendingStickerSets(object_ptr<stickerSets> &&sticker_sets_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 450714593;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<updateTrendingStickerSets> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * The list of recently used stickers was updated.
 */
class updateRecentStickers final : public Update {
 public:
  static jclass Class;
  /// True, if the list of stickers attached to photo or video files was updated, otherwise the list of sent stickers is updated.
  bool is_attached_;
  static jfieldID is_attached_fieldID;
  /// The new list of file identifiers of recently used stickers.
  std::vector<std::int32_t> sticker_ids_;
  static jfieldID sticker_ids_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  updateRecentStickers();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] is_attached_ True, if the list of stickers attached to photo or video files was updated, otherwise the list of sent stickers is updated.
   * \param[in] sticker_ids_ The new list of file identifiers of recently used stickers.
   */
  updateRecentStickers(bool is_attached_, std::vector<std::int32_t> &&sticker_ids_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1906403540;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<updateRecentStickers> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * The list of favorite stickers was updated.
 */
class updateFavoriteStickers final : public Update {
 public:
  static jclass Class;
  /// The new list of file identifiers of favorite stickers.
  std::vector<std::int32_t> sticker_ids_;
  static jfieldID sticker_ids_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  updateFavoriteStickers();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] sticker_ids_ The new list of file identifiers of favorite stickers.
   */
  explicit updateFavoriteStickers(std::vector<std::int32_t> &&sticker_ids_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1662240999;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<updateFavoriteStickers> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * The list of saved animations was updated.
 */
class updateSavedAnimations final : public Update {
 public:
  static jclass Class;
  /// The new list of file identifiers of saved animations.
  std::vector<std::int32_t> animation_ids_;
  static jfieldID animation_ids_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  updateSavedAnimations();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] animation_ids_ The new list of file identifiers of saved animations.
   */
  explicit updateSavedAnimations(std::vector<std::int32_t> &&animation_ids_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 65563814;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<updateSavedAnimations> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Some language pack strings have been updated.
 */
class updateLanguagePackStrings final : public Update {
 public:
  static jclass Class;
  /// Localization target to which the language pack belongs.
  std::string localization_target_;
  static jfieldID localization_target_fieldID;
  /// Identifier of the updated language pack.
  std::string language_pack_id_;
  static jfieldID language_pack_id_fieldID;
  /// List of changed language pack strings.
  std::vector<object_ptr<languagePackString>> strings_;
  static jfieldID strings_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  updateLanguagePackStrings();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] localization_target_ Localization target to which the language pack belongs.
   * \param[in] language_pack_id_ Identifier of the updated language pack.
   * \param[in] strings_ List of changed language pack strings.
   */
  updateLanguagePackStrings(std::string const &localization_target_, std::string const &language_pack_id_, std::vector<object_ptr<languagePackString>> &&strings_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1056319886;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<updateLanguagePackStrings> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * The connection state has changed.
 */
class updateConnectionState final : public Update {
 public:
  static jclass Class;
  /// The new connection state.
  object_ptr<ConnectionState> state_;
  static jfieldID state_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  updateConnectionState();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] state_ The new connection state.
   */
  explicit updateConnectionState(object_ptr<ConnectionState> &&state_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1469292078;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<updateConnectionState> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * New terms of service must be accepted by the user. If the terms of service are declined, then the deleteAccount method should be called with the reason &quot;Decline ToS update&quot;.
 */
class updateTermsOfService final : public Update {
 public:
  static jclass Class;
  /// Identifier of the terms of service.
  std::string terms_of_service_id_;
  static jfieldID terms_of_service_id_fieldID;
  /// The new terms of service.
  object_ptr<termsOfService> terms_of_service_;
  static jfieldID terms_of_service_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  updateTermsOfService();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] terms_of_service_id_ Identifier of the terms of service.
   * \param[in] terms_of_service_ The new terms of service.
   */
  updateTermsOfService(std::string const &terms_of_service_id_, object_ptr<termsOfService> &&terms_of_service_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1304640162;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<updateTermsOfService> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A new incoming inline query; for bots only.
 */
class updateNewInlineQuery final : public Update {
 public:
  static jclass Class;
  /// Unique query identifier.
  std::int64_t id_;
  static jfieldID id_fieldID;
  /// Identifier of the user who sent the query.
  std::int32_t sender_user_id_;
  static jfieldID sender_user_id_fieldID;
  /// User location, provided by the client; may be null.
  object_ptr<location> user_location_;
  static jfieldID user_location_fieldID;
  /// Text of the query.
  std::string query_;
  static jfieldID query_fieldID;
  /// Offset of the first entry to return.
  std::string offset_;
  static jfieldID offset_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  updateNewInlineQuery();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] id_ Unique query identifier.
   * \param[in] sender_user_id_ Identifier of the user who sent the query.
   * \param[in] user_location_ User location, provided by the client; may be null.
   * \param[in] query_ Text of the query.
   * \param[in] offset_ Offset of the first entry to return.
   */
  updateNewInlineQuery(std::int64_t id_, std::int32_t sender_user_id_, object_ptr<location> &&user_location_, std::string const &query_, std::string const &offset_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 2064730634;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<updateNewInlineQuery> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * The user has chosen a result of an inline query; for bots only.
 */
class updateNewChosenInlineResult final : public Update {
 public:
  static jclass Class;
  /// Identifier of the user who sent the query.
  std::int32_t sender_user_id_;
  static jfieldID sender_user_id_fieldID;
  /// User location, provided by the client; may be null.
  object_ptr<location> user_location_;
  static jfieldID user_location_fieldID;
  /// Text of the query.
  std::string query_;
  static jfieldID query_fieldID;
  /// Identifier of the chosen result.
  std::string result_id_;
  static jfieldID result_id_fieldID;
  /// Identifier of the sent inline message, if known.
  std::string inline_message_id_;
  static jfieldID inline_message_id_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  updateNewChosenInlineResult();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] sender_user_id_ Identifier of the user who sent the query.
   * \param[in] user_location_ User location, provided by the client; may be null.
   * \param[in] query_ Text of the query.
   * \param[in] result_id_ Identifier of the chosen result.
   * \param[in] inline_message_id_ Identifier of the sent inline message, if known.
   */
  updateNewChosenInlineResult(std::int32_t sender_user_id_, object_ptr<location> &&user_location_, std::string const &query_, std::string const &result_id_, std::string const &inline_message_id_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 527526965;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<updateNewChosenInlineResult> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A new incoming callback query; for bots only.
 */
class updateNewCallbackQuery final : public Update {
 public:
  static jclass Class;
  /// Unique query identifier.
  std::int64_t id_;
  static jfieldID id_fieldID;
  /// Identifier of the user who sent the query.
  std::int32_t sender_user_id_;
  static jfieldID sender_user_id_fieldID;
  /// Identifier of the chat, in which the query was sent.
  std::int64_t chat_id_;
  static jfieldID chat_id_fieldID;
  /// Identifier of the message, from which the query originated.
  std::int64_t message_id_;
  static jfieldID message_id_fieldID;
  /// Identifier that uniquely corresponds to the chat to which the message was sent.
  std::int64_t chat_instance_;
  static jfieldID chat_instance_fieldID;
  /// Query payload.
  object_ptr<CallbackQueryPayload> payload_;
  static jfieldID payload_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  updateNewCallbackQuery();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] id_ Unique query identifier.
   * \param[in] sender_user_id_ Identifier of the user who sent the query.
   * \param[in] chat_id_ Identifier of the chat, in which the query was sent.
   * \param[in] message_id_ Identifier of the message, from which the query originated.
   * \param[in] chat_instance_ Identifier that uniquely corresponds to the chat to which the message was sent.
   * \param[in] payload_ Query payload.
   */
  updateNewCallbackQuery(std::int64_t id_, std::int32_t sender_user_id_, std::int64_t chat_id_, std::int64_t message_id_, std::int64_t chat_instance_, object_ptr<CallbackQueryPayload> &&payload_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -2044226370;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<updateNewCallbackQuery> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A new incoming callback query from a message sent via a bot; for bots only.
 */
class updateNewInlineCallbackQuery final : public Update {
 public:
  static jclass Class;
  /// Unique query identifier.
  std::int64_t id_;
  static jfieldID id_fieldID;
  /// Identifier of the user who sent the query.
  std::int32_t sender_user_id_;
  static jfieldID sender_user_id_fieldID;
  /// Identifier of the inline message, from which the query originated.
  std::string inline_message_id_;
  static jfieldID inline_message_id_fieldID;
  /// An identifier uniquely corresponding to the chat a message was sent to.
  std::int64_t chat_instance_;
  static jfieldID chat_instance_fieldID;
  /// Query payload.
  object_ptr<CallbackQueryPayload> payload_;
  static jfieldID payload_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  updateNewInlineCallbackQuery();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] id_ Unique query identifier.
   * \param[in] sender_user_id_ Identifier of the user who sent the query.
   * \param[in] inline_message_id_ Identifier of the inline message, from which the query originated.
   * \param[in] chat_instance_ An identifier uniquely corresponding to the chat a message was sent to.
   * \param[in] payload_ Query payload.
   */
  updateNewInlineCallbackQuery(std::int64_t id_, std::int32_t sender_user_id_, std::string const &inline_message_id_, std::int64_t chat_instance_, object_ptr<CallbackQueryPayload> &&payload_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1879154829;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<updateNewInlineCallbackQuery> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A new incoming shipping query; for bots only. Only for invoices with flexible price.
 */
class updateNewShippingQuery final : public Update {
 public:
  static jclass Class;
  /// Unique query identifier.
  std::int64_t id_;
  static jfieldID id_fieldID;
  /// Identifier of the user who sent the query.
  std::int32_t sender_user_id_;
  static jfieldID sender_user_id_fieldID;
  /// Invoice payload.
  std::string invoice_payload_;
  static jfieldID invoice_payload_fieldID;
  /// User shipping address.
  object_ptr<address> shipping_address_;
  static jfieldID shipping_address_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  updateNewShippingQuery();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] id_ Unique query identifier.
   * \param[in] sender_user_id_ Identifier of the user who sent the query.
   * \param[in] invoice_payload_ Invoice payload.
   * \param[in] shipping_address_ User shipping address.
   */
  updateNewShippingQuery(std::int64_t id_, std::int32_t sender_user_id_, std::string const &invoice_payload_, object_ptr<address> &&shipping_address_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -817474682;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<updateNewShippingQuery> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A new incoming pre-checkout query; for bots only. Contains full information about a checkout.
 */
class updateNewPreCheckoutQuery final : public Update {
 public:
  static jclass Class;
  /// Unique query identifier.
  std::int64_t id_;
  static jfieldID id_fieldID;
  /// Identifier of the user who sent the query.
  std::int32_t sender_user_id_;
  static jfieldID sender_user_id_fieldID;
  /// Currency for the product price.
  std::string currency_;
  static jfieldID currency_fieldID;
  /// Total price for the product, in the minimal quantity of the currency.
  std::int64_t total_amount_;
  static jfieldID total_amount_fieldID;
  /// Invoice payload.
  std::string invoice_payload_;
  static jfieldID invoice_payload_fieldID;
  /// Identifier of a shipping option chosen by the user; may be empty if not applicable.
  std::string shipping_option_id_;
  static jfieldID shipping_option_id_fieldID;
  /// Information about the order; may be null.
  object_ptr<orderInfo> order_info_;
  static jfieldID order_info_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  updateNewPreCheckoutQuery();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] id_ Unique query identifier.
   * \param[in] sender_user_id_ Identifier of the user who sent the query.
   * \param[in] currency_ Currency for the product price.
   * \param[in] total_amount_ Total price for the product, in the minimal quantity of the currency.
   * \param[in] invoice_payload_ Invoice payload.
   * \param[in] shipping_option_id_ Identifier of a shipping option chosen by the user; may be empty if not applicable.
   * \param[in] order_info_ Information about the order; may be null.
   */
  updateNewPreCheckoutQuery(std::int64_t id_, std::int32_t sender_user_id_, std::string const &currency_, std::int64_t total_amount_, std::string const &invoice_payload_, std::string const &shipping_option_id_, object_ptr<orderInfo> &&order_info_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 87964006;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<updateNewPreCheckoutQuery> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A new incoming event; for bots only.
 */
class updateNewCustomEvent final : public Update {
 public:
  static jclass Class;
  /// A JSON-serialized event.
  std::string event_;
  static jfieldID event_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  updateNewCustomEvent();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] event_ A JSON-serialized event.
   */
  explicit updateNewCustomEvent(std::string const &event_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1994222092;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<updateNewCustomEvent> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A new incoming query; for bots only.
 */
class updateNewCustomQuery final : public Update {
 public:
  static jclass Class;
  /// The query identifier.
  std::int64_t id_;
  static jfieldID id_fieldID;
  /// JSON-serialized query data.
  std::string data_;
  static jfieldID data_fieldID;
  /// Query timeout.
  std::int32_t timeout_;
  static jfieldID timeout_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  updateNewCustomQuery();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] id_ The query identifier.
   * \param[in] data_ JSON-serialized query data.
   * \param[in] timeout_ Query timeout.
   */
  updateNewCustomQuery(std::int64_t id_, std::string const &data_, std::int32_t timeout_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -687670874;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<updateNewCustomQuery> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Represents a user.
 */
class user final : public Object {
 public:
  static jclass Class;
  /// User identifier.
  std::int32_t id_;
  static jfieldID id_fieldID;
  /// First name of the user.
  std::string first_name_;
  static jfieldID first_name_fieldID;
  /// Last name of the user.
  std::string last_name_;
  static jfieldID last_name_fieldID;
  /// Username of the user.
  std::string username_;
  static jfieldID username_fieldID;
  /// Phone number of the user.
  std::string phone_number_;
  static jfieldID phone_number_fieldID;
  /// Current online status of the user.
  object_ptr<UserStatus> status_;
  static jfieldID status_fieldID;
  /// Profile photo of the user; may be null.
  object_ptr<profilePhoto> profile_photo_;
  static jfieldID profile_photo_fieldID;
  /// Relationship from the current user to the other user.
  object_ptr<LinkState> outgoing_link_;
  static jfieldID outgoing_link_fieldID;
  /// Relationship from the other user to the current user.
  object_ptr<LinkState> incoming_link_;
  static jfieldID incoming_link_fieldID;
  /// True, if the user is verified.
  bool is_verified_;
  static jfieldID is_verified_fieldID;
  /// If non-empty, it contains the reason why access to this user must be restricted. The format of the string is &quot;{type}: {description}&quot;. {type} contains the type of the restriction and at least one of the suffixes &quot;-all&quot;, &quot;-ios&quot;, &quot;-android&quot;, or &quot;-wp&quot;, which describe the platforms on which access should be restricted. (For example, &quot;terms-ios-android&quot;. {description} contains a human-readable description of the restriction, which can be shown to the user.)
  std::string restriction_reason_;
  static jfieldID restriction_reason_fieldID;
  /// If false, the user is inaccessible, and the only information known about the user is inside this class. It can't be passed to any method except GetUser.
  bool have_access_;
  static jfieldID have_access_fieldID;
  /// Type of the user.
  object_ptr<UserType> type_;
  static jfieldID type_fieldID;
  /// IETF language tag of the user's language; only available to bots.
  std::string language_code_;
  static jfieldID language_code_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  user();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] id_ User identifier.
   * \param[in] first_name_ First name of the user.
   * \param[in] last_name_ Last name of the user.
   * \param[in] username_ Username of the user.
   * \param[in] phone_number_ Phone number of the user.
   * \param[in] status_ Current online status of the user.
   * \param[in] profile_photo_ Profile photo of the user; may be null.
   * \param[in] outgoing_link_ Relationship from the current user to the other user.
   * \param[in] incoming_link_ Relationship from the other user to the current user.
   * \param[in] is_verified_ True, if the user is verified.
   * \param[in] restriction_reason_ If non-empty, it contains the reason why access to this user must be restricted. The format of the string is &quot;{type}: {description}&quot;. {type} contains the type of the restriction and at least one of the suffixes &quot;-all&quot;, &quot;-ios&quot;, &quot;-android&quot;, or &quot;-wp&quot;, which describe the platforms on which access should be restricted. (For example, &quot;terms-ios-android&quot;. {description} contains a human-readable description of the restriction, which can be shown to the user.)
   * \param[in] have_access_ If false, the user is inaccessible, and the only information known about the user is inside this class. It can't be passed to any method except GetUser.
   * \param[in] type_ Type of the user.
   * \param[in] language_code_ IETF language tag of the user's language; only available to bots.
   */
  user(std::int32_t id_, std::string const &first_name_, std::string const &last_name_, std::string const &username_, std::string const &phone_number_, object_ptr<UserStatus> &&status_, object_ptr<profilePhoto> &&profile_photo_, object_ptr<LinkState> &&outgoing_link_, object_ptr<LinkState> &&incoming_link_, bool is_verified_, std::string const &restriction_reason_, bool have_access_, object_ptr<UserType> &&type_, std::string const &language_code_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -732086407;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<user> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Contains full information about a user (except the full list of profile photos).
 */
class userFullInfo final : public Object {
 public:
  static jclass Class;
  /// True, if the user is blacklisted by the current user.
  bool is_blocked_;
  static jfieldID is_blocked_fieldID;
  /// True, if the user can be called.
  bool can_be_called_;
  static jfieldID can_be_called_fieldID;
  /// True, if the user can't be called due to their privacy settings.
  bool has_private_calls_;
  static jfieldID has_private_calls_fieldID;
  /// A short user bio.
  std::string bio_;
  static jfieldID bio_fieldID;
  /// For bots, the text that is included with the link when users share the bot.
  std::string share_text_;
  static jfieldID share_text_fieldID;
  /// Number of group chats where both the other user and the current user are a member; 0 for the current user.
  std::int32_t group_in_common_count_;
  static jfieldID group_in_common_count_fieldID;
  /// If the user is a bot, information about the bot; may be null.
  object_ptr<botInfo> bot_info_;
  static jfieldID bot_info_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  userFullInfo();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] is_blocked_ True, if the user is blacklisted by the current user.
   * \param[in] can_be_called_ True, if the user can be called.
   * \param[in] has_private_calls_ True, if the user can't be called due to their privacy settings.
   * \param[in] bio_ A short user bio.
   * \param[in] share_text_ For bots, the text that is included with the link when users share the bot.
   * \param[in] group_in_common_count_ Number of group chats where both the other user and the current user are a member; 0 for the current user.
   * \param[in] bot_info_ If the user is a bot, information about the bot; may be null.
   */
  userFullInfo(bool is_blocked_, bool can_be_called_, bool has_private_calls_, std::string const &bio_, std::string const &share_text_, std::int32_t group_in_common_count_, object_ptr<botInfo> &&bot_info_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1076948004;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<userFullInfo> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * This class is an abstract base class.
 * Describes available user privacy settings.
 */
class UserPrivacySetting: public Object {
 public:
  static jclass Class;

  static object_ptr<UserPrivacySetting> fetch(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A privacy setting for managing whether the user's online status is visible.
 */
class userPrivacySettingShowStatus final : public UserPrivacySetting {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  userPrivacySettingShowStatus();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1862829310;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<userPrivacySettingShowStatus> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A privacy setting for managing whether the user can be invited to chats.
 */
class userPrivacySettingAllowChatInvites final : public UserPrivacySetting {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  userPrivacySettingAllowChatInvites();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1271668007;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<userPrivacySettingAllowChatInvites> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A privacy setting for managing whether the user can be called.
 */
class userPrivacySettingAllowCalls final : public UserPrivacySetting {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  userPrivacySettingAllowCalls();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -906967291;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<userPrivacySettingAllowCalls> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * This class is an abstract base class.
 * Represents a single rule for managing privacy settings.
 */
class UserPrivacySettingRule: public Object {
 public:
  static jclass Class;

  static object_ptr<UserPrivacySettingRule> fetch(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A rule to allow all users to do something.
 */
class userPrivacySettingRuleAllowAll final : public UserPrivacySettingRule {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  userPrivacySettingRuleAllowAll();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1967186881;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<userPrivacySettingRuleAllowAll> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A rule to allow all of a user's contacts to do something.
 */
class userPrivacySettingRuleAllowContacts final : public UserPrivacySettingRule {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  userPrivacySettingRuleAllowContacts();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1892733680;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<userPrivacySettingRuleAllowContacts> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A rule to allow certain specified users to do something.
 */
class userPrivacySettingRuleAllowUsers final : public UserPrivacySettingRule {
 public:
  static jclass Class;
  /// The user identifiers.
  std::vector<std::int32_t> user_ids_;
  static jfieldID user_ids_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  userPrivacySettingRuleAllowUsers();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] user_ids_ The user identifiers.
   */
  explicit userPrivacySettingRuleAllowUsers(std::vector<std::int32_t> &&user_ids_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 427601278;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<userPrivacySettingRuleAllowUsers> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A rule to restrict all users from doing something.
 */
class userPrivacySettingRuleRestrictAll final : public UserPrivacySettingRule {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  userPrivacySettingRuleRestrictAll();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1406495408;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<userPrivacySettingRuleRestrictAll> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A rule to restrict all contacts of a user from doing something.
 */
class userPrivacySettingRuleRestrictContacts final : public UserPrivacySettingRule {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  userPrivacySettingRuleRestrictContacts();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1008389378;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<userPrivacySettingRuleRestrictContacts> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A rule to restrict all specified users from doing something.
 */
class userPrivacySettingRuleRestrictUsers final : public UserPrivacySettingRule {
 public:
  static jclass Class;
  /// The user identifiers.
  std::vector<std::int32_t> user_ids_;
  static jfieldID user_ids_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  userPrivacySettingRuleRestrictUsers();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] user_ids_ The user identifiers.
   */
  explicit userPrivacySettingRuleRestrictUsers(std::vector<std::int32_t> &&user_ids_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 2119951802;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<userPrivacySettingRuleRestrictUsers> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A list of privacy rules. Rules are matched in the specified order. The first matched rule defines the privacy setting for a given user. If no rule matches, the action is not allowed.
 */
class userPrivacySettingRules final : public Object {
 public:
  static jclass Class;
  /// A list of rules.
  std::vector<object_ptr<UserPrivacySettingRule>> rules_;
  static jfieldID rules_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  userPrivacySettingRules();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] rules_ A list of rules.
   */
  explicit userPrivacySettingRules(std::vector<object_ptr<UserPrivacySettingRule>> &&rules_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 322477541;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<userPrivacySettingRules> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Contains part of the list of user photos.
 */
class userProfilePhotos final : public Object {
 public:
  static jclass Class;
  /// Total number of user profile photos.
  std::int32_t total_count_;
  static jfieldID total_count_fieldID;
  /// A list of photos.
  std::vector<object_ptr<photo>> photos_;
  static jfieldID photos_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  userProfilePhotos();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] total_count_ Total number of user profile photos.
   * \param[in] photos_ A list of photos.
   */
  userProfilePhotos(std::int32_t total_count_, std::vector<object_ptr<photo>> &&photos_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1388892074;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<userProfilePhotos> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * This class is an abstract base class.
 * Describes the last time the user was online.
 */
class UserStatus: public Object {
 public:
  static jclass Class;

  static object_ptr<UserStatus> fetch(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * The user status was never changed.
 */
class userStatusEmpty final : public UserStatus {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  userStatusEmpty();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 164646985;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<userStatusEmpty> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * The user is online.
 */
class userStatusOnline final : public UserStatus {
 public:
  static jclass Class;
  /// Point in time (Unix timestamp) when the user's online status will expire.
  std::int32_t expires_;
  static jfieldID expires_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  userStatusOnline();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] expires_ Point in time (Unix timestamp) when the user's online status will expire.
   */
  explicit userStatusOnline(std::int32_t expires_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1529460876;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<userStatusOnline> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * The user is offline.
 */
class userStatusOffline final : public UserStatus {
 public:
  static jclass Class;
  /// Point in time (Unix timestamp) when the user was last online.
  std::int32_t was_online_;
  static jfieldID was_online_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  userStatusOffline();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] was_online_ Point in time (Unix timestamp) when the user was last online.
   */
  explicit userStatusOffline(std::int32_t was_online_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -759984891;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<userStatusOffline> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * The user was online recently.
 */
class userStatusRecently final : public UserStatus {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  userStatusRecently();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -496024847;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<userStatusRecently> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * The user is offline, but was online last week.
 */
class userStatusLastWeek final : public UserStatus {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  userStatusLastWeek();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 129960444;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<userStatusLastWeek> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * The user is offline, but was online last month.
 */
class userStatusLastMonth final : public UserStatus {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  userStatusLastMonth();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 2011940674;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<userStatusLastMonth> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * This class is an abstract base class.
 * Represents the type of the user. The following types are possible: regular users, deleted users and bots.
 */
class UserType: public Object {
 public:
  static jclass Class;

  static object_ptr<UserType> fetch(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A regular user.
 */
class userTypeRegular final : public UserType {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  userTypeRegular();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -598644325;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<userTypeRegular> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A deleted user or deleted bot. No information on the user besides the user_id is available. It is not possible to perform any active actions on this type of user.
 */
class userTypeDeleted final : public UserType {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  userTypeDeleted();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1807729372;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<userTypeDeleted> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * A bot (see https://core.telegram.org/bots).
 */
class userTypeBot final : public UserType {
 public:
  static jclass Class;
  /// True, if the bot can be invited to basic group and supergroup chats.
  bool can_join_groups_;
  static jfieldID can_join_groups_fieldID;
  /// True, if the bot can read all messages in basic group or supergroup chats and not just those addressed to the bot. In private and channel chats a bot can always read all messages.
  bool can_read_all_group_messages_;
  static jfieldID can_read_all_group_messages_fieldID;
  /// True, if the bot supports inline queries.
  bool is_inline_;
  static jfieldID is_inline_fieldID;
  /// Placeholder for inline queries (displayed on the client input field).
  std::string inline_query_placeholder_;
  static jfieldID inline_query_placeholder_fieldID;
  /// True, if the location of the user should be sent with every inline query to this bot.
  bool need_location_;
  static jfieldID need_location_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  userTypeBot();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] can_join_groups_ True, if the bot can be invited to basic group and supergroup chats.
   * \param[in] can_read_all_group_messages_ True, if the bot can read all messages in basic group or supergroup chats and not just those addressed to the bot. In private and channel chats a bot can always read all messages.
   * \param[in] is_inline_ True, if the bot supports inline queries.
   * \param[in] inline_query_placeholder_ Placeholder for inline queries (displayed on the client input field).
   * \param[in] need_location_ True, if the location of the user should be sent with every inline query to this bot.
   */
  userTypeBot(bool can_join_groups_, bool can_read_all_group_messages_, bool is_inline_, std::string const &inline_query_placeholder_, bool need_location_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1262387765;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<userTypeBot> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * No information on the user besides the user_id is available, yet this user has not been deleted. This object is extremely rare and must be handled like a deleted user. It is not possible to perform any actions on users of this type.
 */
class userTypeUnknown final : public UserType {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  userTypeUnknown();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -724541123;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<userTypeUnknown> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Represents a list of users.
 */
class users final : public Object {
 public:
  static jclass Class;
  /// Approximate total count of users found.
  std::int32_t total_count_;
  static jfieldID total_count_fieldID;
  /// A list of user identifiers.
  std::vector<std::int32_t> user_ids_;
  static jfieldID user_ids_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  users();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] total_count_ Approximate total count of users found.
   * \param[in] user_ids_ A list of user identifiers.
   */
  users(std::int32_t total_count_, std::vector<std::int32_t> &&user_ids_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 273760088;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<users> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Contains a temporary identifier of validated order information, which is stored for one hour. Also contains the available shipping options.
 */
class validatedOrderInfo final : public Object {
 public:
  static jclass Class;
  /// Temporary identifier of the order information.
  std::string order_info_id_;
  static jfieldID order_info_id_fieldID;
  /// Available shipping options.
  std::vector<object_ptr<shippingOption>> shipping_options_;
  static jfieldID shipping_options_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  validatedOrderInfo();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] order_info_id_ Temporary identifier of the order information.
   * \param[in] shipping_options_ Available shipping options.
   */
  validatedOrderInfo(std::string const &order_info_id_, std::vector<object_ptr<shippingOption>> &&shipping_options_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1511451484;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<validatedOrderInfo> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Describes a venue.
 */
class venue final : public Object {
 public:
  static jclass Class;
  /// Venue location; as defined by the sender.
  object_ptr<location> location_;
  static jfieldID location_fieldID;
  /// Venue name; as defined by the sender.
  std::string title_;
  static jfieldID title_fieldID;
  /// Venue address; as defined by the sender.
  std::string address_;
  static jfieldID address_fieldID;
  /// Provider of the venue database; as defined by the sender. Currently only &quot;foursquare&quot; needs to be supported.
  std::string provider_;
  static jfieldID provider_fieldID;
  /// Identifier of the venue in the provider database; as defined by the sender.
  std::string id_;
  static jfieldID id_fieldID;
  /// Type of the venue in the provider database; as defined by the sender.
  std::string type_;
  static jfieldID type_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  venue();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] location_ Venue location; as defined by the sender.
   * \param[in] title_ Venue name; as defined by the sender.
   * \param[in] address_ Venue address; as defined by the sender.
   * \param[in] provider_ Provider of the venue database; as defined by the sender. Currently only &quot;foursquare&quot; needs to be supported.
   * \param[in] id_ Identifier of the venue in the provider database; as defined by the sender.
   * \param[in] type_ Type of the venue in the provider database; as defined by the sender.
   */
  venue(object_ptr<location> &&location_, std::string const &title_, std::string const &address_, std::string const &provider_, std::string const &id_, std::string const &type_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1070406393;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<venue> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Describes a video file.
 */
class video final : public Object {
 public:
  static jclass Class;
  /// Duration of the video, in seconds; as defined by the sender.
  std::int32_t duration_;
  static jfieldID duration_fieldID;
  /// Video width; as defined by the sender.
  std::int32_t width_;
  static jfieldID width_fieldID;
  /// Video height; as defined by the sender.
  std::int32_t height_;
  static jfieldID height_fieldID;
  /// Original name of the file; as defined by the sender.
  std::string file_name_;
  static jfieldID file_name_fieldID;
  /// MIME type of the file; as defined by the sender.
  std::string mime_type_;
  static jfieldID mime_type_fieldID;
  /// True, if stickers were added to the photo.
  bool has_stickers_;
  static jfieldID has_stickers_fieldID;
  /// True, if the video should be tried to be streamed.
  bool supports_streaming_;
  static jfieldID supports_streaming_fieldID;
  /// Video thumbnail; as defined by the sender; may be null.
  object_ptr<photoSize> thumbnail_;
  static jfieldID thumbnail_fieldID;
  /// File containing the video.
  object_ptr<file> video_;
  static jfieldID video_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  video();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] duration_ Duration of the video, in seconds; as defined by the sender.
   * \param[in] width_ Video width; as defined by the sender.
   * \param[in] height_ Video height; as defined by the sender.
   * \param[in] file_name_ Original name of the file; as defined by the sender.
   * \param[in] mime_type_ MIME type of the file; as defined by the sender.
   * \param[in] has_stickers_ True, if stickers were added to the photo.
   * \param[in] supports_streaming_ True, if the video should be tried to be streamed.
   * \param[in] thumbnail_ Video thumbnail; as defined by the sender; may be null.
   * \param[in] video_ File containing the video.
   */
  video(std::int32_t duration_, std::int32_t width_, std::int32_t height_, std::string const &file_name_, std::string const &mime_type_, bool has_stickers_, bool supports_streaming_, object_ptr<photoSize> &&thumbnail_, object_ptr<file> &&video_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -437410347;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<video> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Describes a video note. The video must be equal in width and height, cropped to a circle, and stored in MPEG4 format.
 */
class videoNote final : public Object {
 public:
  static jclass Class;
  /// Duration of the video, in seconds; as defined by the sender.
  std::int32_t duration_;
  static jfieldID duration_fieldID;
  /// Video width and height; as defined by the sender.
  std::int32_t length_;
  static jfieldID length_fieldID;
  /// Video thumbnail; as defined by the sender; may be null.
  object_ptr<photoSize> thumbnail_;
  static jfieldID thumbnail_fieldID;
  /// File containing the video.
  object_ptr<file> video_;
  static jfieldID video_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  videoNote();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] duration_ Duration of the video, in seconds; as defined by the sender.
   * \param[in] length_ Video width and height; as defined by the sender.
   * \param[in] thumbnail_ Video thumbnail; as defined by the sender; may be null.
   * \param[in] video_ File containing the video.
   */
  videoNote(std::int32_t duration_, std::int32_t length_, object_ptr<photoSize> &&thumbnail_, object_ptr<file> &&video_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1177396120;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<videoNote> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Describes a voice note. The voice note must be encoded with the Opus codec, and stored inside an OGG container. Voice notes can have only a single audio channel.
 */
class voiceNote final : public Object {
 public:
  static jclass Class;
  /// Duration of the voice note, in seconds; as defined by the sender.
  std::int32_t duration_;
  static jfieldID duration_fieldID;
  /// A waveform representation of the voice note in 5-bit format.
  std::string waveform_;
  static jfieldID waveform_fieldID;
  /// MIME type of the file; as defined by the sender.
  std::string mime_type_;
  static jfieldID mime_type_fieldID;
  /// File containing the voice note.
  object_ptr<file> voice_;
  static jfieldID voice_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  voiceNote();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] duration_ Duration of the voice note, in seconds; as defined by the sender.
   * \param[in] waveform_ A waveform representation of the voice note in 5-bit format.
   * \param[in] mime_type_ MIME type of the file; as defined by the sender.
   * \param[in] voice_ File containing the voice note.
   */
  voiceNote(std::int32_t duration_, std::string const &waveform_, std::string const &mime_type_, object_ptr<file> &&voice_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -2066012058;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<voiceNote> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Contains information about a wallpaper.
 */
class wallpaper final : public Object {
 public:
  static jclass Class;
  /// Unique persistent wallpaper identifier.
  std::int32_t id_;
  static jfieldID id_fieldID;
  /// Available variants of the wallpaper in different sizes. These photos can only be downloaded; they can't be sent in a message.
  std::vector<object_ptr<photoSize>> sizes_;
  static jfieldID sizes_fieldID;
  /// Main color of the wallpaper in RGB24 format; should be treated as background color if no photos are specified.
  std::int32_t color_;
  static jfieldID color_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  wallpaper();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] id_ Unique persistent wallpaper identifier.
   * \param[in] sizes_ Available variants of the wallpaper in different sizes. These photos can only be downloaded; they can't be sent in a message.
   * \param[in] color_ Main color of the wallpaper in RGB24 format; should be treated as background color if no photos are specified.
   */
  wallpaper(std::int32_t id_, std::vector<object_ptr<photoSize>> &&sizes_, std::int32_t color_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 282771691;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<wallpaper> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Contains a list of wallpapers.
 */
class wallpapers final : public Object {
 public:
  static jclass Class;
  /// A list of wallpapers.
  std::vector<object_ptr<wallpaper>> wallpapers_;
  static jfieldID wallpapers_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  wallpapers();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] wallpapers_ A list of wallpapers.
   */
  explicit wallpapers(std::vector<object_ptr<wallpaper>> &&wallpapers_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 877926640;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<wallpapers> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Describes a web page preview.
 */
class webPage final : public Object {
 public:
  static jclass Class;
  /// Original URL of the link.
  std::string url_;
  static jfieldID url_fieldID;
  /// URL to display.
  std::string display_url_;
  static jfieldID display_url_fieldID;
  /// Type of the web page. Can be: article, photo, audio, video, document, profile, app, or something else.
  std::string type_;
  static jfieldID type_fieldID;
  /// Short name of the site (e.g., Google Docs, App Store).
  std::string site_name_;
  static jfieldID site_name_fieldID;
  /// Title of the content.
  std::string title_;
  static jfieldID title_fieldID;
  /// Description of the content.
  std::string description_;
  static jfieldID description_fieldID;
  /// Image representing the content; may be null.
  object_ptr<photo> photo_;
  static jfieldID photo_fieldID;
  /// URL to show in the embedded preview.
  std::string embed_url_;
  static jfieldID embed_url_fieldID;
  /// MIME type of the embedded preview, (e.g., text/html or video/mp4).
  std::string embed_type_;
  static jfieldID embed_type_fieldID;
  /// Width of the embedded preview.
  std::int32_t embed_width_;
  static jfieldID embed_width_fieldID;
  /// Height of the embedded preview.
  std::int32_t embed_height_;
  static jfieldID embed_height_fieldID;
  /// Duration of the content, in seconds.
  std::int32_t duration_;
  static jfieldID duration_fieldID;
  /// Author of the content.
  std::string author_;
  static jfieldID author_fieldID;
  /// Preview of the content as an animation, if available; may be null.
  object_ptr<animation> animation_;
  static jfieldID animation_fieldID;
  /// Preview of the content as an audio file, if available; may be null.
  object_ptr<audio> audio_;
  static jfieldID audio_fieldID;
  /// Preview of the content as a document, if available (currently only available for small PDF files and ZIP archives); may be null.
  object_ptr<document> document_;
  static jfieldID document_fieldID;
  /// Preview of the content as a sticker for small WEBP files, if available; may be null.
  object_ptr<sticker> sticker_;
  static jfieldID sticker_fieldID;
  /// Preview of the content as a video, if available; may be null.
  object_ptr<video> video_;
  static jfieldID video_fieldID;
  /// Preview of the content as a video note, if available; may be null.
  object_ptr<videoNote> video_note_;
  static jfieldID video_note_fieldID;
  /// Preview of the content as a voice note, if available; may be null.
  object_ptr<voiceNote> voice_note_;
  static jfieldID voice_note_fieldID;
  /// True, if the web page has an instant view.
  bool has_instant_view_;
  static jfieldID has_instant_view_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  webPage();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] url_ Original URL of the link.
   * \param[in] display_url_ URL to display.
   * \param[in] type_ Type of the web page. Can be: article, photo, audio, video, document, profile, app, or something else.
   * \param[in] site_name_ Short name of the site (e.g., Google Docs, App Store).
   * \param[in] title_ Title of the content.
   * \param[in] description_ Description of the content.
   * \param[in] photo_ Image representing the content; may be null.
   * \param[in] embed_url_ URL to show in the embedded preview.
   * \param[in] embed_type_ MIME type of the embedded preview, (e.g., text/html or video/mp4).
   * \param[in] embed_width_ Width of the embedded preview.
   * \param[in] embed_height_ Height of the embedded preview.
   * \param[in] duration_ Duration of the content, in seconds.
   * \param[in] author_ Author of the content.
   * \param[in] animation_ Preview of the content as an animation, if available; may be null.
   * \param[in] audio_ Preview of the content as an audio file, if available; may be null.
   * \param[in] document_ Preview of the content as a document, if available (currently only available for small PDF files and ZIP archives); may be null.
   * \param[in] sticker_ Preview of the content as a sticker for small WEBP files, if available; may be null.
   * \param[in] video_ Preview of the content as a video, if available; may be null.
   * \param[in] video_note_ Preview of the content as a video note, if available; may be null.
   * \param[in] voice_note_ Preview of the content as a voice note, if available; may be null.
   * \param[in] has_instant_view_ True, if the web page has an instant view.
   */
  webPage(std::string const &url_, std::string const &display_url_, std::string const &type_, std::string const &site_name_, std::string const &title_, std::string const &description_, object_ptr<photo> &&photo_, std::string const &embed_url_, std::string const &embed_type_, std::int32_t embed_width_, std::int32_t embed_height_, std::int32_t duration_, std::string const &author_, object_ptr<animation> &&animation_, object_ptr<audio> &&audio_, object_ptr<document> &&document_, object_ptr<sticker> &&sticker_, object_ptr<video> &&video_, object_ptr<videoNote> &&video_note_, object_ptr<voiceNote> &&voice_note_, bool has_instant_view_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1465949075;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<webPage> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Describes an instant view page for a web page.
 */
class webPageInstantView final : public Object {
 public:
  static jclass Class;
  /// Content of the web page.
  std::vector<object_ptr<PageBlock>> page_blocks_;
  static jfieldID page_blocks_fieldID;
  /// True, if the instant view contains the full page. A network request might be needed to get the full web page instant view.
  bool is_full_;
  static jfieldID is_full_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  webPageInstantView();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] page_blocks_ Content of the web page.
   * \param[in] is_full_ True, if the instant view contains the full page. A network request might be needed to get the full web page instant view.
   */
  webPageInstantView(std::vector<object_ptr<PageBlock>> &&page_blocks_, bool is_full_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1804324850;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  static object_ptr<webPageInstantView> fetch(JNIEnv *env, jobject &p);

  void store(JNIEnv *env, jobject &s) const final;

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Accepts an incoming call.
 *
 * Returns object_ptr<Ok>.
 */
class acceptCall final : public Function {
 public:
  static jclass Class;
  /// Call identifier.
  std::int32_t call_id_;
  static jfieldID call_id_fieldID;
  /// Description of the call protocols supported by the client.
  object_ptr<callProtocol> protocol_;
  static jfieldID protocol_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  acceptCall();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] call_id_ Call identifier.
   * \param[in] protocol_ Description of the call protocols supported by the client.
   */
  acceptCall(std::int32_t call_id_, object_ptr<callProtocol> &&protocol_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -646618416;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<ok>;

  static object_ptr<acceptCall> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Accepts Telegram terms of services.
 *
 * Returns object_ptr<Ok>.
 */
class acceptTermsOfService final : public Function {
 public:
  static jclass Class;
  /// Terms of service identifier.
  std::string terms_of_service_id_;
  static jfieldID terms_of_service_id_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  acceptTermsOfService();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] terms_of_service_id_ Terms of service identifier.
   */
  explicit acceptTermsOfService(std::string const &terms_of_service_id_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 2130576356;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<ok>;

  static object_ptr<acceptTermsOfService> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Adds a new member to a chat. Members can't be added to private or secret chats. Members will not be added until the chat state has been synchronized with the server.
 *
 * Returns object_ptr<Ok>.
 */
class addChatMember final : public Function {
 public:
  static jclass Class;
  /// Chat identifier.
  std::int64_t chat_id_;
  static jfieldID chat_id_fieldID;
  /// Identifier of the user.
  std::int32_t user_id_;
  static jfieldID user_id_fieldID;
  /// The number of earlier messages from the chat to be forwarded to the new member; up to 300. Ignored for supergroups and channels.
  std::int32_t forward_limit_;
  static jfieldID forward_limit_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  addChatMember();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] chat_id_ Chat identifier.
   * \param[in] user_id_ Identifier of the user.
   * \param[in] forward_limit_ The number of earlier messages from the chat to be forwarded to the new member; up to 300. Ignored for supergroups and channels.
   */
  addChatMember(std::int64_t chat_id_, std::int32_t user_id_, std::int32_t forward_limit_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1182817962;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<ok>;

  static object_ptr<addChatMember> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Adds multiple new members to a chat. Currently this option is only available for supergroups and channels. This option can't be used to join a chat. Members can't be added to a channel if it has more than 200 members. Members will not be added until the chat state has been synchronized with the server.
 *
 * Returns object_ptr<Ok>.
 */
class addChatMembers final : public Function {
 public:
  static jclass Class;
  /// Chat identifier.
  std::int64_t chat_id_;
  static jfieldID chat_id_fieldID;
  /// Identifiers of the users to be added to the chat.
  std::vector<std::int32_t> user_ids_;
  static jfieldID user_ids_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  addChatMembers();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] chat_id_ Chat identifier.
   * \param[in] user_ids_ Identifiers of the users to be added to the chat.
   */
  addChatMembers(std::int64_t chat_id_, std::vector<std::int32_t> &&user_ids_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1234094617;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<ok>;

  static object_ptr<addChatMembers> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Adds a new sticker to the list of favorite stickers. The new sticker is added to the top of the list. If the sticker was already in the list, it is removed from the list first. Only stickers belonging to a sticker set can be added to this list.
 *
 * Returns object_ptr<Ok>.
 */
class addFavoriteSticker final : public Function {
 public:
  static jclass Class;
  /// Sticker file to add.
  object_ptr<InputFile> sticker_;
  static jfieldID sticker_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  addFavoriteSticker();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] sticker_ Sticker file to add.
   */
  explicit addFavoriteSticker(object_ptr<InputFile> &&sticker_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 324504799;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<ok>;

  static object_ptr<addFavoriteSticker> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Adds a local message to a chat. The message is persistent across application restarts only if the message database is used. Returns the added message.
 *
 * Returns object_ptr<Message>.
 */
class addLocalMessage final : public Function {
 public:
  static jclass Class;
  /// Target chat.
  std::int64_t chat_id_;
  static jfieldID chat_id_fieldID;
  /// Identifier of the user who will be shown as the sender of the message; may be 0 for channel posts.
  std::int32_t sender_user_id_;
  static jfieldID sender_user_id_fieldID;
  /// Identifier of the message to reply to or 0.
  std::int64_t reply_to_message_id_;
  static jfieldID reply_to_message_id_fieldID;
  /// Pass true to disable notification for the message.
  bool disable_notification_;
  static jfieldID disable_notification_fieldID;
  /// The content of the message to be added.
  object_ptr<InputMessageContent> input_message_content_;
  static jfieldID input_message_content_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  addLocalMessage();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] chat_id_ Target chat.
   * \param[in] sender_user_id_ Identifier of the user who will be shown as the sender of the message; may be 0 for channel posts.
   * \param[in] reply_to_message_id_ Identifier of the message to reply to or 0.
   * \param[in] disable_notification_ Pass true to disable notification for the message.
   * \param[in] input_message_content_ The content of the message to be added.
   */
  addLocalMessage(std::int64_t chat_id_, std::int32_t sender_user_id_, std::int64_t reply_to_message_id_, bool disable_notification_, object_ptr<InputMessageContent> &&input_message_content_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -348943149;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<message>;

  static object_ptr<addLocalMessage> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Adds the specified data to data usage statistics. Can be called before authorization.
 *
 * Returns object_ptr<Ok>.
 */
class addNetworkStatistics final : public Function {
 public:
  static jclass Class;
  /// The network statistics entry with the data to be added to statistics.
  object_ptr<NetworkStatisticsEntry> entry_;
  static jfieldID entry_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  addNetworkStatistics();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] entry_ The network statistics entry with the data to be added to statistics.
   */
  explicit addNetworkStatistics(object_ptr<NetworkStatisticsEntry> &&entry_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1264825305;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<ok>;

  static object_ptr<addNetworkStatistics> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Adds a proxy server for network requests. Can be called before authorization.
 *
 * Returns object_ptr<Proxy>.
 */
class addProxy final : public Function {
 public:
  static jclass Class;
  /// Proxy server IP address.
  std::string server_;
  static jfieldID server_fieldID;
  /// Proxy server port.
  std::int32_t port_;
  static jfieldID port_fieldID;
  /// True, if the proxy should be enabled.
  bool enable_;
  static jfieldID enable_fieldID;
  /// Proxy type.
  object_ptr<ProxyType> type_;
  static jfieldID type_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  addProxy();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] server_ Proxy server IP address.
   * \param[in] port_ Proxy server port.
   * \param[in] enable_ True, if the proxy should be enabled.
   * \param[in] type_ Proxy type.
   */
  addProxy(std::string const &server_, std::int32_t port_, bool enable_, object_ptr<ProxyType> &&type_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 331529432;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<proxy>;

  static object_ptr<addProxy> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Manually adds a new sticker to the list of recently used stickers. The new sticker is added to the top of the list. If the sticker was already in the list, it is removed from the list first. Only stickers belonging to a sticker set can be added to this list.
 *
 * Returns object_ptr<Stickers>.
 */
class addRecentSticker final : public Function {
 public:
  static jclass Class;
  /// Pass true to add the sticker to the list of stickers recently attached to photo or video files; pass false to add the sticker to the list of recently sent stickers.
  bool is_attached_;
  static jfieldID is_attached_fieldID;
  /// Sticker file to add.
  object_ptr<InputFile> sticker_;
  static jfieldID sticker_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  addRecentSticker();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] is_attached_ Pass true to add the sticker to the list of stickers recently attached to photo or video files; pass false to add the sticker to the list of recently sent stickers.
   * \param[in] sticker_ Sticker file to add.
   */
  addRecentSticker(bool is_attached_, object_ptr<InputFile> &&sticker_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1478109026;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<stickers>;

  static object_ptr<addRecentSticker> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Adds a chat to the list of recently found chats. The chat is added to the beginning of the list. If the chat is already in the list, it will be removed from the list first.
 *
 * Returns object_ptr<Ok>.
 */
class addRecentlyFoundChat final : public Function {
 public:
  static jclass Class;
  /// Identifier of the chat to add.
  std::int64_t chat_id_;
  static jfieldID chat_id_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  addRecentlyFoundChat();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] chat_id_ Identifier of the chat to add.
   */
  explicit addRecentlyFoundChat(std::int64_t chat_id_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1746396787;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<ok>;

  static object_ptr<addRecentlyFoundChat> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Manually adds a new animation to the list of saved animations. The new animation is added to the beginning of the list. If the animation was already in the list, it is removed first. Only non-secret video animations with MIME type &quot;video/mp4&quot; can be added to the list.
 *
 * Returns object_ptr<Ok>.
 */
class addSavedAnimation final : public Function {
 public:
  static jclass Class;
  /// The animation file to be added. Only animations known to the server (i.e. successfully sent via a message) can be added to the list.
  object_ptr<InputFile> animation_;
  static jfieldID animation_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  addSavedAnimation();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] animation_ The animation file to be added. Only animations known to the server (i.e. successfully sent via a message) can be added to the list.
   */
  explicit addSavedAnimation(object_ptr<InputFile> &&animation_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1538525088;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<ok>;

  static object_ptr<addSavedAnimation> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Adds a new sticker to a set; for bots only. Returns the sticker set.
 *
 * Returns object_ptr<StickerSet>.
 */
class addStickerToSet final : public Function {
 public:
  static jclass Class;
  /// Sticker set owner.
  std::int32_t user_id_;
  static jfieldID user_id_fieldID;
  /// Sticker set name.
  std::string name_;
  static jfieldID name_fieldID;
  /// Sticker to add to the set.
  object_ptr<inputSticker> sticker_;
  static jfieldID sticker_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  addStickerToSet();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] user_id_ Sticker set owner.
   * \param[in] name_ Sticker set name.
   * \param[in] sticker_ Sticker to add to the set.
   */
  addStickerToSet(std::int32_t user_id_, std::string const &name_, object_ptr<inputSticker> &&sticker_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1422402800;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<stickerSet>;

  static object_ptr<addStickerToSet> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Sets the result of a callback query; for bots only.
 *
 * Returns object_ptr<Ok>.
 */
class answerCallbackQuery final : public Function {
 public:
  static jclass Class;
  /// Identifier of the callback query.
  std::int64_t callback_query_id_;
  static jfieldID callback_query_id_fieldID;
  /// Text of the answer.
  std::string text_;
  static jfieldID text_fieldID;
  /// If true, an alert should be shown to the user instead of a toast notification.
  bool show_alert_;
  static jfieldID show_alert_fieldID;
  /// URL to be opened.
  std::string url_;
  static jfieldID url_fieldID;
  /// Time during which the result of the query can be cached, in seconds.
  std::int32_t cache_time_;
  static jfieldID cache_time_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  answerCallbackQuery();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] callback_query_id_ Identifier of the callback query.
   * \param[in] text_ Text of the answer.
   * \param[in] show_alert_ If true, an alert should be shown to the user instead of a toast notification.
   * \param[in] url_ URL to be opened.
   * \param[in] cache_time_ Time during which the result of the query can be cached, in seconds.
   */
  answerCallbackQuery(std::int64_t callback_query_id_, std::string const &text_, bool show_alert_, std::string const &url_, std::int32_t cache_time_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1153028490;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<ok>;

  static object_ptr<answerCallbackQuery> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Answers a custom query; for bots only.
 *
 * Returns object_ptr<Ok>.
 */
class answerCustomQuery final : public Function {
 public:
  static jclass Class;
  /// Identifier of a custom query.
  std::int64_t custom_query_id_;
  static jfieldID custom_query_id_fieldID;
  /// JSON-serialized answer to the query.
  std::string data_;
  static jfieldID data_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  answerCustomQuery();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] custom_query_id_ Identifier of a custom query.
   * \param[in] data_ JSON-serialized answer to the query.
   */
  answerCustomQuery(std::int64_t custom_query_id_, std::string const &data_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1293603521;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<ok>;

  static object_ptr<answerCustomQuery> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Sets the result of an inline query; for bots only.
 *
 * Returns object_ptr<Ok>.
 */
class answerInlineQuery final : public Function {
 public:
  static jclass Class;
  /// Identifier of the inline query.
  std::int64_t inline_query_id_;
  static jfieldID inline_query_id_fieldID;
  /// True, if the result of the query can be cached for the specified user.
  bool is_personal_;
  static jfieldID is_personal_fieldID;
  /// The results of the query.
  std::vector<object_ptr<InputInlineQueryResult>> results_;
  static jfieldID results_fieldID;
  /// Allowed time to cache the results of the query, in seconds.
  std::int32_t cache_time_;
  static jfieldID cache_time_fieldID;
  /// Offset for the next inline query; pass an empty string if there are no more results.
  std::string next_offset_;
  static jfieldID next_offset_fieldID;
  /// If non-empty, this text should be shown on the button that opens a private chat with the bot and sends a start message to the bot with the parameter switch_pm_parameter.
  std::string switch_pm_text_;
  static jfieldID switch_pm_text_fieldID;
  /// The parameter for the bot start message.
  std::string switch_pm_parameter_;
  static jfieldID switch_pm_parameter_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  answerInlineQuery();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] inline_query_id_ Identifier of the inline query.
   * \param[in] is_personal_ True, if the result of the query can be cached for the specified user.
   * \param[in] results_ The results of the query.
   * \param[in] cache_time_ Allowed time to cache the results of the query, in seconds.
   * \param[in] next_offset_ Offset for the next inline query; pass an empty string if there are no more results.
   * \param[in] switch_pm_text_ If non-empty, this text should be shown on the button that opens a private chat with the bot and sends a start message to the bot with the parameter switch_pm_parameter.
   * \param[in] switch_pm_parameter_ The parameter for the bot start message.
   */
  answerInlineQuery(std::int64_t inline_query_id_, bool is_personal_, std::vector<object_ptr<InputInlineQueryResult>> &&results_, std::int32_t cache_time_, std::string const &next_offset_, std::string const &switch_pm_text_, std::string const &switch_pm_parameter_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 485879477;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<ok>;

  static object_ptr<answerInlineQuery> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Sets the result of a pre-checkout query; for bots only.
 *
 * Returns object_ptr<Ok>.
 */
class answerPreCheckoutQuery final : public Function {
 public:
  static jclass Class;
  /// Identifier of the pre-checkout query.
  std::int64_t pre_checkout_query_id_;
  static jfieldID pre_checkout_query_id_fieldID;
  /// An error message, empty on success.
  std::string error_message_;
  static jfieldID error_message_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  answerPreCheckoutQuery();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] pre_checkout_query_id_ Identifier of the pre-checkout query.
   * \param[in] error_message_ An error message, empty on success.
   */
  answerPreCheckoutQuery(std::int64_t pre_checkout_query_id_, std::string const &error_message_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1486789653;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<ok>;

  static object_ptr<answerPreCheckoutQuery> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Sets the result of a shipping query; for bots only.
 *
 * Returns object_ptr<Ok>.
 */
class answerShippingQuery final : public Function {
 public:
  static jclass Class;
  /// Identifier of the shipping query.
  std::int64_t shipping_query_id_;
  static jfieldID shipping_query_id_fieldID;
  /// Available shipping options.
  std::vector<object_ptr<shippingOption>> shipping_options_;
  static jfieldID shipping_options_fieldID;
  /// An error message, empty on success.
  std::string error_message_;
  static jfieldID error_message_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  answerShippingQuery();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] shipping_query_id_ Identifier of the shipping query.
   * \param[in] shipping_options_ Available shipping options.
   * \param[in] error_message_ An error message, empty on success.
   */
  answerShippingQuery(std::int64_t shipping_query_id_, std::vector<object_ptr<shippingOption>> &&shipping_options_, std::string const &error_message_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -434601324;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<ok>;

  static object_ptr<answerShippingQuery> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Adds a user to the blacklist.
 *
 * Returns object_ptr<Ok>.
 */
class blockUser final : public Function {
 public:
  static jclass Class;
  /// User identifier.
  std::int32_t user_id_;
  static jfieldID user_id_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  blockUser();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] user_id_ User identifier.
   */
  explicit blockUser(std::int32_t user_id_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1239315139;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<ok>;

  static object_ptr<blockUser> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Stops the downloading of a file. If a file has already been downloaded, does nothing.
 *
 * Returns object_ptr<Ok>.
 */
class cancelDownloadFile final : public Function {
 public:
  static jclass Class;
  /// Identifier of a file to stop downloading.
  std::int32_t file_id_;
  static jfieldID file_id_fieldID;
  /// Pass true to stop downloading only if it hasn't been started, i.e. request hasn't been sent to server.
  bool only_if_pending_;
  static jfieldID only_if_pending_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  cancelDownloadFile();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] file_id_ Identifier of a file to stop downloading.
   * \param[in] only_if_pending_ Pass true to stop downloading only if it hasn't been started, i.e. request hasn't been sent to server.
   */
  cancelDownloadFile(std::int32_t file_id_, bool only_if_pending_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1954524450;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<ok>;

  static object_ptr<cancelDownloadFile> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Stops the uploading of a file. Supported only for files uploaded by using uploadFile. For other files the behavior is undefined.
 *
 * Returns object_ptr<Ok>.
 */
class cancelUploadFile final : public Function {
 public:
  static jclass Class;
  /// Identifier of the file to stop uploading.
  std::int32_t file_id_;
  static jfieldID file_id_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  cancelUploadFile();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] file_id_ Identifier of the file to stop uploading.
   */
  explicit cancelUploadFile(std::int32_t file_id_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1623539600;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<ok>;

  static object_ptr<cancelUploadFile> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Used to let the server know whether a chat is spam or not. Can be used only if ChatReportSpamState.can_report_spam is true. After this request, ChatReportSpamState.can_report_spam becomes false forever.
 *
 * Returns object_ptr<Ok>.
 */
class changeChatReportSpamState final : public Function {
 public:
  static jclass Class;
  /// Chat identifier.
  std::int64_t chat_id_;
  static jfieldID chat_id_fieldID;
  /// If true, the chat will be reported as spam; otherwise it will be marked as not spam.
  bool is_spam_chat_;
  static jfieldID is_spam_chat_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  changeChatReportSpamState();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] chat_id_ Chat identifier.
   * \param[in] is_spam_chat_ If true, the chat will be reported as spam; otherwise it will be marked as not spam.
   */
  changeChatReportSpamState(std::int64_t chat_id_, bool is_spam_chat_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1768597097;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<ok>;

  static object_ptr<changeChatReportSpamState> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Changes imported contacts using the list of current user contacts saved on the device. Imports newly added contacts and, if at least the file database is enabled, deletes recently deleted contacts. Query result depends on the result of the previous query, so only one query is possible at the same time.
 *
 * Returns object_ptr<ImportedContacts>.
 */
class changeImportedContacts final : public Function {
 public:
  static jclass Class;
  /// The new list of contacts, contact's vCard are ignored and are not imported.
  std::vector<object_ptr<contact>> contacts_;
  static jfieldID contacts_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  changeImportedContacts();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] contacts_ The new list of contacts, contact's vCard are ignored and are not imported.
   */
  explicit changeImportedContacts(std::vector<object_ptr<contact>> &&contacts_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1968207955;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<importedContacts>;

  static object_ptr<changeImportedContacts> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Changes the phone number of the user and sends an authentication code to the user's new phone number. On success, returns information about the sent code.
 *
 * Returns object_ptr<AuthenticationCodeInfo>.
 */
class changePhoneNumber final : public Function {
 public:
  static jclass Class;
  /// The new phone number of the user in international format.
  std::string phone_number_;
  static jfieldID phone_number_fieldID;
  /// Pass true if the code can be sent via flash call to the specified phone number.
  bool allow_flash_call_;
  static jfieldID allow_flash_call_fieldID;
  /// Pass true if the phone number is used on the current device. Ignored if allow_flash_call is false.
  bool is_current_phone_number_;
  static jfieldID is_current_phone_number_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  changePhoneNumber();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] phone_number_ The new phone number of the user in international format.
   * \param[in] allow_flash_call_ Pass true if the code can be sent via flash call to the specified phone number.
   * \param[in] is_current_phone_number_ Pass true if the phone number is used on the current device. Ignored if allow_flash_call is false.
   */
  changePhoneNumber(std::string const &phone_number_, bool allow_flash_call_, bool is_current_phone_number_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1510625218;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<authenticationCodeInfo>;

  static object_ptr<changePhoneNumber> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Installs/uninstalls or activates/archives a sticker set.
 *
 * Returns object_ptr<Ok>.
 */
class changeStickerSet final : public Function {
 public:
  static jclass Class;
  /// Identifier of the sticker set.
  std::int64_t set_id_;
  static jfieldID set_id_fieldID;
  /// The new value of is_installed.
  bool is_installed_;
  static jfieldID is_installed_fieldID;
  /// The new value of is_archived. A sticker set can't be installed and archived simultaneously.
  bool is_archived_;
  static jfieldID is_archived_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  changeStickerSet();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] set_id_ Identifier of the sticker set.
   * \param[in] is_installed_ The new value of is_installed.
   * \param[in] is_archived_ The new value of is_archived. A sticker set can't be installed and archived simultaneously.
   */
  changeStickerSet(std::int64_t set_id_, bool is_installed_, bool is_archived_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 449357293;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<ok>;

  static object_ptr<changeStickerSet> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Checks the authentication token of a bot; to log in as a bot. Works only when the current authorization state is authorizationStateWaitPhoneNumber. Can be used instead of setAuthenticationPhoneNumber and checkAuthenticationCode to log in.
 *
 * Returns object_ptr<Ok>.
 */
class checkAuthenticationBotToken final : public Function {
 public:
  static jclass Class;
  /// The bot token.
  std::string token_;
  static jfieldID token_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  checkAuthenticationBotToken();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] token_ The bot token.
   */
  explicit checkAuthenticationBotToken(std::string const &token_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 639321206;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<ok>;

  static object_ptr<checkAuthenticationBotToken> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Checks the authentication code. Works only when the current authorization state is authorizationStateWaitCode.
 *
 * Returns object_ptr<Ok>.
 */
class checkAuthenticationCode final : public Function {
 public:
  static jclass Class;
  /// The verification code received via SMS, Telegram message, phone call, or flash call.
  std::string code_;
  static jfieldID code_fieldID;
  /// If the user is not yet registered, the first name of the user; 1-255 characters.
  std::string first_name_;
  static jfieldID first_name_fieldID;
  /// If the user is not yet registered; the last name of the user; optional; 0-255 characters.
  std::string last_name_;
  static jfieldID last_name_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  checkAuthenticationCode();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] code_ The verification code received via SMS, Telegram message, phone call, or flash call.
   * \param[in] first_name_ If the user is not yet registered, the first name of the user; 1-255 characters.
   * \param[in] last_name_ If the user is not yet registered; the last name of the user; optional; 0-255 characters.
   */
  checkAuthenticationCode(std::string const &code_, std::string const &first_name_, std::string const &last_name_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -707293555;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<ok>;

  static object_ptr<checkAuthenticationCode> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Checks the authentication password for correctness. Works only when the current authorization state is authorizationStateWaitPassword.
 *
 * Returns object_ptr<Ok>.
 */
class checkAuthenticationPassword final : public Function {
 public:
  static jclass Class;
  /// The password to check.
  std::string password_;
  static jfieldID password_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  checkAuthenticationPassword();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] password_ The password to check.
   */
  explicit checkAuthenticationPassword(std::string const &password_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -2025698400;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<ok>;

  static object_ptr<checkAuthenticationPassword> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Checks the authentication code sent to confirm a new phone number of the user.
 *
 * Returns object_ptr<Ok>.
 */
class checkChangePhoneNumberCode final : public Function {
 public:
  static jclass Class;
  /// Verification code received by SMS, phone call or flash call.
  std::string code_;
  static jfieldID code_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  checkChangePhoneNumberCode();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] code_ Verification code received by SMS, phone call or flash call.
   */
  explicit checkChangePhoneNumberCode(std::string const &code_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1720278429;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<ok>;

  static object_ptr<checkChangePhoneNumberCode> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Checks the validity of an invite link for a chat and returns information about the corresponding chat.
 *
 * Returns object_ptr<ChatInviteLinkInfo>.
 */
class checkChatInviteLink final : public Function {
 public:
  static jclass Class;
  /// Invite link to be checked; should begin with &quot;https://t.me/joinchat/&quot;, &quot;https://telegram.me/joinchat/&quot;, or &quot;https://telegram.dog/joinchat/&quot;.
  std::string invite_link_;
  static jfieldID invite_link_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  checkChatInviteLink();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] invite_link_ Invite link to be checked; should begin with &quot;https://t.me/joinchat/&quot;, &quot;https://telegram.me/joinchat/&quot;, or &quot;https://telegram.dog/joinchat/&quot;.
   */
  explicit checkChatInviteLink(std::string const &invite_link_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -496940997;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<chatInviteLinkInfo>;

  static object_ptr<checkChatInviteLink> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Checks whether a username can be set for a chat.
 *
 * Returns object_ptr<CheckChatUsernameResult>.
 */
class checkChatUsername final : public Function {
 public:
  static jclass Class;
  /// Chat identifier; should be identifier of a supergroup chat, or a channel chat, or a private chat with self, or zero if chat is being created.
  std::int64_t chat_id_;
  static jfieldID chat_id_fieldID;
  /// Username to be checked.
  std::string username_;
  static jfieldID username_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  checkChatUsername();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] chat_id_ Chat identifier; should be identifier of a supergroup chat, or a channel chat, or a private chat with self, or zero if chat is being created.
   * \param[in] username_ Username to be checked.
   */
  checkChatUsername(std::int64_t chat_id_, std::string const &username_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -2003506154;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<CheckChatUsernameResult>;

  static object_ptr<checkChatUsername> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Checks the database encryption key for correctness. Works only when the current authorization state is authorizationStateWaitEncryptionKey.
 *
 * Returns object_ptr<Ok>.
 */
class checkDatabaseEncryptionKey final : public Function {
 public:
  static jclass Class;
  /// Encryption key to check or set up.
  std::string encryption_key_;
  static jfieldID encryption_key_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  checkDatabaseEncryptionKey();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] encryption_key_ Encryption key to check or set up.
   */
  explicit checkDatabaseEncryptionKey(std::string const &encryption_key_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1018769307;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<ok>;

  static object_ptr<checkDatabaseEncryptionKey> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Checks the email address verification code for Telegram Passport.
 *
 * Returns object_ptr<Ok>.
 */
class checkEmailAddressVerificationCode final : public Function {
 public:
  static jclass Class;
  /// Verification code.
  std::string code_;
  static jfieldID code_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  checkEmailAddressVerificationCode();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] code_ Verification code.
   */
  explicit checkEmailAddressVerificationCode(std::string const &code_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -426386685;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<ok>;

  static object_ptr<checkEmailAddressVerificationCode> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Checks phone number confirmation code.
 *
 * Returns object_ptr<Ok>.
 */
class checkPhoneNumberConfirmationCode final : public Function {
 public:
  static jclass Class;
  /// The phone number confirmation code.
  std::string code_;
  static jfieldID code_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  checkPhoneNumberConfirmationCode();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] code_ The phone number confirmation code.
   */
  explicit checkPhoneNumberConfirmationCode(std::string const &code_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1348060966;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<ok>;

  static object_ptr<checkPhoneNumberConfirmationCode> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Checks the phone number verification code for Telegram Passport.
 *
 * Returns object_ptr<Ok>.
 */
class checkPhoneNumberVerificationCode final : public Function {
 public:
  static jclass Class;
  /// Verification code.
  std::string code_;
  static jfieldID code_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  checkPhoneNumberVerificationCode();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] code_ Verification code.
   */
  explicit checkPhoneNumberVerificationCode(std::string const &code_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1497462718;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<ok>;

  static object_ptr<checkPhoneNumberVerificationCode> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Removes potentially dangerous characters from the name of a file. The encoding of the file name is supposed to be UTF-8. Returns an empty string on failure. This is an offline method. Can be called before authorization. Can be called synchronously.
 *
 * Returns object_ptr<Text>.
 */
class cleanFileName final : public Function {
 public:
  static jclass Class;
  /// File name or path to the file.
  std::string file_name_;
  static jfieldID file_name_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  cleanFileName();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] file_name_ File name or path to the file.
   */
  explicit cleanFileName(std::string const &file_name_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 967964667;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<text>;

  static object_ptr<cleanFileName> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Clears draft messages in all chats.
 *
 * Returns object_ptr<Ok>.
 */
class clearAllDraftMessages final : public Function {
 public:
  static jclass Class;
  /// If true, local draft messages in secret chats will not be cleared.
  bool exclude_secret_chats_;
  static jfieldID exclude_secret_chats_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  clearAllDraftMessages();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] exclude_secret_chats_ If true, local draft messages in secret chats will not be cleared.
   */
  explicit clearAllDraftMessages(bool exclude_secret_chats_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -46369573;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<ok>;

  static object_ptr<clearAllDraftMessages> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Clears all imported contacts, contacts list remains unchanged.
 *
 * Returns object_ptr<Ok>.
 */
class clearImportedContacts final : public Function {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  clearImportedContacts();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 869503298;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<ok>;

  static object_ptr<clearImportedContacts> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Clears the list of recently used stickers.
 *
 * Returns object_ptr<Ok>.
 */
class clearRecentStickers final : public Function {
 public:
  static jclass Class;
  /// Pass true to clear the list of stickers recently attached to photo or video files; pass false to clear the list of recently sent stickers.
  bool is_attached_;
  static jfieldID is_attached_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  clearRecentStickers();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] is_attached_ Pass true to clear the list of stickers recently attached to photo or video files; pass false to clear the list of recently sent stickers.
   */
  explicit clearRecentStickers(bool is_attached_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -321242684;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<ok>;

  static object_ptr<clearRecentStickers> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Clears the list of recently found chats.
 *
 * Returns object_ptr<Ok>.
 */
class clearRecentlyFoundChats final : public Function {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  clearRecentlyFoundChats();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -285582542;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<ok>;

  static object_ptr<clearRecentlyFoundChats> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Closes the TDLib instance. All databases will be flushed to disk and properly closed. After the close completes, updateAuthorizationState with authorizationStateClosed will be sent.
 *
 * Returns object_ptr<Ok>.
 */
class close final : public Function {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  close();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1187782273;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<ok>;

  static object_ptr<close> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * This method should be called if the chat is closed by the user. Many useful activities depend on the chat being opened or closed.
 *
 * Returns object_ptr<Ok>.
 */
class closeChat final : public Function {
 public:
  static jclass Class;
  /// Chat identifier.
  std::int64_t chat_id_;
  static jfieldID chat_id_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  closeChat();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] chat_id_ Chat identifier.
   */
  explicit closeChat(std::int64_t chat_id_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 39749353;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<ok>;

  static object_ptr<closeChat> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Closes a secret chat, effectively transfering its state to secretChatStateClosed.
 *
 * Returns object_ptr<Ok>.
 */
class closeSecretChat final : public Function {
 public:
  static jclass Class;
  /// Secret chat identifier.
  std::int32_t secret_chat_id_;
  static jfieldID secret_chat_id_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  closeSecretChat();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] secret_chat_id_ Secret chat identifier.
   */
  explicit closeSecretChat(std::int32_t secret_chat_id_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -471006133;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<ok>;

  static object_ptr<closeSecretChat> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Returns an existing chat corresponding to a known basic group.
 *
 * Returns object_ptr<Chat>.
 */
class createBasicGroupChat final : public Function {
 public:
  static jclass Class;
  /// Basic group identifier.
  std::int32_t basic_group_id_;
  static jfieldID basic_group_id_fieldID;
  /// If true, the chat will be created without network request. In this case all information about the chat except its type, title and photo can be incorrect.
  bool force_;
  static jfieldID force_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  createBasicGroupChat();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] basic_group_id_ Basic group identifier.
   * \param[in] force_ If true, the chat will be created without network request. In this case all information about the chat except its type, title and photo can be incorrect.
   */
  createBasicGroupChat(std::int32_t basic_group_id_, bool force_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 642492777;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<chat>;

  static object_ptr<createBasicGroupChat> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Creates a new call.
 *
 * Returns object_ptr<CallId>.
 */
class createCall final : public Function {
 public:
  static jclass Class;
  /// Identifier of the user to be called.
  std::int32_t user_id_;
  static jfieldID user_id_fieldID;
  /// Description of the call protocols supported by the client.
  object_ptr<callProtocol> protocol_;
  static jfieldID protocol_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  createCall();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] user_id_ Identifier of the user to be called.
   * \param[in] protocol_ Description of the call protocols supported by the client.
   */
  createCall(std::int32_t user_id_, object_ptr<callProtocol> &&protocol_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1742408159;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<callId>;

  static object_ptr<createCall> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Creates a new basic group and sends a corresponding messageBasicGroupChatCreate. Returns the newly created chat.
 *
 * Returns object_ptr<Chat>.
 */
class createNewBasicGroupChat final : public Function {
 public:
  static jclass Class;
  /// Identifiers of users to be added to the basic group.
  std::vector<std::int32_t> user_ids_;
  static jfieldID user_ids_fieldID;
  /// Title of the new basic group; 1-255 characters.
  std::string title_;
  static jfieldID title_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  createNewBasicGroupChat();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] user_ids_ Identifiers of users to be added to the basic group.
   * \param[in] title_ Title of the new basic group; 1-255 characters.
   */
  createNewBasicGroupChat(std::vector<std::int32_t> &&user_ids_, std::string const &title_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1874532069;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<chat>;

  static object_ptr<createNewBasicGroupChat> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Creates a new secret chat. Returns the newly created chat.
 *
 * Returns object_ptr<Chat>.
 */
class createNewSecretChat final : public Function {
 public:
  static jclass Class;
  /// Identifier of the target user.
  std::int32_t user_id_;
  static jfieldID user_id_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  createNewSecretChat();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] user_id_ Identifier of the target user.
   */
  explicit createNewSecretChat(std::int32_t user_id_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1689344881;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<chat>;

  static object_ptr<createNewSecretChat> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Creates a new sticker set; for bots only. Returns the newly created sticker set.
 *
 * Returns object_ptr<StickerSet>.
 */
class createNewStickerSet final : public Function {
 public:
  static jclass Class;
  /// Sticker set owner.
  std::int32_t user_id_;
  static jfieldID user_id_fieldID;
  /// Sticker set title; 1-64 characters.
  std::string title_;
  static jfieldID title_fieldID;
  /// Sticker set name. Can contain only English letters, digits and underscores. Must end with *&quot;_by_&lt;bot username&gt;&quot;* (*&lt;bot_username&gt;* is case insensitive); 1-64 characters.
  std::string name_;
  static jfieldID name_fieldID;
  /// True, if stickers are masks.
  bool is_masks_;
  static jfieldID is_masks_fieldID;
  /// List of stickers to be added to the set.
  std::vector<object_ptr<inputSticker>> stickers_;
  static jfieldID stickers_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  createNewStickerSet();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] user_id_ Sticker set owner.
   * \param[in] title_ Sticker set title; 1-64 characters.
   * \param[in] name_ Sticker set name. Can contain only English letters, digits and underscores. Must end with *&quot;_by_&lt;bot username&gt;&quot;* (*&lt;bot_username&gt;* is case insensitive); 1-64 characters.
   * \param[in] is_masks_ True, if stickers are masks.
   * \param[in] stickers_ List of stickers to be added to the set.
   */
  createNewStickerSet(std::int32_t user_id_, std::string const &title_, std::string const &name_, bool is_masks_, std::vector<object_ptr<inputSticker>> &&stickers_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 205093058;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<stickerSet>;

  static object_ptr<createNewStickerSet> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Creates a new supergroup or channel and sends a corresponding messageSupergroupChatCreate. Returns the newly created chat.
 *
 * Returns object_ptr<Chat>.
 */
class createNewSupergroupChat final : public Function {
 public:
  static jclass Class;
  /// Title of the new chat; 1-255 characters.
  std::string title_;
  static jfieldID title_fieldID;
  /// True, if a channel chat should be created.
  bool is_channel_;
  static jfieldID is_channel_fieldID;
  /// Chat description; 0-255 characters.
  std::string description_;
  static jfieldID description_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  createNewSupergroupChat();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] title_ Title of the new chat; 1-255 characters.
   * \param[in] is_channel_ True, if a channel chat should be created.
   * \param[in] description_ Chat description; 0-255 characters.
   */
  createNewSupergroupChat(std::string const &title_, bool is_channel_, std::string const &description_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1284982268;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<chat>;

  static object_ptr<createNewSupergroupChat> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Returns an existing chat corresponding to a given user.
 *
 * Returns object_ptr<Chat>.
 */
class createPrivateChat final : public Function {
 public:
  static jclass Class;
  /// User identifier.
  std::int32_t user_id_;
  static jfieldID user_id_fieldID;
  /// If true, the chat will be created without network request. In this case all information about the chat except its type, title and photo can be incorrect.
  bool force_;
  static jfieldID force_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  createPrivateChat();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] user_id_ User identifier.
   * \param[in] force_ If true, the chat will be created without network request. In this case all information about the chat except its type, title and photo can be incorrect.
   */
  createPrivateChat(std::int32_t user_id_, bool force_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1807530364;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<chat>;

  static object_ptr<createPrivateChat> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Returns an existing chat corresponding to a known secret chat.
 *
 * Returns object_ptr<Chat>.
 */
class createSecretChat final : public Function {
 public:
  static jclass Class;
  /// Secret chat identifier.
  std::int32_t secret_chat_id_;
  static jfieldID secret_chat_id_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  createSecretChat();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] secret_chat_id_ Secret chat identifier.
   */
  explicit createSecretChat(std::int32_t secret_chat_id_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1930285615;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<chat>;

  static object_ptr<createSecretChat> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Returns an existing chat corresponding to a known supergroup or channel.
 *
 * Returns object_ptr<Chat>.
 */
class createSupergroupChat final : public Function {
 public:
  static jclass Class;
  /// Supergroup or channel identifier.
  std::int32_t supergroup_id_;
  static jfieldID supergroup_id_fieldID;
  /// If true, the chat will be created without network request. In this case all information about the chat except its type, title and photo can be incorrect.
  bool force_;
  static jfieldID force_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  createSupergroupChat();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] supergroup_id_ Supergroup or channel identifier.
   * \param[in] force_ If true, the chat will be created without network request. In this case all information about the chat except its type, title and photo can be incorrect.
   */
  createSupergroupChat(std::int32_t supergroup_id_, bool force_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 352742758;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<chat>;

  static object_ptr<createSupergroupChat> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Creates a new temporary password for processing payments.
 *
 * Returns object_ptr<TemporaryPasswordState>.
 */
class createTemporaryPassword final : public Function {
 public:
  static jclass Class;
  /// Persistent user password.
  std::string password_;
  static jfieldID password_fieldID;
  /// Time during which the temporary password will be valid, in seconds; should be between 60 and 86400.
  std::int32_t valid_for_;
  static jfieldID valid_for_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  createTemporaryPassword();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] password_ Persistent user password.
   * \param[in] valid_for_ Time during which the temporary password will be valid, in seconds; should be between 60 and 86400.
   */
  createTemporaryPassword(std::string const &password_, std::int32_t valid_for_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1626509434;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<temporaryPasswordState>;

  static object_ptr<createTemporaryPassword> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Deletes the account of the current user, deleting all information associated with the user from the server. The phone number of the account can be used to create a new account. Can be called before authorization when the current authorization state is authorizationStateWaitPassword.
 *
 * Returns object_ptr<Ok>.
 */
class deleteAccount final : public Function {
 public:
  static jclass Class;
  /// The reason why the account was deleted; optional.
  std::string reason_;
  static jfieldID reason_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  deleteAccount();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] reason_ The reason why the account was deleted; optional.
   */
  explicit deleteAccount(std::string const &reason_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1203056508;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<ok>;

  static object_ptr<deleteAccount> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Deletes all messages in the chat only for the user. Cannot be used in channels and public supergroups.
 *
 * Returns object_ptr<Ok>.
 */
class deleteChatHistory final : public Function {
 public:
  static jclass Class;
  /// Chat identifier.
  std::int64_t chat_id_;
  static jfieldID chat_id_fieldID;
  /// Pass true if the chat should be removed from the chats list.
  bool remove_from_chat_list_;
  static jfieldID remove_from_chat_list_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  deleteChatHistory();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] chat_id_ Chat identifier.
   * \param[in] remove_from_chat_list_ Pass true if the chat should be removed from the chats list.
   */
  deleteChatHistory(std::int64_t chat_id_, bool remove_from_chat_list_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1384632722;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<ok>;

  static object_ptr<deleteChatHistory> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Deletes all messages sent by the specified user to a chat. Supported only in supergroups; requires can_delete_messages administrator privileges.
 *
 * Returns object_ptr<Ok>.
 */
class deleteChatMessagesFromUser final : public Function {
 public:
  static jclass Class;
  /// Chat identifier.
  std::int64_t chat_id_;
  static jfieldID chat_id_fieldID;
  /// User identifier.
  std::int32_t user_id_;
  static jfieldID user_id_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  deleteChatMessagesFromUser();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] chat_id_ Chat identifier.
   * \param[in] user_id_ User identifier.
   */
  deleteChatMessagesFromUser(std::int64_t chat_id_, std::int32_t user_id_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1599689199;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<ok>;

  static object_ptr<deleteChatMessagesFromUser> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Deletes the default reply markup from a chat. Must be called after a one-time keyboard or a ForceReply reply markup has been used. UpdateChatReplyMarkup will be sent if the reply markup will be changed.
 *
 * Returns object_ptr<Ok>.
 */
class deleteChatReplyMarkup final : public Function {
 public:
  static jclass Class;
  /// Chat identifier.
  std::int64_t chat_id_;
  static jfieldID chat_id_fieldID;
  /// The message identifier of the used keyboard.
  std::int64_t message_id_;
  static jfieldID message_id_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  deleteChatReplyMarkup();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] chat_id_ Chat identifier.
   * \param[in] message_id_ The message identifier of the used keyboard.
   */
  deleteChatReplyMarkup(std::int64_t chat_id_, std::int64_t message_id_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 100637531;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<ok>;

  static object_ptr<deleteChatReplyMarkup> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Deletes a file from the TDLib file cache.
 *
 * Returns object_ptr<Ok>.
 */
class deleteFile final : public Function {
 public:
  static jclass Class;
  /// Identifier of the file to delete.
  std::int32_t file_id_;
  static jfieldID file_id_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  deleteFile();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] file_id_ Identifier of the file to delete.
   */
  explicit deleteFile(std::int32_t file_id_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1807653676;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<ok>;

  static object_ptr<deleteFile> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Deletes all information about a language pack in the current localization target. The language pack that is currently in use can't be deleted.
 *
 * Returns object_ptr<Ok>.
 */
class deleteLanguagePack final : public Function {
 public:
  static jclass Class;
  /// Identifier of the language pack to delete.
  std::string language_pack_id_;
  static jfieldID language_pack_id_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  deleteLanguagePack();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] language_pack_id_ Identifier of the language pack to delete.
   */
  explicit deleteLanguagePack(std::string const &language_pack_id_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -2108761026;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<ok>;

  static object_ptr<deleteLanguagePack> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Deletes messages.
 *
 * Returns object_ptr<Ok>.
 */
class deleteMessages final : public Function {
 public:
  static jclass Class;
  /// Chat identifier.
  std::int64_t chat_id_;
  static jfieldID chat_id_fieldID;
  /// Identifiers of the messages to be deleted.
  std::vector<std::int64_t> message_ids_;
  static jfieldID message_ids_fieldID;
  /// Pass true to try to delete outgoing messages for all chat members (may fail if messages are too old). Always true for supergroups, channels and secret chats.
  bool revoke_;
  static jfieldID revoke_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  deleteMessages();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] chat_id_ Chat identifier.
   * \param[in] message_ids_ Identifiers of the messages to be deleted.
   * \param[in] revoke_ Pass true to try to delete outgoing messages for all chat members (may fail if messages are too old). Always true for supergroups, channels and secret chats.
   */
  deleteMessages(std::int64_t chat_id_, std::vector<std::int64_t> &&message_ids_, bool revoke_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1130090173;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<ok>;

  static object_ptr<deleteMessages> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Deletes a Telegram Passport element.
 *
 * Returns object_ptr<Ok>.
 */
class deletePassportElement final : public Function {
 public:
  static jclass Class;
  /// Element type.
  object_ptr<PassportElementType> type_;
  static jfieldID type_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  deletePassportElement();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] type_ Element type.
   */
  explicit deletePassportElement(object_ptr<PassportElementType> &&type_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1719555468;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<ok>;

  static object_ptr<deletePassportElement> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Deletes a profile photo. If something changes, updateUser will be sent.
 *
 * Returns object_ptr<Ok>.
 */
class deleteProfilePhoto final : public Function {
 public:
  static jclass Class;
  /// Identifier of the profile photo to delete.
  std::int64_t profile_photo_id_;
  static jfieldID profile_photo_id_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  deleteProfilePhoto();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] profile_photo_id_ Identifier of the profile photo to delete.
   */
  explicit deleteProfilePhoto(std::int64_t profile_photo_id_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1319794625;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<ok>;

  static object_ptr<deleteProfilePhoto> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Deletes saved credentials for all payment provider bots.
 *
 * Returns object_ptr<Ok>.
 */
class deleteSavedCredentials final : public Function {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  deleteSavedCredentials();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 826300114;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<ok>;

  static object_ptr<deleteSavedCredentials> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Deletes saved order info.
 *
 * Returns object_ptr<Ok>.
 */
class deleteSavedOrderInfo final : public Function {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  deleteSavedOrderInfo();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1629058164;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<ok>;

  static object_ptr<deleteSavedOrderInfo> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Deletes a supergroup or channel along with all messages in the corresponding chat. This will release the supergroup or channel username and remove all members; requires creator privileges in the supergroup or channel. Chats with more than 1000 members can't be deleted using this method.
 *
 * Returns object_ptr<Ok>.
 */
class deleteSupergroup final : public Function {
 public:
  static jclass Class;
  /// Identifier of the supergroup or channel.
  std::int32_t supergroup_id_;
  static jfieldID supergroup_id_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  deleteSupergroup();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] supergroup_id_ Identifier of the supergroup or channel.
   */
  explicit deleteSupergroup(std::int32_t supergroup_id_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1999855965;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<ok>;

  static object_ptr<deleteSupergroup> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Closes the TDLib instance, destroying all local data without a proper logout. The current user session will remain in the list of all active sessions. All local data will be destroyed. After the destruction completes updateAuthorizationState with authorizationStateClosed will be sent.
 *
 * Returns object_ptr<Ok>.
 */
class destroy final : public Function {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  destroy();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 685331274;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<ok>;

  static object_ptr<destroy> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Disables the currently enabled proxy. Can be called before authorization.
 *
 * Returns object_ptr<Ok>.
 */
class disableProxy final : public Function {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  disableProxy();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -2100095102;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<ok>;

  static object_ptr<disableProxy> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Discards a call.
 *
 * Returns object_ptr<Ok>.
 */
class discardCall final : public Function {
 public:
  static jclass Class;
  /// Call identifier.
  std::int32_t call_id_;
  static jfieldID call_id_fieldID;
  /// True, if the user was disconnected.
  bool is_disconnected_;
  static jfieldID is_disconnected_fieldID;
  /// The call duration, in seconds.
  std::int32_t duration_;
  static jfieldID duration_fieldID;
  /// Identifier of the connection used during the call.
  std::int64_t connection_id_;
  static jfieldID connection_id_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  discardCall();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] call_id_ Call identifier.
   * \param[in] is_disconnected_ True, if the user was disconnected.
   * \param[in] duration_ The call duration, in seconds.
   * \param[in] connection_id_ Identifier of the connection used during the call.
   */
  discardCall(std::int32_t call_id_, bool is_disconnected_, std::int32_t duration_, std::int64_t connection_id_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -923187372;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<ok>;

  static object_ptr<discardCall> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Disconnects all websites from the current user's Telegram account.
 *
 * Returns object_ptr<Ok>.
 */
class disconnectAllWebsites final : public Function {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  disconnectAllWebsites();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1082985981;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<ok>;

  static object_ptr<disconnectAllWebsites> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Disconnects website from the current user's Telegram account.
 *
 * Returns object_ptr<Ok>.
 */
class disconnectWebsite final : public Function {
 public:
  static jclass Class;
  /// Website identifier.
  std::int64_t website_id_;
  static jfieldID website_id_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  disconnectWebsite();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] website_id_ Website identifier.
   */
  explicit disconnectWebsite(std::int64_t website_id_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -778767395;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<ok>;

  static object_ptr<disconnectWebsite> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Asynchronously downloads a file from the cloud. updateFile will be used to notify about the download progress and successful completion of the download. Returns file state just after the download has been started.
 *
 * Returns object_ptr<File>.
 */
class downloadFile final : public Function {
 public:
  static jclass Class;
  /// Identifier of the file to download.
  std::int32_t file_id_;
  static jfieldID file_id_fieldID;
  /// Priority of the download (1-32). The higher the priority, the earlier the file will be downloaded. If the priorities of two files are equal, then the last one for which downloadFile was called will be downloaded first.
  std::int32_t priority_;
  static jfieldID priority_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  downloadFile();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] file_id_ Identifier of the file to download.
   * \param[in] priority_ Priority of the download (1-32). The higher the priority, the earlier the file will be downloaded. If the priorities of two files are equal, then the last one for which downloadFile was called will be downloaded first.
   */
  downloadFile(std::int32_t file_id_, std::int32_t priority_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1531851978;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<file>;

  static object_ptr<downloadFile> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Edits information about a custom language pack in the current localization target.
 *
 * Returns object_ptr<Ok>.
 */
class editCustomLanguagePackInfo final : public Function {
 public:
  static jclass Class;
  /// New information about the custom language pack.
  object_ptr<languagePackInfo> info_;
  static jfieldID info_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  editCustomLanguagePackInfo();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] info_ New information about the custom language pack.
   */
  explicit editCustomLanguagePackInfo(object_ptr<languagePackInfo> &&info_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1320751257;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<ok>;

  static object_ptr<editCustomLanguagePackInfo> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Edits the caption of an inline message sent via a bot; for bots only.
 *
 * Returns object_ptr<Ok>.
 */
class editInlineMessageCaption final : public Function {
 public:
  static jclass Class;
  /// Inline message identifier.
  std::string inline_message_id_;
  static jfieldID inline_message_id_fieldID;
  /// The new message reply markup.
  object_ptr<ReplyMarkup> reply_markup_;
  static jfieldID reply_markup_fieldID;
  /// New message content caption; 0-GetOption(&quot;message_caption_length_max&quot;) characters.
  object_ptr<formattedText> caption_;
  static jfieldID caption_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  editInlineMessageCaption();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] inline_message_id_ Inline message identifier.
   * \param[in] reply_markup_ The new message reply markup.
   * \param[in] caption_ New message content caption; 0-GetOption(&quot;message_caption_length_max&quot;) characters.
   */
  editInlineMessageCaption(std::string const &inline_message_id_, object_ptr<ReplyMarkup> &&reply_markup_, object_ptr<formattedText> &&caption_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -760985929;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<ok>;

  static object_ptr<editInlineMessageCaption> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Edits the content of a live location in an inline message sent via a bot; for bots only.
 *
 * Returns object_ptr<Ok>.
 */
class editInlineMessageLiveLocation final : public Function {
 public:
  static jclass Class;
  /// Inline message identifier.
  std::string inline_message_id_;
  static jfieldID inline_message_id_fieldID;
  /// The new message reply markup.
  object_ptr<ReplyMarkup> reply_markup_;
  static jfieldID reply_markup_fieldID;
  /// New location content of the message; may be null. Pass null to stop sharing the live location.
  object_ptr<location> location_;
  static jfieldID location_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  editInlineMessageLiveLocation();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] inline_message_id_ Inline message identifier.
   * \param[in] reply_markup_ The new message reply markup.
   * \param[in] location_ New location content of the message; may be null. Pass null to stop sharing the live location.
   */
  editInlineMessageLiveLocation(std::string const &inline_message_id_, object_ptr<ReplyMarkup> &&reply_markup_, object_ptr<location> &&location_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 655046316;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<ok>;

  static object_ptr<editInlineMessageLiveLocation> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Edits the content of a message with an animation, an audio, a document, a photo or a video in an inline message sent via a bot; for bots only.
 *
 * Returns object_ptr<Ok>.
 */
class editInlineMessageMedia final : public Function {
 public:
  static jclass Class;
  /// Inline message identifier.
  std::string inline_message_id_;
  static jfieldID inline_message_id_fieldID;
  /// The new message reply markup; for bots only.
  object_ptr<ReplyMarkup> reply_markup_;
  static jfieldID reply_markup_fieldID;
  /// New content of the message. Must be one of the following types: InputMessageAnimation, InputMessageAudio, InputMessageDocument, InputMessagePhoto or InputMessageVideo.
  object_ptr<InputMessageContent> input_message_content_;
  static jfieldID input_message_content_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  editInlineMessageMedia();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] inline_message_id_ Inline message identifier.
   * \param[in] reply_markup_ The new message reply markup; for bots only.
   * \param[in] input_message_content_ New content of the message. Must be one of the following types: InputMessageAnimation, InputMessageAudio, InputMessageDocument, InputMessagePhoto or InputMessageVideo.
   */
  editInlineMessageMedia(std::string const &inline_message_id_, object_ptr<ReplyMarkup> &&reply_markup_, object_ptr<InputMessageContent> &&input_message_content_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 23553921;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<ok>;

  static object_ptr<editInlineMessageMedia> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Edits the reply markup of an inline message sent via a bot; for bots only.
 *
 * Returns object_ptr<Ok>.
 */
class editInlineMessageReplyMarkup final : public Function {
 public:
  static jclass Class;
  /// Inline message identifier.
  std::string inline_message_id_;
  static jfieldID inline_message_id_fieldID;
  /// The new message reply markup.
  object_ptr<ReplyMarkup> reply_markup_;
  static jfieldID reply_markup_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  editInlineMessageReplyMarkup();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] inline_message_id_ Inline message identifier.
   * \param[in] reply_markup_ The new message reply markup.
   */
  editInlineMessageReplyMarkup(std::string const &inline_message_id_, object_ptr<ReplyMarkup> &&reply_markup_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -67565858;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<ok>;

  static object_ptr<editInlineMessageReplyMarkup> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Edits the text of an inline text or game message sent via a bot; for bots only.
 *
 * Returns object_ptr<Ok>.
 */
class editInlineMessageText final : public Function {
 public:
  static jclass Class;
  /// Inline message identifier.
  std::string inline_message_id_;
  static jfieldID inline_message_id_fieldID;
  /// The new message reply markup.
  object_ptr<ReplyMarkup> reply_markup_;
  static jfieldID reply_markup_fieldID;
  /// New text content of the message. Should be of type InputMessageText.
  object_ptr<InputMessageContent> input_message_content_;
  static jfieldID input_message_content_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  editInlineMessageText();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] inline_message_id_ Inline message identifier.
   * \param[in] reply_markup_ The new message reply markup.
   * \param[in] input_message_content_ New text content of the message. Should be of type InputMessageText.
   */
  editInlineMessageText(std::string const &inline_message_id_, object_ptr<ReplyMarkup> &&reply_markup_, object_ptr<InputMessageContent> &&input_message_content_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -855457307;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<ok>;

  static object_ptr<editInlineMessageText> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Edits the message content caption. Returns the edited message after the edit is completed on the server side.
 *
 * Returns object_ptr<Message>.
 */
class editMessageCaption final : public Function {
 public:
  static jclass Class;
  /// The chat the message belongs to.
  std::int64_t chat_id_;
  static jfieldID chat_id_fieldID;
  /// Identifier of the message.
  std::int64_t message_id_;
  static jfieldID message_id_fieldID;
  /// The new message reply markup; for bots only.
  object_ptr<ReplyMarkup> reply_markup_;
  static jfieldID reply_markup_fieldID;
  /// New message content caption; 0-GetOption(&quot;message_caption_length_max&quot;) characters.
  object_ptr<formattedText> caption_;
  static jfieldID caption_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  editMessageCaption();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] chat_id_ The chat the message belongs to.
   * \param[in] message_id_ Identifier of the message.
   * \param[in] reply_markup_ The new message reply markup; for bots only.
   * \param[in] caption_ New message content caption; 0-GetOption(&quot;message_caption_length_max&quot;) characters.
   */
  editMessageCaption(std::int64_t chat_id_, std::int64_t message_id_, object_ptr<ReplyMarkup> &&reply_markup_, object_ptr<formattedText> &&caption_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1154677038;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<message>;

  static object_ptr<editMessageCaption> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Edits the message content of a live location. Messages can be edited for a limited period of time specified in the live location. Returns the edited message after the edit is completed on the server side.
 *
 * Returns object_ptr<Message>.
 */
class editMessageLiveLocation final : public Function {
 public:
  static jclass Class;
  /// The chat the message belongs to.
  std::int64_t chat_id_;
  static jfieldID chat_id_fieldID;
  /// Identifier of the message.
  std::int64_t message_id_;
  static jfieldID message_id_fieldID;
  /// The new message reply markup; for bots only.
  object_ptr<ReplyMarkup> reply_markup_;
  static jfieldID reply_markup_fieldID;
  /// New location content of the message; may be null. Pass null to stop sharing the live location.
  object_ptr<location> location_;
  static jfieldID location_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  editMessageLiveLocation();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] chat_id_ The chat the message belongs to.
   * \param[in] message_id_ Identifier of the message.
   * \param[in] reply_markup_ The new message reply markup; for bots only.
   * \param[in] location_ New location content of the message; may be null. Pass null to stop sharing the live location.
   */
  editMessageLiveLocation(std::int64_t chat_id_, std::int64_t message_id_, object_ptr<ReplyMarkup> &&reply_markup_, object_ptr<location> &&location_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1146772745;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<message>;

  static object_ptr<editMessageLiveLocation> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Edits the content of a message with an animation, an audio, a document, a photo or a video. The media in the message can't be replaced if the message was set to self-destruct. Media can't be replaced by self-destructing media. Media in an album can be edited only to contain a photo or a video. Returns the edited message after the edit is completed on the server side.
 *
 * Returns object_ptr<Message>.
 */
class editMessageMedia final : public Function {
 public:
  static jclass Class;
  /// The chat the message belongs to.
  std::int64_t chat_id_;
  static jfieldID chat_id_fieldID;
  /// Identifier of the message.
  std::int64_t message_id_;
  static jfieldID message_id_fieldID;
  /// The new message reply markup; for bots only.
  object_ptr<ReplyMarkup> reply_markup_;
  static jfieldID reply_markup_fieldID;
  /// New content of the message. Must be one of the following types: InputMessageAnimation, InputMessageAudio, InputMessageDocument, InputMessagePhoto or InputMessageVideo.
  object_ptr<InputMessageContent> input_message_content_;
  static jfieldID input_message_content_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  editMessageMedia();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] chat_id_ The chat the message belongs to.
   * \param[in] message_id_ Identifier of the message.
   * \param[in] reply_markup_ The new message reply markup; for bots only.
   * \param[in] input_message_content_ New content of the message. Must be one of the following types: InputMessageAnimation, InputMessageAudio, InputMessageDocument, InputMessagePhoto or InputMessageVideo.
   */
  editMessageMedia(std::int64_t chat_id_, std::int64_t message_id_, object_ptr<ReplyMarkup> &&reply_markup_, object_ptr<InputMessageContent> &&input_message_content_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1152678125;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<message>;

  static object_ptr<editMessageMedia> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Edits the message reply markup; for bots only. Returns the edited message after the edit is completed on the server side.
 *
 * Returns object_ptr<Message>.
 */
class editMessageReplyMarkup final : public Function {
 public:
  static jclass Class;
  /// The chat the message belongs to.
  std::int64_t chat_id_;
  static jfieldID chat_id_fieldID;
  /// Identifier of the message.
  std::int64_t message_id_;
  static jfieldID message_id_fieldID;
  /// The new message reply markup.
  object_ptr<ReplyMarkup> reply_markup_;
  static jfieldID reply_markup_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  editMessageReplyMarkup();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] chat_id_ The chat the message belongs to.
   * \param[in] message_id_ Identifier of the message.
   * \param[in] reply_markup_ The new message reply markup.
   */
  editMessageReplyMarkup(std::int64_t chat_id_, std::int64_t message_id_, object_ptr<ReplyMarkup> &&reply_markup_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 332127881;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<message>;

  static object_ptr<editMessageReplyMarkup> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Edits the text of a message (or a text of a game message). Returns the edited message after the edit is completed on the server side.
 *
 * Returns object_ptr<Message>.
 */
class editMessageText final : public Function {
 public:
  static jclass Class;
  /// The chat the message belongs to.
  std::int64_t chat_id_;
  static jfieldID chat_id_fieldID;
  /// Identifier of the message.
  std::int64_t message_id_;
  static jfieldID message_id_fieldID;
  /// The new message reply markup; for bots only.
  object_ptr<ReplyMarkup> reply_markup_;
  static jfieldID reply_markup_fieldID;
  /// New text content of the message. Should be of type InputMessageText.
  object_ptr<InputMessageContent> input_message_content_;
  static jfieldID input_message_content_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  editMessageText();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] chat_id_ The chat the message belongs to.
   * \param[in] message_id_ Identifier of the message.
   * \param[in] reply_markup_ The new message reply markup; for bots only.
   * \param[in] input_message_content_ New text content of the message. Should be of type InputMessageText.
   */
  editMessageText(std::int64_t chat_id_, std::int64_t message_id_, object_ptr<ReplyMarkup> &&reply_markup_, object_ptr<InputMessageContent> &&input_message_content_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 196272567;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<message>;

  static object_ptr<editMessageText> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Edits an existing proxy server for network requests. Can be called before authorization.
 *
 * Returns object_ptr<Proxy>.
 */
class editProxy final : public Function {
 public:
  static jclass Class;
  /// Proxy identifier.
  std::int32_t proxy_id_;
  static jfieldID proxy_id_fieldID;
  /// Proxy server IP address.
  std::string server_;
  static jfieldID server_fieldID;
  /// Proxy server port.
  std::int32_t port_;
  static jfieldID port_fieldID;
  /// True, if the proxy should be enabled.
  bool enable_;
  static jfieldID enable_fieldID;
  /// Proxy type.
  object_ptr<ProxyType> type_;
  static jfieldID type_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  editProxy();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] proxy_id_ Proxy identifier.
   * \param[in] server_ Proxy server IP address.
   * \param[in] port_ Proxy server port.
   * \param[in] enable_ True, if the proxy should be enabled.
   * \param[in] type_ Proxy type.
   */
  editProxy(std::int32_t proxy_id_, std::string const &server_, std::int32_t port_, bool enable_, object_ptr<ProxyType> &&type_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1605883821;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<proxy>;

  static object_ptr<editProxy> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Enables a proxy. Only one proxy can be enabled at a time. Can be called before authorization.
 *
 * Returns object_ptr<Ok>.
 */
class enableProxy final : public Function {
 public:
  static jclass Class;
  /// Proxy identifier.
  std::int32_t proxy_id_;
  static jfieldID proxy_id_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  enableProxy();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] proxy_id_ Proxy identifier.
   */
  explicit enableProxy(std::int32_t proxy_id_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1494450838;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<ok>;

  static object_ptr<enableProxy> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Finishes the file generation.
 *
 * Returns object_ptr<Ok>.
 */
class finishFileGeneration final : public Function {
 public:
  static jclass Class;
  /// The identifier of the generation process.
  std::int64_t generation_id_;
  static jfieldID generation_id_fieldID;
  /// If set, means that file generation has failed and should be terminated.
  object_ptr<error> error_;
  static jfieldID error_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  finishFileGeneration();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] generation_id_ The identifier of the generation process.
   * \param[in] error_ If set, means that file generation has failed and should be terminated.
   */
  finishFileGeneration(std::int64_t generation_id_, object_ptr<error> &&error_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1055060835;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<ok>;

  static object_ptr<finishFileGeneration> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Forwards previously sent messages. Returns the forwarded messages in the same order as the message identifiers passed in message_ids. If a message can't be forwarded, null will be returned instead of the message.
 *
 * Returns object_ptr<Messages>.
 */
class forwardMessages final : public Function {
 public:
  static jclass Class;
  /// Identifier of the chat to which to forward messages.
  std::int64_t chat_id_;
  static jfieldID chat_id_fieldID;
  /// Identifier of the chat from which to forward messages.
  std::int64_t from_chat_id_;
  static jfieldID from_chat_id_fieldID;
  /// Identifiers of the messages to forward.
  std::vector<std::int64_t> message_ids_;
  static jfieldID message_ids_fieldID;
  /// Pass true to disable notification for the message, doesn't work if messages are forwarded to a secret chat.
  bool disable_notification_;
  static jfieldID disable_notification_fieldID;
  /// Pass true if the message is sent from the background.
  bool from_background_;
  static jfieldID from_background_fieldID;
  /// True, if the messages should be grouped into an album after forwarding. For this to work, no more than 10 messages may be forwarded, and all of them must be photo or video messages.
  bool as_album_;
  static jfieldID as_album_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  forwardMessages();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] chat_id_ Identifier of the chat to which to forward messages.
   * \param[in] from_chat_id_ Identifier of the chat from which to forward messages.
   * \param[in] message_ids_ Identifiers of the messages to forward.
   * \param[in] disable_notification_ Pass true to disable notification for the message, doesn't work if messages are forwarded to a secret chat.
   * \param[in] from_background_ Pass true if the message is sent from the background.
   * \param[in] as_album_ True, if the messages should be grouped into an album after forwarding. For this to work, no more than 10 messages may be forwarded, and all of them must be photo or video messages.
   */
  forwardMessages(std::int64_t chat_id_, std::int64_t from_chat_id_, std::vector<std::int64_t> &&message_ids_, bool disable_notification_, bool from_background_, bool as_album_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -537573308;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<messages>;

  static object_ptr<forwardMessages> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Generates a new invite link for a chat; the previously generated link is revoked. Available for basic groups, supergroups, and channels. In basic groups this can be called only by the group's creator; in supergroups and channels this requires appropriate administrator rights.
 *
 * Returns object_ptr<ChatInviteLink>.
 */
class generateChatInviteLink final : public Function {
 public:
  static jclass Class;
  /// Chat identifier.
  std::int64_t chat_id_;
  static jfieldID chat_id_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  generateChatInviteLink();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] chat_id_ Chat identifier.
   */
  explicit generateChatInviteLink(std::int64_t chat_id_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1945532500;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<chatInviteLink>;

  static object_ptr<generateChatInviteLink> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Returns the period of inactivity after which the account of the current user will automatically be deleted.
 *
 * Returns object_ptr<AccountTtl>.
 */
class getAccountTtl final : public Function {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  getAccountTtl();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -443905161;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<accountTtl>;

  static object_ptr<getAccountTtl> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Returns all active live locations that should be updated by the client. The list is persistent across application restarts only if the message database is used.
 *
 * Returns object_ptr<Messages>.
 */
class getActiveLiveLocationMessages final : public Function {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  getActiveLiveLocationMessages();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1425459567;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<messages>;

  static object_ptr<getActiveLiveLocationMessages> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Returns all active sessions of the current user.
 *
 * Returns object_ptr<Sessions>.
 */
class getActiveSessions final : public Function {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  getActiveSessions();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1119710526;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<sessions>;

  static object_ptr<getActiveSessions> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Returns all available Telegram Passport elements.
 *
 * Returns object_ptr<PassportElements>.
 */
class getAllPassportElements final : public Function {
 public:
  static jclass Class;
  /// Password of the current user.
  std::string password_;
  static jfieldID password_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  getAllPassportElements();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] password_ Password of the current user.
   */
  explicit getAllPassportElements(std::string const &password_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -2038945045;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<passportElements>;

  static object_ptr<getAllPassportElements> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Returns a list of archived sticker sets.
 *
 * Returns object_ptr<StickerSets>.
 */
class getArchivedStickerSets final : public Function {
 public:
  static jclass Class;
  /// Pass true to return mask stickers sets; pass false to return ordinary sticker sets.
  bool is_masks_;
  static jfieldID is_masks_fieldID;
  /// Identifier of the sticker set from which to return the result.
  std::int64_t offset_sticker_set_id_;
  static jfieldID offset_sticker_set_id_fieldID;
  /// Maximum number of sticker sets to return.
  std::int32_t limit_;
  static jfieldID limit_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  getArchivedStickerSets();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] is_masks_ Pass true to return mask stickers sets; pass false to return ordinary sticker sets.
   * \param[in] offset_sticker_set_id_ Identifier of the sticker set from which to return the result.
   * \param[in] limit_ Maximum number of sticker sets to return.
   */
  getArchivedStickerSets(bool is_masks_, std::int64_t offset_sticker_set_id_, std::int32_t limit_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1996943238;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<stickerSets>;

  static object_ptr<getArchivedStickerSets> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Returns a list of sticker sets attached to a file. Currently only photos and videos can have attached sticker sets.
 *
 * Returns object_ptr<StickerSets>.
 */
class getAttachedStickerSets final : public Function {
 public:
  static jclass Class;
  /// File identifier.
  std::int32_t file_id_;
  static jfieldID file_id_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  getAttachedStickerSets();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] file_id_ File identifier.
   */
  explicit getAttachedStickerSets(std::int32_t file_id_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1302172429;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<stickerSets>;

  static object_ptr<getAttachedStickerSets> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Returns the current authorization state; this is an offline request. For informational purposes only. Use updateAuthorizationState instead to maintain the current authorization state.
 *
 * Returns object_ptr<AuthorizationState>.
 */
class getAuthorizationState final : public Function {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  getAuthorizationState();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1949154877;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<AuthorizationState>;

  static object_ptr<getAuthorizationState> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Returns information about a basic group by its identifier. This is an offline request if the current user is not a bot.
 *
 * Returns object_ptr<BasicGroup>.
 */
class getBasicGroup final : public Function {
 public:
  static jclass Class;
  /// Basic group identifier.
  std::int32_t basic_group_id_;
  static jfieldID basic_group_id_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  getBasicGroup();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] basic_group_id_ Basic group identifier.
   */
  explicit getBasicGroup(std::int32_t basic_group_id_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 561775568;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<basicGroup>;

  static object_ptr<getBasicGroup> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Returns full information about a basic group by its identifier.
 *
 * Returns object_ptr<BasicGroupFullInfo>.
 */
class getBasicGroupFullInfo final : public Function {
 public:
  static jclass Class;
  /// Basic group identifier.
  std::int32_t basic_group_id_;
  static jfieldID basic_group_id_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  getBasicGroupFullInfo();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] basic_group_id_ Basic group identifier.
   */
  explicit getBasicGroupFullInfo(std::int32_t basic_group_id_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1770517905;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<basicGroupFullInfo>;

  static object_ptr<getBasicGroupFullInfo> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Returns users that were blocked by the current user.
 *
 * Returns object_ptr<Users>.
 */
class getBlockedUsers final : public Function {
 public:
  static jclass Class;
  /// Number of users to skip in the result; must be non-negative.
  std::int32_t offset_;
  static jfieldID offset_fieldID;
  /// Maximum number of users to return; up to 100.
  std::int32_t limit_;
  static jfieldID limit_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  getBlockedUsers();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] offset_ Number of users to skip in the result; must be non-negative.
   * \param[in] limit_ Maximum number of users to return; up to 100.
   */
  getBlockedUsers(std::int32_t offset_, std::int32_t limit_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -742912777;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<users>;

  static object_ptr<getBlockedUsers> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Sends a callback query to a bot and returns an answer. Returns an error with code 502 if the bot fails to answer the query before the query timeout expires.
 *
 * Returns object_ptr<CallbackQueryAnswer>.
 */
class getCallbackQueryAnswer final : public Function {
 public:
  static jclass Class;
  /// Identifier of the chat with the message.
  std::int64_t chat_id_;
  static jfieldID chat_id_fieldID;
  /// Identifier of the message from which the query originated.
  std::int64_t message_id_;
  static jfieldID message_id_fieldID;
  /// Query payload.
  object_ptr<CallbackQueryPayload> payload_;
  static jfieldID payload_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  getCallbackQueryAnswer();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] chat_id_ Identifier of the chat with the message.
   * \param[in] message_id_ Identifier of the message from which the query originated.
   * \param[in] payload_ Query payload.
   */
  getCallbackQueryAnswer(std::int64_t chat_id_, std::int64_t message_id_, object_ptr<CallbackQueryPayload> &&payload_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 116357727;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<callbackQueryAnswer>;

  static object_ptr<getCallbackQueryAnswer> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Returns information about a chat by its identifier, this is an offline request if the current user is not a bot.
 *
 * Returns object_ptr<Chat>.
 */
class getChat final : public Function {
 public:
  static jclass Class;
  /// Chat identifier.
  std::int64_t chat_id_;
  static jfieldID chat_id_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  getChat();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] chat_id_ Chat identifier.
   */
  explicit getChat(std::int64_t chat_id_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1866601536;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<chat>;

  static object_ptr<getChat> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Returns a list of users who are administrators of the chat.
 *
 * Returns object_ptr<Users>.
 */
class getChatAdministrators final : public Function {
 public:
  static jclass Class;
  /// Chat identifier.
  std::int64_t chat_id_;
  static jfieldID chat_id_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  getChatAdministrators();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] chat_id_ Chat identifier.
   */
  explicit getChatAdministrators(std::int64_t chat_id_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 508231041;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<users>;

  static object_ptr<getChatAdministrators> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Returns a list of service actions taken by chat members and administrators in the last 48 hours. Available only in supergroups and channels. Requires administrator rights. Returns results in reverse chronological order (i. e., in order of decreasing event_id).
 *
 * Returns object_ptr<ChatEvents>.
 */
class getChatEventLog final : public Function {
 public:
  static jclass Class;
  /// Chat identifier.
  std::int64_t chat_id_;
  static jfieldID chat_id_fieldID;
  /// Search query by which to filter events.
  std::string query_;
  static jfieldID query_fieldID;
  /// Identifier of an event from which to return results. Use 0 to get results from the latest events.
  std::int64_t from_event_id_;
  static jfieldID from_event_id_fieldID;
  /// Maximum number of events to return; up to 100.
  std::int32_t limit_;
  static jfieldID limit_fieldID;
  /// The types of events to return. By default, all types will be returned.
  object_ptr<chatEventLogFilters> filters_;
  static jfieldID filters_fieldID;
  /// User identifiers by which to filter events. By default, events relating to all users will be returned.
  std::vector<std::int32_t> user_ids_;
  static jfieldID user_ids_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  getChatEventLog();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] chat_id_ Chat identifier.
   * \param[in] query_ Search query by which to filter events.
   * \param[in] from_event_id_ Identifier of an event from which to return results. Use 0 to get results from the latest events.
   * \param[in] limit_ Maximum number of events to return; up to 100.
   * \param[in] filters_ The types of events to return. By default, all types will be returned.
   * \param[in] user_ids_ User identifiers by which to filter events. By default, events relating to all users will be returned.
   */
  getChatEventLog(std::int64_t chat_id_, std::string const &query_, std::int64_t from_event_id_, std::int32_t limit_, object_ptr<chatEventLogFilters> &&filters_, std::vector<std::int32_t> &&user_ids_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 206900967;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<chatEvents>;

  static object_ptr<getChatEventLog> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Returns messages in a chat. The messages are returned in a reverse chronological order (i.e., in order of decreasing message_id). For optimal performance the number of returned messages is chosen by the library. This is an offline request if only_local is true.
 *
 * Returns object_ptr<Messages>.
 */
class getChatHistory final : public Function {
 public:
  static jclass Class;
  /// Chat identifier.
  std::int64_t chat_id_;
  static jfieldID chat_id_fieldID;
  /// Identifier of the message starting from which history must be fetched; use 0 to get results from the last message.
  std::int64_t from_message_id_;
  static jfieldID from_message_id_fieldID;
  /// Specify 0 to get results from exactly the from_message_id or a negative offset to get the specified message and some newer messages.
  std::int32_t offset_;
  static jfieldID offset_fieldID;
  /// The maximum number of messages to be returned; must be positive and can't be greater than 100. If the offset is negative, the limit must be greater than -offset. Fewer messages may be returned than specified by the limit, even if the end of the message history has not been reached.
  std::int32_t limit_;
  static jfieldID limit_fieldID;
  /// If true, returns only messages that are available locally without sending network requests.
  bool only_local_;
  static jfieldID only_local_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  getChatHistory();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] chat_id_ Chat identifier.
   * \param[in] from_message_id_ Identifier of the message starting from which history must be fetched; use 0 to get results from the last message.
   * \param[in] offset_ Specify 0 to get results from exactly the from_message_id or a negative offset to get the specified message and some newer messages.
   * \param[in] limit_ The maximum number of messages to be returned; must be positive and can't be greater than 100. If the offset is negative, the limit must be greater than -offset. Fewer messages may be returned than specified by the limit, even if the end of the message history has not been reached.
   * \param[in] only_local_ If true, returns only messages that are available locally without sending network requests.
   */
  getChatHistory(std::int64_t chat_id_, std::int64_t from_message_id_, std::int32_t offset_, std::int32_t limit_, bool only_local_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -799960451;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<messages>;

  static object_ptr<getChatHistory> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Returns information about a single member of a chat.
 *
 * Returns object_ptr<ChatMember>.
 */
class getChatMember final : public Function {
 public:
  static jclass Class;
  /// Chat identifier.
  std::int64_t chat_id_;
  static jfieldID chat_id_fieldID;
  /// User identifier.
  std::int32_t user_id_;
  static jfieldID user_id_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  getChatMember();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] chat_id_ Chat identifier.
   * \param[in] user_id_ User identifier.
   */
  getChatMember(std::int64_t chat_id_, std::int32_t user_id_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 677085892;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<chatMember>;

  static object_ptr<getChatMember> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Returns the last message sent in a chat no later than the specified date.
 *
 * Returns object_ptr<Message>.
 */
class getChatMessageByDate final : public Function {
 public:
  static jclass Class;
  /// Chat identifier.
  std::int64_t chat_id_;
  static jfieldID chat_id_fieldID;
  /// Point in time (Unix timestamp) relative to which to search for messages.
  std::int32_t date_;
  static jfieldID date_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  getChatMessageByDate();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] chat_id_ Chat identifier.
   * \param[in] date_ Point in time (Unix timestamp) relative to which to search for messages.
   */
  getChatMessageByDate(std::int64_t chat_id_, std::int32_t date_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1062564150;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<message>;

  static object_ptr<getChatMessageByDate> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Returns approximate number of messages of the specified type in the chat.
 *
 * Returns object_ptr<Count>.
 */
class getChatMessageCount final : public Function {
 public:
  static jclass Class;
  /// Identifier of the chat in which to count messages.
  std::int64_t chat_id_;
  static jfieldID chat_id_fieldID;
  /// Filter for message content; searchMessagesFilterEmpty is unsupported in this function.
  object_ptr<SearchMessagesFilter> filter_;
  static jfieldID filter_fieldID;
  /// If true, returns count that is available locally without sending network requests, returning -1 if the number of messages is unknown.
  bool return_local_;
  static jfieldID return_local_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  getChatMessageCount();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] chat_id_ Identifier of the chat in which to count messages.
   * \param[in] filter_ Filter for message content; searchMessagesFilterEmpty is unsupported in this function.
   * \param[in] return_local_ If true, returns count that is available locally without sending network requests, returning -1 if the number of messages is unknown.
   */
  getChatMessageCount(std::int64_t chat_id_, object_ptr<SearchMessagesFilter> &&filter_, bool return_local_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 205435308;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<count>;

  static object_ptr<getChatMessageCount> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Returns information about a pinned chat message.
 *
 * Returns object_ptr<Message>.
 */
class getChatPinnedMessage final : public Function {
 public:
  static jclass Class;
  /// Identifier of the chat the message belongs to.
  std::int64_t chat_id_;
  static jfieldID chat_id_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  getChatPinnedMessage();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] chat_id_ Identifier of the chat the message belongs to.
   */
  explicit getChatPinnedMessage(std::int64_t chat_id_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 359865008;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<message>;

  static object_ptr<getChatPinnedMessage> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Returns information on whether the current chat can be reported as spam.
 *
 * Returns object_ptr<ChatReportSpamState>.
 */
class getChatReportSpamState final : public Function {
 public:
  static jclass Class;
  /// Chat identifier.
  std::int64_t chat_id_;
  static jfieldID chat_id_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  getChatReportSpamState();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] chat_id_ Chat identifier.
   */
  explicit getChatReportSpamState(std::int64_t chat_id_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -748866856;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<chatReportSpamState>;

  static object_ptr<getChatReportSpamState> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Returns an ordered list of chats. Chats are sorted by the pair (order, chat_id) in decreasing order. (For example, to get a list of chats from the beginning, the offset_order should be equal to 2^63 - 1). For optimal performance the number of returned chats is chosen by the library.
 *
 * Returns object_ptr<Chats>.
 */
class getChats final : public Function {
 public:
  static jclass Class;
  /// Chat order to return chats from.
  std::int64_t offset_order_;
  static jfieldID offset_order_fieldID;
  /// Chat identifier to return chats from.
  std::int64_t offset_chat_id_;
  static jfieldID offset_chat_id_fieldID;
  /// The maximum number of chats to be returned. It is possible that fewer chats than the limit are returned even if the end of the list is not reached.
  std::int32_t limit_;
  static jfieldID limit_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  getChats();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] offset_order_ Chat order to return chats from.
   * \param[in] offset_chat_id_ Chat identifier to return chats from.
   * \param[in] limit_ The maximum number of chats to be returned. It is possible that fewer chats than the limit are returned even if the end of the list is not reached.
   */
  getChats(std::int64_t offset_order_, std::int64_t offset_chat_id_, std::int32_t limit_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -2121381601;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<chats>;

  static object_ptr<getChats> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Returns all website where the current user used Telegram to log in.
 *
 * Returns object_ptr<ConnectedWebsites>.
 */
class getConnectedWebsites final : public Function {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  getConnectedWebsites();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -170536110;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<connectedWebsites>;

  static object_ptr<getConnectedWebsites> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Returns all user contacts.
 *
 * Returns object_ptr<Users>.
 */
class getContacts final : public Function {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  getContacts();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1417722768;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<users>;

  static object_ptr<getContacts> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Uses current user IP to found his country. Returns two-letter ISO 3166-1 alpha-2 country code. Can be called before authorization.
 *
 * Returns object_ptr<Text>.
 */
class getCountryCode final : public Function {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  getCountryCode();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1540593906;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<text>;

  static object_ptr<getCountryCode> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Returns a list of public chats created by the user.
 *
 * Returns object_ptr<Chats>.
 */
class getCreatedPublicChats final : public Function {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  getCreatedPublicChats();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1609082914;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<chats>;

  static object_ptr<getCreatedPublicChats> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Returns information about a tg:// deep link. Use &quot;tg://need_update_for_some_feature&quot; or &quot;tg:some_unsupported_feature&quot; for testing. Returns a 404 error for unknown links. Can be called before authorization.
 *
 * Returns object_ptr<DeepLinkInfo>.
 */
class getDeepLinkInfo final : public Function {
 public:
  static jclass Class;
  /// The link.
  std::string link_;
  static jfieldID link_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  getDeepLinkInfo();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] link_ The link.
   */
  explicit getDeepLinkInfo(std::string const &link_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 680673150;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<deepLinkInfo>;

  static object_ptr<getDeepLinkInfo> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Returns favorite stickers.
 *
 * Returns object_ptr<Stickers>.
 */
class getFavoriteStickers final : public Function {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  getFavoriteStickers();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -338964672;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<stickers>;

  static object_ptr<getFavoriteStickers> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Returns information about a file; this is an offline request.
 *
 * Returns object_ptr<File>.
 */
class getFile final : public Function {
 public:
  static jclass Class;
  /// Identifier of the file to get.
  std::int32_t file_id_;
  static jfieldID file_id_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  getFile();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] file_id_ Identifier of the file to get.
   */
  explicit getFile(std::int32_t file_id_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1553923406;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<file>;

  static object_ptr<getFile> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Returns the extension of a file, guessed by its MIME type. Returns an empty string on failure. This is an offline method. Can be called before authorization. Can be called synchronously.
 *
 * Returns object_ptr<Text>.
 */
class getFileExtension final : public Function {
 public:
  static jclass Class;
  /// The MIME type of the file.
  std::string mime_type_;
  static jfieldID mime_type_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  getFileExtension();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] mime_type_ The MIME type of the file.
   */
  explicit getFileExtension(std::string const &mime_type_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -106055372;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<text>;

  static object_ptr<getFileExtension> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Returns the MIME type of a file, guessed by its extension. Returns an empty string on failure. This is an offline method. Can be called before authorization. Can be called synchronously.
 *
 * Returns object_ptr<Text>.
 */
class getFileMimeType final : public Function {
 public:
  static jclass Class;
  /// The name of the file or path to the file.
  std::string file_name_;
  static jfieldID file_name_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  getFileMimeType();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] file_name_ The name of the file or path to the file.
   */
  explicit getFileMimeType(std::string const &file_name_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -2073879671;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<text>;

  static object_ptr<getFileMimeType> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Returns the high scores for a game and some part of the high score table in the range of the specified user; for bots only.
 *
 * Returns object_ptr<GameHighScores>.
 */
class getGameHighScores final : public Function {
 public:
  static jclass Class;
  /// The chat that contains the message with the game.
  std::int64_t chat_id_;
  static jfieldID chat_id_fieldID;
  /// Identifier of the message.
  std::int64_t message_id_;
  static jfieldID message_id_fieldID;
  /// User identifier.
  std::int32_t user_id_;
  static jfieldID user_id_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  getGameHighScores();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] chat_id_ The chat that contains the message with the game.
   * \param[in] message_id_ Identifier of the message.
   * \param[in] user_id_ User identifier.
   */
  getGameHighScores(std::int64_t chat_id_, std::int64_t message_id_, std::int32_t user_id_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1920923753;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<gameHighScores>;

  static object_ptr<getGameHighScores> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Returns a list of common chats with a given user. Chats are sorted by their type and creation date.
 *
 * Returns object_ptr<Chats>.
 */
class getGroupsInCommon final : public Function {
 public:
  static jclass Class;
  /// User identifier.
  std::int32_t user_id_;
  static jfieldID user_id_fieldID;
  /// Chat identifier starting from which to return chats; use 0 for the first request.
  std::int64_t offset_chat_id_;
  static jfieldID offset_chat_id_fieldID;
  /// Maximum number of chats to be returned; up to 100.
  std::int32_t limit_;
  static jfieldID limit_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  getGroupsInCommon();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] user_id_ User identifier.
   * \param[in] offset_chat_id_ Chat identifier starting from which to return chats; use 0 for the first request.
   * \param[in] limit_ Maximum number of chats to be returned; up to 100.
   */
  getGroupsInCommon(std::int32_t user_id_, std::int64_t offset_chat_id_, std::int32_t limit_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -23238689;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<chats>;

  static object_ptr<getGroupsInCommon> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Returns the total number of imported contacts.
 *
 * Returns object_ptr<Count>.
 */
class getImportedContactCount final : public Function {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  getImportedContactCount();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -656336346;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<count>;

  static object_ptr<getImportedContactCount> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Returns game high scores and some part of the high score table in the range of the specified user; for bots only.
 *
 * Returns object_ptr<GameHighScores>.
 */
class getInlineGameHighScores final : public Function {
 public:
  static jclass Class;
  /// Inline message identifier.
  std::string inline_message_id_;
  static jfieldID inline_message_id_fieldID;
  /// User identifier.
  std::int32_t user_id_;
  static jfieldID user_id_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  getInlineGameHighScores();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] inline_message_id_ Inline message identifier.
   * \param[in] user_id_ User identifier.
   */
  getInlineGameHighScores(std::string const &inline_message_id_, std::int32_t user_id_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1833445800;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<gameHighScores>;

  static object_ptr<getInlineGameHighScores> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Sends an inline query to a bot and returns its results. Returns an error with code 502 if the bot fails to answer the query before the query timeout expires.
 *
 * Returns object_ptr<InlineQueryResults>.
 */
class getInlineQueryResults final : public Function {
 public:
  static jclass Class;
  /// The identifier of the target bot.
  std::int32_t bot_user_id_;
  static jfieldID bot_user_id_fieldID;
  /// Identifier of the chat, where the query was sent.
  std::int64_t chat_id_;
  static jfieldID chat_id_fieldID;
  /// Location of the user, only if needed.
  object_ptr<location> user_location_;
  static jfieldID user_location_fieldID;
  /// Text of the query.
  std::string query_;
  static jfieldID query_fieldID;
  /// Offset of the first entry to return.
  std::string offset_;
  static jfieldID offset_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  getInlineQueryResults();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] bot_user_id_ The identifier of the target bot.
   * \param[in] chat_id_ Identifier of the chat, where the query was sent.
   * \param[in] user_location_ Location of the user, only if needed.
   * \param[in] query_ Text of the query.
   * \param[in] offset_ Offset of the first entry to return.
   */
  getInlineQueryResults(std::int32_t bot_user_id_, std::int64_t chat_id_, object_ptr<location> &&user_location_, std::string const &query_, std::string const &offset_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1182511172;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<inlineQueryResults>;

  static object_ptr<getInlineQueryResults> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Returns a list of installed sticker sets.
 *
 * Returns object_ptr<StickerSets>.
 */
class getInstalledStickerSets final : public Function {
 public:
  static jclass Class;
  /// Pass true to return mask sticker sets; pass false to return ordinary sticker sets.
  bool is_masks_;
  static jfieldID is_masks_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  getInstalledStickerSets();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] is_masks_ Pass true to return mask sticker sets; pass false to return ordinary sticker sets.
   */
  explicit getInstalledStickerSets(bool is_masks_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1214523749;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<stickerSets>;

  static object_ptr<getInstalledStickerSets> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Returns the default text for invitation messages to be used as a placeholder when the current user invites friends to Telegram.
 *
 * Returns object_ptr<Text>.
 */
class getInviteText final : public Function {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  getInviteText();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 794573512;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<text>;

  static object_ptr<getInviteText> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Returns a string stored in the local database from the specified localization target and language pack by its key. Returns a 404 error if the string is not found. This is an offline method. Can be called before authorization. Can be called synchronously.
 *
 * Returns object_ptr<LanguagePackStringValue>.
 */
class getLanguagePackString final : public Function {
 public:
  static jclass Class;
  /// Path to the language pack database in which strings are stored.
  std::string language_pack_database_path_;
  static jfieldID language_pack_database_path_fieldID;
  /// Localization target to which the language pack belongs.
  std::string localization_target_;
  static jfieldID localization_target_fieldID;
  /// Language pack identifier.
  std::string language_pack_id_;
  static jfieldID language_pack_id_fieldID;
  /// Language pack key of the string to be returned.
  std::string key_;
  static jfieldID key_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  getLanguagePackString();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] language_pack_database_path_ Path to the language pack database in which strings are stored.
   * \param[in] localization_target_ Localization target to which the language pack belongs.
   * \param[in] language_pack_id_ Language pack identifier.
   * \param[in] key_ Language pack key of the string to be returned.
   */
  getLanguagePackString(std::string const &language_pack_database_path_, std::string const &localization_target_, std::string const &language_pack_id_, std::string const &key_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 150789747;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<LanguagePackStringValue>;

  static object_ptr<getLanguagePackString> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Returns strings from a language pack in the current localization target by their keys.
 *
 * Returns object_ptr<LanguagePackStrings>.
 */
class getLanguagePackStrings final : public Function {
 public:
  static jclass Class;
  /// Language pack identifier of the strings to be returned.
  std::string language_pack_id_;
  static jfieldID language_pack_id_fieldID;
  /// Language pack keys of the strings to be returned; leave empty to request all available strings.
  std::vector<std::string> keys_;
  static jfieldID keys_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  getLanguagePackStrings();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] language_pack_id_ Language pack identifier of the strings to be returned.
   * \param[in] keys_ Language pack keys of the strings to be returned; leave empty to request all available strings.
   */
  getLanguagePackStrings(std::string const &language_pack_id_, std::vector<std::string> &&keys_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1246259088;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<languagePackStrings>;

  static object_ptr<getLanguagePackStrings> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Returns information about the current localization target. This is an offline request if only_local is true.
 *
 * Returns object_ptr<LocalizationTargetInfo>.
 */
class getLocalizationTargetInfo final : public Function {
 public:
  static jclass Class;
  /// If true, returns only locally available information without sending network requests.
  bool only_local_;
  static jfieldID only_local_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  getLocalizationTargetInfo();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] only_local_ If true, returns only locally available information without sending network requests.
   */
  explicit getLocalizationTargetInfo(bool only_local_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1849499526;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<localizationTargetInfo>;

  static object_ptr<getLocalizationTargetInfo> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Returns information about a file with a map thumbnail in PNG format. Only map thumbnail files with size less than 1MB can be downloaded.
 *
 * Returns object_ptr<File>.
 */
class getMapThumbnailFile final : public Function {
 public:
  static jclass Class;
  /// Location of the map center.
  object_ptr<location> location_;
  static jfieldID location_fieldID;
  /// Map zoom level; 13-20.
  std::int32_t zoom_;
  static jfieldID zoom_fieldID;
  /// Map width in pixels before applying scale; 16-1024.
  std::int32_t width_;
  static jfieldID width_fieldID;
  /// Map height in pixels before applying scale; 16-1024.
  std::int32_t height_;
  static jfieldID height_fieldID;
  /// Map scale; 1-3.
  std::int32_t scale_;
  static jfieldID scale_fieldID;
  /// Identifier of a chat, in which the thumbnail will be shown. Use 0 if unknown.
  std::int64_t chat_id_;
  static jfieldID chat_id_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  getMapThumbnailFile();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] location_ Location of the map center.
   * \param[in] zoom_ Map zoom level; 13-20.
   * \param[in] width_ Map width in pixels before applying scale; 16-1024.
   * \param[in] height_ Map height in pixels before applying scale; 16-1024.
   * \param[in] scale_ Map scale; 1-3.
   * \param[in] chat_id_ Identifier of a chat, in which the thumbnail will be shown. Use 0 if unknown.
   */
  getMapThumbnailFile(object_ptr<location> &&location_, std::int32_t zoom_, std::int32_t width_, std::int32_t height_, std::int32_t scale_, std::int64_t chat_id_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -152660070;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<file>;

  static object_ptr<getMapThumbnailFile> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Returns the current user.
 *
 * Returns object_ptr<User>.
 */
class getMe final : public Function {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  getMe();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -191516033;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<user>;

  static object_ptr<getMe> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Returns information about a message.
 *
 * Returns object_ptr<Message>.
 */
class getMessage final : public Function {
 public:
  static jclass Class;
  /// Identifier of the chat the message belongs to.
  std::int64_t chat_id_;
  static jfieldID chat_id_fieldID;
  /// Identifier of the message to get.
  std::int64_t message_id_;
  static jfieldID message_id_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  getMessage();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] chat_id_ Identifier of the chat the message belongs to.
   * \param[in] message_id_ Identifier of the message to get.
   */
  getMessage(std::int64_t chat_id_, std::int64_t message_id_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1821196160;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<message>;

  static object_ptr<getMessage> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Returns information about messages. If a message is not found, returns null on the corresponding position of the result.
 *
 * Returns object_ptr<Messages>.
 */
class getMessages final : public Function {
 public:
  static jclass Class;
  /// Identifier of the chat the messages belong to.
  std::int64_t chat_id_;
  static jfieldID chat_id_fieldID;
  /// Identifiers of the messages to get.
  std::vector<std::int64_t> message_ids_;
  static jfieldID message_ids_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  getMessages();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] chat_id_ Identifier of the chat the messages belong to.
   * \param[in] message_ids_ Identifiers of the messages to get.
   */
  getMessages(std::int64_t chat_id_, std::vector<std::int64_t> &&message_ids_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 425299338;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<messages>;

  static object_ptr<getMessages> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Returns network data usage statistics. Can be called before authorization.
 *
 * Returns object_ptr<NetworkStatistics>.
 */
class getNetworkStatistics final : public Function {
 public:
  static jclass Class;
  /// If true, returns only data for the current library launch.
  bool only_current_;
  static jfieldID only_current_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  getNetworkStatistics();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] only_current_ If true, returns only data for the current library launch.
   */
  explicit getNetworkStatistics(bool only_current_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -986228706;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<networkStatistics>;

  static object_ptr<getNetworkStatistics> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Returns the value of an option by its name. (Check the list of available options on https://core.telegram.org/tdlib/options.) Can be called before authorization.
 *
 * Returns object_ptr<OptionValue>.
 */
class getOption final : public Function {
 public:
  static jclass Class;
  /// The name of the option.
  std::string name_;
  static jfieldID name_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  getOption();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] name_ The name of the option.
   */
  explicit getOption(std::string const &name_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1572495746;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<OptionValue>;

  static object_ptr<getOption> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Returns a Telegram Passport authorization form for sharing data with a service.
 *
 * Returns object_ptr<PassportAuthorizationForm>.
 */
class getPassportAuthorizationForm final : public Function {
 public:
  static jclass Class;
  /// User identifier of the service's bot.
  std::int32_t bot_user_id_;
  static jfieldID bot_user_id_fieldID;
  /// Telegram Passport element types requested by the service.
  std::string scope_;
  static jfieldID scope_fieldID;
  /// Service's public_key.
  std::string public_key_;
  static jfieldID public_key_fieldID;
  /// Authorization form nonce provided by the service.
  std::string nonce_;
  static jfieldID nonce_fieldID;
  /// Password of the current user.
  std::string password_;
  static jfieldID password_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  getPassportAuthorizationForm();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] bot_user_id_ User identifier of the service's bot.
   * \param[in] scope_ Telegram Passport element types requested by the service.
   * \param[in] public_key_ Service's public_key.
   * \param[in] nonce_ Authorization form nonce provided by the service.
   * \param[in] password_ Password of the current user.
   */
  getPassportAuthorizationForm(std::int32_t bot_user_id_, std::string const &scope_, std::string const &public_key_, std::string const &nonce_, std::string const &password_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1998294216;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<passportAuthorizationForm>;

  static object_ptr<getPassportAuthorizationForm> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Returns one of the available Telegram Passport elements.
 *
 * Returns object_ptr<PassportElement>.
 */
class getPassportElement final : public Function {
 public:
  static jclass Class;
  /// Telegram Passport element type.
  object_ptr<PassportElementType> type_;
  static jfieldID type_fieldID;
  /// Password of the current user.
  std::string password_;
  static jfieldID password_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  getPassportElement();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] type_ Telegram Passport element type.
   * \param[in] password_ Password of the current user.
   */
  getPassportElement(object_ptr<PassportElementType> &&type_, std::string const &password_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1882398342;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<PassportElement>;

  static object_ptr<getPassportElement> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Returns the current state of 2-step verification.
 *
 * Returns object_ptr<PasswordState>.
 */
class getPasswordState final : public Function {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  getPasswordState();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -174752904;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<passwordState>;

  static object_ptr<getPasswordState> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Returns an invoice payment form. This method should be called when the user presses inlineKeyboardButtonBuy.
 *
 * Returns object_ptr<PaymentForm>.
 */
class getPaymentForm final : public Function {
 public:
  static jclass Class;
  /// Chat identifier of the Invoice message.
  std::int64_t chat_id_;
  static jfieldID chat_id_fieldID;
  /// Message identifier.
  std::int64_t message_id_;
  static jfieldID message_id_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  getPaymentForm();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] chat_id_ Chat identifier of the Invoice message.
   * \param[in] message_id_ Message identifier.
   */
  getPaymentForm(std::int64_t chat_id_, std::int64_t message_id_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -2146950882;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<paymentForm>;

  static object_ptr<getPaymentForm> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Returns information about a successful payment.
 *
 * Returns object_ptr<PaymentReceipt>.
 */
class getPaymentReceipt final : public Function {
 public:
  static jclass Class;
  /// Chat identifier of the PaymentSuccessful message.
  std::int64_t chat_id_;
  static jfieldID chat_id_fieldID;
  /// Message identifier.
  std::int64_t message_id_;
  static jfieldID message_id_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  getPaymentReceipt();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] chat_id_ Chat identifier of the PaymentSuccessful message.
   * \param[in] message_id_ Message identifier.
   */
  getPaymentReceipt(std::int64_t chat_id_, std::int64_t message_id_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1013758294;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<paymentReceipt>;

  static object_ptr<getPaymentReceipt> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Returns an IETF language tag of the language preferred in the country, which should be used to fill native fields in Telegram Passport personal details. Returns a 404 error if unknown.
 *
 * Returns object_ptr<Text>.
 */
class getPreferredCountryLanguage final : public Function {
 public:
  static jclass Class;
  /// A two-letter ISO 3166-1 alpha-2 country code.
  std::string country_code_;
  static jfieldID country_code_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  getPreferredCountryLanguage();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] country_code_ A two-letter ISO 3166-1 alpha-2 country code.
   */
  explicit getPreferredCountryLanguage(std::string const &country_code_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -933049386;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<text>;

  static object_ptr<getPreferredCountryLanguage> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Returns list of proxies that are currently set up. Can be called before authorization.
 *
 * Returns object_ptr<Proxies>.
 */
class getProxies final : public Function {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  getProxies();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -95026381;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<proxies>;

  static object_ptr<getProxies> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Returns an HTTPS link, which can be used to add a proxy. Available only for SOCKS5 and MTProto proxies. Can be called before authorization.
 *
 * Returns object_ptr<Text>.
 */
class getProxyLink final : public Function {
 public:
  static jclass Class;
  /// Proxy identifier.
  std::int32_t proxy_id_;
  static jfieldID proxy_id_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  getProxyLink();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] proxy_id_ Proxy identifier.
   */
  explicit getProxyLink(std::int32_t proxy_id_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1285597664;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<text>;

  static object_ptr<getProxyLink> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Returns a public HTTPS link to a message. Available only for messages in public supergroups and channels.
 *
 * Returns object_ptr<PublicMessageLink>.
 */
class getPublicMessageLink final : public Function {
 public:
  static jclass Class;
  /// Identifier of the chat to which the message belongs.
  std::int64_t chat_id_;
  static jfieldID chat_id_fieldID;
  /// Identifier of the message.
  std::int64_t message_id_;
  static jfieldID message_id_fieldID;
  /// Pass true if a link for a whole media album should be returned.
  bool for_album_;
  static jfieldID for_album_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  getPublicMessageLink();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] chat_id_ Identifier of the chat to which the message belongs.
   * \param[in] message_id_ Identifier of the message.
   * \param[in] for_album_ Pass true if a link for a whole media album should be returned.
   */
  getPublicMessageLink(std::int64_t chat_id_, std::int64_t message_id_, bool for_album_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -374642839;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<publicMessageLink>;

  static object_ptr<getPublicMessageLink> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Returns up to 20 recently used inline bots in the order of their last usage.
 *
 * Returns object_ptr<Users>.
 */
class getRecentInlineBots final : public Function {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  getRecentInlineBots();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1437823548;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<users>;

  static object_ptr<getRecentInlineBots> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Returns a list of recently used stickers.
 *
 * Returns object_ptr<Stickers>.
 */
class getRecentStickers final : public Function {
 public:
  static jclass Class;
  /// Pass true to return stickers and masks that were recently attached to photos or video files; pass false to return recently sent stickers.
  bool is_attached_;
  static jfieldID is_attached_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  getRecentStickers();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] is_attached_ Pass true to return stickers and masks that were recently attached to photos or video files; pass false to return recently sent stickers.
   */
  explicit getRecentStickers(bool is_attached_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -579622241;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<stickers>;

  static object_ptr<getRecentStickers> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Returns t.me URLs recently visited by a newly registered user.
 *
 * Returns object_ptr<TMeUrls>.
 */
class getRecentlyVisitedTMeUrls final : public Function {
 public:
  static jclass Class;
  /// Google Play referrer to identify the user.
  std::string referrer_;
  static jfieldID referrer_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  getRecentlyVisitedTMeUrls();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] referrer_ Google Play referrer to identify the user.
   */
  explicit getRecentlyVisitedTMeUrls(std::string const &referrer_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 806754961;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<tMeUrls>;

  static object_ptr<getRecentlyVisitedTMeUrls> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Returns a recovery email address that was previously set up. This method can be used to verify a password provided by the user.
 *
 * Returns object_ptr<RecoveryEmailAddress>.
 */
class getRecoveryEmailAddress final : public Function {
 public:
  static jclass Class;
  /// The password for the current user.
  std::string password_;
  static jfieldID password_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  getRecoveryEmailAddress();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] password_ The password for the current user.
   */
  explicit getRecoveryEmailAddress(std::string const &password_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1594770947;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<recoveryEmailAddress>;

  static object_ptr<getRecoveryEmailAddress> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Returns information about a file by its remote ID; this is an offline request. Can be used to register a URL as a file for further uploading, or sending as a message.
 *
 * Returns object_ptr<File>.
 */
class getRemoteFile final : public Function {
 public:
  static jclass Class;
  /// Remote identifier of the file to get.
  std::string remote_file_id_;
  static jfieldID remote_file_id_fieldID;
  /// File type, if known.
  object_ptr<FileType> file_type_;
  static jfieldID file_type_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  getRemoteFile();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] remote_file_id_ Remote identifier of the file to get.
   * \param[in] file_type_ File type, if known.
   */
  getRemoteFile(std::string const &remote_file_id_, object_ptr<FileType> &&file_type_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 2137204530;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<file>;

  static object_ptr<getRemoteFile> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Returns information about a message that is replied by given message.
 *
 * Returns object_ptr<Message>.
 */
class getRepliedMessage final : public Function {
 public:
  static jclass Class;
  /// Identifier of the chat the message belongs to.
  std::int64_t chat_id_;
  static jfieldID chat_id_fieldID;
  /// Identifier of the message reply to which get.
  std::int64_t message_id_;
  static jfieldID message_id_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  getRepliedMessage();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] chat_id_ Identifier of the chat the message belongs to.
   * \param[in] message_id_ Identifier of the message reply to which get.
   */
  getRepliedMessage(std::int64_t chat_id_, std::int64_t message_id_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -641918531;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<message>;

  static object_ptr<getRepliedMessage> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Returns saved animations.
 *
 * Returns object_ptr<Animations>.
 */
class getSavedAnimations final : public Function {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  getSavedAnimations();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 7051032;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<animations>;

  static object_ptr<getSavedAnimations> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Returns saved order info, if any.
 *
 * Returns object_ptr<OrderInfo>.
 */
class getSavedOrderInfo final : public Function {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  getSavedOrderInfo();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1152016675;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<orderInfo>;

  static object_ptr<getSavedOrderInfo> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Returns the notification settings for chats of a given type.
 *
 * Returns object_ptr<ScopeNotificationSettings>.
 */
class getScopeNotificationSettings final : public Function {
 public:
  static jclass Class;
  /// Types of chats for which to return the notification settings information.
  object_ptr<NotificationSettingsScope> scope_;
  static jfieldID scope_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  getScopeNotificationSettings();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] scope_ Types of chats for which to return the notification settings information.
   */
  explicit getScopeNotificationSettings(object_ptr<NotificationSettingsScope> &&scope_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -995613361;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<scopeNotificationSettings>;

  static object_ptr<getScopeNotificationSettings> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Returns information about a secret chat by its identifier. This is an offline request.
 *
 * Returns object_ptr<SecretChat>.
 */
class getSecretChat final : public Function {
 public:
  static jclass Class;
  /// Secret chat identifier.
  std::int32_t secret_chat_id_;
  static jfieldID secret_chat_id_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  getSecretChat();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] secret_chat_id_ Secret chat identifier.
   */
  explicit getSecretChat(std::int32_t secret_chat_id_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 40599169;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<secretChat>;

  static object_ptr<getSecretChat> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Returns emoji corresponding to a sticker.
 *
 * Returns object_ptr<StickerEmojis>.
 */
class getStickerEmojis final : public Function {
 public:
  static jclass Class;
  /// Sticker file identifier.
  object_ptr<InputFile> sticker_;
  static jfieldID sticker_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  getStickerEmojis();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] sticker_ Sticker file identifier.
   */
  explicit getStickerEmojis(object_ptr<InputFile> &&sticker_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 95352475;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<stickerEmojis>;

  static object_ptr<getStickerEmojis> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Returns information about a sticker set by its identifier.
 *
 * Returns object_ptr<StickerSet>.
 */
class getStickerSet final : public Function {
 public:
  static jclass Class;
  /// Identifier of the sticker set.
  std::int64_t set_id_;
  static jfieldID set_id_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  getStickerSet();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] set_id_ Identifier of the sticker set.
   */
  explicit getStickerSet(std::int64_t set_id_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1052318659;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<stickerSet>;

  static object_ptr<getStickerSet> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Returns stickers from the installed sticker sets that correspond to a given emoji. If the emoji is not empty, favorite and recently used stickers may also be returned.
 *
 * Returns object_ptr<Stickers>.
 */
class getStickers final : public Function {
 public:
  static jclass Class;
  /// String representation of emoji. If empty, returns all known installed stickers.
  std::string emoji_;
  static jfieldID emoji_fieldID;
  /// Maximum number of stickers to be returned.
  std::int32_t limit_;
  static jfieldID limit_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  getStickers();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] emoji_ String representation of emoji. If empty, returns all known installed stickers.
   * \param[in] limit_ Maximum number of stickers to be returned.
   */
  getStickers(std::string const &emoji_, std::int32_t limit_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1594919556;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<stickers>;

  static object_ptr<getStickers> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Returns storage usage statistics.
 *
 * Returns object_ptr<StorageStatistics>.
 */
class getStorageStatistics final : public Function {
 public:
  static jclass Class;
  /// Maximum number of chats with the largest storage usage for which separate statistics should be returned. All other chats will be grouped in entries with chat_id == 0. If the chat info database is not used, the chat_limit is ignored and is always set to 0.
  std::int32_t chat_limit_;
  static jfieldID chat_limit_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  getStorageStatistics();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] chat_limit_ Maximum number of chats with the largest storage usage for which separate statistics should be returned. All other chats will be grouped in entries with chat_id == 0. If the chat info database is not used, the chat_limit is ignored and is always set to 0.
   */
  explicit getStorageStatistics(std::int32_t chat_limit_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -853193929;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<storageStatistics>;

  static object_ptr<getStorageStatistics> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Quickly returns approximate storage usage statistics.
 *
 * Returns object_ptr<StorageStatisticsFast>.
 */
class getStorageStatisticsFast final : public Function {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  getStorageStatisticsFast();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 61368066;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<storageStatisticsFast>;

  static object_ptr<getStorageStatisticsFast> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Returns information about a supergroup or channel by its identifier. This is an offline request if the current user is not a bot.
 *
 * Returns object_ptr<Supergroup>.
 */
class getSupergroup final : public Function {
 public:
  static jclass Class;
  /// Supergroup or channel identifier.
  std::int32_t supergroup_id_;
  static jfieldID supergroup_id_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  getSupergroup();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] supergroup_id_ Supergroup or channel identifier.
   */
  explicit getSupergroup(std::int32_t supergroup_id_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -2063063706;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<supergroup>;

  static object_ptr<getSupergroup> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Returns full information about a supergroup or channel by its identifier, cached for up to 1 minute.
 *
 * Returns object_ptr<SupergroupFullInfo>.
 */
class getSupergroupFullInfo final : public Function {
 public:
  static jclass Class;
  /// Supergroup or channel identifier.
  std::int32_t supergroup_id_;
  static jfieldID supergroup_id_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  getSupergroupFullInfo();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] supergroup_id_ Supergroup or channel identifier.
   */
  explicit getSupergroupFullInfo(std::int32_t supergroup_id_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1150331262;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<supergroupFullInfo>;

  static object_ptr<getSupergroupFullInfo> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Returns information about members or banned users in a supergroup or channel. Can be used only if SupergroupFullInfo.can_get_members == true; additionally, administrator privileges may be required for some filters.
 *
 * Returns object_ptr<ChatMembers>.
 */
class getSupergroupMembers final : public Function {
 public:
  static jclass Class;
  /// Identifier of the supergroup or channel.
  std::int32_t supergroup_id_;
  static jfieldID supergroup_id_fieldID;
  /// The type of users to return. By default, supergroupMembersRecent.
  object_ptr<SupergroupMembersFilter> filter_;
  static jfieldID filter_fieldID;
  /// Number of users to skip.
  std::int32_t offset_;
  static jfieldID offset_fieldID;
  /// The maximum number of users be returned; up to 200.
  std::int32_t limit_;
  static jfieldID limit_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  getSupergroupMembers();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] supergroup_id_ Identifier of the supergroup or channel.
   * \param[in] filter_ The type of users to return. By default, supergroupMembersRecent.
   * \param[in] offset_ Number of users to skip.
   * \param[in] limit_ The maximum number of users be returned; up to 200.
   */
  getSupergroupMembers(std::int32_t supergroup_id_, object_ptr<SupergroupMembersFilter> &&filter_, std::int32_t offset_, std::int32_t limit_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1427643098;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<chatMembers>;

  static object_ptr<getSupergroupMembers> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Returns a user that can be contacted to get support.
 *
 * Returns object_ptr<User>.
 */
class getSupportUser final : public Function {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  getSupportUser();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1733497700;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<user>;

  static object_ptr<getSupportUser> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Returns information about the current temporary password.
 *
 * Returns object_ptr<TemporaryPasswordState>.
 */
class getTemporaryPasswordState final : public Function {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  getTemporaryPasswordState();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -12670830;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<temporaryPasswordState>;

  static object_ptr<getTemporaryPasswordState> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Returns all entities (mentions, hashtags, cashtags, bot commands, URLs, and email addresses) contained in the text. This is an offline method. Can be called before authorization. Can be called synchronously.
 *
 * Returns object_ptr<TextEntities>.
 */
class getTextEntities final : public Function {
 public:
  static jclass Class;
  /// The text in which to look for entites.
  std::string text_;
  static jfieldID text_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  getTextEntities();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] text_ The text in which to look for entites.
   */
  explicit getTextEntities(std::string const &text_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -341490693;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<textEntities>;

  static object_ptr<getTextEntities> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Returns a list of frequently used chats. Supported only if the chat info database is enabled.
 *
 * Returns object_ptr<Chats>.
 */
class getTopChats final : public Function {
 public:
  static jclass Class;
  /// Category of chats to be returned.
  object_ptr<TopChatCategory> category_;
  static jfieldID category_fieldID;
  /// Maximum number of chats to be returned; up to 30.
  std::int32_t limit_;
  static jfieldID limit_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  getTopChats();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] category_ Category of chats to be returned.
   * \param[in] limit_ Maximum number of chats to be returned; up to 30.
   */
  getTopChats(object_ptr<TopChatCategory> &&category_, std::int32_t limit_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -388410847;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<chats>;

  static object_ptr<getTopChats> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Returns a list of trending sticker sets.
 *
 * Returns object_ptr<StickerSets>.
 */
class getTrendingStickerSets final : public Function {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  getTrendingStickerSets();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1729129957;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<stickerSets>;

  static object_ptr<getTrendingStickerSets> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Returns information about a user by their identifier. This is an offline request if the current user is not a bot.
 *
 * Returns object_ptr<User>.
 */
class getUser final : public Function {
 public:
  static jclass Class;
  /// User identifier.
  std::int32_t user_id_;
  static jfieldID user_id_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  getUser();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] user_id_ User identifier.
   */
  explicit getUser(std::int32_t user_id_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -47586017;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<user>;

  static object_ptr<getUser> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Returns full information about a user by their identifier.
 *
 * Returns object_ptr<UserFullInfo>.
 */
class getUserFullInfo final : public Function {
 public:
  static jclass Class;
  /// User identifier.
  std::int32_t user_id_;
  static jfieldID user_id_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  getUserFullInfo();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] user_id_ User identifier.
   */
  explicit getUserFullInfo(std::int32_t user_id_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -655443263;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<userFullInfo>;

  static object_ptr<getUserFullInfo> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Returns the current privacy settings.
 *
 * Returns object_ptr<UserPrivacySettingRules>.
 */
class getUserPrivacySettingRules final : public Function {
 public:
  static jclass Class;
  /// The privacy setting.
  object_ptr<UserPrivacySetting> setting_;
  static jfieldID setting_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  getUserPrivacySettingRules();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] setting_ The privacy setting.
   */
  explicit getUserPrivacySettingRules(object_ptr<UserPrivacySetting> &&setting_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -2077223311;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<userPrivacySettingRules>;

  static object_ptr<getUserPrivacySettingRules> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Returns the profile photos of a user. The result of this query may be outdated: some photos might have been deleted already.
 *
 * Returns object_ptr<UserProfilePhotos>.
 */
class getUserProfilePhotos final : public Function {
 public:
  static jclass Class;
  /// User identifier.
  std::int32_t user_id_;
  static jfieldID user_id_fieldID;
  /// The number of photos to skip; must be non-negative.
  std::int32_t offset_;
  static jfieldID offset_fieldID;
  /// Maximum number of photos to be returned; up to 100.
  std::int32_t limit_;
  static jfieldID limit_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  getUserProfilePhotos();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] user_id_ User identifier.
   * \param[in] offset_ The number of photos to skip; must be non-negative.
   * \param[in] limit_ Maximum number of photos to be returned; up to 100.
   */
  getUserProfilePhotos(std::int32_t user_id_, std::int32_t offset_, std::int32_t limit_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -2062927433;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<userProfilePhotos>;

  static object_ptr<getUserProfilePhotos> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Returns background wallpapers.
 *
 * Returns object_ptr<Wallpapers>.
 */
class getWallpapers final : public Function {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  getWallpapers();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 2097518555;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<wallpapers>;

  static object_ptr<getWallpapers> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Returns an instant view version of a web page if available. Returns a 404 error if the web page has no instant view page.
 *
 * Returns object_ptr<WebPageInstantView>.
 */
class getWebPageInstantView final : public Function {
 public:
  static jclass Class;
  /// The web page URL.
  std::string url_;
  static jfieldID url_fieldID;
  /// If true, the full instant view for the web page will be returned.
  bool force_full_;
  static jfieldID force_full_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  getWebPageInstantView();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] url_ The web page URL.
   * \param[in] force_full_ If true, the full instant view for the web page will be returned.
   */
  getWebPageInstantView(std::string const &url_, bool force_full_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1962649975;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<webPageInstantView>;

  static object_ptr<getWebPageInstantView> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Returns a web page preview by the text of the message. Do not call this function too often. Returns a 404 error if the web page has no preview.
 *
 * Returns object_ptr<WebPage>.
 */
class getWebPagePreview final : public Function {
 public:
  static jclass Class;
  /// Message text with formatting.
  object_ptr<formattedText> text_;
  static jfieldID text_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  getWebPagePreview();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] text_ Message text with formatting.
   */
  explicit getWebPagePreview(object_ptr<formattedText> &&text_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 573441580;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<webPage>;

  static object_ptr<getWebPagePreview> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Adds new contacts or edits existing contacts; contacts' user identifiers are ignored.
 *
 * Returns object_ptr<ImportedContacts>.
 */
class importContacts final : public Function {
 public:
  static jclass Class;
  /// The list of contacts to import or edit, contact's vCard are ignored and are not imported.
  std::vector<object_ptr<contact>> contacts_;
  static jfieldID contacts_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  importContacts();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] contacts_ The list of contacts to import or edit, contact's vCard are ignored and are not imported.
   */
  explicit importContacts(std::vector<object_ptr<contact>> &&contacts_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -215132767;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<importedContacts>;

  static object_ptr<importContacts> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Adds current user as a new member to a chat. Private and secret chats can't be joined using this method.
 *
 * Returns object_ptr<Ok>.
 */
class joinChat final : public Function {
 public:
  static jclass Class;
  /// Chat identifier.
  std::int64_t chat_id_;
  static jfieldID chat_id_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  joinChat();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] chat_id_ Chat identifier.
   */
  explicit joinChat(std::int64_t chat_id_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 326769313;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<ok>;

  static object_ptr<joinChat> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Uses an invite link to add the current user to the chat if possible. The new member will not be added until the chat state has been synchronized with the server.
 *
 * Returns object_ptr<Chat>.
 */
class joinChatByInviteLink final : public Function {
 public:
  static jclass Class;
  /// Invite link to import; should begin with &quot;https://t.me/joinchat/&quot;, &quot;https://telegram.me/joinchat/&quot;, or &quot;https://telegram.dog/joinchat/&quot;.
  std::string invite_link_;
  static jfieldID invite_link_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  joinChatByInviteLink();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] invite_link_ Invite link to import; should begin with &quot;https://t.me/joinchat/&quot;, &quot;https://telegram.me/joinchat/&quot;, or &quot;https://telegram.dog/joinchat/&quot;.
   */
  explicit joinChatByInviteLink(std::string const &invite_link_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1049973882;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<chat>;

  static object_ptr<joinChatByInviteLink> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Removes current user from chat members. Private and secret chats can't be left using this method.
 *
 * Returns object_ptr<Ok>.
 */
class leaveChat final : public Function {
 public:
  static jclass Class;
  /// Chat identifier.
  std::int64_t chat_id_;
  static jfieldID chat_id_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  leaveChat();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] chat_id_ Chat identifier.
   */
  explicit leaveChat(std::int64_t chat_id_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1825080735;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<ok>;

  static object_ptr<leaveChat> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Closes the TDLib instance after a proper logout. Requires an available network connection. All local data will be destroyed. After the logout completes, updateAuthorizationState with authorizationStateClosed will be sent.
 *
 * Returns object_ptr<Ok>.
 */
class logOut final : public Function {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  logOut();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1581923301;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<ok>;

  static object_ptr<logOut> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * This method should be called if the chat is opened by the user. Many useful activities depend on the chat being opened or closed (e.g., in supergroups and channels all updates are received only for opened chats).
 *
 * Returns object_ptr<Ok>.
 */
class openChat final : public Function {
 public:
  static jclass Class;
  /// Chat identifier.
  std::int64_t chat_id_;
  static jfieldID chat_id_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  openChat();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] chat_id_ Chat identifier.
   */
  explicit openChat(std::int64_t chat_id_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -323371509;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<ok>;

  static object_ptr<openChat> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * This method should be called if the message content has been opened (e.g., the user has opened a photo, video, document, location or venue, or has listened to an audio file or voice note message). An updateMessageContentOpened update will be generated if something has changed.
 *
 * Returns object_ptr<Ok>.
 */
class openMessageContent final : public Function {
 public:
  static jclass Class;
  /// Chat identifier of the message.
  std::int64_t chat_id_;
  static jfieldID chat_id_fieldID;
  /// Identifier of the message with the opened content.
  std::int64_t message_id_;
  static jfieldID message_id_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  openMessageContent();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] chat_id_ Chat identifier of the message.
   * \param[in] message_id_ Identifier of the message with the opened content.
   */
  openMessageContent(std::int64_t chat_id_, std::int64_t message_id_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -739088005;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<ok>;

  static object_ptr<openMessageContent> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Optimizes storage usage, i.e. deletes some files and returns new storage usage statistics. Secret thumbnails can't be deleted.
 *
 * Returns object_ptr<StorageStatistics>.
 */
class optimizeStorage final : public Function {
 public:
  static jclass Class;
  /// Limit on the total size of files after deletion. Pass -1 to use the default limit.
  std::int64_t size_;
  static jfieldID size_fieldID;
  /// Limit on the time that has passed since the last time a file was accessed (or creation time for some filesystems). Pass -1 to use the default limit.
  std::int32_t ttl_;
  static jfieldID ttl_fieldID;
  /// Limit on the total count of files after deletion. Pass -1 to use the default limit.
  std::int32_t count_;
  static jfieldID count_fieldID;
  /// The amount of time after the creation of a file during which it can't be deleted, in seconds. Pass -1 to use the default value.
  std::int32_t immunity_delay_;
  static jfieldID immunity_delay_fieldID;
  /// If not empty, only files with the given type(s) are considered. By default, all types except thumbnails, profile photos, stickers and wallpapers are deleted.
  std::vector<object_ptr<FileType>> file_types_;
  static jfieldID file_types_fieldID;
  /// If not empty, only files from the given chats are considered. Use 0 as chat identifier to delete files not belonging to any chat (e.g., profile photos).
  std::vector<std::int64_t> chat_ids_;
  static jfieldID chat_ids_fieldID;
  /// If not empty, files from the given chats are excluded. Use 0 as chat identifier to exclude all files not belonging to any chat (e.g., profile photos).
  std::vector<std::int64_t> exclude_chat_ids_;
  static jfieldID exclude_chat_ids_fieldID;
  /// Same as in getStorageStatistics. Affects only returned statistics.
  std::int32_t chat_limit_;
  static jfieldID chat_limit_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  optimizeStorage();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] size_ Limit on the total size of files after deletion. Pass -1 to use the default limit.
   * \param[in] ttl_ Limit on the time that has passed since the last time a file was accessed (or creation time for some filesystems). Pass -1 to use the default limit.
   * \param[in] count_ Limit on the total count of files after deletion. Pass -1 to use the default limit.
   * \param[in] immunity_delay_ The amount of time after the creation of a file during which it can't be deleted, in seconds. Pass -1 to use the default value.
   * \param[in] file_types_ If not empty, only files with the given type(s) are considered. By default, all types except thumbnails, profile photos, stickers and wallpapers are deleted.
   * \param[in] chat_ids_ If not empty, only files from the given chats are considered. Use 0 as chat identifier to delete files not belonging to any chat (e.g., profile photos).
   * \param[in] exclude_chat_ids_ If not empty, files from the given chats are excluded. Use 0 as chat identifier to exclude all files not belonging to any chat (e.g., profile photos).
   * \param[in] chat_limit_ Same as in getStorageStatistics. Affects only returned statistics.
   */
  optimizeStorage(std::int64_t size_, std::int32_t ttl_, std::int32_t count_, std::int32_t immunity_delay_, std::vector<object_ptr<FileType>> &&file_types_, std::vector<std::int64_t> &&chat_ids_, std::vector<std::int64_t> &&exclude_chat_ids_, std::int32_t chat_limit_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 980397489;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<storageStatistics>;

  static object_ptr<optimizeStorage> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Parses Bold, Italic, Code, Pre, PreCode and TextUrl entities contained in the text. This is an offline method. Can be called before authorization. Can be called synchronously.
 *
 * Returns object_ptr<FormattedText>.
 */
class parseTextEntities final : public Function {
 public:
  static jclass Class;
  /// The text which should be parsed.
  std::string text_;
  static jfieldID text_fieldID;
  /// Text parse mode.
  object_ptr<TextParseMode> parse_mode_;
  static jfieldID parse_mode_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  parseTextEntities();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] text_ The text which should be parsed.
   * \param[in] parse_mode_ Text parse mode.
   */
  parseTextEntities(std::string const &text_, object_ptr<TextParseMode> &&parse_mode_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1709194593;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<formattedText>;

  static object_ptr<parseTextEntities> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Pins a message in a supergroup or channel; requires appropriate administrator rights in the supergroup or channel.
 *
 * Returns object_ptr<Ok>.
 */
class pinSupergroupMessage final : public Function {
 public:
  static jclass Class;
  /// Identifier of the supergroup or channel.
  std::int32_t supergroup_id_;
  static jfieldID supergroup_id_fieldID;
  /// Identifier of the new pinned message.
  std::int64_t message_id_;
  static jfieldID message_id_fieldID;
  /// True, if there should be no notification about the pinned message.
  bool disable_notification_;
  static jfieldID disable_notification_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  pinSupergroupMessage();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] supergroup_id_ Identifier of the supergroup or channel.
   * \param[in] message_id_ Identifier of the new pinned message.
   * \param[in] disable_notification_ True, if there should be no notification about the pinned message.
   */
  pinSupergroupMessage(std::int32_t supergroup_id_, std::int64_t message_id_, bool disable_notification_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1141187557;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<ok>;

  static object_ptr<pinSupergroupMessage> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Computes time needed to receive a response from a Telegram server through a proxy. Can be called before authorization.
 *
 * Returns object_ptr<Seconds>.
 */
class pingProxy final : public Function {
 public:
  static jclass Class;
  /// Proxy identifier. Use 0 to ping a Telegram server without a proxy.
  std::int32_t proxy_id_;
  static jfieldID proxy_id_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  pingProxy();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] proxy_id_ Proxy identifier. Use 0 to ping a Telegram server without a proxy.
   */
  explicit pingProxy(std::int32_t proxy_id_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -979681103;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<seconds>;

  static object_ptr<pingProxy> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Handles a DC_UPDATE push service notification. Can be called before authorization.
 *
 * Returns object_ptr<Ok>.
 */
class processDcUpdate final : public Function {
 public:
  static jclass Class;
  /// Value of the &quot;dc&quot; parameter of the notification.
  std::string dc_;
  static jfieldID dc_fieldID;
  /// Value of the &quot;addr&quot; parameter of the notification.
  std::string addr_;
  static jfieldID addr_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  processDcUpdate();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] dc_ Value of the &quot;dc&quot; parameter of the notification.
   * \param[in] addr_ Value of the &quot;addr&quot; parameter of the notification.
   */
  processDcUpdate(std::string const &dc_, std::string const &addr_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1806562997;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<ok>;

  static object_ptr<processDcUpdate> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Marks all mentions in a chat as read.
 *
 * Returns object_ptr<Ok>.
 */
class readAllChatMentions final : public Function {
 public:
  static jclass Class;
  /// Chat identifier.
  std::int64_t chat_id_;
  static jfieldID chat_id_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  readAllChatMentions();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] chat_id_ Chat identifier.
   */
  explicit readAllChatMentions(std::int64_t chat_id_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1357558453;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<ok>;

  static object_ptr<readAllChatMentions> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Recovers the password with a password recovery code sent to an email address that was previously set up. Works only when the current authorization state is authorizationStateWaitPassword.
 *
 * Returns object_ptr<Ok>.
 */
class recoverAuthenticationPassword final : public Function {
 public:
  static jclass Class;
  /// Recovery code to check.
  std::string recovery_code_;
  static jfieldID recovery_code_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  recoverAuthenticationPassword();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] recovery_code_ Recovery code to check.
   */
  explicit recoverAuthenticationPassword(std::string const &recovery_code_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 787436412;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<ok>;

  static object_ptr<recoverAuthenticationPassword> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Recovers the password using a recovery code sent to an email address that was previously set up.
 *
 * Returns object_ptr<PasswordState>.
 */
class recoverPassword final : public Function {
 public:
  static jclass Class;
  /// Recovery code to check.
  std::string recovery_code_;
  static jfieldID recovery_code_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  recoverPassword();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] recovery_code_ Recovery code to check.
   */
  explicit recoverPassword(std::string const &recovery_code_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1660185903;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<passwordState>;

  static object_ptr<recoverPassword> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Registers the currently used device for receiving push notifications.
 *
 * Returns object_ptr<Ok>.
 */
class registerDevice final : public Function {
 public:
  static jclass Class;
  /// Device token.
  object_ptr<DeviceToken> device_token_;
  static jfieldID device_token_fieldID;
  /// List of at most 100 user identifiers of other users currently using the client.
  std::vector<std::int32_t> other_user_ids_;
  static jfieldID other_user_ids_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  registerDevice();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] device_token_ Device token.
   * \param[in] other_user_ids_ List of at most 100 user identifiers of other users currently using the client.
   */
  registerDevice(object_ptr<DeviceToken> &&device_token_, std::vector<std::int32_t> &&other_user_ids_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -413637293;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<ok>;

  static object_ptr<registerDevice> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Removes users from the contacts list.
 *
 * Returns object_ptr<Ok>.
 */
class removeContacts final : public Function {
 public:
  static jclass Class;
  /// Identifiers of users to be deleted.
  std::vector<std::int32_t> user_ids_;
  static jfieldID user_ids_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  removeContacts();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] user_ids_ Identifiers of users to be deleted.
   */
  explicit removeContacts(std::vector<std::int32_t> &&user_ids_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -615510759;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<ok>;

  static object_ptr<removeContacts> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Removes a sticker from the list of favorite stickers.
 *
 * Returns object_ptr<Ok>.
 */
class removeFavoriteSticker final : public Function {
 public:
  static jclass Class;
  /// Sticker file to delete from the list.
  object_ptr<InputFile> sticker_;
  static jfieldID sticker_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  removeFavoriteSticker();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] sticker_ Sticker file to delete from the list.
   */
  explicit removeFavoriteSticker(object_ptr<InputFile> &&sticker_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1152945264;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<ok>;

  static object_ptr<removeFavoriteSticker> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Removes a proxy server. Can be called before authorization.
 *
 * Returns object_ptr<Ok>.
 */
class removeProxy final : public Function {
 public:
  static jclass Class;
  /// Proxy identifier.
  std::int32_t proxy_id_;
  static jfieldID proxy_id_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  removeProxy();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] proxy_id_ Proxy identifier.
   */
  explicit removeProxy(std::int32_t proxy_id_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1369219847;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<ok>;

  static object_ptr<removeProxy> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Removes a hashtag from the list of recently used hashtags.
 *
 * Returns object_ptr<Ok>.
 */
class removeRecentHashtag final : public Function {
 public:
  static jclass Class;
  /// Hashtag to delete.
  std::string hashtag_;
  static jfieldID hashtag_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  removeRecentHashtag();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] hashtag_ Hashtag to delete.
   */
  explicit removeRecentHashtag(std::string const &hashtag_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1013735260;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<ok>;

  static object_ptr<removeRecentHashtag> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Removes a sticker from the list of recently used stickers.
 *
 * Returns object_ptr<Ok>.
 */
class removeRecentSticker final : public Function {
 public:
  static jclass Class;
  /// Pass true to remove the sticker from the list of stickers recently attached to photo or video files; pass false to remove the sticker from the list of recently sent stickers.
  bool is_attached_;
  static jfieldID is_attached_fieldID;
  /// Sticker file to delete.
  object_ptr<InputFile> sticker_;
  static jfieldID sticker_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  removeRecentSticker();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] is_attached_ Pass true to remove the sticker from the list of stickers recently attached to photo or video files; pass false to remove the sticker from the list of recently sent stickers.
   * \param[in] sticker_ Sticker file to delete.
   */
  removeRecentSticker(bool is_attached_, object_ptr<InputFile> &&sticker_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1246577677;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<ok>;

  static object_ptr<removeRecentSticker> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Removes a chat from the list of recently found chats.
 *
 * Returns object_ptr<Ok>.
 */
class removeRecentlyFoundChat final : public Function {
 public:
  static jclass Class;
  /// Identifier of the chat to be removed.
  std::int64_t chat_id_;
  static jfieldID chat_id_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  removeRecentlyFoundChat();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] chat_id_ Identifier of the chat to be removed.
   */
  explicit removeRecentlyFoundChat(std::int64_t chat_id_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 717340444;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<ok>;

  static object_ptr<removeRecentlyFoundChat> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Removes an animation from the list of saved animations.
 *
 * Returns object_ptr<Ok>.
 */
class removeSavedAnimation final : public Function {
 public:
  static jclass Class;
  /// Animation file to be removed.
  object_ptr<InputFile> animation_;
  static jfieldID animation_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  removeSavedAnimation();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] animation_ Animation file to be removed.
   */
  explicit removeSavedAnimation(object_ptr<InputFile> &&animation_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -495605479;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<ok>;

  static object_ptr<removeSavedAnimation> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Removes a sticker from the set to which it belongs; for bots only. The sticker set must have been created by the bot.
 *
 * Returns object_ptr<Ok>.
 */
class removeStickerFromSet final : public Function {
 public:
  static jclass Class;
  /// Sticker.
  object_ptr<InputFile> sticker_;
  static jfieldID sticker_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  removeStickerFromSet();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] sticker_ Sticker.
   */
  explicit removeStickerFromSet(object_ptr<InputFile> &&sticker_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1642196644;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<ok>;

  static object_ptr<removeStickerFromSet> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Removes a chat from the list of frequently used chats. Supported only if the chat info database is enabled.
 *
 * Returns object_ptr<Ok>.
 */
class removeTopChat final : public Function {
 public:
  static jclass Class;
  /// Category of frequently used chats.
  object_ptr<TopChatCategory> category_;
  static jfieldID category_fieldID;
  /// Chat identifier.
  std::int64_t chat_id_;
  static jfieldID chat_id_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  removeTopChat();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] category_ Category of frequently used chats.
   * \param[in] chat_id_ Chat identifier.
   */
  removeTopChat(object_ptr<TopChatCategory> &&category_, std::int64_t chat_id_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1907876267;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<ok>;

  static object_ptr<removeTopChat> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Changes the order of installed sticker sets.
 *
 * Returns object_ptr<Ok>.
 */
class reorderInstalledStickerSets final : public Function {
 public:
  static jclass Class;
  /// Pass true to change the order of mask sticker sets; pass false to change the order of ordinary sticker sets.
  bool is_masks_;
  static jfieldID is_masks_fieldID;
  /// Identifiers of installed sticker sets in the new correct order.
  std::vector<std::int64_t> sticker_set_ids_;
  static jfieldID sticker_set_ids_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  reorderInstalledStickerSets();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] is_masks_ Pass true to change the order of mask sticker sets; pass false to change the order of ordinary sticker sets.
   * \param[in] sticker_set_ids_ Identifiers of installed sticker sets in the new correct order.
   */
  reorderInstalledStickerSets(bool is_masks_, std::vector<std::int64_t> &&sticker_set_ids_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1114537563;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<ok>;

  static object_ptr<reorderInstalledStickerSets> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Reports a chat to the Telegram moderators. Supported only for supergroups, channels, or private chats with bots, since other chats can't be checked by moderators.
 *
 * Returns object_ptr<Ok>.
 */
class reportChat final : public Function {
 public:
  static jclass Class;
  /// Chat identifier.
  std::int64_t chat_id_;
  static jfieldID chat_id_fieldID;
  /// The reason for reporting the chat.
  object_ptr<ChatReportReason> reason_;
  static jfieldID reason_fieldID;
  /// Identifiers of reported messages, if any.
  std::vector<std::int64_t> message_ids_;
  static jfieldID message_ids_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  reportChat();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] chat_id_ Chat identifier.
   * \param[in] reason_ The reason for reporting the chat.
   * \param[in] message_ids_ Identifiers of reported messages, if any.
   */
  reportChat(std::int64_t chat_id_, object_ptr<ChatReportReason> &&reason_, std::vector<std::int64_t> &&message_ids_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -312579772;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<ok>;

  static object_ptr<reportChat> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Reports some messages from a user in a supergroup as spam; requires administrator rights in the supergroup.
 *
 * Returns object_ptr<Ok>.
 */
class reportSupergroupSpam final : public Function {
 public:
  static jclass Class;
  /// Supergroup identifier.
  std::int32_t supergroup_id_;
  static jfieldID supergroup_id_fieldID;
  /// User identifier.
  std::int32_t user_id_;
  static jfieldID user_id_fieldID;
  /// Identifiers of messages sent in the supergroup by the user. This list must be non-empty.
  std::vector<std::int64_t> message_ids_;
  static jfieldID message_ids_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  reportSupergroupSpam();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] supergroup_id_ Supergroup identifier.
   * \param[in] user_id_ User identifier.
   * \param[in] message_ids_ Identifiers of messages sent in the supergroup by the user. This list must be non-empty.
   */
  reportSupergroupSpam(std::int32_t supergroup_id_, std::int32_t user_id_, std::vector<std::int64_t> &&message_ids_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -2125451498;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<ok>;

  static object_ptr<reportSupergroupSpam> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Requests to send a password recovery code to an email address that was previously set up. Works only when the current authorization state is authorizationStateWaitPassword.
 *
 * Returns object_ptr<Ok>.
 */
class requestAuthenticationPasswordRecovery final : public Function {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  requestAuthenticationPasswordRecovery();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1393896118;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<ok>;

  static object_ptr<requestAuthenticationPasswordRecovery> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Requests to send a password recovery code to an email address that was previously set up.
 *
 * Returns object_ptr<EmailAddressAuthenticationCodeInfo>.
 */
class requestPasswordRecovery final : public Function {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  requestPasswordRecovery();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -13777582;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<emailAddressAuthenticationCodeInfo>;

  static object_ptr<requestPasswordRecovery> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Re-sends an authentication code to the user. Works only when the current authorization state is authorizationStateWaitCode and the next_code_type of the result is not null.
 *
 * Returns object_ptr<Ok>.
 */
class resendAuthenticationCode final : public Function {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  resendAuthenticationCode();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -814377191;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<ok>;

  static object_ptr<resendAuthenticationCode> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Re-sends the authentication code sent to confirm a new phone number for the user. Works only if the previously received authenticationCodeInfo next_code_type was not null.
 *
 * Returns object_ptr<AuthenticationCodeInfo>.
 */
class resendChangePhoneNumberCode final : public Function {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  resendChangePhoneNumberCode();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -786772060;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<authenticationCodeInfo>;

  static object_ptr<resendChangePhoneNumberCode> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Re-sends the code to verify an email address to be added to a user's Telegram Passport.
 *
 * Returns object_ptr<EmailAddressAuthenticationCodeInfo>.
 */
class resendEmailAddressVerificationCode final : public Function {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  resendEmailAddressVerificationCode();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1872416732;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<emailAddressAuthenticationCodeInfo>;

  static object_ptr<resendEmailAddressVerificationCode> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Resends phone number confirmation code.
 *
 * Returns object_ptr<AuthenticationCodeInfo>.
 */
class resendPhoneNumberConfirmationCode final : public Function {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  resendPhoneNumberConfirmationCode();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 2069068522;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<authenticationCodeInfo>;

  static object_ptr<resendPhoneNumberConfirmationCode> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Re-sends the code to verify a phone number to be added to a user's Telegram Passport.
 *
 * Returns object_ptr<AuthenticationCodeInfo>.
 */
class resendPhoneNumberVerificationCode final : public Function {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  resendPhoneNumberVerificationCode();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1367629820;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<authenticationCodeInfo>;

  static object_ptr<resendPhoneNumberVerificationCode> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Resets all notification settings to their default values. By default, all chats are unmuted, the sound is set to &quot;default&quot; and message previews are shown.
 *
 * Returns object_ptr<Ok>.
 */
class resetAllNotificationSettings final : public Function {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  resetAllNotificationSettings();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -174020359;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<ok>;

  static object_ptr<resetAllNotificationSettings> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Resets all network data usage statistics to zero. Can be called before authorization.
 *
 * Returns object_ptr<Ok>.
 */
class resetNetworkStatistics final : public Function {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  resetNetworkStatistics();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1646452102;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<ok>;

  static object_ptr<resetNetworkStatistics> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Searches for call messages. Returns the results in reverse chronological order (i. e., in order of decreasing message_id). For optimal performance the number of returned messages is chosen by the library.
 *
 * Returns object_ptr<Messages>.
 */
class searchCallMessages final : public Function {
 public:
  static jclass Class;
  /// Identifier of the message from which to search; use 0 to get results from the last message.
  std::int64_t from_message_id_;
  static jfieldID from_message_id_fieldID;
  /// The maximum number of messages to be returned; up to 100. Fewer messages may be returned than specified by the limit, even if the end of the message history has not been reached.
  std::int32_t limit_;
  static jfieldID limit_fieldID;
  /// If true, returns only messages with missed calls.
  bool only_missed_;
  static jfieldID only_missed_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  searchCallMessages();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] from_message_id_ Identifier of the message from which to search; use 0 to get results from the last message.
   * \param[in] limit_ The maximum number of messages to be returned; up to 100. Fewer messages may be returned than specified by the limit, even if the end of the message history has not been reached.
   * \param[in] only_missed_ If true, returns only messages with missed calls.
   */
  searchCallMessages(std::int64_t from_message_id_, std::int32_t limit_, bool only_missed_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1077230820;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<messages>;

  static object_ptr<searchCallMessages> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Searches for a specified query in the first name, last name and username of the members of a specified chat. Requires administrator rights in channels.
 *
 * Returns object_ptr<ChatMembers>.
 */
class searchChatMembers final : public Function {
 public:
  static jclass Class;
  /// Chat identifier.
  std::int64_t chat_id_;
  static jfieldID chat_id_fieldID;
  /// Query to search for.
  std::string query_;
  static jfieldID query_fieldID;
  /// The maximum number of users to be returned.
  std::int32_t limit_;
  static jfieldID limit_fieldID;
  /// The type of users to return. By default, chatMembersFilterMembers.
  object_ptr<ChatMembersFilter> filter_;
  static jfieldID filter_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  searchChatMembers();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] chat_id_ Chat identifier.
   * \param[in] query_ Query to search for.
   * \param[in] limit_ The maximum number of users to be returned.
   * \param[in] filter_ The type of users to return. By default, chatMembersFilterMembers.
   */
  searchChatMembers(std::int64_t chat_id_, std::string const &query_, std::int32_t limit_, object_ptr<ChatMembersFilter> &&filter_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -445823291;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<chatMembers>;

  static object_ptr<searchChatMembers> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Searches for messages with given words in the chat. Returns the results in reverse chronological order, i.e. in order of decreasing message_id. Cannot be used in secret chats with a non-empty query (searchSecretMessages should be used instead), or without an enabled message database. For optimal performance the number of returned messages is chosen by the library.
 *
 * Returns object_ptr<Messages>.
 */
class searchChatMessages final : public Function {
 public:
  static jclass Class;
  /// Identifier of the chat in which to search messages.
  std::int64_t chat_id_;
  static jfieldID chat_id_fieldID;
  /// Query to search for.
  std::string query_;
  static jfieldID query_fieldID;
  /// If not 0, only messages sent by the specified user will be returned. Not supported in secret chats.
  std::int32_t sender_user_id_;
  static jfieldID sender_user_id_fieldID;
  /// Identifier of the message starting from which history must be fetched; use 0 to get results from the last message.
  std::int64_t from_message_id_;
  static jfieldID from_message_id_fieldID;
  /// Specify 0 to get results from exactly the from_message_id or a negative offset to get the specified message and some newer messages.
  std::int32_t offset_;
  static jfieldID offset_fieldID;
  /// The maximum number of messages to be returned; must be positive and can't be greater than 100. If the offset is negative, the limit must be greater than -offset. Fewer messages may be returned than specified by the limit, even if the end of the message history has not been reached.
  std::int32_t limit_;
  static jfieldID limit_fieldID;
  /// Filter for message content in the search results.
  object_ptr<SearchMessagesFilter> filter_;
  static jfieldID filter_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  searchChatMessages();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] chat_id_ Identifier of the chat in which to search messages.
   * \param[in] query_ Query to search for.
   * \param[in] sender_user_id_ If not 0, only messages sent by the specified user will be returned. Not supported in secret chats.
   * \param[in] from_message_id_ Identifier of the message starting from which history must be fetched; use 0 to get results from the last message.
   * \param[in] offset_ Specify 0 to get results from exactly the from_message_id or a negative offset to get the specified message and some newer messages.
   * \param[in] limit_ The maximum number of messages to be returned; must be positive and can't be greater than 100. If the offset is negative, the limit must be greater than -offset. Fewer messages may be returned than specified by the limit, even if the end of the message history has not been reached.
   * \param[in] filter_ Filter for message content in the search results.
   */
  searchChatMessages(std::int64_t chat_id_, std::string const &query_, std::int32_t sender_user_id_, std::int64_t from_message_id_, std::int32_t offset_, std::int32_t limit_, object_ptr<SearchMessagesFilter> &&filter_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1528846671;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<messages>;

  static object_ptr<searchChatMessages> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Returns information about the recent locations of chat members that were sent to the chat. Returns up to 1 location message per user.
 *
 * Returns object_ptr<Messages>.
 */
class searchChatRecentLocationMessages final : public Function {
 public:
  static jclass Class;
  /// Chat identifier.
  std::int64_t chat_id_;
  static jfieldID chat_id_fieldID;
  /// Maximum number of messages to be returned.
  std::int32_t limit_;
  static jfieldID limit_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  searchChatRecentLocationMessages();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] chat_id_ Chat identifier.
   * \param[in] limit_ Maximum number of messages to be returned.
   */
  searchChatRecentLocationMessages(std::int64_t chat_id_, std::int32_t limit_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 950238950;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<messages>;

  static object_ptr<searchChatRecentLocationMessages> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Searches for the specified query in the title and username of already known chats, this is an offline request. Returns chats in the order seen in the chat list.
 *
 * Returns object_ptr<Chats>.
 */
class searchChats final : public Function {
 public:
  static jclass Class;
  /// Query to search for. If the query is empty, returns up to 20 recently found chats.
  std::string query_;
  static jfieldID query_fieldID;
  /// Maximum number of chats to be returned.
  std::int32_t limit_;
  static jfieldID limit_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  searchChats();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] query_ Query to search for. If the query is empty, returns up to 20 recently found chats.
   * \param[in] limit_ Maximum number of chats to be returned.
   */
  searchChats(std::string const &query_, std::int32_t limit_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1879787060;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<chats>;

  static object_ptr<searchChats> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Searches for the specified query in the title and username of already known chats via request to the server. Returns chats in the order seen in the chat list.
 *
 * Returns object_ptr<Chats>.
 */
class searchChatsOnServer final : public Function {
 public:
  static jclass Class;
  /// Query to search for.
  std::string query_;
  static jfieldID query_fieldID;
  /// Maximum number of chats to be returned.
  std::int32_t limit_;
  static jfieldID limit_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  searchChatsOnServer();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] query_ Query to search for.
   * \param[in] limit_ Maximum number of chats to be returned.
   */
  searchChatsOnServer(std::string const &query_, std::int32_t limit_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1158402188;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<chats>;

  static object_ptr<searchChatsOnServer> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Searches for the specified query in the first names, last names and usernames of the known user contacts.
 *
 * Returns object_ptr<Users>.
 */
class searchContacts final : public Function {
 public:
  static jclass Class;
  /// Query to search for; can be empty to return all contacts.
  std::string query_;
  static jfieldID query_fieldID;
  /// Maximum number of users to be returned.
  std::int32_t limit_;
  static jfieldID limit_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  searchContacts();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] query_ Query to search for; can be empty to return all contacts.
   * \param[in] limit_ Maximum number of users to be returned.
   */
  searchContacts(std::string const &query_, std::int32_t limit_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1794690715;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<users>;

  static object_ptr<searchContacts> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Searches for recently used hashtags by their prefix.
 *
 * Returns object_ptr<Hashtags>.
 */
class searchHashtags final : public Function {
 public:
  static jclass Class;
  /// Hashtag prefix to search for.
  std::string prefix_;
  static jfieldID prefix_fieldID;
  /// Maximum number of hashtags to be returned.
  std::int32_t limit_;
  static jfieldID limit_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  searchHashtags();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] prefix_ Hashtag prefix to search for.
   * \param[in] limit_ Maximum number of hashtags to be returned.
   */
  searchHashtags(std::string const &prefix_, std::int32_t limit_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1043637617;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<hashtags>;

  static object_ptr<searchHashtags> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Searches for installed sticker sets by looking for specified query in their title and name.
 *
 * Returns object_ptr<StickerSets>.
 */
class searchInstalledStickerSets final : public Function {
 public:
  static jclass Class;
  /// Pass true to return mask sticker sets; pass false to return ordinary sticker sets.
  bool is_masks_;
  static jfieldID is_masks_fieldID;
  /// Query to search for.
  std::string query_;
  static jfieldID query_fieldID;
  /// Maximum number of sticker sets to return.
  std::int32_t limit_;
  static jfieldID limit_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  searchInstalledStickerSets();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] is_masks_ Pass true to return mask sticker sets; pass false to return ordinary sticker sets.
   * \param[in] query_ Query to search for.
   * \param[in] limit_ Maximum number of sticker sets to return.
   */
  searchInstalledStickerSets(bool is_masks_, std::string const &query_, std::int32_t limit_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 681171344;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<stickerSets>;

  static object_ptr<searchInstalledStickerSets> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Searches for messages in all chats except secret chats. Returns the results in reverse chronological order (i.e., in order of decreasing (date, chat_id, message_id)). For optimal performance the number of returned messages is chosen by the library.
 *
 * Returns object_ptr<Messages>.
 */
class searchMessages final : public Function {
 public:
  static jclass Class;
  /// Query to search for.
  std::string query_;
  static jfieldID query_fieldID;
  /// The date of the message starting from which the results should be fetched. Use 0 or any date in the future to get results from the last message.
  std::int32_t offset_date_;
  static jfieldID offset_date_fieldID;
  /// The chat identifier of the last found message, or 0 for the first request.
  std::int64_t offset_chat_id_;
  static jfieldID offset_chat_id_fieldID;
  /// The message identifier of the last found message, or 0 for the first request.
  std::int64_t offset_message_id_;
  static jfieldID offset_message_id_fieldID;
  /// The maximum number of messages to be returned, up to 100. Fewer messages may be returned than specified by the limit, even if the end of the message history has not been reached.
  std::int32_t limit_;
  static jfieldID limit_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  searchMessages();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] query_ Query to search for.
   * \param[in] offset_date_ The date of the message starting from which the results should be fetched. Use 0 or any date in the future to get results from the last message.
   * \param[in] offset_chat_id_ The chat identifier of the last found message, or 0 for the first request.
   * \param[in] offset_message_id_ The message identifier of the last found message, or 0 for the first request.
   * \param[in] limit_ The maximum number of messages to be returned, up to 100. Fewer messages may be returned than specified by the limit, even if the end of the message history has not been reached.
   */
  searchMessages(std::string const &query_, std::int32_t offset_date_, std::int64_t offset_chat_id_, std::int64_t offset_message_id_, std::int32_t limit_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1579305146;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<messages>;

  static object_ptr<searchMessages> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Searches a public chat by its username. Currently only private chats, supergroups and channels can be public. Returns the chat if found; otherwise an error is returned.
 *
 * Returns object_ptr<Chat>.
 */
class searchPublicChat final : public Function {
 public:
  static jclass Class;
  /// Username to be resolved.
  std::string username_;
  static jfieldID username_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  searchPublicChat();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] username_ Username to be resolved.
   */
  explicit searchPublicChat(std::string const &username_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 857135533;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<chat>;

  static object_ptr<searchPublicChat> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Searches public chats by looking for specified query in their username and title. Currently only private chats, supergroups and channels can be public. Returns a meaningful number of results. Returns nothing if the length of the searched username prefix is less than 5. Excludes private chats with contacts and chats from the chat list from the results.
 *
 * Returns object_ptr<Chats>.
 */
class searchPublicChats final : public Function {
 public:
  static jclass Class;
  /// Query to search for.
  std::string query_;
  static jfieldID query_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  searchPublicChats();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] query_ Query to search for.
   */
  explicit searchPublicChats(std::string const &query_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 970385337;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<chats>;

  static object_ptr<searchPublicChats> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Searches for messages in secret chats. Returns the results in reverse chronological order. For optimal performance the number of returned messages is chosen by the library.
 *
 * Returns object_ptr<FoundMessages>.
 */
class searchSecretMessages final : public Function {
 public:
  static jclass Class;
  /// Identifier of the chat in which to search. Specify 0 to search in all secret chats.
  std::int64_t chat_id_;
  static jfieldID chat_id_fieldID;
  /// Query to search for. If empty, searchChatMessages should be used instead.
  std::string query_;
  static jfieldID query_fieldID;
  /// The identifier from the result of a previous request, use 0 to get results from the last message.
  std::int64_t from_search_id_;
  static jfieldID from_search_id_fieldID;
  /// Maximum number of messages to be returned; up to 100. Fewer messages may be returned than specified by the limit, even if the end of the message history has not been reached.
  std::int32_t limit_;
  static jfieldID limit_fieldID;
  /// A filter for the content of messages in the search results.
  object_ptr<SearchMessagesFilter> filter_;
  static jfieldID filter_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  searchSecretMessages();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] chat_id_ Identifier of the chat in which to search. Specify 0 to search in all secret chats.
   * \param[in] query_ Query to search for. If empty, searchChatMessages should be used instead.
   * \param[in] from_search_id_ The identifier from the result of a previous request, use 0 to get results from the last message.
   * \param[in] limit_ Maximum number of messages to be returned; up to 100. Fewer messages may be returned than specified by the limit, even if the end of the message history has not been reached.
   * \param[in] filter_ A filter for the content of messages in the search results.
   */
  searchSecretMessages(std::int64_t chat_id_, std::string const &query_, std::int64_t from_search_id_, std::int32_t limit_, object_ptr<SearchMessagesFilter> &&filter_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1670627915;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<foundMessages>;

  static object_ptr<searchSecretMessages> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Searches for a sticker set by its name.
 *
 * Returns object_ptr<StickerSet>.
 */
class searchStickerSet final : public Function {
 public:
  static jclass Class;
  /// Name of the sticker set.
  std::string name_;
  static jfieldID name_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  searchStickerSet();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] name_ Name of the sticker set.
   */
  explicit searchStickerSet(std::string const &name_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1157930222;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<stickerSet>;

  static object_ptr<searchStickerSet> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Searches for ordinary sticker sets by looking for specified query in their title and name. Excludes installed sticker sets from the results.
 *
 * Returns object_ptr<StickerSets>.
 */
class searchStickerSets final : public Function {
 public:
  static jclass Class;
  /// Query to search for.
  std::string query_;
  static jfieldID query_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  searchStickerSets();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] query_ Query to search for.
   */
  explicit searchStickerSets(std::string const &query_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1082314629;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<stickerSets>;

  static object_ptr<searchStickerSets> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Searches for stickers from public sticker sets that correspond to a given emoji.
 *
 * Returns object_ptr<Stickers>.
 */
class searchStickers final : public Function {
 public:
  static jclass Class;
  /// String representation of emoji; must be non-empty.
  std::string emoji_;
  static jfieldID emoji_fieldID;
  /// Maximum number of stickers to be returned.
  std::int32_t limit_;
  static jfieldID limit_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  searchStickers();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] emoji_ String representation of emoji; must be non-empty.
   * \param[in] limit_ Maximum number of stickers to be returned.
   */
  searchStickers(std::string const &emoji_, std::int32_t limit_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1555771203;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<stickers>;

  static object_ptr<searchStickers> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Invites a bot to a chat (if it is not yet a member) and sends it the /start command. Bots can't be invited to a private chat other than the chat with the bot. Bots can't be invited to channels (although they can be added as admins) and secret chats. Returns the sent message.
 *
 * Returns object_ptr<Message>.
 */
class sendBotStartMessage final : public Function {
 public:
  static jclass Class;
  /// Identifier of the bot.
  std::int32_t bot_user_id_;
  static jfieldID bot_user_id_fieldID;
  /// Identifier of the target chat.
  std::int64_t chat_id_;
  static jfieldID chat_id_fieldID;
  /// A hidden parameter sent to the bot for deep linking purposes (https://api.telegram.org/bots\#deep-linking).
  std::string parameter_;
  static jfieldID parameter_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  sendBotStartMessage();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] bot_user_id_ Identifier of the bot.
   * \param[in] chat_id_ Identifier of the target chat.
   * \param[in] parameter_ A hidden parameter sent to the bot for deep linking purposes (https://api.telegram.org/bots\#deep-linking).
   */
  sendBotStartMessage(std::int32_t bot_user_id_, std::int64_t chat_id_, std::string const &parameter_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1112181339;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<message>;

  static object_ptr<sendBotStartMessage> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Sends debug information for a call.
 *
 * Returns object_ptr<Ok>.
 */
class sendCallDebugInformation final : public Function {
 public:
  static jclass Class;
  /// Call identifier.
  std::int32_t call_id_;
  static jfieldID call_id_fieldID;
  /// Debug information in application-specific format.
  std::string debug_information_;
  static jfieldID debug_information_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  sendCallDebugInformation();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] call_id_ Call identifier.
   * \param[in] debug_information_ Debug information in application-specific format.
   */
  sendCallDebugInformation(std::int32_t call_id_, std::string const &debug_information_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 2019243839;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<ok>;

  static object_ptr<sendCallDebugInformation> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Sends a call rating.
 *
 * Returns object_ptr<Ok>.
 */
class sendCallRating final : public Function {
 public:
  static jclass Class;
  /// Call identifier.
  std::int32_t call_id_;
  static jfieldID call_id_fieldID;
  /// Call rating; 1-5.
  std::int32_t rating_;
  static jfieldID rating_fieldID;
  /// An optional user comment if the rating is less than 5.
  std::string comment_;
  static jfieldID comment_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  sendCallRating();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] call_id_ Call identifier.
   * \param[in] rating_ Call rating; 1-5.
   * \param[in] comment_ An optional user comment if the rating is less than 5.
   */
  sendCallRating(std::int32_t call_id_, std::int32_t rating_, std::string const &comment_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 243075146;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<ok>;

  static object_ptr<sendCallRating> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Sends a notification about user activity in a chat.
 *
 * Returns object_ptr<Ok>.
 */
class sendChatAction final : public Function {
 public:
  static jclass Class;
  /// Chat identifier.
  std::int64_t chat_id_;
  static jfieldID chat_id_fieldID;
  /// The action description.
  object_ptr<ChatAction> action_;
  static jfieldID action_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  sendChatAction();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] chat_id_ Chat identifier.
   * \param[in] action_ The action description.
   */
  sendChatAction(std::int64_t chat_id_, object_ptr<ChatAction> &&action_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -841357536;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<ok>;

  static object_ptr<sendChatAction> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Sends a notification about a screenshot taken in a chat. Supported only in private and secret chats.
 *
 * Returns object_ptr<Ok>.
 */
class sendChatScreenshotTakenNotification final : public Function {
 public:
  static jclass Class;
  /// Chat identifier.
  std::int64_t chat_id_;
  static jfieldID chat_id_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  sendChatScreenshotTakenNotification();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] chat_id_ Chat identifier.
   */
  explicit sendChatScreenshotTakenNotification(std::int64_t chat_id_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 448399457;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<ok>;

  static object_ptr<sendChatScreenshotTakenNotification> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Changes the current TTL setting (sets a new self-destruct timer) in a secret chat and sends the corresponding message.
 *
 * Returns object_ptr<Message>.
 */
class sendChatSetTtlMessage final : public Function {
 public:
  static jclass Class;
  /// Chat identifier.
  std::int64_t chat_id_;
  static jfieldID chat_id_fieldID;
  /// New TTL value, in seconds.
  std::int32_t ttl_;
  static jfieldID ttl_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  sendChatSetTtlMessage();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] chat_id_ Chat identifier.
   * \param[in] ttl_ New TTL value, in seconds.
   */
  sendChatSetTtlMessage(std::int64_t chat_id_, std::int32_t ttl_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1432535564;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<message>;

  static object_ptr<sendChatSetTtlMessage> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Sends a custom request; for bots only.
 *
 * Returns object_ptr<CustomRequestResult>.
 */
class sendCustomRequest final : public Function {
 public:
  static jclass Class;
  /// The method name.
  std::string method_;
  static jfieldID method_fieldID;
  /// JSON-serialized method parameters.
  std::string parameters_;
  static jfieldID parameters_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  sendCustomRequest();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] method_ The method name.
   * \param[in] parameters_ JSON-serialized method parameters.
   */
  sendCustomRequest(std::string const &method_, std::string const &parameters_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 285045153;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<customRequestResult>;

  static object_ptr<sendCustomRequest> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Sends a code to verify an email address to be added to a user's Telegram Passport.
 *
 * Returns object_ptr<EmailAddressAuthenticationCodeInfo>.
 */
class sendEmailAddressVerificationCode final : public Function {
 public:
  static jclass Class;
  /// Email address.
  std::string email_address_;
  static jfieldID email_address_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  sendEmailAddressVerificationCode();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] email_address_ Email address.
   */
  explicit sendEmailAddressVerificationCode(std::string const &email_address_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -221621379;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<emailAddressAuthenticationCodeInfo>;

  static object_ptr<sendEmailAddressVerificationCode> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Sends the result of an inline query as a message. Returns the sent message. Always clears a chat draft message.
 *
 * Returns object_ptr<Message>.
 */
class sendInlineQueryResultMessage final : public Function {
 public:
  static jclass Class;
  /// Target chat.
  std::int64_t chat_id_;
  static jfieldID chat_id_fieldID;
  /// Identifier of a message to reply to or 0.
  std::int64_t reply_to_message_id_;
  static jfieldID reply_to_message_id_fieldID;
  /// Pass true to disable notification for the message. Not supported in secret chats.
  bool disable_notification_;
  static jfieldID disable_notification_fieldID;
  /// Pass true if the message is sent from background.
  bool from_background_;
  static jfieldID from_background_fieldID;
  /// Identifier of the inline query.
  std::int64_t query_id_;
  static jfieldID query_id_fieldID;
  /// Identifier of the inline result.
  std::string result_id_;
  static jfieldID result_id_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  sendInlineQueryResultMessage();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] chat_id_ Target chat.
   * \param[in] reply_to_message_id_ Identifier of a message to reply to or 0.
   * \param[in] disable_notification_ Pass true to disable notification for the message. Not supported in secret chats.
   * \param[in] from_background_ Pass true if the message is sent from background.
   * \param[in] query_id_ Identifier of the inline query.
   * \param[in] result_id_ Identifier of the inline result.
   */
  sendInlineQueryResultMessage(std::int64_t chat_id_, std::int64_t reply_to_message_id_, bool disable_notification_, bool from_background_, std::int64_t query_id_, std::string const &result_id_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -643910868;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<message>;

  static object_ptr<sendInlineQueryResultMessage> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Sends a message. Returns the sent message.
 *
 * Returns object_ptr<Message>.
 */
class sendMessage final : public Function {
 public:
  static jclass Class;
  /// Target chat.
  std::int64_t chat_id_;
  static jfieldID chat_id_fieldID;
  /// Identifier of the message to reply to or 0.
  std::int64_t reply_to_message_id_;
  static jfieldID reply_to_message_id_fieldID;
  /// Pass true to disable notification for the message. Not supported in secret chats.
  bool disable_notification_;
  static jfieldID disable_notification_fieldID;
  /// Pass true if the message is sent from the background.
  bool from_background_;
  static jfieldID from_background_fieldID;
  /// Markup for replying to the message; for bots only.
  object_ptr<ReplyMarkup> reply_markup_;
  static jfieldID reply_markup_fieldID;
  /// The content of the message to be sent.
  object_ptr<InputMessageContent> input_message_content_;
  static jfieldID input_message_content_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  sendMessage();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] chat_id_ Target chat.
   * \param[in] reply_to_message_id_ Identifier of the message to reply to or 0.
   * \param[in] disable_notification_ Pass true to disable notification for the message. Not supported in secret chats.
   * \param[in] from_background_ Pass true if the message is sent from the background.
   * \param[in] reply_markup_ Markup for replying to the message; for bots only.
   * \param[in] input_message_content_ The content of the message to be sent.
   */
  sendMessage(std::int64_t chat_id_, std::int64_t reply_to_message_id_, bool disable_notification_, bool from_background_, object_ptr<ReplyMarkup> &&reply_markup_, object_ptr<InputMessageContent> &&input_message_content_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1694632114;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<message>;

  static object_ptr<sendMessage> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Sends messages grouped together into an album. Currently only photo and video messages can be grouped into an album. Returns sent messages.
 *
 * Returns object_ptr<Messages>.
 */
class sendMessageAlbum final : public Function {
 public:
  static jclass Class;
  /// Target chat.
  std::int64_t chat_id_;
  static jfieldID chat_id_fieldID;
  /// Identifier of a message to reply to or 0.
  std::int64_t reply_to_message_id_;
  static jfieldID reply_to_message_id_fieldID;
  /// Pass true to disable notification for the messages. Not supported in secret chats.
  bool disable_notification_;
  static jfieldID disable_notification_fieldID;
  /// Pass true if the messages are sent from the background.
  bool from_background_;
  static jfieldID from_background_fieldID;
  /// Contents of messages to be sent.
  std::vector<object_ptr<InputMessageContent>> input_message_contents_;
  static jfieldID input_message_contents_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  sendMessageAlbum();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] chat_id_ Target chat.
   * \param[in] reply_to_message_id_ Identifier of a message to reply to or 0.
   * \param[in] disable_notification_ Pass true to disable notification for the messages. Not supported in secret chats.
   * \param[in] from_background_ Pass true if the messages are sent from the background.
   * \param[in] input_message_contents_ Contents of messages to be sent.
   */
  sendMessageAlbum(std::int64_t chat_id_, std::int64_t reply_to_message_id_, bool disable_notification_, bool from_background_, std::vector<object_ptr<InputMessageContent>> &&input_message_contents_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -291823014;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<messages>;

  static object_ptr<sendMessageAlbum> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Sends a Telegram Passport authorization form, effectively sharing data with the service.
 *
 * Returns object_ptr<Ok>.
 */
class sendPassportAuthorizationForm final : public Function {
 public:
  static jclass Class;
  /// Authorization form identifier.
  std::int32_t autorization_form_id_;
  static jfieldID autorization_form_id_fieldID;
  /// Types of Telegram Passport elements chosen by user to complete the authorization form.
  std::vector<object_ptr<PassportElementType>> types_;
  static jfieldID types_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  sendPassportAuthorizationForm();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] autorization_form_id_ Authorization form identifier.
   * \param[in] types_ Types of Telegram Passport elements chosen by user to complete the authorization form.
   */
  sendPassportAuthorizationForm(std::int32_t autorization_form_id_, std::vector<object_ptr<PassportElementType>> &&types_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -602402218;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<ok>;

  static object_ptr<sendPassportAuthorizationForm> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Sends a filled-out payment form to the bot for final verification.
 *
 * Returns object_ptr<PaymentResult>.
 */
class sendPaymentForm final : public Function {
 public:
  static jclass Class;
  /// Chat identifier of the Invoice message.
  std::int64_t chat_id_;
  static jfieldID chat_id_fieldID;
  /// Message identifier.
  std::int64_t message_id_;
  static jfieldID message_id_fieldID;
  /// Identifier returned by ValidateOrderInfo, or an empty string.
  std::string order_info_id_;
  static jfieldID order_info_id_fieldID;
  /// Identifier of a chosen shipping option, if applicable.
  std::string shipping_option_id_;
  static jfieldID shipping_option_id_fieldID;
  /// The credentials chosen by user for payment.
  object_ptr<InputCredentials> credentials_;
  static jfieldID credentials_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  sendPaymentForm();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] chat_id_ Chat identifier of the Invoice message.
   * \param[in] message_id_ Message identifier.
   * \param[in] order_info_id_ Identifier returned by ValidateOrderInfo, or an empty string.
   * \param[in] shipping_option_id_ Identifier of a chosen shipping option, if applicable.
   * \param[in] credentials_ The credentials chosen by user for payment.
   */
  sendPaymentForm(std::int64_t chat_id_, std::int64_t message_id_, std::string const &order_info_id_, std::string const &shipping_option_id_, object_ptr<InputCredentials> &&credentials_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 591581572;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<paymentResult>;

  static object_ptr<sendPaymentForm> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Sends phone number confirmation code. Should be called when user presses &quot;https://t.me/confirmphone?phone=*******&amp;hash=**********&quot; or &quot;tg://confirmphone?phone=*******&amp;hash=**********&quot; link.
 *
 * Returns object_ptr<AuthenticationCodeInfo>.
 */
class sendPhoneNumberConfirmationCode final : public Function {
 public:
  static jclass Class;
  /// Value of the &quot;hash&quot; parameter from the link.
  std::string hash_;
  static jfieldID hash_fieldID;
  /// Value of the &quot;phone&quot; parameter from the link.
  std::string phone_number_;
  static jfieldID phone_number_fieldID;
  /// Pass true if the authentication code may be sent via flash call to the specified phone number.
  bool allow_flash_call_;
  static jfieldID allow_flash_call_fieldID;
  /// Pass true if the phone number is used on the current device. Ignored if allow_flash_call is false.
  bool is_current_phone_number_;
  static jfieldID is_current_phone_number_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  sendPhoneNumberConfirmationCode();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] hash_ Value of the &quot;hash&quot; parameter from the link.
   * \param[in] phone_number_ Value of the &quot;phone&quot; parameter from the link.
   * \param[in] allow_flash_call_ Pass true if the authentication code may be sent via flash call to the specified phone number.
   * \param[in] is_current_phone_number_ Pass true if the phone number is used on the current device. Ignored if allow_flash_call is false.
   */
  sendPhoneNumberConfirmationCode(std::string const &hash_, std::string const &phone_number_, bool allow_flash_call_, bool is_current_phone_number_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1975492794;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<authenticationCodeInfo>;

  static object_ptr<sendPhoneNumberConfirmationCode> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Sends a code to verify a phone number to be added to a user's Telegram Passport.
 *
 * Returns object_ptr<AuthenticationCodeInfo>.
 */
class sendPhoneNumberVerificationCode final : public Function {
 public:
  static jclass Class;
  /// The phone number of the user, in international format.
  std::string phone_number_;
  static jfieldID phone_number_fieldID;
  /// Pass true if the authentication code may be sent via flash call to the specified phone number.
  bool allow_flash_call_;
  static jfieldID allow_flash_call_fieldID;
  /// Pass true if the phone number is used on the current device. Ignored if allow_flash_call is false.
  bool is_current_phone_number_;
  static jfieldID is_current_phone_number_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  sendPhoneNumberVerificationCode();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] phone_number_ The phone number of the user, in international format.
   * \param[in] allow_flash_call_ Pass true if the authentication code may be sent via flash call to the specified phone number.
   * \param[in] is_current_phone_number_ Pass true if the phone number is used on the current device. Ignored if allow_flash_call is false.
   */
  sendPhoneNumberVerificationCode(std::string const &phone_number_, bool allow_flash_call_, bool is_current_phone_number_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -280632685;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<authenticationCodeInfo>;

  static object_ptr<sendPhoneNumberVerificationCode> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Changes the period of inactivity after which the account of the current user will automatically be deleted.
 *
 * Returns object_ptr<Ok>.
 */
class setAccountTtl final : public Function {
 public:
  static jclass Class;
  /// New account TTL.
  object_ptr<accountTtl> ttl_;
  static jfieldID ttl_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  setAccountTtl();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] ttl_ New account TTL.
   */
  explicit setAccountTtl(object_ptr<accountTtl> &&ttl_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 701389032;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<ok>;

  static object_ptr<setAccountTtl> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Succeeds after a specified amount of time has passed. Can be called before authorization. Can be called before initialization.
 *
 * Returns object_ptr<Ok>.
 */
class setAlarm final : public Function {
 public:
  static jclass Class;
  /// Number of seconds before the function returns.
  double seconds_;
  static jfieldID seconds_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  setAlarm();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] seconds_ Number of seconds before the function returns.
   */
  explicit setAlarm(double seconds_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -873497067;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<ok>;

  static object_ptr<setAlarm> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Sets the phone number of the user and sends an authentication code to the user. Works only when the current authorization state is authorizationStateWaitPhoneNumber.
 *
 * Returns object_ptr<Ok>.
 */
class setAuthenticationPhoneNumber final : public Function {
 public:
  static jclass Class;
  /// The phone number of the user, in international format.
  std::string phone_number_;
  static jfieldID phone_number_fieldID;
  /// Pass true if the authentication code may be sent via flash call to the specified phone number.
  bool allow_flash_call_;
  static jfieldID allow_flash_call_fieldID;
  /// Pass true if the phone number is used on the current device. Ignored if allow_flash_call is false.
  bool is_current_phone_number_;
  static jfieldID is_current_phone_number_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  setAuthenticationPhoneNumber();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] phone_number_ The phone number of the user, in international format.
   * \param[in] allow_flash_call_ Pass true if the authentication code may be sent via flash call to the specified phone number.
   * \param[in] is_current_phone_number_ Pass true if the phone number is used on the current device. Ignored if allow_flash_call is false.
   */
  setAuthenticationPhoneNumber(std::string const &phone_number_, bool allow_flash_call_, bool is_current_phone_number_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -856055465;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<ok>;

  static object_ptr<setAuthenticationPhoneNumber> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Changes the bio of the current user.
 *
 * Returns object_ptr<Ok>.
 */
class setBio final : public Function {
 public:
  static jclass Class;
  /// The new value of the user bio; 0-70 characters without line feeds.
  std::string bio_;
  static jfieldID bio_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  setBio();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] bio_ The new value of the user bio; 0-70 characters without line feeds.
   */
  explicit setBio(std::string const &bio_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1619582124;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<ok>;

  static object_ptr<setBio> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Informs the server about the number of pending bot updates if they haven't been processed for a long time; for bots only.
 *
 * Returns object_ptr<Ok>.
 */
class setBotUpdatesStatus final : public Function {
 public:
  static jclass Class;
  /// The number of pending updates.
  std::int32_t pending_update_count_;
  static jfieldID pending_update_count_fieldID;
  /// The last error message.
  std::string error_message_;
  static jfieldID error_message_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  setBotUpdatesStatus();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] pending_update_count_ The number of pending updates.
   * \param[in] error_message_ The last error message.
   */
  setBotUpdatesStatus(std::int32_t pending_update_count_, std::string const &error_message_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1154926191;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<ok>;

  static object_ptr<setBotUpdatesStatus> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Changes client data associated with a chat.
 *
 * Returns object_ptr<Ok>.
 */
class setChatClientData final : public Function {
 public:
  static jclass Class;
  /// Chat identifier.
  std::int64_t chat_id_;
  static jfieldID chat_id_fieldID;
  /// New value of client_data.
  std::string client_data_;
  static jfieldID client_data_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  setChatClientData();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] chat_id_ Chat identifier.
   * \param[in] client_data_ New value of client_data.
   */
  setChatClientData(std::int64_t chat_id_, std::string const &client_data_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -827119811;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<ok>;

  static object_ptr<setChatClientData> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Changes the draft message in a chat.
 *
 * Returns object_ptr<Ok>.
 */
class setChatDraftMessage final : public Function {
 public:
  static jclass Class;
  /// Chat identifier.
  std::int64_t chat_id_;
  static jfieldID chat_id_fieldID;
  /// New draft message; may be null.
  object_ptr<draftMessage> draft_message_;
  static jfieldID draft_message_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  setChatDraftMessage();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] chat_id_ Chat identifier.
   * \param[in] draft_message_ New draft message; may be null.
   */
  setChatDraftMessage(std::int64_t chat_id_, object_ptr<draftMessage> &&draft_message_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -588175579;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<ok>;

  static object_ptr<setChatDraftMessage> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Changes the status of a chat member, needs appropriate privileges. This function is currently not suitable for adding new members to the chat; instead, use addChatMember. The chat member status will not be changed until it has been synchronized with the server.
 *
 * Returns object_ptr<Ok>.
 */
class setChatMemberStatus final : public Function {
 public:
  static jclass Class;
  /// Chat identifier.
  std::int64_t chat_id_;
  static jfieldID chat_id_fieldID;
  /// User identifier.
  std::int32_t user_id_;
  static jfieldID user_id_fieldID;
  /// The new status of the member in the chat.
  object_ptr<ChatMemberStatus> status_;
  static jfieldID status_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  setChatMemberStatus();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] chat_id_ Chat identifier.
   * \param[in] user_id_ User identifier.
   * \param[in] status_ The new status of the member in the chat.
   */
  setChatMemberStatus(std::int64_t chat_id_, std::int32_t user_id_, object_ptr<ChatMemberStatus> &&status_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1754439241;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<ok>;

  static object_ptr<setChatMemberStatus> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Changes the notification settings of a chat.
 *
 * Returns object_ptr<Ok>.
 */
class setChatNotificationSettings final : public Function {
 public:
  static jclass Class;
  /// Chat identifier.
  std::int64_t chat_id_;
  static jfieldID chat_id_fieldID;
  /// New notification settings for the chat.
  object_ptr<chatNotificationSettings> notification_settings_;
  static jfieldID notification_settings_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  setChatNotificationSettings();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] chat_id_ Chat identifier.
   * \param[in] notification_settings_ New notification settings for the chat.
   */
  setChatNotificationSettings(std::int64_t chat_id_, object_ptr<chatNotificationSettings> &&notification_settings_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 777199614;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<ok>;

  static object_ptr<setChatNotificationSettings> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Changes the photo of a chat. Supported only for basic groups, supergroups and channels. Requires administrator rights in basic groups and the appropriate administrator rights in supergroups and channels. The photo will not be changed before request to the server has been completed.
 *
 * Returns object_ptr<Ok>.
 */
class setChatPhoto final : public Function {
 public:
  static jclass Class;
  /// Chat identifier.
  std::int64_t chat_id_;
  static jfieldID chat_id_fieldID;
  /// New chat photo. You can use a zero InputFileId to delete the chat photo. Files that are accessible only by HTTP URL are not acceptable.
  object_ptr<InputFile> photo_;
  static jfieldID photo_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  setChatPhoto();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] chat_id_ Chat identifier.
   * \param[in] photo_ New chat photo. You can use a zero InputFileId to delete the chat photo. Files that are accessible only by HTTP URL are not acceptable.
   */
  setChatPhoto(std::int64_t chat_id_, object_ptr<InputFile> &&photo_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 132244217;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<ok>;

  static object_ptr<setChatPhoto> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Changes the chat title. Supported only for basic groups, supergroups and channels. Requires administrator rights in basic groups and the appropriate administrator rights in supergroups and channels. The title will not be changed until the request to the server has been completed.
 *
 * Returns object_ptr<Ok>.
 */
class setChatTitle final : public Function {
 public:
  static jclass Class;
  /// Chat identifier.
  std::int64_t chat_id_;
  static jfieldID chat_id_fieldID;
  /// New title of the chat; 1-255 characters.
  std::string title_;
  static jfieldID title_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  setChatTitle();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] chat_id_ Chat identifier.
   * \param[in] title_ New title of the chat; 1-255 characters.
   */
  setChatTitle(std::int64_t chat_id_, std::string const &title_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 164282047;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<ok>;

  static object_ptr<setChatTitle> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Adds or changes a custom language pack to the current localization target.
 *
 * Returns object_ptr<Ok>.
 */
class setCustomLanguagePack final : public Function {
 public:
  static jclass Class;
  /// Information about the language pack. Language pack ID must start with 'X', consist only of English letters, digits and hyphens, and must not exceed 64 characters.
  object_ptr<languagePackInfo> info_;
  static jfieldID info_fieldID;
  /// Strings of the new language pack.
  std::vector<object_ptr<languagePackString>> strings_;
  static jfieldID strings_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  setCustomLanguagePack();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] info_ Information about the language pack. Language pack ID must start with 'X', consist only of English letters, digits and hyphens, and must not exceed 64 characters.
   * \param[in] strings_ Strings of the new language pack.
   */
  setCustomLanguagePack(object_ptr<languagePackInfo> &&info_, std::vector<object_ptr<languagePackString>> &&strings_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -296742819;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<ok>;

  static object_ptr<setCustomLanguagePack> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Adds, edits or deletes a string in a custom language pack.
 *
 * Returns object_ptr<Ok>.
 */
class setCustomLanguagePackString final : public Function {
 public:
  static jclass Class;
  /// Identifier of a previously added custom language pack in the current localization target.
  std::string language_pack_id_;
  static jfieldID language_pack_id_fieldID;
  /// New language pack string.
  object_ptr<languagePackString> new_string_;
  static jfieldID new_string_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  setCustomLanguagePackString();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] language_pack_id_ Identifier of a previously added custom language pack in the current localization target.
   * \param[in] new_string_ New language pack string.
   */
  setCustomLanguagePackString(std::string const &language_pack_id_, object_ptr<languagePackString> &&new_string_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1316365592;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<ok>;

  static object_ptr<setCustomLanguagePackString> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Changes the database encryption key. Usually the encryption key is never changed and is stored in some OS keychain.
 *
 * Returns object_ptr<Ok>.
 */
class setDatabaseEncryptionKey final : public Function {
 public:
  static jclass Class;
  /// New encryption key.
  std::string new_encryption_key_;
  static jfieldID new_encryption_key_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  setDatabaseEncryptionKey();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] new_encryption_key_ New encryption key.
   */
  explicit setDatabaseEncryptionKey(std::string const &new_encryption_key_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1204599371;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<ok>;

  static object_ptr<setDatabaseEncryptionKey> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * The next part of a file was generated.
 *
 * Returns object_ptr<Ok>.
 */
class setFileGenerationProgress final : public Function {
 public:
  static jclass Class;
  /// The identifier of the generation process.
  std::int64_t generation_id_;
  static jfieldID generation_id_fieldID;
  /// Expected size of the generated file, in bytes; 0 if unknown.
  std::int32_t expected_size_;
  static jfieldID expected_size_fieldID;
  /// The number of bytes already generated.
  std::int32_t local_prefix_size_;
  static jfieldID local_prefix_size_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  setFileGenerationProgress();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] generation_id_ The identifier of the generation process.
   * \param[in] expected_size_ Expected size of the generated file, in bytes; 0 if unknown.
   * \param[in] local_prefix_size_ The number of bytes already generated.
   */
  setFileGenerationProgress(std::int64_t generation_id_, std::int32_t expected_size_, std::int32_t local_prefix_size_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -540459953;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<ok>;

  static object_ptr<setFileGenerationProgress> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Updates the game score of the specified user in the game; for bots only.
 *
 * Returns object_ptr<Message>.
 */
class setGameScore final : public Function {
 public:
  static jclass Class;
  /// The chat to which the message with the game.
  std::int64_t chat_id_;
  static jfieldID chat_id_fieldID;
  /// Identifier of the message.
  std::int64_t message_id_;
  static jfieldID message_id_fieldID;
  /// True, if the message should be edited.
  bool edit_message_;
  static jfieldID edit_message_fieldID;
  /// User identifier.
  std::int32_t user_id_;
  static jfieldID user_id_fieldID;
  /// The new score.
  std::int32_t score_;
  static jfieldID score_fieldID;
  /// Pass true to update the score even if it decreases. If the score is 0, the user will be deleted from the high score table.
  bool force_;
  static jfieldID force_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  setGameScore();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] chat_id_ The chat to which the message with the game.
   * \param[in] message_id_ Identifier of the message.
   * \param[in] edit_message_ True, if the message should be edited.
   * \param[in] user_id_ User identifier.
   * \param[in] score_ The new score.
   * \param[in] force_ Pass true to update the score even if it decreases. If the score is 0, the user will be deleted from the high score table.
   */
  setGameScore(std::int64_t chat_id_, std::int64_t message_id_, bool edit_message_, std::int32_t user_id_, std::int32_t score_, bool force_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1768307069;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<message>;

  static object_ptr<setGameScore> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Updates the game score of the specified user in a game; for bots only.
 *
 * Returns object_ptr<Ok>.
 */
class setInlineGameScore final : public Function {
 public:
  static jclass Class;
  /// Inline message identifier.
  std::string inline_message_id_;
  static jfieldID inline_message_id_fieldID;
  /// True, if the message should be edited.
  bool edit_message_;
  static jfieldID edit_message_fieldID;
  /// User identifier.
  std::int32_t user_id_;
  static jfieldID user_id_fieldID;
  /// The new score.
  std::int32_t score_;
  static jfieldID score_fieldID;
  /// Pass true to update the score even if it decreases. If the score is 0, the user will be deleted from the high score table.
  bool force_;
  static jfieldID force_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  setInlineGameScore();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] inline_message_id_ Inline message identifier.
   * \param[in] edit_message_ True, if the message should be edited.
   * \param[in] user_id_ User identifier.
   * \param[in] score_ The new score.
   * \param[in] force_ Pass true to update the score even if it decreases. If the score is 0, the user will be deleted from the high score table.
   */
  setInlineGameScore(std::string const &inline_message_id_, bool edit_message_, std::int32_t user_id_, std::int32_t score_, bool force_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 758435487;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<ok>;

  static object_ptr<setInlineGameScore> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Changes the first and last name of the current user. If something changes, updateUser will be sent.
 *
 * Returns object_ptr<Ok>.
 */
class setName final : public Function {
 public:
  static jclass Class;
  /// The new value of the first name for the user; 1-255 characters.
  std::string first_name_;
  static jfieldID first_name_fieldID;
  /// The new value of the optional last name for the user; 0-255 characters.
  std::string last_name_;
  static jfieldID last_name_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  setName();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] first_name_ The new value of the first name for the user; 1-255 characters.
   * \param[in] last_name_ The new value of the optional last name for the user; 0-255 characters.
   */
  setName(std::string const &first_name_, std::string const &last_name_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1711693584;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<ok>;

  static object_ptr<setName> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Sets the current network type. Can be called before authorization. Calling this method forces all network connections to reopen, mitigating the delay in switching between different networks, so it should be called whenever the network is changed, even if the network type remains the same. Network type is used to check whether the library can use the network at all and also for collecting detailed network data usage statistics.
 *
 * Returns object_ptr<Ok>.
 */
class setNetworkType final : public Function {
 public:
  static jclass Class;
  /// The new network type. By default, networkTypeOther.
  object_ptr<NetworkType> type_;
  static jfieldID type_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  setNetworkType();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] type_ The new network type. By default, networkTypeOther.
   */
  explicit setNetworkType(object_ptr<NetworkType> &&type_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -701635234;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<ok>;

  static object_ptr<setNetworkType> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Sets the value of an option. (Check the list of available options on https://core.telegram.org/tdlib/options.) Only writable options can be set. Can be called before authorization.
 *
 * Returns object_ptr<Ok>.
 */
class setOption final : public Function {
 public:
  static jclass Class;
  /// The name of the option.
  std::string name_;
  static jfieldID name_fieldID;
  /// The new value of the option.
  object_ptr<OptionValue> value_;
  static jfieldID value_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  setOption();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] name_ The name of the option.
   * \param[in] value_ The new value of the option.
   */
  setOption(std::string const &name_, object_ptr<OptionValue> &&value_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 2114670322;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<ok>;

  static object_ptr<setOption> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Adds an element to the user's Telegram Passport. May return an error with a message &quot;PHONE_VERIFICATION_NEEDED&quot; or &quot;EMAIL_VERIFICATION_NEEDED&quot; if the chosen phone number or the chosen email address must be verified first.
 *
 * Returns object_ptr<PassportElement>.
 */
class setPassportElement final : public Function {
 public:
  static jclass Class;
  /// Input Telegram Passport element.
  object_ptr<InputPassportElement> element_;
  static jfieldID element_fieldID;
  /// Password of the current user.
  std::string password_;
  static jfieldID password_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  setPassportElement();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] element_ Input Telegram Passport element.
   * \param[in] password_ Password of the current user.
   */
  setPassportElement(object_ptr<InputPassportElement> &&element_, std::string const &password_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 2068173212;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<PassportElement>;

  static object_ptr<setPassportElement> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Informs the user that some of the elements in their Telegram Passport contain errors; for bots only. The user will not be able to resend the elements, until the errors are fixed.
 *
 * Returns object_ptr<Ok>.
 */
class setPassportElementErrors final : public Function {
 public:
  static jclass Class;
  /// User identifier.
  std::int32_t user_id_;
  static jfieldID user_id_fieldID;
  /// The errors.
  std::vector<object_ptr<inputPassportElementError>> errors_;
  static jfieldID errors_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  setPassportElementErrors();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] user_id_ User identifier.
   * \param[in] errors_ The errors.
   */
  setPassportElementErrors(std::int32_t user_id_, std::vector<object_ptr<inputPassportElementError>> &&errors_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1455869875;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<ok>;

  static object_ptr<setPassportElementErrors> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Changes the password for the user. If a new recovery email address is specified, then the error EMAIL_UNCONFIRMED is returned and the password change will not be applied until the new recovery email address has been confirmed. The application should periodically call getPasswordState to check whether the new email address has been confirmed.
 *
 * Returns object_ptr<PasswordState>.
 */
class setPassword final : public Function {
 public:
  static jclass Class;
  /// Previous password of the user.
  std::string old_password_;
  static jfieldID old_password_fieldID;
  /// New password of the user; may be empty to remove the password.
  std::string new_password_;
  static jfieldID new_password_fieldID;
  /// New password hint; may be empty.
  std::string new_hint_;
  static jfieldID new_hint_fieldID;
  /// Pass true if the recovery email address should be changed.
  bool set_recovery_email_address_;
  static jfieldID set_recovery_email_address_fieldID;
  /// New recovery email address; may be empty.
  std::string new_recovery_email_address_;
  static jfieldID new_recovery_email_address_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  setPassword();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] old_password_ Previous password of the user.
   * \param[in] new_password_ New password of the user; may be empty to remove the password.
   * \param[in] new_hint_ New password hint; may be empty.
   * \param[in] set_recovery_email_address_ Pass true if the recovery email address should be changed.
   * \param[in] new_recovery_email_address_ New recovery email address; may be empty.
   */
  setPassword(std::string const &old_password_, std::string const &new_password_, std::string const &new_hint_, bool set_recovery_email_address_, std::string const &new_recovery_email_address_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1193589027;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<passwordState>;

  static object_ptr<setPassword> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Changes the order of pinned chats.
 *
 * Returns object_ptr<Ok>.
 */
class setPinnedChats final : public Function {
 public:
  static jclass Class;
  /// The new list of pinned chats.
  std::vector<std::int64_t> chat_ids_;
  static jfieldID chat_ids_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  setPinnedChats();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] chat_ids_ The new list of pinned chats.
   */
  explicit setPinnedChats(std::vector<std::int64_t> &&chat_ids_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1369665719;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<ok>;

  static object_ptr<setPinnedChats> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Uploads a new profile photo for the current user. If something changes, updateUser will be sent.
 *
 * Returns object_ptr<Ok>.
 */
class setProfilePhoto final : public Function {
 public:
  static jclass Class;
  /// Profile photo to set. inputFileId and inputFileRemote may still be unsupported.
  object_ptr<InputFile> photo_;
  static jfieldID photo_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  setProfilePhoto();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] photo_ Profile photo to set. inputFileId and inputFileRemote may still be unsupported.
   */
  explicit setProfilePhoto(object_ptr<InputFile> &&photo_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1594734550;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<ok>;

  static object_ptr<setProfilePhoto> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Changes the recovery email address of the user. If a new recovery email address is specified, then the error EMAIL_UNCONFIRMED is returned and the email address will not be changed until the new email has been confirmed. The application should periodically call getPasswordState to check whether the email address has been confirmed. If new_recovery_email_address is the same as the email address that is currently set up, this call succeeds immediately and aborts all other requests waiting for an email confirmation.
 *
 * Returns object_ptr<PasswordState>.
 */
class setRecoveryEmailAddress final : public Function {
 public:
  static jclass Class;
  /// Password of the current user.
  std::string password_;
  static jfieldID password_fieldID;
  /// New recovery email address.
  std::string new_recovery_email_address_;
  static jfieldID new_recovery_email_address_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  setRecoveryEmailAddress();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] password_ Password of the current user.
   * \param[in] new_recovery_email_address_ New recovery email address.
   */
  setRecoveryEmailAddress(std::string const &password_, std::string const &new_recovery_email_address_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1981836385;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<passwordState>;

  static object_ptr<setRecoveryEmailAddress> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Changes notification settings for chats of a given type.
 *
 * Returns object_ptr<Ok>.
 */
class setScopeNotificationSettings final : public Function {
 public:
  static jclass Class;
  /// Types of chats for which to change the notification settings.
  object_ptr<NotificationSettingsScope> scope_;
  static jfieldID scope_fieldID;
  /// The new notification settings for the given scope.
  object_ptr<scopeNotificationSettings> notification_settings_;
  static jfieldID notification_settings_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  setScopeNotificationSettings();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] scope_ Types of chats for which to change the notification settings.
   * \param[in] notification_settings_ The new notification settings for the given scope.
   */
  setScopeNotificationSettings(object_ptr<NotificationSettingsScope> &&scope_, object_ptr<scopeNotificationSettings> &&notification_settings_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -2049984966;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<ok>;

  static object_ptr<setScopeNotificationSettings> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Changes the position of a sticker in the set to which it belongs; for bots only. The sticker set must have been created by the bot.
 *
 * Returns object_ptr<Ok>.
 */
class setStickerPositionInSet final : public Function {
 public:
  static jclass Class;
  /// Sticker.
  object_ptr<InputFile> sticker_;
  static jfieldID sticker_fieldID;
  /// New position of the sticker in the set, zero-based.
  std::int32_t position_;
  static jfieldID position_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  setStickerPositionInSet();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] sticker_ Sticker.
   * \param[in] position_ New position of the sticker in the set, zero-based.
   */
  setStickerPositionInSet(object_ptr<InputFile> &&sticker_, std::int32_t position_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 2075281185;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<ok>;

  static object_ptr<setStickerPositionInSet> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Changes information about a supergroup or channel; requires appropriate administrator rights.
 *
 * Returns object_ptr<Ok>.
 */
class setSupergroupDescription final : public Function {
 public:
  static jclass Class;
  /// Identifier of the supergroup or channel.
  std::int32_t supergroup_id_;
  static jfieldID supergroup_id_fieldID;
  /// New supergroup or channel description; 0-255 characters.
  std::string description_;
  static jfieldID description_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  setSupergroupDescription();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] supergroup_id_ Identifier of the supergroup or channel.
   * \param[in] description_ New supergroup or channel description; 0-255 characters.
   */
  setSupergroupDescription(std::int32_t supergroup_id_, std::string const &description_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 227623488;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<ok>;

  static object_ptr<setSupergroupDescription> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Changes the sticker set of a supergroup; requires appropriate rights in the supergroup.
 *
 * Returns object_ptr<Ok>.
 */
class setSupergroupStickerSet final : public Function {
 public:
  static jclass Class;
  /// Identifier of the supergroup.
  std::int32_t supergroup_id_;
  static jfieldID supergroup_id_fieldID;
  /// New value of the supergroup sticker set identifier. Use 0 to remove the supergroup sticker set.
  std::int64_t sticker_set_id_;
  static jfieldID sticker_set_id_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  setSupergroupStickerSet();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] supergroup_id_ Identifier of the supergroup.
   * \param[in] sticker_set_id_ New value of the supergroup sticker set identifier. Use 0 to remove the supergroup sticker set.
   */
  setSupergroupStickerSet(std::int32_t supergroup_id_, std::int64_t sticker_set_id_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -295782298;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<ok>;

  static object_ptr<setSupergroupStickerSet> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Changes the username of a supergroup or channel, requires creator privileges in the supergroup or channel.
 *
 * Returns object_ptr<Ok>.
 */
class setSupergroupUsername final : public Function {
 public:
  static jclass Class;
  /// Identifier of the supergroup or channel.
  std::int32_t supergroup_id_;
  static jfieldID supergroup_id_fieldID;
  /// New value of the username. Use an empty string to remove the username.
  std::string username_;
  static jfieldID username_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  setSupergroupUsername();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] supergroup_id_ Identifier of the supergroup or channel.
   * \param[in] username_ New value of the username. Use an empty string to remove the username.
   */
  setSupergroupUsername(std::int32_t supergroup_id_, std::string const &username_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1428333122;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<ok>;

  static object_ptr<setSupergroupUsername> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Sets the parameters for TDLib initialization. Works only when the current authorization state is authorizationStateWaitTdlibParameters.
 *
 * Returns object_ptr<Ok>.
 */
class setTdlibParameters final : public Function {
 public:
  static jclass Class;
  /// Parameters.
  object_ptr<tdlibParameters> parameters_;
  static jfieldID parameters_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  setTdlibParameters();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] parameters_ Parameters.
   */
  explicit setTdlibParameters(object_ptr<tdlibParameters> &&parameters_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1912557997;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<ok>;

  static object_ptr<setTdlibParameters> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Changes user privacy settings.
 *
 * Returns object_ptr<Ok>.
 */
class setUserPrivacySettingRules final : public Function {
 public:
  static jclass Class;
  /// The privacy setting.
  object_ptr<UserPrivacySetting> setting_;
  static jfieldID setting_fieldID;
  /// The new privacy rules.
  object_ptr<userPrivacySettingRules> rules_;
  static jfieldID rules_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  setUserPrivacySettingRules();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] setting_ The privacy setting.
   * \param[in] rules_ The new privacy rules.
   */
  setUserPrivacySettingRules(object_ptr<UserPrivacySetting> &&setting_, object_ptr<userPrivacySettingRules> &&rules_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -473812741;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<ok>;

  static object_ptr<setUserPrivacySettingRules> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Changes the username of the current user. If something changes, updateUser will be sent.
 *
 * Returns object_ptr<Ok>.
 */
class setUsername final : public Function {
 public:
  static jclass Class;
  /// The new value of the username. Use an empty string to remove the username.
  std::string username_;
  static jfieldID username_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  setUsername();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] username_ The new value of the username. Use an empty string to remove the username.
   */
  explicit setUsername(std::string const &username_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 439901214;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<ok>;

  static object_ptr<setUsername> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Terminates all other sessions of the current user.
 *
 * Returns object_ptr<Ok>.
 */
class terminateAllOtherSessions final : public Function {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  terminateAllOtherSessions();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1874485523;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<ok>;

  static object_ptr<terminateAllOtherSessions> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Terminates a session of the current user.
 *
 * Returns object_ptr<Ok>.
 */
class terminateSession final : public Function {
 public:
  static jclass Class;
  /// Session identifier.
  std::int64_t session_id_;
  static jfieldID session_id_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  terminateSession();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] session_id_ Session identifier.
   */
  explicit terminateSession(std::int64_t session_id_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -407385812;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<ok>;

  static object_ptr<terminateSession> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Returns the received bytes; for testing only.
 *
 * Returns object_ptr<TestBytes>.
 */
class testCallBytes final : public Function {
 public:
  static jclass Class;
  /// Bytes to return.
  std::string x_;
  static jfieldID x_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  testCallBytes();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] x_ Bytes to return.
   */
  explicit testCallBytes(std::string const &x_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -736011607;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<testBytes>;

  static object_ptr<testCallBytes> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Does nothing; for testing only.
 *
 * Returns object_ptr<Ok>.
 */
class testCallEmpty final : public Function {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  testCallEmpty();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -627291626;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<ok>;

  static object_ptr<testCallEmpty> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Returns the received string; for testing only.
 *
 * Returns object_ptr<TestString>.
 */
class testCallString final : public Function {
 public:
  static jclass Class;
  /// String to return.
  std::string x_;
  static jfieldID x_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  testCallString();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] x_ String to return.
   */
  explicit testCallString(std::string const &x_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1732818385;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<testString>;

  static object_ptr<testCallString> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Returns the received vector of numbers; for testing only.
 *
 * Returns object_ptr<TestVectorInt>.
 */
class testCallVectorInt final : public Function {
 public:
  static jclass Class;
  /// Vector of numbers to return.
  std::vector<std::int32_t> x_;
  static jfieldID x_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  testCallVectorInt();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] x_ Vector of numbers to return.
   */
  explicit testCallVectorInt(std::vector<std::int32_t> &&x_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -2137277793;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<testVectorInt>;

  static object_ptr<testCallVectorInt> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Returns the received vector of objects containing a number; for testing only.
 *
 * Returns object_ptr<TestVectorIntObject>.
 */
class testCallVectorIntObject final : public Function {
 public:
  static jclass Class;
  /// Vector of objects to return.
  std::vector<object_ptr<testInt>> x_;
  static jfieldID x_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  testCallVectorIntObject();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] x_ Vector of objects to return.
   */
  explicit testCallVectorIntObject(std::vector<object_ptr<testInt>> &&x_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1825428218;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<testVectorIntObject>;

  static object_ptr<testCallVectorIntObject> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * For testing only request. Returns the received vector of strings; for testing only.
 *
 * Returns object_ptr<TestVectorString>.
 */
class testCallVectorString final : public Function {
 public:
  static jclass Class;
  /// Vector of strings to return.
  std::vector<std::string> x_;
  static jfieldID x_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  testCallVectorString();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] x_ Vector of strings to return.
   */
  explicit testCallVectorString(std::vector<std::string> &&x_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -408600900;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<testVectorString>;

  static object_ptr<testCallVectorString> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Returns the received vector of objects containing a string; for testing only.
 *
 * Returns object_ptr<TestVectorStringObject>.
 */
class testCallVectorStringObject final : public Function {
 public:
  static jclass Class;
  /// Vector of objects to return.
  std::vector<object_ptr<testString>> x_;
  static jfieldID x_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  testCallVectorStringObject();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] x_ Vector of objects to return.
   */
  explicit testCallVectorStringObject(std::vector<object_ptr<testString>> &&x_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1527666429;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<testVectorStringObject>;

  static object_ptr<testCallVectorStringObject> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Forces an updates.getDifference call to the Telegram servers; for testing only.
 *
 * Returns object_ptr<Ok>.
 */
class testGetDifference final : public Function {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  testGetDifference();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1747084069;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<ok>;

  static object_ptr<testGetDifference> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Sends a simple network request to the Telegram servers; for testing only.
 *
 * Returns object_ptr<Ok>.
 */
class testNetwork final : public Function {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  testNetwork();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1343998901;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<ok>;

  static object_ptr<testNetwork> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Returns the squared received number; for testing only.
 *
 * Returns object_ptr<TestInt>.
 */
class testSquareInt final : public Function {
 public:
  static jclass Class;
  /// Number to square.
  std::int32_t x_;
  static jfieldID x_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  testSquareInt();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] x_ Number to square.
   */
  explicit testSquareInt(std::int32_t x_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -60135024;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<testInt>;

  static object_ptr<testSquareInt> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Does nothing and ensures that the Error object is used; for testing only.
 *
 * Returns object_ptr<Error>.
 */
class testUseError final : public Function {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  testUseError();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 528842186;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<error>;

  static object_ptr<testUseError> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Does nothing and ensures that the Update object is used; for testing only.
 *
 * Returns object_ptr<Update>.
 */
class testUseUpdate final : public Function {
 public:
  static jclass Class;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  testUseUpdate();

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 717094686;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<Update>;

  static object_ptr<testUseUpdate> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Toggles the &quot;All members are admins&quot; setting in basic groups; requires creator privileges in the group.
 *
 * Returns object_ptr<Ok>.
 */
class toggleBasicGroupAdministrators final : public Function {
 public:
  static jclass Class;
  /// Identifier of the basic group.
  std::int32_t basic_group_id_;
  static jfieldID basic_group_id_fieldID;
  /// New value of everyone_is_administrator.
  bool everyone_is_administrator_;
  static jfieldID everyone_is_administrator_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  toggleBasicGroupAdministrators();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] basic_group_id_ Identifier of the basic group.
   * \param[in] everyone_is_administrator_ New value of everyone_is_administrator.
   */
  toggleBasicGroupAdministrators(std::int32_t basic_group_id_, bool everyone_is_administrator_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -591395611;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<ok>;

  static object_ptr<toggleBasicGroupAdministrators> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Changes the value of the default disable_notification parameter, used when a message is sent to a chat.
 *
 * Returns object_ptr<Ok>.
 */
class toggleChatDefaultDisableNotification final : public Function {
 public:
  static jclass Class;
  /// Chat identifier.
  std::int64_t chat_id_;
  static jfieldID chat_id_fieldID;
  /// New value of default_disable_notification.
  bool default_disable_notification_;
  static jfieldID default_disable_notification_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  toggleChatDefaultDisableNotification();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] chat_id_ Chat identifier.
   * \param[in] default_disable_notification_ New value of default_disable_notification.
   */
  toggleChatDefaultDisableNotification(std::int64_t chat_id_, bool default_disable_notification_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 314794002;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<ok>;

  static object_ptr<toggleChatDefaultDisableNotification> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Changes the marked as unread state of a chat.
 *
 * Returns object_ptr<Ok>.
 */
class toggleChatIsMarkedAsUnread final : public Function {
 public:
  static jclass Class;
  /// Chat identifier.
  std::int64_t chat_id_;
  static jfieldID chat_id_fieldID;
  /// New value of is_marked_as_unread.
  bool is_marked_as_unread_;
  static jfieldID is_marked_as_unread_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  toggleChatIsMarkedAsUnread();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] chat_id_ Chat identifier.
   * \param[in] is_marked_as_unread_ New value of is_marked_as_unread.
   */
  toggleChatIsMarkedAsUnread(std::int64_t chat_id_, bool is_marked_as_unread_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -986129697;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<ok>;

  static object_ptr<toggleChatIsMarkedAsUnread> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Changes the pinned state of a chat. You can pin up to GetOption(&quot;pinned_chat_count_max&quot;) non-secret chats and the same number of secret chats.
 *
 * Returns object_ptr<Ok>.
 */
class toggleChatIsPinned final : public Function {
 public:
  static jclass Class;
  /// Chat identifier.
  std::int64_t chat_id_;
  static jfieldID chat_id_fieldID;
  /// New value of is_pinned.
  bool is_pinned_;
  static jfieldID is_pinned_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  toggleChatIsPinned();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] chat_id_ Chat identifier.
   * \param[in] is_pinned_ New value of is_pinned.
   */
  toggleChatIsPinned(std::int64_t chat_id_, bool is_pinned_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1166802621;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<ok>;

  static object_ptr<toggleChatIsPinned> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Toggles whether all members of a supergroup can add new members; requires appropriate administrator rights in the supergroup.
 *
 * Returns object_ptr<Ok>.
 */
class toggleSupergroupInvites final : public Function {
 public:
  static jclass Class;
  /// Identifier of the supergroup.
  std::int32_t supergroup_id_;
  static jfieldID supergroup_id_fieldID;
  /// New value of anyone_can_invite.
  bool anyone_can_invite_;
  static jfieldID anyone_can_invite_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  toggleSupergroupInvites();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] supergroup_id_ Identifier of the supergroup.
   * \param[in] anyone_can_invite_ New value of anyone_can_invite.
   */
  toggleSupergroupInvites(std::int32_t supergroup_id_, bool anyone_can_invite_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -797384141;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<ok>;

  static object_ptr<toggleSupergroupInvites> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Toggles whether the message history of a supergroup is available to new members; requires appropriate administrator rights in the supergroup.
 *
 * Returns object_ptr<Ok>.
 */
class toggleSupergroupIsAllHistoryAvailable final : public Function {
 public:
  static jclass Class;
  /// The identifier of the supergroup.
  std::int32_t supergroup_id_;
  static jfieldID supergroup_id_fieldID;
  /// The new value of is_all_history_available.
  bool is_all_history_available_;
  static jfieldID is_all_history_available_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  toggleSupergroupIsAllHistoryAvailable();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] supergroup_id_ The identifier of the supergroup.
   * \param[in] is_all_history_available_ The new value of is_all_history_available.
   */
  toggleSupergroupIsAllHistoryAvailable(std::int32_t supergroup_id_, bool is_all_history_available_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1701526555;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<ok>;

  static object_ptr<toggleSupergroupIsAllHistoryAvailable> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Toggles sender signatures messages sent in a channel; requires appropriate administrator rights in the channel.
 *
 * Returns object_ptr<Ok>.
 */
class toggleSupergroupSignMessages final : public Function {
 public:
  static jclass Class;
  /// Identifier of the channel.
  std::int32_t supergroup_id_;
  static jfieldID supergroup_id_fieldID;
  /// New value of sign_messages.
  bool sign_messages_;
  static jfieldID sign_messages_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  toggleSupergroupSignMessages();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] supergroup_id_ Identifier of the channel.
   * \param[in] sign_messages_ New value of sign_messages.
   */
  toggleSupergroupSignMessages(std::int32_t supergroup_id_, bool sign_messages_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -558196581;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<ok>;

  static object_ptr<toggleSupergroupSignMessages> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Removes a user from the blacklist.
 *
 * Returns object_ptr<Ok>.
 */
class unblockUser final : public Function {
 public:
  static jclass Class;
  /// User identifier.
  std::int32_t user_id_;
  static jfieldID user_id_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  unblockUser();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] user_id_ User identifier.
   */
  explicit unblockUser(std::int32_t user_id_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -307286367;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<ok>;

  static object_ptr<unblockUser> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Removes the pinned message from a supergroup or channel; requires appropriate administrator rights in the supergroup or channel.
 *
 * Returns object_ptr<Ok>.
 */
class unpinSupergroupMessage final : public Function {
 public:
  static jclass Class;
  /// Identifier of the supergroup or channel.
  std::int32_t supergroup_id_;
  static jfieldID supergroup_id_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  unpinSupergroupMessage();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] supergroup_id_ Identifier of the supergroup or channel.
   */
  explicit unpinSupergroupMessage(std::int32_t supergroup_id_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1987029530;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<ok>;

  static object_ptr<unpinSupergroupMessage> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Creates a new supergroup from an existing basic group and sends a corresponding messageChatUpgradeTo and messageChatUpgradeFrom. Deactivates the original basic group.
 *
 * Returns object_ptr<Chat>.
 */
class upgradeBasicGroupChatToSupergroupChat final : public Function {
 public:
  static jclass Class;
  /// Identifier of the chat to upgrade.
  std::int64_t chat_id_;
  static jfieldID chat_id_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  upgradeBasicGroupChatToSupergroupChat();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] chat_id_ Identifier of the chat to upgrade.
   */
  explicit upgradeBasicGroupChatToSupergroupChat(std::int64_t chat_id_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 300488122;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<chat>;

  static object_ptr<upgradeBasicGroupChatToSupergroupChat> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Asynchronously uploads a file to the cloud without sending it in a message. updateFile will be used to notify about upload progress and successful completion of the upload. The file will not have a persistent remote identifier until it will be sent in a message.
 *
 * Returns object_ptr<File>.
 */
class uploadFile final : public Function {
 public:
  static jclass Class;
  /// File to upload.
  object_ptr<InputFile> file_;
  static jfieldID file_fieldID;
  /// File type.
  object_ptr<FileType> file_type_;
  static jfieldID file_type_fieldID;
  /// Priority of the upload (1-32). The higher the priority, the earlier the file will be uploaded. If the priorities of two files are equal, then the first one for which uploadFile was called will be uploaded first.
  std::int32_t priority_;
  static jfieldID priority_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  uploadFile();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] file_ File to upload.
   * \param[in] file_type_ File type.
   * \param[in] priority_ Priority of the upload (1-32). The higher the priority, the earlier the file will be uploaded. If the priorities of two files are equal, then the first one for which uploadFile was called will be uploaded first.
   */
  uploadFile(object_ptr<InputFile> &&file_, object_ptr<FileType> &&file_type_, std::int32_t priority_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -745597786;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<file>;

  static object_ptr<uploadFile> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Uploads a PNG image with a sticker; for bots only; returns the uploaded file.
 *
 * Returns object_ptr<File>.
 */
class uploadStickerFile final : public Function {
 public:
  static jclass Class;
  /// Sticker file owner.
  std::int32_t user_id_;
  static jfieldID user_id_fieldID;
  /// PNG image with the sticker; must be up to 512 kB in size and fit in 512x512 square.
  object_ptr<InputFile> png_sticker_;
  static jfieldID png_sticker_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  uploadStickerFile();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] user_id_ Sticker file owner.
   * \param[in] png_sticker_ PNG image with the sticker; must be up to 512 kB in size and fit in 512x512 square.
   */
  uploadStickerFile(std::int32_t user_id_, object_ptr<InputFile> &&png_sticker_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 1134087551;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<file>;

  static object_ptr<uploadStickerFile> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Validates the order information provided by a user and returns the available shipping options for a flexible invoice.
 *
 * Returns object_ptr<ValidatedOrderInfo>.
 */
class validateOrderInfo final : public Function {
 public:
  static jclass Class;
  /// Chat identifier of the Invoice message.
  std::int64_t chat_id_;
  static jfieldID chat_id_fieldID;
  /// Message identifier.
  std::int64_t message_id_;
  static jfieldID message_id_fieldID;
  /// The order information, provided by the user.
  object_ptr<orderInfo> order_info_;
  static jfieldID order_info_fieldID;
  /// True, if the order information can be saved.
  bool allow_save_;
  static jfieldID allow_save_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  validateOrderInfo();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] chat_id_ Chat identifier of the Invoice message.
   * \param[in] message_id_ Message identifier.
   * \param[in] order_info_ The order information, provided by the user.
   * \param[in] allow_save_ True, if the order information can be saved.
   */
  validateOrderInfo(std::int64_t chat_id_, std::int64_t message_id_, object_ptr<orderInfo> &&order_info_, bool allow_save_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = 9480644;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<validatedOrderInfo>;

  static object_ptr<validateOrderInfo> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * This method should be called if messages are being viewed by the user. Many useful activities depend on whether the messages are currently being viewed or not (e.g., marking messages as read, incrementing a view counter, updating a view counter, removing deleted messages in supergroups and channels).
 *
 * Returns object_ptr<Ok>.
 */
class viewMessages final : public Function {
 public:
  static jclass Class;
  /// Chat identifier.
  std::int64_t chat_id_;
  static jfieldID chat_id_fieldID;
  /// The identifiers of the messages being viewed.
  std::vector<std::int64_t> message_ids_;
  static jfieldID message_ids_fieldID;
  /// True, if messages in closed chats should be marked as read.
  bool force_read_;
  static jfieldID force_read_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  viewMessages();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] chat_id_ Chat identifier.
   * \param[in] message_ids_ The identifiers of the messages being viewed.
   * \param[in] force_read_ True, if messages in closed chats should be marked as read.
   */
  viewMessages(std::int64_t chat_id_, std::vector<std::int64_t> &&message_ids_, bool force_read_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -1925784915;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<ok>;

  static object_ptr<viewMessages> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

/**
 * Informs the server that some trending sticker sets have been viewed by the user.
 *
 * Returns object_ptr<Ok>.
 */
class viewTrendingStickerSets final : public Function {
 public:
  static jclass Class;
  /// Identifiers of viewed trending sticker sets.
  std::vector<std::int64_t> sticker_set_ids_;
  static jfieldID sticker_set_ids_fieldID;

  /**
   * Default constructor. All fields will be value-initilaized.
   */
  viewTrendingStickerSets();

  /**
   * Constructor for initialization of all fields.
   *
   * \param[in] sticker_set_ids_ Identifiers of viewed trending sticker sets.
   */
  explicit viewTrendingStickerSets(std::vector<std::int64_t> &&sticker_set_ids_);

  /// Identifier uniquely determining a type of the object.
  static const std::int32_t ID = -952416520;
  /**
   * Returns identifier uniquely determining a type of the object.
   * \return this->ID.
   */
  std::int32_t get_id() const final {
    return ID;
  }

  /// Typedef for the type returned by the function.
  using ReturnType = object_ptr<ok>;

  static object_ptr<viewTrendingStickerSets> fetch(JNIEnv *env, jobject &p);

  /**
   * Helper function for to_string method. Appends string representation of the object to the storer.
   * \param[in] s Storer to which object string representation will be appended.
   * \param[in] field_name Object field_name if applicable.
   */
  void store(TlStorerToString &s, const char *field_name) const final;

  static ReturnType fetch_result(JNIEnv *env, jobject &p);

  static void init_jni_vars(JNIEnv *env, const char *package_name);
};

}  // namespace td_api
}  // namespace td
